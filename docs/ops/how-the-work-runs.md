# How the work runs

**One page on who does what, where each thing lands, and what only Ivo can do.** Written because
the machinery grew faster than the map of it (2026-09-03). If this page and the code disagree, the
code is right and this page is a bug.

## The three places work happens

| where | what it does | needs the laptop |
|---|---|---|
| **a session with the agent** (this repo, `live`) | everything that changes the store, the register or the deliverable: pricing a corpus, an ingest, an export, a package, a commit | yes |
| **the fleet** (`i-staykov/ark-fleet`, three self-hosted runners on the VPS, GitHub Actions schedule) | one wave every 20 minutes, paced against the token window: a scout proposes a source, a price leg measures it read-only, a verify leg tries to refute it, an improver tunes one knob | no |
| **the collectors** (launchd jobs on the laptop since 2026-09-09) | long CDX sweeps that write journals; `just collectors pause` and `resume` | yes, awake |

The fleet never touches the store. It clones `live` fresh on every wave, reads the shipped files
that `just sync` rsyncs to the VPS, and writes **findings**: a markdown file in the register voice
and a `finding.json` sidecar in the fleet's schema. Nothing it finds is a record until the laptop
takes it in, and no figure it reports is booked on its own: `just sync` prices every confirmed FIND
again on the live store and the register row carries both numbers.

**The fleet has its own one page, `harness.md` in `i-staykov/ark-fleet`, and it is the single
source of truth on all of it**: the lanes and what each one delivers, the pacer, which account
pays, the five kinds of decision, the download backlog, what the improver may change on its own,
the three collector commands, and every file with what it decides. This page stops at the
boundary; that one crosses it. What only Ivo can do is below, and it is the same list either side.

## The loop, and where each step leaves a mark

1. **Discover.** A lens proposes a source. A fleet scout leg, or a session, or Ivo. A scout lands
   it as `ark-fleet/leads/<slug>.json` at `status: scouted`, with the artifact, one quoted stamp
   and a size estimate; a session works from `docs/registers/sources-closed.md` outward.
2. **Screen.** Does it collide with a closed family, and what dates one item? `just screen`. A
   collision prints the measurement that killed it, so nobody re-argues it.
3. **Price.** Net-new equivalent-English against the live store and against the reviewer's own
   files, never gross. `just price` for registrable grain, `just price-hosts` for hostname grain.
   Read-only, so it is safe while collectors run.
4. **Decide.** Under the standing rule of 2026-08-29 the loop writes its own `Decision:` line when
   all four conditions hold. Otherwise the source parks as `pending` in
   `docs/registers/approved-sources-list.md` and waits for Ivo. **This is the main queue that only he can
   drain.**
5. **Ingest.** `ark ingest` / `ark ingest-hostnames`, which refuse any class without a decision.
   Every source gets its link and its dating sentence in `docs/registers/sources.md` first.
6. **Export.** `ark export` writes annual additions under `output/netnew/`, split into registrable
   and subdomain-hostname files, with evidence manifests and separate candidate collections.
   Both annual halves retain exact-host year evidence (brief IV.8); the registrable roll-up is
   secondary. `www.` and bare forms may each qualify, but neither evidences the other. Shipping
   predicates exclude baseline overlap, reverse DNS and pre-delegation TLDs.
7. **Check.** `ark check`, eighteen data invariants. Green is a precondition for every commit.
8. **Package and send.** `just ship` builds the archive, verifies it as a reviewer would, re-scores
   it with his own calculator, writes the mail draft and stops. **Sending is always Ivo** (C-63).
9. **Intake.** His next release arrives: `just intake <zip>` verifies, extracts, remeasures with his
   calculator and repoints `data/baseline.json`; `ark ingest-legacy` loads it. The denominator moves,
   so this comes before any new pricing.

## The numbers, and which page owns each

| number | owner |
|---|---|
| where the round stands | `docs/ROUND.md`, generated by `just state`; never edited |
| what a source measured, and why it was closed | `docs/registers/sources.md`, `docs/registers/sources-closed.md` |
| what may date a year | `docs/registers/approved-sources-list.md`, enforced by the ingest |
| the measured laws of pricing | `docs/lore/laws.md` |
| mistakes already paid for | `docs/lore/traps.md` |
| what needs Ivo | `docs/lore/key-decisions.md` |
| questions put to the reviewer | `docs/registers/questions.md` |
| what is on disk and whether it is deletable | `docs/registers/retention.md`, generated |

## What only Ivo can do

- **Decide a source class.** A `Decision:` line the standing rule cannot write.
- **Send anything to the reviewer.** Packaging is automatic, sending is not.
- **Merge a PR.** `main` is reached only by a PR, on both repositories, and the agent never pushes it.
- **Approve a deletion** of code or data (the single table, C-61).
- **Answer a question the documents cannot.** Current asks are in `docs/lore/key-decisions.md`;
   settled hostname-output rules are in `docs/brief/ding/project-brief.md`, not an open question.

## What the agent does without being asked

Runs the gate before every commit, keeps the register honest (a negative is logged as carefully as
a find), refuses to quote a figure that does not come from the shipped files, and writes
`private/handoff.md` at every pause so the next session starts where this one stopped.

## The shape of a good day

The fleet proposes and prices while nobody watches. A session banks what cleared the bar, books the
negatives, and leaves one clean commit per logical unit on `live`. Ivo drains the decision queue,
merges, and sends. The round's percentage grows, his next release resets the denominator, and the
score now falls with every day of delay: since 2026-09-03, `t_i` counts whole calendar days from
the day the task was first assigned and never resets, so an early partial submission beats a late
complete one.
