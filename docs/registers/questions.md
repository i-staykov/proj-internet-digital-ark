# Questions put to the reviewer, with his answers when they come. `just ship` copies every open row past its remind-on into the mail draft, one line each.

Status is `open`, `answered` (his words quoted in the row) or `withdrawn`. An asked-on of `draft` means
the row has not been sent: the owner approves the wording or deletes the row before the next ship.
Export-ignored: the page does not travel in the delivery.

| asked-on | question | status | remind-on |
|---|---|---|---|
| 2026-09-02 | Rules 3 and 4 of the hostname standard as we apply them (phase-7 report, section 2): a hostname record needs its own machine-written observation of the host serving web content in that year, so a DNS listing dates the parent registrable and writes no hostname row; and `www.<parent>` is the registrable's own site, never a second record. Your 2026-09-02 mail praises the separation and the web-content basis but rules on neither hold-out. Do both hold? | answered 2026-09-05, and the two halves SPLIT: rule 3 holds, rule 4 does not. On rule 4, he merged the round as sent: "The official annual merge accepts the submitted `additions/` and `hostnames/` files. These records have valid hostname structure and their own year-specific web-content evidence, such as an exact-host capture or an explicit URL listing." 95.0% of that hostname half was `www.<parent>`, so the shape is accepted (C-72). On rule 3 he confirmed the condition: "The ISC Survey hostname collection is preserved separately for reference, but is not included in the annual master, Equivalent-English increment, or score. Its DNS-survey observation establishes that a hostname was observed in DNS, but does not independently establish that the hostname served web content." (C-73) Completed 2026-09-06: the same collection is now a NAMED, SCORED candidate asset, "a separate, provenance-linked candidate collection ... an equally important discovery asset", promotable per hostname-year on later website-level evidence (C-74) | done |
| draft | Your 0906 update classes ISC / Network Wizards survey records as DNS-observation evidence and sends raw ISC HOSTNAME lists to a candidate collection. Our annual masters also hold **1,314,476 registrable domains dated by `isc_survey` alone**, admitted under your ruling of 2026-07-24 that a survey host under a domain "is sufficient to establish the domain's existence in that year". Does the 0906 classification reach that registrable-grain use too, and if so should those domain-years move to the candidate pool? They are already merged into your annual files, so nothing is re-claimable either way; we are asking so the next round applies the right rule. | open | phase-9 mail |
| draft | Two of our conventions, stated so you can stop either. **(1)** A capture of `www.example.com` writes both a `www.example.com` hostname record and an `example.com` registrable record for that year, on the reading that a host cannot resolve without its registrable. Your 0906 sentence that a domain "for which the evidence cannot distinguish between the bare domain and the www. hostname should not be automatically expanded into two records" may be aimed exactly at this; our `www.` records each carry their own exact-host capture, but the bare record is inferred from it. **(2)** We would ship the ISC collection as a candidate asset under your 0906 rule. Confirm the shape you want: one file, or per survey edition, and which provenance columns. | open | phase-9 mail |
| draft | Which release starts the scoring clock t_i, and is elapsed time counted in whole days or fractional? Your 2026-08-20 rule names the latest benchmark package, but S_6 = 6.88 and S_7 = 6.302372 both fit a clock started at the 2026-08-21 11:19 release with elapsed time rounded up to whole days (t_7 = 12 for a submission sent 2026-09-02 05:50). | open (interim: Ivo counts whole days from the day the baseline is named after, partial days up, 2026-09-03, C-58) | phase-8 mail |
| 2026-09-04 | Your 0903 update redefines t_i as max(1, receipt_date_i - task_assignment_date_member) in whole calendar days. **You then scored round 8 as S = 10 x (18.769714 / 33) = 5.687792, and we cannot reproduce the 33**: the benchmark interval gives t = 1 and the assignment interval t = 45 counting from 2026-07-21, the earliest date our records support. A divisor of 33 implies an origin of 2026-08-02. Which date is t_i counted from, and does the revision re-score the awarded rounds? | open, and now sharpened by his own figure of 2026-09-05 rather than answered by it | next mail |
| draft | Confirming what your merges already show, so we stop holding records back: merged260902-3 and merged260903-3 both contain all 1,917,606 hostnames of our 2026-09-02 submission, including all 1,313,547 `www.` forms, and for 1,106,188 of those the bare name is in the same year file too. So `www.example.com` and `example.com` are two records in the same year, and round 7 was credited 7.562846% with them included. Is that the rule we should keep applying? | answered 2026-09-06, and it turns on the EVIDENCE rather than the shape: "A domain for which the evidence cannot distinguish between the bare domain and the www. hostname should not be automatically expanded into two records." Ours can distinguish, because the invariant `a_www_record_has_its_own_evidence` refuses any `www.<parent>` row whose evidence does not name that exact host, so nothing is expanded automatically and C-69 stands. Withdrawn from the next mail (C-74) | done |
