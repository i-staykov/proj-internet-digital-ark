# Sources

One entry per source: what it is, where to obtain it, and **what fixes one item to one year**. That
last clause is the whole of the evidence argument, so it is stated per source rather than in a
preamble, and a source with no per-item date is not admitted whatever else it offers.

Paths are relative to the repository root. Every ingest command assumes the file has been placed at
the path shown.

Sources evaluated and closed are here too, each with the single measurement that closed it, so the
same ground is not broken twice. [discovery.md](discovery.md) is the method used to price one before
building a collector.

**Where a figure appears below it is the measurement a source was admitted or rejected on, at the
date given, not a current count.** A source re-measured later against a larger store reads lower, so
the two are different quantities and both are kept. The authoritative current counts are in
`audit/source_contribution.csv`, which `ark export` rewrites from the store on every run and whose
`netnew_pairs` column sums to the report's headline increment. A hand-copied snapshot once lived here
claiming to be generated, and by the time anyone checked it had omitted the round's largest
contributor entirely.

---

## `prior_task`: the supplied baseline

The six annual files supplied with the task, 8,224,963 hostname lines. In the delivery archive at
`baseline/original/`; the baseline additions are scored against sits at `baseline/<release>/`, named
by `CURRENT_BASELINE_MARKER` in `src/ark/baseline.py`.

```bash
cp -R <archive>/baseline/original legacy-data
uv run ark ingest-legacy --legacy-dir legacy-data --marker-prefix original
```

Both flags are required, or the six files are skipped as already ingested.

**Dating: the file a line appears in is its year. No inference.**

`prior_reused`. Excluded from the scored metric, being baseline rather than addition.

---

## `isc_survey`: Internet Domain Survey host lists

The Network Wizards / ISC twice-yearly DNS walk: five intact `.domains` lists for 1996-1997 plus 583
per-TLD host files. ISC's own copies fail their gzip integrity check, so these come from a 1996
Wayback crawl of `nw.com` and the survey author's live site.

```bash
mkdir -p data/raw/isc_survey && cd data/raw/isc_survey
curl -O http://web.archive.org/web/19961112163532id_/http://nw.com:80/zone/9507.domains.gz
curl -O http://web.archive.org/web/19961112163635id_/http://nw.com:80/zone/9601.domains.gz
curl -O http://web.archive.org/web/19961112163826id_/http://nw.com:80/zone/9607.domains.gz
curl -O http://3waylabs.com/zone/9707.domains.gz
cd - && uv run ark ingest isc_survey data/raw/isc_survey/*.gz
```

```bash
uv run python scripts/sources/directories/fetch_nw_host_files.py   # resumable, three connections, ~2h for 116 MB
uv run ark ingest isc_survey data/raw/isc_survey/*.gz
```

**Dating: the survey date is the `YYMM` code in the filename (`9607` = July 1996), and every host in
that file was observed in DNS on that date, so the file's provenance fixes the year for all of its
lines.**

`artifact_listing`. 42,299 net-new pairs, **14,956.3877 equivalent-English**, in the two years the
Internet Archive cannot supply in bulk: **the best 1996-1997 source in the project.** Hard caps: no
`com.gz` and no `edu.gz` in any edition, and the `.domains` lists stop at 9707.

---

## `afnic_fr`: `.fr` registry open data

The monthly `.fr` open-data file, one row per domain. Open licence, attribution only.

```bash
mkdir -p data/raw/afnic && cd data/raw/afnic
# from https://opendata.afnic.fr/ download the current "A" file (Noms de domaine en .fr)
unzip '*_OPENDATA_A.zip'
cd - && uv run ark ingest afnic_fr data/raw/afnic/*NomsDeDomaineEnPointFr.csv
```

**Dating: the row's registry-written creation date and permanent-deletion date (blank while
registered). AFNIC's Technical Integration Guide v3.0 (27 February 2015) states `<domain:crDate>` is
"the last creation date of the domain name" or the date of the last transmission, and both events
fall after any prior deletion, so `[crDate, deletion-or-now]` contains no deletion event: it is
continuous by construction.**
<https://www.afnic.fr/medias/documents/technique/integration-guide-en-2015-02-27.pdf>

`whois_creation`, master for every in-window year the interval covers; each row stores its interval
verbatim (`registered 16-03-1999..active`).

`crDate` can only be later than the true first registration, so the tranche undercounts and cannot
overcount. Omits names deleted before 28 January 2014. Republished monthly, so unpinnable; this
delivery used the June 2026 edition.

---

## `ukwa_link_source` and `ukwa_link_target`: UK Web Archive host link graph

The JISC UK Web Domain Dataset host link graph 1996-2010, rows of
`year|source_host|target_host<TAB>count`. Wayback only: the original address answers HTTP 200 with a
159-byte stub and the DOI is dead. The archived stream drops partway, giving 2 GiB of the advertised
20.9 GB.

**The file is not year-sorted.** The year column decreases 14 times, so it is 15 concatenated shards
each sorted internally, with **2,468,674** in-window rows spread across all of them; a parser must
not stop at the first post-2001 row.

```bash
mkdir -p data/raw/ukwa && cd data/raw/ukwa
curl -L -o host-linkage.tsv.gz \
  "https://web.archive.org/web/2019id_/https://www.webarchive.org.uk/datasets/ukwa.ds.2/linkage/host-linkage.tsv.gz"
cd -
uv run ark ingest ukwa_link_source data/raw/ukwa/host-linkage.tsv.gz
uv run ark ingest ukwa_link_target data/raw/ukwa/host-linkage.tsv.gz
```

**Dating: the year column of each row, the crawl year that observed the link.**

The source host was crawled successfully that year to produce the row, so `link_source` is
master-eligible; the target was merely linked to, so `link_target` is candidate-only. One file,
ingested twice. `.uk`-weighted.

---

## `arquivo_ia` and `arquivo_roteiro`: Arquivo.pt capture indexes

Two CDXJ indexes from the Portuguese web archive: `IA.cdxj`, 47 GB donated by the Internet Archive,
covering 1996-2007, and the smaller early-Portuguese-web `Roteiro.cdxj`.

```bash
mkdir -p data/raw/arquivo && cd data/raw/arquivo
curl -C - -O https://arquivo.pt/datasets/cdxj/IA.cdxj
curl -C - -O https://arquivo.pt/datasets/cdxj/Roteiro.cdxj
cd -
uv run ark ingest arquivo_ia data/raw/arquivo/IA.cdxj
uv run ark ingest arquivo_roteiro data/raw/arquivo/Roteiro.cdxj
```

**Dating: the 14-digit crawler-written capture timestamp on each line.**

`cdx_timestamp`, a capture with an in-year timestamp and HTTP 200. Portuguese-web weighted; skipping
both costs 17,696 pairs over 7,001 domains.

---

## `odp`: Open Directory Project (DMOZ) RDF content dumps

Three surviving dumps: a truncated prefix of the August 2000 full dump and two complete 2001
Kids-and-Teens dumps. The live URLs serve a stub, so these come from Wayback.

```bash
curl -s "https://web.archive.org/cdx/search/cdx?url=dmoz.org/rdf/*&from=2000&to=2001&filter=statuscode:200&fl=timestamp,original"
mkdir -p data/raw/odp
# then, for each capture of interest:
curl -o data/raw/odp/c2000.gz "https://web.archive.org/web/<timestamp>id_/http://dmoz.org/rdf/content.rdf.u8.gz"
uv run ark ingest odp data/raw/odp/*.gz
```

**Dating: the dump's own generation stamp, corroborated by the Wayback capture timestamp and the
filename (`c2000` = 2000, `kt200106` = June 2001).**

`artifact_listing`: a dated data file, not an undated directory page, so every catalogued external
URL inside it is a line in that file and the file's date fixes the year.

The August 2000 full dump is unrecoverable, Wayback holding only that year's `structure.rdf`, which
carries no external links. The 2001 full content dumps are not retrievable.

---

## `early_web_cdx`: Internet Archive Early Web CDX dataset

A published CDX dataset of early-web captures, 224 gzipped index files.

```bash
uvx --from internetarchive ia download early-web_cdx-lang-cdxa \
    --glob='*.cdx.gz' --destdir=data/raw/early_web --no-directories
uv run ark ingest early_web data/raw/early_web/*.cdx.gz
```

Item: <https://archive.org/details/early-web_cdx-lang-cdxa>

**Dating: the 14-digit capture timestamp on each line.**

`cdx_timestamp`. It overlaps the supplied baseline almost completely, itself derived from the same
archive, so its 2.28M evidence rows serve as corroboration.

---


## `ia_cdx_bulk`: Wayback CDX verification engine

A query engine, not a file: one collapsed CDX query per domain covering all six years, run against domains missing a year they are bracketed by. Endpoint <https://web.archive.org/cdx/search/cdx>; the response journals ship under `journals/`, so ingest replays offline.

```bash
uv run ark gaps                                             # choose targets
uv run ark cdx data/raw/cdx/gap_candidates.txt --workers 8  # query, writes a journal
uv run ark ingest cdx_snapshot data/raw/cdx/cdx_*.jsonl.gz  # journal -> evidence
```

**Dating: the 14-digit capture timestamps the archive returns for that domain, written by the crawler at capture time, collapsed to distinct years client-side.**

Evidence type: `cdx_timestamp`.

## `dartmouth_nber_captures`: the archive's own capture census

A 2017 research release publishing, for every host the Wayback Machine then held, captures per calendar year; 228 MB of `host<TAB>year<TAB>count`. From archive.org item `DARTMOUTH-NBER-RESEARCH-2017-metadata`, which no longer serves. The input does not ship:

```bash
uv run ark ingest dartmouth_nber_captures data/raw/dartmouth_nber/domain-year-captures.txt
```

**Dating: a row states the archive holds N captures of that host inside that calendar year, the same machine-counted fact a CDX query returns, so it dates that year and no other.**

**Evidence type: `cdx_timestamp`.** Self-dating, no corroboration split. Agrees with our own CDX querying on **138,760 (domain, year) pairs**. Yield **227,273 net-new pairs, 142,084.0 equivalent-English**. Approved `master` by Ivo 2026-08-17.

## `domain_creation_bulk`: published registry creation dates in bulk

A published WHOIS/DNS compilation of 171 million domains, each carrying the registry's own creation date parsed from a port-43 answer. 25.9 GB semicolon-separated CSV, fetched with the Kaggle CLI: `kaggle datasets download -d wotschofsky/171-million-domain-names-whois-dns-dnssec`.

```bash
uv run ark ingest domain_creation_bulk data/raw/domain_creation/domains.csv
```

**Dating: the registry-written Creation Date field in the record. A creation date in 1998 writes 1998 and no other year, because the parser emits one evidence row for one year.** Falsification: of 21,698 in-window rows in the six TLDs delegated in 2001, **zero** predate 2001.

**Evidence type: `whois_creation`.** Filed under the `registry` lineage, so it cannot corroborate our own `rdap` sweeps. Yield **2,165,523 net-new pairs, 1,241,812.0 equivalent-English**. Approved `master` by Ivo 2026-08-17.


## `rdap` and `rdap_snapshot`: registry creation dates

Registry RDAP lookups, direct to the authoritative registry resolved per TLD from the IANA bootstrap,
`rdap.org` fallback only. `rdap_snapshot` is the journalled path, `rdap` an earlier tranche.

```bash
uv run ark gaps --creation --out data/raw/rdap/creation_candidates.txt
uv run ark rdap data/raw/rdap/creation_candidates.txt
uv run ark ingest rdap_snapshot data/raw/rdap/rdap_*.jsonl.gz
```

```bash
uv run python scripts/build_rdap_pool_list.py --tlds com,net --limit 1400000 \
    --out data/raw/rdap/pool_targets_verisign.txt
bash scripts/rdap_pool_sweep.sh 6 100000 32
uv run ark ingest rdap_snapshot data/raw/rdap/rdap_pool_*.jsonl.gz
```

Bootstrap <https://data.iana.org/rdap/dns.json>, fallback <https://rdap.org/>

**Dating: the `registration` event date, a registry-written timestamp in the response. RDAP gives the
current state plus that one historical timestamp, no registration history.**

`whois_creation`, creation year only: it does not establish registration in a later year. Dated
outside 1996-2001, the name stays a candidate.

| closed here | what closed it | reopen |
| --- | --- | --- |
| PIR `.org` | 403 for 9,253 consecutive requests after ~850 answers | slow probe, stop on first refusal |
| Nominet `.uk` | three refusals in the first fourteen queries at 0.5 q/s | |
| auDA `.au` | namespace re-registered 2002, dates stamped with the migration | |
| `.gov` pool | pool-to-dated ratio 182.0 against `.com` 0.3, names invented | |

---

## `page_directory` and `page_expansion`: archived curated directory pages

Wayback captures of curated catalogue pages, read for the sites they list. Seeds under
`seeds/expansion/`; primary catalogue the WWW Virtual Library, <http://vlib.org/>

```bash
uv run ark download seeds/expansion/seeds_round2.txt --out data/raw/expand/round2/expand_round2.jsonl.gz
uv run python scripts/engines/split_expansion_journal.py data/raw/expand/round2/expand_round2.jsonl.gz --write
uv run ark ingest expansion_directory data/raw/expand/round2/*_corroborated.jsonl.gz --round 2
uv run ark ingest expansion_links     data/raw/expand/round2/*_unverified.jsonl.gz --round 2
```

**Dating: the crawler-stamped Wayback capture timestamp of the page. A listing dated 1998 evidences
its entries for 1998 only.**

`dated_directory` for a page recorded as a curated catalogue. Every other name, and any name no other
source attests, carries `link_target` under `page_expansion` and is candidate-only, a listing being a
claim by the linking page.

Rejected: the 1996-1997 Yahoo tree under `www.yahoo.com/<Category>/`, 11 pairs and 7.7295 EE from 55
archive requests.

---

## `internet_scout`: Internet Scout Report archive

Weekly curated review of scholarly, government and educational sites, harvested by OAI-PMH from
<https://archives.internetscout.org/OAI>.

```bash
mkdir -p data/raw/scout
curl -A "Mozilla/5.0" \
  "https://archives.internetscout.org/OAI?verb=ListRecords&metadataPrefix=oai_dc" \
  >> data/raw/scout/scout_oai.xml
# then repeat with &resumptionToken=<token from the previous page> until none is returned
uv run ark ingest internet_scout data/raw/scout/scout_oai.xml
```

**Dating: `dc:date` on each record gives the issue year and `dc:identifier` the reviewed URL, archive
metadata fields, not prose.**

`dated_directory`. Scholarly and US-weighted; the live feed cannot be hash-pinned.

---

## `ncsa_whats_new`: NCSA "What's New" announcement pages

Dated issues of the era's list of newly launched sites, from Wayback captures of the NCSA Mosaic site.
The only surviving 1996 editorial directory artifact here.

```bash
curl -s "https://web.archive.org/cdx/search/cdx?url=ncsa.uiuc.edu/SDG/Software/Mosaic/Docs/whats-new*&from=1996&to=1996&filter=statuscode:200&fl=timestamp,original"
# fetch each monthly issue with the id_ modifier, then extract the announced entries
uv run ark ingest ncsa_whats_new data/raw/ncsa-whats-new/ncsa_1996_domain_date_pairs.tsv
```

**Dating: the issue date carried by the page holding the entry. Every row is 1996.**

`dated_directory`. Navigation and masthead links are excluded. One of the 4,916 names is attested by
no other source.

---

## `ia_cdx`: per-year CDX verification (superseded)

Kept only so its 11 rows stay attributable. Superseded by the collapsed six-year query in
`ia_cdx_bulk`.

---

## NYPW first-capture index: rejected

IA's "Not Your Parents' Web" first-capture index (`https://archive.org/details/nypw_urls_CDXfirstentry`)
yields **60 net-new pairs over 53 net-new domains** from 2,413,003 in-window pairs, 99.998% overlap
with the same IA CDX the baseline drains. The 19.35 GB `nypw_timemaps` sibling samples that URL
universe too, and it is a separate source with a separate verdict: see below.

## `nypw_timemaps`: NYPW TimeMaps, master

Item: `https://archive.org/details/nypw_timemaps`. The THIRTY-FOUR parts ingested on 2026-09-01,
each linked so the bytes can be pulled again. The entry first listed only three of these while
eleven were already in the store, and the tarballs are deleted once converted to `.cdx.gz`, so the
missing links were the difference between a refetchable partition and a lost one:

- `https://archive.org/download/nypw_timemaps/1999/nypw_timemaps1999_deeplinks_part00o.tar.gz`, 81,558,295 B
- `https://archive.org/download/nypw_timemaps/1999/nypw_timemaps1999_rootURLs_part01r.tar.gz`, 548,991,394 B
- `https://archive.org/download/nypw_timemaps/1999/nypw_timemaps1999_rootURLs_part02r.tar.gz`, 147,027,083 B
- `https://archive.org/download/nypw_timemaps/1999/nypw_timemaps1999_rootURLs_part03r.tar.gz`, 150,725,864 B
- `https://archive.org/download/nypw_timemaps/1999/nypw_timemaps1999_rootURLs_part04r.tar.gz`, 210,555,238 B
- `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_deeplinks_part00o.tar.gz`, 31,659,131 B
- `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_deeplinks_part01o.tar.gz`, 31,397,597 B
- `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_deeplinks_part02o.tar.gz`, 56,689,031 B
- `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_rootURLs_part01r.tar.gz`, 228,359,937 B
- `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_rootURLs_part02r.tar.gz`, 119,945,969 B
- `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_rootURLs_part03r.tar.gz`, 119,365,472 B
- `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_rootURLs_part04r.tar.gz`, 825,494,346 B
- `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_rootURLs_part05r.tar.gz`, 406,711,686 B
- `https://archive.org/download/nypw_timemaps/2001/nypw_timemaps2001_deeplinks_part00o.tar.gz`, 148,848,304 B
- `https://archive.org/download/nypw_timemaps/1997/nypw_timemaps1997_deeplinks_part00o.tar.gz`, 30,569,952 B
- `https://archive.org/download/nypw_timemaps/1997/nypw_timemaps1997_rootURLs_part01r.tar.gz`, 137,487,874 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_deeplinks_part00o.tar.gz`, 17,958,107 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_deeplinks_part01o.tar.gz`, 13,698,867 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_deeplinks_part02o.tar.gz`, 19,014,371 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_rootURLs_part00r.tar.gz`, 617,413,931 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_rootURLs_part01r.tar.gz`, 708,524,984 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_rootURLs_part03r.tar.gz`, 449,293,008 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_rootURLs_part04r.tar.gz`, 325,150,887 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_rootURLs_part05r.tar.gz`, 726,115,622 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_rootURLs_part06r.tar.gz`, 83,518,037 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_rootURLs_part07r.tar.gz`, 521,097,923 B
- `https://archive.org/download/nypw_timemaps/1998/nypw_timemaps1998_rootURLs_part08r.tar.gz`, 425,797,234 B

Banked, read out of the 2026-09-01 01:22 export rather than projected: **87,905.0 EE net-new
post-split over 197,938 pairs**, mean weight 0.4441, of which 193,428 pairs carry a 2001 year.

**Seven further parts were pulled and ingested by the operator after that reading, taking the
item to THIRTY-FOUR parts and closing every in-window folder.** They are, with their year rows
straight out of `ark ingest`, which is the cheapest honest per-partition figure this project has:

- `.../1999/nypw_timemaps1999_rootURLs_part00r.tar.gz`, 1,434,653,466 B, **47,229 year rows**
- `.../1997/nypw_timemaps1997_rootURLs_part00r.tar.gz`, 3,477,105 records, **5,222 year rows**
- `.../2000/nypw_timemaps2000_rootURLs_part00r.tar.gz`, 3,945,494 records, **94,695 year rows**
- `.../1996/nypw_timemaps1996_deeplinks_part00o.tar.gz` + `rootURLs_part00r` + `part01r`,
  9,897,209 lines together, **4,512 year rows between all three**
- `.../1996/nypw_timemaps1996_rootURLs_part02r.tar.gz`, 291,061,385 B, **761 year rows**

**The per-folder verdict, now that every in-window folder has been measured rather than argued
about.** Year rows per million records: 2000 about 24,000, 1999 about 10,000, 1997 about 1,500,
1996 about 640, 2001 **4**. So the payload is 1999 and 2000, 1997 is thin but real, 1996 is the
saturated head exactly as the original 2026-08-24 closure found, and the 2001 folder is dead
because a domain first captured in 2001 carries only a 2001 pair and the IA-derived baseline holds
that by construction. The whole round moved from 1.2567% to **2.3318%** on this item.

**The out-of-window folders are closed too, measured rather than assumed.** The item runs to
2021 and only 1996-2001 was ever pulled, on the reasoning that a URL first captured in 2002
cannot carry an in-window capture. That reasoning is now checked: `2002_deeplinks_part02o`,
4,036,843 rows, contains **one** row stamped 1996-2001. So folder year really is absolute first
capture, and the roughly 60 GB of 2002-2021 folders is dead. One 112 MB download settled it.

**Do not re-price this item by argument.** One run on 2026-09-01 reasoned from a plausible
mechanism that the 2001 folder was the real seam and recommended redirecting the download queue
there; the ledger says that partition wrote six year rows. See the correction on that row in the
evaluated table below.

**Which folder to pull next, measured as a census over all 197,938 pairs rather than sampled.**
Attributing each pair to the partition whose row supplied its `evidence_url` leaves nothing
unattributed and gives, per folder: 1997 651.3 EE over 2 parts, 1998 23,284.0 over 11, 1999
19,807.8 over 5, 2000 44,159.3 over 8, and **2001 just 2.5 EE over 1**. The yield rises from 1997
to 2000 and collapses at 2001, because folder year is the year of FIRST capture: a 2001-folder
domain has no earlier year to offer and its 2001 year is held by an IA-derived baseline by
construction. **So "97.7% of the net-new is dated 2001" is not a reason to pull the 2001 folder,
and reading it that way inverts the source.** `rootURLs` parts outpay `deeplinks` 3.9x per part
(77,912.2 EE over 18 against 9,992.7 over 9), so the remaining 1999 and 2000 `rootURLs` parts are
the queue. The 1997 and 1998 folders had been projected at "under 100 EE combined" and in fact
paid **23,935.4 EE**, the projection having been made on the same folder-versus-capture confusion.

**What dates one item:** field 3 of a TimeMap row is Wayback's own 14-digit capture timestamp,
written by the crawler at the moment of the capture, so the row evidences that year and no other.
A row entire, from `2000/TM_other/TM_x00o2000_10000.txt` inside the first tarball:

    https://4free.net/mousepads.shtml net,4free)/mousepads.shtml 20010124104200
    http://www.4free.net:80/mousepads.shtml text/html 200 NT5S4OFZGCGRFF3TKTOCLK7IYFJKQKP6 4009

Class `cdx_timestamp`, already master-eligible, and nothing in the row was typed by a human, so
pre-split equals post-split. Terms: CC BY 4.0, stated in the item's own `nypw_timemaps_readme.txt`.

**Why the sibling's rejection does not carry over, and why the 2026-08-24 closure of this item was
wrong.** The first-capture index gives one row per URL, so it can only ever offer a domain its
FIRST year, which the IA-derived baseline holds by construction. A TimeMap gives every capture of
that URL. The folder year is the year of first capture, not of the content, so folder Y can only
add years Y+1..2001: the 1996 folder the old test used is the saturated head, and the 2001 folder
is held by construction at 108,863 of 108,870 pairs. The paying folders are 1997 to 2000.

**Banked, net-new post-split, read back out of the store after the ingest:** 10,072 pairs and
**4,084.3 EE**, mean weight 0.4055, TLDs `com` 4,818, `de` 1,001, `org` 641, `net` 377, `co.jp` 320,
`it` 284. **10,070 of the 10,072 land at 2001.** Per part, `year_rows`: 2000 deeplinks part00o 3,566,
2000 rootURLs part02r 6,500, 2001 deeplinks part00o 6.

The pre-ingest pricing said 6,424 pairs at 4,146.8 EE and mean weight 0.6455, so the store took 57%
more pairs for 1.5% less EE. The head the pricing counted got covered in the days between and what
was left is the ccTLD tail. Worth carrying forward for any source priced more than a day before it
is banked: the pair count and the EE move in opposite directions, so a price that still looks right
in EE can be right for the wrong reason.

**What is left.** In-window folders are 19,350,762,163 B across 47 tarballs and the rootURLs lane is
about 90% of the bytes and was never opened before. At the measured 21 to 51 EE per MB the rest of
the 2000 folder alone projects five figures before saturation, which has to be re-measured as
banking proceeds. Take 1999 next: it has holes at both 2000 and 2001. Skip 1996 and 2001, both
measured dead.

## Australian Web Archive: rejected, endpoint correction kept

`https://webarchive.nla.gov.au/awa/cdx` returns an Anubis challenge, but
**`https://web.archive.org.au/awa/cdx` answers normally**, a pywb server supporting `url`,
`matchType=domain`, `from`/`to`, `limit`, `collapse`, `output=json`. It is a lookup API, not a bulk
dump. Paired with the PANDORA titles list
(`https://github.com/GLAM-Workbench/trove-web-archives-titles`, CC0) it gives 35,391 registered
domains, 29,595 in no annual file, but a random 60-domain sample returned **60 answers, 0 transport
failures and 0 with any in-window capture**.

## Source names that are not separate sources

`cdx_snapshot` writes under `ia_cdx_bulk`, `rdap_snapshot` under `rdap_snapshot`, `early_web` under
`early_web_cdx`, `expansion_directory` and `expansion_links` under `page_directory` and
`page_expansion`. `deduplicated_urls_2001-2002` and `mid_slice` are candidate-only names with zero
evidence rows, retained so earlier seeding runs stay attributable.

---


## `trade_press` and `trade_press_mention`: scanned computer magazines

archive.org's scanned, OCR'd computer press, via `archive.org/advancedsearch.php` then
`archive.org/download/<id>/<id>_djvu.txt`. Hobbyist corpus `collection:computermagazines OR
collection:byte-magazine OR boardwatch`, 4,030 in-window items; American `collection:computerworld
OR collection:pub_computerworld OR collection:applemagazines OR (identifier:bub_gb* AND
(title:infoworld OR title:"network world" OR title:computerworld OR title:"pc mag"))`, 1,288.

**Dating: the item's `year` metadata field, the publication date of the issue. No inference.**

`dated_directory` after the corroboration split, because OCR fabricates hostnames: names seen only
here go to the candidate pool. Lineage `trade_press`. Yield 1,590 pairs, 1,004.33
equivalent-English.

```bash
uv run python scripts/sources/trade_press/collect_trade_press.py --discover     # collection sizes, run this first
uv run python scripts/sources/trade_press/collect_trade_press.py --limit 5000 --query "$HOBBYIST"   # first corpus
uv run python scripts/sources/trade_press/split_trade_press.py --write
uv run ark ingest tradepress_dated      data/raw/tradepress/tradepress_dated.jsonl.gz
uv run ark ingest tradepress_candidates data/raw/tradepress/tradepress_candidates.jsonl.gz

uv run python scripts/sources/trade_press/collect_trade_press.py --limit 1400   # second corpus, now the default
uv run python scripts/sources/trade_press/split_trade_press.py \
    --journal data/raw/tradepress/tradepress_20260808T172417Z.jsonl.gz --tag american --write
uv run ark ingest tradepress_dated      data/raw/tradepress/tradepress_dated_american.jsonl.gz
uv run ark ingest tradepress_candidates data/raw/tradepress/tradepress_candidates_american.jsonl.gz
```

The extractor also reads bare `foo.com`, `http://foo.com/` and `bob@foo.com`; re-reading cached OCR
under it sends no request:

```bash
uv run python scripts/sources/trade_press/reextract_trade_press.py --write
uv run python scripts/sources/trade_press/split_trade_press.py \
    --journal data/raw/tradepress/tradepress_reextract_<stamp>.jsonl.gz --tag reextract --write
uv run ark ingest tradepress_dated      data/raw/tradepress/tradepress_dated_reextract.jsonl.gz
uv run ark ingest tradepress_candidates data/raw/tradepress/tradepress_candidates_reextract.jsonl.gz
```

| closed | the fact that closed it |
| --- | --- |
| widening the query | `--discover` finds no `pub_infoworld`, `pub_network-world`, `pub_pc-week`, `pub_internet-world`, `pub_cio`, `pub_web-techniques`, `drdobbs`, `linuxjournal`, `maccompendium`, `boardwatch`, `pcmag`, `wired-magazine` or `internet-magazines` collection, and no `sim_*` microfilm run of a computing title except Computerworld |
| `magazine_rack` | 34,288 items, 0.4 net-new pairs each |
| `folkscanomy_computer` | 519 items, 36 of 40 unreachable |
| `sim_microfilm` at large | 57,245 in-window items, but a 1,500-item sample is journals, gazettes and "Table of Contents" stubs |

---

## `usenet_address` and `usenet_address_mention`: the addresses the extractor never read

The 19,083 already-ingested Usenet archives at `data/raw/usenet/*.mbox.zip`, re-read for `ftp://`
hosts, `mailto:` links and typed body addresses. **Dating: the posting date of the message carrying
the address, from the message header, identical to `usenet_announce`.** `dated_directory` after the
corroboration split, `link_target` otherwise. Lineage `usenet`: a post and an address inside it are
one observation. Yield 102,577 pairs, 62,820.7 equivalent-English.

```bash
uv run python scripts/sources/usenet/collect_usenet_addresses.py --workers 10
uv run python scripts/sources/usenet/split_usenet_addresses.py --write
uv run ark ingest usenet_addr_dated      data/raw/usenet_addr/usenet_addr_dated.jsonl.gz
uv run ark ingest usenet_addr_candidates data/raw/usenet_addr/usenet_addr_candidates.jsonl.gz
```

Closed: a generic dot-rule token scan of the same text gave 354 net-new tokens worth at most 193
equivalent-English, contaminated (`ads.my`, `lol.ie`). Closed: mining `.edu` from the pool, since
these extractors supply 213,703 of its 216,185 `.edu` names and they are anti-harvester munged
addresses, CDX hit rate 0.003 over 1,709 answers.

---

## `usenet_bare` and `usenet_bare_mention`: the bare `foo.com` in the message bodies

The same 19,231 archives read a third time for a plain `foo.com` in prose, no scheme, no `www.`, no
`@`. No network. **Dating: the posting date of the message, identical to `usenet_announce`.**
`dated_directory` after the corroboration split, `link_target` otherwise; 36.3% of rows were
uncorroborated and went to the pool, so a company or file name asserts nothing. Lineage `usenet`.
Guards in `ark.usenet` are unit-pinned: TLD allowlist, lookbehind, lookahead, all-digits rule, and
body text only, never `Path:`, `Xref:` or `Newsgroups:`. Yield 42,139 pairs, 28,460.3
equivalent-English.

```bash
uv run python scripts/sources/usenet/collect_usenet_bare.py --sample 400 --workers 8    # project first
uv run python scripts/sources/usenet/project_usenet_bare.py --journal data/raw/usenet_bare/<file> --archives 400
just usenet-bare                                                        # or the whole corpus
```

Limitation: 1,200 of the 42,139 pairs come from `comp.mail.maps` or `can.uucp.maps`, which
`ark.uucp` already parses under the `registry` lineage, so one posting can be read twice; every
evidence row names its group.

Coverage gap, measured 2026-08-30 by matching every `group` field in the journals against the
archives on disk: **9,050 of the 16,846 archives, 29.96 GB, are named in no `usenet_bare` journal at
all**, the largest being `alt.sex.erotica` 1.53 GB, `free.ucp` 674 MB, `us.politics` 646 MB and
`linux.kernel` 449 MB. That is unread bytes in an existing pipeline, not a new source, and it is the
cheapest unclaimed work in the register: no request, no terms, no new evidence class.

---


## `uucp_map_registry`, `uucp_map_creation`, `uucp_map_mention`: the UUCP maps

UUCP maps posted to `comp.mail.maps`, the `.CA` portion machine-generated from the Canadian domain
registry. On disk at `data/raw/usenet/comp.mail.maps.mbox.zip` (205,143,394 bytes), identical to
`https://archive.org/download/usenet-comp/comp.mail.maps.mbox.zip`.

```bash
uv run python scripts/sources/usenet/split_uucp_maps.py --write
uv run ark ingest uucp_listing  data/raw/uucp/uucp_listing.jsonl.gz
uv run ark ingest uucp_creation data/raw/uucp/uucp_creation.jsonl.gz
uv run ark ingest uucp_mentions data/raw/uucp/uucp_mentions.jsonl.gz
```

**Dating.** A registry-generated file declares its own provenance (`#R Automatically generated from a
.CA domain registration form`) and is regenerated from the live registration database at posting time,
so it takes the posting date: all 8,309 in-window registry postings carry an internal generation stamp
in the same year as their `Date:` header.

`artifact_listing` (posting date), `whois_creation` (registrar's `approved:`/`received:` date), lineage
`registry`. Hand-maintained maps are `link_target`, candidate-only.

---

## `rtfm_faq` and `rtfm_faq_mention`: the Usenet FAQ mirror

The `rtfm.mit.edu` FTP mirror, 19,478 FAQ documents under `pub/usenet-by-group`, at
`https://archive.org/download/ftp_rtfm.mit.edu_2014.07/2014.07.rtfm.mit.edu.tar` (1,691,248,640
bytes); the live host refuses connections.

```bash
tar -xf 2014.07.rtfm.mit.edu.tar -C data/raw/rtfm rtfm.mit.edu/pub/usenet-by-group
uv run python scripts/sources/usenet/split_rtfm_faqs.py --write
uv run ark ingest rtfm_dated      data/raw/rtfm/rtfm_dated.jsonl.gz
uv run ark ingest rtfm_candidates data/raw/rtfm/rtfm_candidates.jsonl.gz
```

**Dating.** The document's own `Last-modified:` / `X-Last-Updated:` / `Version:` revision header, not
`Date:`, which is the auto-reposter's stamp on the single copy rtfm keeps: of 12,318 documents carrying
both, 6,610 disagree, 3,296 with the repost later against 4 earlier.

`dated_directory`, after the corroboration split, because these URLs are prose typed by a human.
Lineage `usenet`.

---

## `ukwa_geoindex`: the UKWA Geoindex, found served after being closed as unreachable

Geographic index of the JISC UK Web Domain Dataset: every `.uk` resource the Internet Archive held for
1996-2013, each row a 14-digit timestamp, the URL, a tab, a postcode. 11,217,295,098 bytes over 12
members, CC Public Domain Mark 1.0:

```bash
curl -L -r 0-0 https://bl.iro.bl.uk/downloads/090bbffa-d82c-4641-ba72-0089e8ef885f
```

Extracted whole by `scripts/sources/ukwa/ukwa_geoindex_pull.sh`.

**Dating.** The row's own 14-digit IA capture timestamp prefix, a machine record of the capture, so it
dates that year and no other, and nothing in the file was typed by a human.

`cdx_timestamp`, master-eligible, no corroboration split. **Admitted master 2026-08-24** at 4,591
net-new pairs, 4,493.0 EE. `bl.iro.bl.uk` robots names ClaudeBot `Disallow: /`; `/downloads/` is allowed.

| closed | closing fact |
|---|---|
| rest of `bl.iro.bl.uk` | all 20,871 file_sets enumerated, the geoindex is the only web dataset; `woa1.zip` at 16.7 GB is 583 War Office photographs |
| DOI `10.5259/ukwa.ds.2/cdx/1` | the record has no file attached, one of 389 works in a 2021-10-20 bulk metadata import; cheaply re-testable |

## `iedr_register`: the archived IE Domain Registry register listing

The IE Domain Registry regenerated the whole `.ie` register as static pages at
`/statistics/<letter>-doms.html`, one per initial letter, captured by Wayback; 26 in-window pages hold
24,805 distinct `.ie` names.

```bash
uv run python scripts/sources/registries/collect_iedr_register.py
uv run ark ingest iedr_register data/raw/iedr/*-doms.html
```

**Dating.** Each page's own machine-written line, `updated automatically at 14:51 GMT on Friday, 21
December 2001`, stamped by the cron that rebuilt the register. A page whose own line falls outside the
window is dropped whole: `l-doms.html` resolves to a 28 March 2002 edition, and taking the capture
stamp instead would have imported 931 names into 2001.

`artifact_listing`, lineage `registry`: the registry stating which names were registered at a stated
instant, the same instrument as an InterNIC zone file. Nobody typed the list, so no corroboration
split, and it says nothing about any other year. **Admitted master by Ivo, 2026-08-24**, at 19,263
net-new pairs worth 18,769.9 EE. **829 of those come from the earlier `/lists/` tree, 812 at 1999 and
17 at 2000, and they carry a different footer**: a plain `Last updated 27 Nov 1999` rather than the
`updated automatically at 14:51 GMT on Friday, 21 December 2001` cron stamp the 2001 editions carry.
They rest on the page being a generated register listing, not on the word "automatically", which is a
weaker ground and is stated so it can be discarded separately.

`stalled.html` is excluded by filename before its date is read: it lists pending applications, names
nobody had registered.

---


## Closed by the reviewer himself, 0901 update

Ding's update of 2026-09-01 (Update_Log, 11:31:59 UTC+8) publishes his own negative
results: families "recorded as high-overlap, low-yield, noisy, incomplete, or otherwise
unsuitable for further work in their current form". They are register-closures of the
strongest kind, since the reviewer cannot be scooped by himself. Do not re-propose any of
them; where we hold our own verdict on the same family, both are cited.

| His closure | Our own verdict, where one exists |
|---|---|
| Internet Archive early-web language annotations | never tried here; CLOSED on his word |
| New Riders WWW Yellow Pages CD-ROM | never tried here; the curated-directory floor (0.013-0.024 pairs/listed domain) predicted this class dead, and his reading agrees |
| LAW / WebGraph WebBase-2001 | CLOSED here twice: novelty screen 2026-08-08, re-tested on the current screen 2026-09-01 at exactly 0 EE (all 603,245 registrable domains held AT 2001; SPEC names WebBase as an original-project source) |
| Dated Usenet archive copies | our Usenet channel is banked and its seams measured to saturation (bare-hostname arm projects 514-1,007 EE over all remaining archives); his "high-overlap" reading matches |
| Sampled scanned magazines | `trade_press` banked small here; prose-density ceiling 0.042 pairs/item documented 2026-08-2x |
| Specific raw ISC archive copies | `isc_survey` banked and closed for good here 2026-08-1x |

## Evaluated and rejected

| Source | Verdict |
|---|---|
| **The `uk.*` and `de.*` national Usenet hierarchies, priced on the year-hole rather than on TLD weight (2026-08-31)** | **CLOSED at 974 EE projected for 36.85 GB, and the finding is that the corroboration split has gone CIRCULAR on the whole Usenet channel.** Two disjoint fresh `uk.*` groups (`https://archive.org/download/usenet-uk/uk.net.web.authoring.mbox.zip`, 39,510,468 B, and `uk.local.geordie.mbox.zip`, 39,001,072 B; 78.5 MB, 165,462 messages, 84,435 in window) plus the 388.1 MB on-disk `de.*` probe. What dates one item is sound and is not the reason this closes: the message's own machine-written `Date:` header, quoted per pair with its `Message-ID`. Priced against the current baseline the uk probe reads **182 net-new post-split pairs, 123.83 EE, 1.5772 EE/MB, projecting 22,838 EE** and reproduces the hypothesis's 18,110 + 9,171 EE floor almost exactly. Priced against **merged260810**, the last reviewer cut before our own spool sweeps, **119 of 121 announce-extractor pairs (98.6% of the EE) lose their corroborating source**: those names were shipped by our own Usenet collector last round, and a new Usenet file now earns the same name a second year off it. Two independent proofs, both off files already on disk: baseline vintage (`nospambigfoot.com`, `nospam.ac.uk`, `deletethis.com`, `free.uk` are absent from merged260802-2 and merged260810 and first appear at merged260817-2), and direct provenance (**84 of the 118 net-new domains, 71.2%, a lower bound over three journal families, appear by name in our own `data/raw/usenet_bare`, `usenet_addr`, `usenet_dated*`**). `split_usenet.py` guards the candidate pool against this loop and does not guard `domain_year` one level up. Independent-split and garbage-stripped: uk **3 pairs, 1.36 EE, 0.0174 EE/MB, 252 EE** over 14.48 GB; de **67 pairs, 0.0777 EE/MB against merged260817-2**, decayed by the measured 0.458x baseline-vintage factor to 0.0356 EE/MB, **722 EE** over 20.3 GB. Second kill, independent of the first: the store-wide year-hole cells do not transfer to the population a discussion corpus names - measured **`.uk` 0.051 missing 2001 and 0.376 missing 1999, `.com` 0.092 missing 2001** against the 0.309 / 0.769 / 0.611 the hypothesis priced on, a 2.0x to 6.7x shortfall and 12.2x on `.org`, the magazine-archive law reproduced on two national hierarchies. Saturation was NOT the killer and the hypothesis was right about that: the two disjoint uk groups share 552 of 8,816 domains (6.3%) and lose 4.1% of EE on union; three extractors beat one by 1.59x self-corroborated, 1.25x independent. 27.2% of even the self-corroborated uk EE is demonstrable garbage - address-munging artifacts (`nospamblueyonder.co.uk`, `btnospaminternet.com`) and bare public suffixes (`ac.ie`, `com.hu`, `gov.gb`, `free.uk`), 9 of the 22 high-weight `.uk` pairs. Terms are clear (archive.org robots.txt is 12 lines, `Disallow: /control/` and `/report/` only, no ClaudeBot group; `licenseurl` and `rights` both null, same collection as the banked `usenet_bulk`), so this is a yield closure and not a permission one. Cost: 36.85 GB for 974 EE is ~68 EE/hour on download time alone against the gap queue's ~3,000, 44x misallocated. Method, four reusable pieces: (1) **test the corroboration split against a baseline cut from BEFORE our own collector last ran in the same channel** - any source whose split partner is a class we have already swept at scale will self-corroborate, and the failure is invisible in the headline, 22,838 EE one way and 252 the other from the same bytes and the same parse, a 91x spread; the two checks are greps over `feedback/*/merged*` and over our own journals and both ran in minutes. (2) Measure the year-hole on the corpus's own population, never on the store-wide cell. (3) **Grep `data/logs/` for a journal's `year_rows` before re-deriving anything** - the de probe had been fetched, parsed and ingested on 2026-08-21 and never priced, and `year_rows: 250` against `journal_lines: 7146` was the whole answer sitting in the ingest log. (4) A false zero caught in-flight: the journals write `"domain":"x"` with no space, so a pattern file built as `"domain": "x"` grepped 9.6 GB and returned a confident 0 of 118 - check the field spelling against one decompressed line before trusting a journal-wide grep. Residue, needing no new bytes: screen the 495 + 842 group names already in `data/raw/usenet_catalog.json` and `data/raw/usenet_de/meta.json` for `admin`/`news.groups`/`announce`/`newgroup` shapes, since a robot-posted registration feed is the `can.domain` class and not what this probe measured. Separately, every Usenet-lineage figure banked since 2026-08-17 should be re-checked for the same circularity. |
| **Open-relay and dial-up blocklists, priced on the record unit before the fetch (2026-08-31)** | **CLOSED at 0 EE, no fetch made.** ORDB, `relays.osirusoft.com`, `orbs.dorkslayers.com` and wirehub's dynablock/dialups all key on an IPv4 address (reverse-octet DNS or CIDR range), never a hostname, so no item in any edition can date a registrable domain; no dump of any edition survives (`advancedsearch.php`: `osirusoft` numFound 0, `ordb` 14, `orbs+relay` 5, all noise; `www.sdsc.edu/~jeff/spam/2002/` 404). Already closed once in docs/sources.md line 788 on a false premise (these lists are IP-keyed, not domain-keyed as claimed there); the correct kill is the record-unit check, run before any size or survival question. |
| **Web-archiving research group IA-derivative deposits beyond NYPW (2026-09-01)** | **CLOSED at 0 EE.** `archive.org/metadata` uploader field and `advancedsearch.php` against `open-research-datasets` (35,408 items) and `webwidecrawl`, plus 11 DataCite genre queries, found no second in-window CDX/TimeMap deposit outside NYPW; what dates one item is field 3 of a TimeMap row, IA's own 14-digit capture stamp (`cdx_timestamp`). |
| **Law 1 conditional test: is IA-derived net-new only on domains we've never queried? (2026-09-01)** | **FIND, 0 EE net-new banked, redirects an existing collector.** Measured against `data/raw/cdx/*.jsonl.gz` (1,137 journals) and NYPW's 1,306,611 pairs: what dates one item is the TimeMap's `cdx_timestamp`. Law 1 splits into discovery (holds absolutely: 14 of 652,853 domains, 0.002%) and completeness (false by two orders of magnitude: the real driver is capture year, not prior query history). | **CORRECTION, 2026-09-01 01:55, from the ingest ledger.** This run's `next` section told the operator to redirect the NYPW download queue to the 2001 folder, on a claim that `nypw_timemaps2001_deeplinks_part00o` paid 147,449 net-new pairs and 66,312 EE against 1,398 pairs for twelve 1999 and 2000 partitions, a 1,265x per-record ratio. **That is wrong by roughly four orders of magnitude and the redirect was not made.** `ark ingest` reports the 2001 part at **`'year_rows': 6`**, six, against 94,695 for `2000_rootURLs_part00r`, 42,757 for `part04r` and about 234,000 across the thirteen 1999 and 2000 partitions. Two other runs the same night agree with the ledger and not with this one: the re-opener measured that same file at 2.6 EE with 108,863 of 108,870 pairs already held, and the saturation fit put the 2001 folder at 0.1 EE/MB against rootURLs 376.4. The conditional itself, 0.1198 net-new on never-asked domains against 0.0320 on asked ones, is not disputed here and is worth keeping; only the partition recommendation is withdrawn. **The general lesson is the one CLAUDE.md already states: verify every number, including a subagent's. What made this one catchable is that the ingest ledger records `year_rows` per file, so any claim about what a partition paid can be checked against what it actually wrote, for free.**
| **Registry zone-file snapshots deposited as research data (2026-09-01)** | **CLOSED at 0 EE.** DataCite title/description phrase search (union index over Zenodo, figshare, Dryad, Harvard Dataverse, DRUM) and CAIDA's own catalogue found no in-window zone deposit anywhere; what would have dated one item is the deposit's own machine-written zone serial (`artifact_listing`), never reached because no artifact exists. Only pre-2002 zone archive anywhere is DNS-OARC's root zone (TLD delegations, not registrable domains, members-only). |
| **NYPW ingest saturation curve, fit by lane (2026-09-01)** | **CLOSED at 0 EE, a decision instrument not a source.** Fit over the ingest ledger's own per-file `year_rows` (14 points): no saturation yet (K pinned at scan-grid top). What dates one item is unchanged, field 3 of a TimeMap row (`cdx_timestamp`). Marginal rate is lane-dependent: rootURLs 376.4 EE/MB, deeplinks 130.6 EE/MB, the 2001 folder dead at 0.1 EE/MB. |
| **Register audit: which other closed corpus was priced on its most-saturated partition? (2026-09-01)** | **CLOSED at 0 EE.** Mechanical regex pass over all 369 closed leads in `data/logs/closed_register.txt` for slice-shaped verdicts (year folder/edition/part/shard): 27 name a slice, and of those only NYPW (already reopened) was measured on the retired novelty screen rather than the current held-and-missing-year screen. |
| **Novelty-screen reopen sweep, seven candidates from the retired screen (2026-09-01)** | **CLOSED at 0 EE new.** Read 84 pre-2026-08-25 closed verdicts; 7 rested on the retired novelty screen, of which 5 were already reopened and banked (NYPW, squidGuard), 1 is undated-fatal (`deduplicated_urls_*`), and Cybermetrics Wolverhampton repriced today at a 109 EE ceiling, killed by its own header (`UK 2002 database crawled July 2002`, out of window). |
| **HathiTrust Extracted Features, the 71 on-disk in-window volumes (2026-08-31)** | **CLOSED at 48.30 EE.** `data/raw/source_probe_260806/hathitrust_ef/`, 71 `*.json.bz2`. What dates one item: `metadata.pubDate`, the volume's publication year, `dated_directory`, takes the split. 84 net-new post-split pairs after stripping 2013-gTLD false positives from Usenet-shaped tokens, reproducing `sources.md:735`'s 49.4 EE within 2.3%; confirms the prose-density ceiling at its worst measured value (0.0147 EE/domain). |
| **IA/Alexa donated crawl indexes, access request probe (2026-08-31)** | **BLOCKED at 0 EE.** What dates one item: the CDX row's own `cdx_timestamp`, already master-approved, dies on access, not on dating. All six file-derivative classes (`.cdx.gz`, `.arc.gz`, WAT, parsed JSON) 401 at `https://archive.org/download/arc39.20001202212536/`, refusal declared at the parent sub-collection (`archive.org/metadata/alexa_ia`: "This data is currently not publicly accessible"), no `licenseurl`/`rights` published. Corrects the standing 226,901-item 1996 floor: that figure is the collection total, true in-window population is ~2,044 items by the `year` facet. |
| **UKWA geoindex re-read for a second attribute, and the publisher's copy found gone (2026-08-31)** | **CLOSED at 0 EE.** Full re-read of all 12 on-disk shards (288,187,351 B, 17,912,511 rows) reproduces `sources.md` exactly: 201,765 registrable domains, 289,857 (domain, year) pairs, **100.0000% already held, 0 net-new**. What dates one item: the row's own 14-digit IA capture timestamp (`cdx_timestamp`, already approved). The store now holds 96.40% of these domains at 2001, the corpus's own densest year, which is why it is exhausted. Separately: the publisher's DSpace-migrated copy (`https://bl.iro.bl.uk/server/api/core/bitstreams/35436f31-e29b-4da6-a364-7376f4813600/content`) is now a 1-byte stub against an original 11.2 GB, a batch-migration defect, so the on-disk extract is the only surviving copy. |
| **NU2 "What's New Too!" submission queue, the submitted half of the Nerd World pipeline (2026-09-01)** | **CLOSED at 0 EE.** `https://web.archive.org/web/19981212020109id_/http://nu2.com/` and `https://web.archive.org/web/20000306183340id_/http://www.nu2.com/perl/query?s=1358285&p=27`. What dates one item: the CGI's own per-entry stamp (`11 Dec 98` etc.), `artifact_listing`. 44 distinct domains sampled, 44 of 44 already held and already carrying the artifact's own year, held-and-missing-year = 0; no 2001 capture of the service exists in Wayback. |
| **Nerd World what's-new tree, second and third Wayback captures of the same parent FIND (2026-09-01)** | **CLOSED at 0.0 EE marginal**, plus a correction to the parked parent figure. Requests at `20011224` and `20011030` on `www.nerdworld.com/whatsnew.html` resolve to byte-identical captures (md5-confirmed); what dates one item is the SERVED capture stamp, and most of the 62 category pages actually serve at `20020227`, out of window, so the parent's 235.0 EE parked figure should be read as an upper bound of roughly 45 EE until all 62 served stamps are resolved. |
| **Regional and national what's-new feeds in high-weight namespaces, NZ and ZA (2026-09-01)** | **CLOSED at 1.0 EE.** Three feeds (`www.searchnz.co.nz/newsites.shtml`, `www.nzsearch.co.nz/newsites.asp`, `www.aardvark.co.za/cgi-bin/cb/AfricaSearch.cgi?go=new`), each dated by its own Wayback capture stamp (`dated_directory`). 66 distinct domains, 44 already held at 2001, held-and-missing-2001 = 1; a submission feed's names are either novel (take the split) or already carry the crawl's own year, unlike Nerd World's accumulated register shape. Expected-EE-per-held-name table by suffix included in `private/findings/`; `.ie` closed to future 2001-dated artifacts by our own IEDR ingest (0.0677 EE/held name). |
| **Retired-screen reopen sweep, availability recheck over 40 BLOCKED/offline-closed leads (2026-09-01)** | **CLOSED at 0 EE new.** `reprobe_closed.py --limit 40`; two hosts now answer 200 (PANDORA `webarchive.nla.gov.au`, USPTO `data.uspto.gov`) but both remain blocked on the artifact path itself (`pandora.nla.gov.au/robots.txt` still disallows `/pan`; USPTO still serves an Angular shell behind AWS WAF), so both closures hold. |
| **WebRing/RingSurf member rosters at the scale that could beat the roster law (2026-08-31)** | **CLOSED at 0 EE, no new fetch.** The million-member roster is unreadable: WebRing's member hostnames sit behind the `go.webring.org` redirector (closed 2026-08-05) and RingSurf's ring pages are 0 of 16 sampled archived. The only hub that prints member hostnames as text was already measured on 2026-08-27 at 20 rings / 500 hosts, 19.1 net-new EE (0.0481 EE/listed domain), so a million listed members would be ~48,100 EE on density alone but the retrievable inventory is in the thousands of rings, not a million members. Nothing left on WebRing, RingSurf or LinkExchange; the only live remnant is the pending `bomis_ring_member_lists` source. |
| **Registrar expiry-queue dumps beside namewinner, `https://web.archive.org/web/20011130182855id_/http://clubdrop.com:80/` and `https://web.archive.org/web/20011214232242id_/http://namewinner.com/whole_list.php?del=tab` (2026-08-31)** | **CLOSED at 0 EE.** eNom's Club Drop captures are byte-identical 481 B framesets pointing at an uncaptured target; namewinner's one residual capture is a genuine 0-byte HTTP 200; 415 links extracted from 12 other registrar homepages contained no drop-list path. What would date one item: a per-row machine-printed expiry stamp like namewinner's own `25-OCT-01`, but no sibling artifact survives to carry one. |
| **BIND resolver cache dump, `https://discmaster.textfiles.com/file/19864/ftp.cs.arizona.edu.tar/ftp.cs.arizona.edu/llp/univers.tar.Z/univers.tar/database/named/named_dump.db` (2026-08-31)** | **CLOSED at 0 EE.** The only in-window-shaped `named_dump.db` on preserved media is dated 1989 (`; Dumped at Tue Oct 24 17:41:26 1989`, BIND's own machine-written first line) and pays 0; discmaster's whole index has 7 `named_dump`-family files ever and none in 1996-2001. Even in window the class prices at ~96 EE/MB since 97.0% of its 168 domains are already held (a delegation graph, not a visitor long tail). |
| **USPTO trademark domain-mark filings, TRCFECO2 landing page and seven other USPTO/EUIPO/UK-IPO routes (2026-08-31)** | **BLOCKED, 0 EE, no artifact fetched.** All eight named routes are dead or refused: `data.uspto.gov` and `developer.uspto.gov/ds-api` serve only an Angular shell or a 301 to a retired product, `assignment-api.uspto.gov` has no A record, `tmsearch.uspto.gov` is a bare S3 bucket behind AWS WAF, EUIPO's robots.txt disallows the portal, UK IPO serves a captcha page, and Justia/Trademarkia/uspto.report all wall their robots.txt behind Cloudflare. What would date one item: the machine-filed application filing date, and on a 1(a) filing the date of first use in commerce; value re-priced at ~4,700 EE (not the hypothesis's 11,800) since the peak filing cohort lands on 2000, the store's best-covered year. Reopens only with a data.uspto.gov Open Data Portal API key. |
| **Per-domain parking/expiry notice pages, `https://web.archive.org/web/20020118000000id_/http://innersquare.com/` and 3 sibling captures (2026-09-01)** | **CLOSED at 0 EE.** What dates one item: nothing, the 4 notice pages found among 46 real bodies are undated templates ("recently registered", "coming soon", "under construction") carrying no stamp beyond the Wayback capture instant; namewinner already banked the paying shape of this population. |
| **UK government web index, `https://web.archive.org/web/20001101000000id_/http://www.open.gov.uk/index/orgindex.htm` (2026-09-01)** | **CLOSED at 0 EE.** What dates one item: the capture instant on the CCTA-generated index (`dated_directory`), but every reachable capture is 2000, not 2001; 926 distinct domains, 100.0% already held, 0.0% held-and-missing-2000. |
| **Netcraft SSL Server Survey, `https://web.archive.org/web/19991007040624/http://www.netcraft.com/surveys/analysis/https/1998/Jul/` (2026-09-01)** | **CLOSED at 1.3 EE.** What dates one item: the capture-stamped, month-labelled survey page (1998), but it publishes only aggregate counts and vendor share, never surveyed hostnames; the ~52 names present are CAs and server vendors, 2 net-new pairs. |
| **Nerd World "What's New" register tree, `https://web.archive.org/web/20011030063818id_/http://www.nerdworld.com/whatsnew.html` (2026-09-01)** | **FIND, 235.0 EE, parked pending (fails standing-rule condition 2).** What dates one item: the Wayback capture stamp on each category page (`dated_directory`), machine-written but not printed inside the artifact itself; 399 of 2,059 distinct domains held-and-missing-2001. |
| **W3C/WDG validator and link-checker "recent checks" pages, 8 front-page replays from `validator.w3.org` and siblings (2026-09-01)** | **CLOSED at 0 EE.** What dates one item: nothing, no such artifact exists. Full link sets of 6 replayed HTTP 200 front pages read whole; no "recently validated" or submission-queue page was ever published. |
| **Novelty-screen reopen re-verification, 100 closed verdicts against the current screen (2026-09-01)** | **CLOSED at 0 EE.** Independent re-read of 100 verdicts (60 pre-08-25, 30 recent, 40 availability-closed) found 0 reopenable beyond the already-banked L657 sweep. Corrects one closure: `https://archive.org/download/inktomi_arc39.20011005194750-c/inktomi_arc39.20011005194750-c.cdx.gz` is access-blocked (HTTP 401), not absent; dated by its own `cdx_timestamp`. |
| **Novelty-screen reopen re-verification, 393 closed leads via `screen_hypothesis.py --list-closed` (2026-09-01)** | **CLOSED at 0 EE, third same-day confirmation.** 54 pre-08-25 verdicts read in full, the rest scanned for retired-screen wording; 0 rest unresolved. Repriced the one deferral neither sweep had touched, Khoj non-English portals (445 EE on 2026-08-08), on the current screen: 0.0 EE net-new post-split. |
| **Spinellis CACM/IEEE Computer URL-decay resolution log, `https://www.spinellis.gr/sw/url-decay/log.rl.20000530.gz` (2026-09-01)** | **CLOSED at 16.5 EE.** What dates one item: the log's own per-record stamp, `date Tue, 30 May 2000 00:15:42 GMT`, inside `log.rl.20000530.gz`; 7 pairs / 4.9 EE at 2000 plus 16 pairs / 11.6 EE across 1996-1999 by citing-article year, 98.6% of hosts already held at the log's own year. |
| **`.au` registry web presence, 26 archived pages across auDA, AUNIC, Melbourne IT, INA and `www.gov.au` (2026-09-01)** | **CLOSED at 1.0 EE.** What dates one item: nothing, no `.au` registry page in window carries a name list; 18 distinct names recovered, 15 already held, 1 net-new post-split pair. Adjacent-year ceiling measured separately at 30,598 EE, 83.3% of it in `com.au`, which the registry operator never published as a list. |
| **Archived ISP customer DNS zones, discmaster filename sweep for `named.conf`/`named.boot`/`*.zone`/`*.hosts`/`httpd.conf` (2026-09-01)** | **CLOSED at 0 EE.** What dates one item: nothing found; a real zone file would carry a machine-written SOA serial plus the enclosing tarball's mtime. All in-window hits across five signatures are vendor-shipped distribution files, 0 operator configs at any date. |
| **Archive Team's "List of websites excluded from the Wayback Machine" plus its three subpages, `https://wiki.archiveteam.org/index.php?title=List_of_websites_excluded_from_the_Wayback_Machine&action=raw` (2026-09-01)** | **CLOSED at 0 EE.** What dates one item: nothing, every entry is dated by when a community member observed the exclusion (2018-2026), not by when the domain existed; 107 of 2,171 distinct domains held-2000-missing-2001, a 57.74 EE ceiling that is unreachable because no entry carries an in-window date. |
| **ODP content dump re-dated to 2001 as a counterfactual test of the frozen-mirror instruction, `data/raw/odp/c2000.gz` (2026-09-01)** | **CLOSED, 136.2 EE counterfactual ceiling only, not bankable.** What dates one item: nothing new, the same 12,222 names the store already holds, re-dated to 2001; confirms aiming at 2001 helps (0.0 EE at 2000 vs 136.2 EE at 2001) but the 32x curated-directory discount does not transfer to head-selected link corpora. |
| **The two on-disk capture indexes read as artifact FINDERS, ranking by URL PATH and stored byte LENGTH (2026-08-31)** | **CLOSED at 0.98 EE measured, and the instrument was refuted on its own kill screen at ZERO requests, because neither corpus holds the column the hypothesis reads.** `data/raw/early_web` (224 CDX shards, 184,858,264 B, 4,383,611 rows) is a homepage-only index: across 5 shards and 1,329,118 rows sampled, **60 rows (0.0045%) carry any path at all** and every one is junk (38 bare-IP `/?`, three `..` traversals, no filename anywhere), while over all 224 shards the `length` column has **ZERO rows above 100,000 B**, max 88,933, so the corpus caps out **6.5x below** the 581,560 B namewinner artifact this finder was built to locate. `data/raw/ukwa` (12 shards, 288,187,351 B, 17,912,511 rows) has paths but **no length column at all** (`timestamp/URL<TAB>postcode`), the exact blind-ranking defect the hypothesis claimed to fix; the twelve dump signatures matched on path return **30 rows over 17.9M, 28 of them one host**, `www.latto.uk.btinternet.co.uk`, a hobbyist BT Internet page, the other two false positives (an order form, a charity coin appeal). Three probe fetches turned the projection into a measurement: the candidate's three largest pages are 8,463 + 4,339 + 5,501 B (namewinner is 70x larger) holding **302 distinct registrable names, 298 of them `.co.uk`**, of which **3 are held in any year (0.99%)** and **1 is held-and-missing-2001** (`flexor.co.uk`), so **0.98 EE**, and all 8 pages extrapolate to ~2.6 EE, two orders below the 200 EE stop line. Dating was never the problem: the CDX capture timestamp is machine-written and sound, but the names on the page are human-typed speculator inventory and take the split. Method, three reusable pieces: (1) **check that the column exists before pricing the instrument that reads it** - two `awk` passes over bytes already held killed both arms in under two minutes; (2) **0.99% held is a new floor on the pre-download discriminator scale**, below even forged-header spam (~5%), because a coined-name for-sale inventory is names *bought and never used*, the one population guaranteed to have no capture, so **any "someone listed domains for sale" hypothesis now prices at ~1 to 30% held before it is proposed**; (3) **a path-signature finder is bounded by its corpus's SUBJECT, not its size** - UKWA is a UK postcode geoindex, so 17.9M rows return 28 paths on 3 hosts. Do not rebuild a path or size finder over either corpus. The parked condition-2 FIND at the WebBase route keeps its 290 unscreened candidates at roughly 1,900 EE and remains the only live arm in this family; this run neither adds to it nor re-prices it. |
| **PANDORA's dated instance table, the NLA's own selective-harvest stamp (2026-08-31)** | **BLOCKED at 0 EE, closed on access.** Offline pricing of the 35,391 hosts already on disk (`data/raw/pandora-titles/pandora_hosts.txt`) gives a 943 EE adjacent-year ceiling even before any fetch, but every route to the dated instance table is shut: `webarchive.nla.gov.au` answers a blanket 403 to `/robots.txt` and the TEP endpoint; `pandora.nla.gov.au/robots.txt` disallows `/pan`, the instance path itself; `web.archive.org.au/robots.txt` is now `Disallow: /`, a new closure superseding the standing note at line 347; Trove API v3 needs a key we do not hold. What would date one item is the PANDORA instance stamp `/pan/<id>/<YYYYMMDD-HHMM>/`, a machine-written crawl fetch time, but it is unreachable. |
| **URLMerchant's for-sale inventory, continued past its first 244 pages (2026-08-31)** | **FIND, in progress: 606.3 EE measured over 974 net-new post-split pairs from 95 pages fetched at 2001 captures, projecting to 9,254 EE across the full ~1,562-page namespace; still fetching.** Same artifact and method as the banked `urlmerchant_inventory` row (1,591.9 EE over 244 pages): distinct-domain sampling gives 13.6% held, 75.6% of held missing 2001, mean weight 0.6225. What dates one item is unchanged, the page's own `<META NAME="UPDATED">` generator stamp. The 1999/2000 captures of the same namespace are unmeasured and would add a second and third year to the same held names at no new discovery cost. |
| **The banked 2001 squidGuard tarball's never-opened `urls` lane, the per-path file disjoint from the `domains` file by construction (2026-08-31)** | **CLOSED at 7.59 EE measured over the full population, against the hypothesis' 1,200 EE half-rate floor and 2,538 EE central estimate, for zero bytes fetched.** The artifact was already on disk at `data/raw/squidguard2001/bl/blacklists/` from the banked `squidguard-1.2.0/samples/dest/blacklists.tar.gz` (1,558,996 B): **ten** base `urls` files (not eleven; `mail` has `domains` but no `urls`) totalling 29,806 non-comment host-lines, plus **113** dated `urls.*.diff` lanes (not 112) spanning 2001-08-13 to 2001-12-18, 67,760 host-lines. What dates one item is the compiler's own machine-written stamp at the head of every base file, `# This list was compiled in 0:00:20 on 2001.12.18 15:04:29.`, plus the per-date `urls.YYYYMMDD.diff` filename; the class is already approved for this exact tarball, so dating was never the issue, headroom was. Priced against `data/exports/{1996..2001}.txt` (25,747,724 pairs, regenerated 2026-08-31), sampling **distinct registrable domains** through the repo's own PSL path: 29,806 base host-lines collapse to **5,910** registrable, **100.0% held and 100.0% already held AT 2001, net-new 0**; the base is a strict subset of the diff set, so the combined population is **6,612** registrable, 99.95% held any year, **99.77% held at 2001**, 3 novel names earning no year, and **held-and-missing-2001 is 12 domains, all `.com`, 7.5852 EE**, none of them in the candidate arm. The block's structural claim was CORRECT and did not help: **5,640 of the 5,910 base `urls` domains (95.4%) are absent from the banked `domains` base**, so this genuinely is the population the domains file cannot contain; it is novel to the ARTIFACT and not to the STORE. Its arithmetic error was borrowing the `domains` file's 84.8% known / 57.9% held-at-2001 rates for a differently selected population, and projecting 9,400 registrable from 10,273 hostnames when 29,806 host-lines collapse to 6,612 (paths repeat hosts ~4.5x). Method, two reusable results: (1) **price against `data/exports/*.txt` with `LC_ALL=C comm`, not the duckdb** - the 28.4 GB store was write-locked by a running collector and refuses even `read_only=True`, while the six sorted per-year exports total the store's own pair count and give exact held / held-and-missing-Y sets in seconds, so any parallel run blocked on that lock should do this instead of waiting; (2) **the head/tail screen discriminates WITHIN one artifact, not just between artifacts** - same tarball, same edition, same compile stamp, same evidence class, and the `domains` file paid 10,736 EE at 57.9% held-at-2001 while the `urls` file pays 7.59 at 99.77%, a **~1,400x spread with the date held constant**, because squidGuard lists a host in `urls` only when it is NOT blocked wholesale, i.e. it is mainstream with one bad path (`microsoft.com/ie` and `cnnsi.com/features/...` are the first two lines of the first diff lane). **Generalisation: "disjoint from the part we banked" is not evidence of headroom, and when the disjointness rule is itself a popularity filter it is positive evidence AGAINST it.** Ask which side of the head/tail split a sub-file's selection rule puts it on before pricing it. Not re-tested; the two further 2001 editions on disk (`data/raw/probes/squidguard/ed_jul`, `ed_sep`) are same-family and same-year and expected near zero, and only the two unopened `data/raw/squidguard_contrib_2001/` tarballs have a different compiler. |
| **squidGuard's 1999 editions, the proven blocklist family re-aimed at the year its sweeps predate (2026-08-31)** | **CLOSED at a 71.8 EE gross pre-split ceiling against the hypothesis' own 4,000 EE floor, for zero bytes of the target downloaded.** The decisive number needed no fetch, because the family's banked 2001 corpus is already on disk: `data/raw/squidguard2001/squidguard-1.2.0/samples/dest/blacklists.tar.gz` (1,558,996 B), eleven base `domains` files, 74,697 hostnames collapsing to **34,091 distinct registrable domains, 100.0% held** since the 2001 edition was banked. Priced at the candidate year 1999 on both screens against the live 25,749,045-pair store, sampling distinct domains and never `domain_year` rows: naive held-any-year-and-missing-1999 **18,990 domains / 11,059.5 EE**, adjacent held-1998-and-missing-1999 **110 / 66.1 EE**. The naive screen, which is exactly what the hypothesis' store-wide U-shape table produces, **overstates this population's 1999 headroom 167x**. Full adjacent profile on the same domains: 1997 87 / 53.1, 1998 224 / 133.2, 1999 110 / 66.1, 2000 116 / 68.0, 2001 12 / 7.0 (the last reads low because the edition is banked). The residual arm answers the "an earlier edition holds names 2001 had dropped" objection rather than assuming it: the 2001 per-date diff removal lanes give 3,505 removed hostnames, 2,426 registrable domains, 2,211 absent from the base, 2,081 held (94.12%), adjacent screen **1999 9 domains / 5.69 EE**, 2000 6 / 3.79, 2001 389 / 237.70. Base plus residual is 71.8 EE gross, and a 1999 edition takes the corroboration split on top, so the hypothesis is out by 56x on its own population before the split. **The artifacts are named but not captured, so the arm was dead twice over.** The origin archive `http://ftp.ost.eltele.no/pub/www/proxy/squidGuard/archive/`, read through the Wayback renderings of its 2001-05-02 and 2001-12-25 captures (6,199 B and 6,382 B), lists twenty-two tarballs of which fourteen are 1999-dated, `squidGuard-0.9.3` 04-Jan-1999 through `squidGuard-1.1.0.tar.gz` 24-Aug-1999. `archive.org/wayback/available` returns `{"archived_snapshots": {}}` for **every one tested** (1.1.0, 1.1.0.beta1, 1.0.0, 0.9.11, with and without the `:80` host form): the payloads were never archived and only the listing survives, which is measured law 2 in its plainest form. `squidguard.org` resolves to 78.47.242.83 and refuses connections on 80 and 443, direct and by IP with a `Host:` header, so no robots.txt was readable and nothing beyond the three failed connects was attempted. The distro route is independently dead: `archive.debian.org` (no robots.txt, HTTP 404) has no squidGuard source package in slink or potato, `dists/slink/main/source/Sources.gz` 189,664 B and `dists/potato/main/source/Sources.gz` 330,421 B carry `squid` and `junkbuster` and no `squidguard`, confirming the package entered Debian only at woody. **Nothing dates a 1999 edition anyway**: the 2001 win rested on a machine-written liveness header (`# This list was compiled in 0:00:20 on 2001.12.18 15:04:29.` from `squidGuardRobot-2.3.4`) and took no split, while the only 1999-stamped artifact the family ever produced, `test/blacklist/domains` in squidGuard-1.1.4, carries no compile header at all, so it is a listing, takes the split, and the register already measured it at 8.5 EE. **Method, and it is worth more than the source was.** First, **price a candidate year on a corpus you already hold from the same family before fetching the candidate**: a blocklist recompiles a rolling artifact, so the edition in hand is a near-superset of the edition wanted and the store's coverage of year Y is measurable on it offline. Second, **the removal lanes of a dated diff series bound what an EARLIER edition could hold that the later one does not**, which prices an artifact that no longer exists. Third, and the reason the arm looked alive: **P(store lacks Y \| held) is a store-wide population average and inverts on a blocklist**, whose names were registered in the two years before its compile, so the store's 1999 gap over them is non-existence rather than missing data. The register had measured this once on `chastity-list` at 141x; this run reproduces it independently on squidGuard at **167x**, on a different population, which promotes it from a finding to a law. 12 requests total (2 archive.debian.org, 3 failed connects, 3 Wayback renders, 4 `wayback/available`), no `web.archive.org/cdx` call. The family now has a year profile from four independent copies, 1999 = 8.5 EE measured and 71.8 EE ceiling, 2000 = 17.2 EE, 2001 = 10,736 EE. **Do not re-test squidGuard, MESD or chastity at any year before their own compile.** |
| **Ranking the metered CDX gap queue by `evidence.source`, the channel that first contributed each candidate name (2026-08-31)** | **CLOSED at +26 EE/hour against the hypothesis' own 300 to 500 EE/hour floor.** Zero requests: a retrospective join over journals already on disk (2,957,495 CDX rows, 33,778,743 RDAP rows) and a read-only `ark.duckdb`. This is a RANKING SIGNAL, never a source: `evidence.source` evidences no year and any pair produced is still `cdx_timestamp`. **The kill screen passed and the hypothesis died anyway.** P(walk returned 2001 \| held 2000, RDAP 200) runs `ukwa_link_source` 0.9892 down to `uucp_map_registry` 0.5096, survives TLD stratification, and survives conditioning on liveness, so it is genuinely independent (live `.com`: `prior_task` 0.8621 n=95,562 against `isc_survey` 0.7315 n=7,952, difference 0.1306, se 0.00509, **z = 25.6**). It is worth nothing because **the queue it sorts has no entropy in it**: the 6,561,914-name held-2000-missing-2001 queue is **84.272% `prior_task`** by construction, since a name is in it precisely because the baseline held it at 2000; five sources cover 97.9%, and the high-rate sources are rounding error in it (`dartmouth_nber` 294 names, `ukwa_link_source` 603, `tucows_mention` 0). Priced over the 3,428,184-name `.com` queue with tied key-groups consumed proportionally: mixed 0.4515 EE/query, source alone 0.5137 at N=200k decaying to 0.4589 at N=2M, the already-banked RDAP-404 prior 0.5320 flat, RDAP then source fitted within stratum 0.5450, **oracle 0.5451**. Source therefore adds **+0.0130 EE/query = +26 EE/hour** over the free signal already in hand, and that equals the oracle increment to three decimals, so **+26 EE/hour is the ceiling on ALL reordering of this queue by any key whatsoever**: the run retires the family, not one member. The naive version is a measured **-34 EE/hour regression**, worse than not sorting, because pooled per-source rates invert under Simpson's (`prior_task` pooled 0.7002 against live-only 0.8621, so the pooled key ranks 63,693 `rdap_snapshot` names above 1,510,081 `prior_task`-live ones, and in the dead stratum the order really does flip, `usenet_mention` 0.6632 over `prior_task` 0.5039). Over the 2,652,431 RDAP-uncovered names, adding source scores 0.2878 against 0.6052 for the TLD sort already shipping. Method, four reusable parts: **screen any proposed queue key with one `group by` over the queue BEFORE measuring its lift**, because a signal's value is bounded by its entropy over the population being RANKED, not its spread over the population it was measured on; **price a new ranker against the best ranker already banked, never against unsorted**, which turned a headline +124 EE/hour into an honest +26; **compute the ORACLE**, one extra line that converts "this key adds little" into a family closure; and **consume tied key-groups proportionally**, since deterministic tie-breaking swung a figure 10% on row order alone. The pipeline was validated end to end by independently repricing the banked RDAP prior at +0.0805 EE per `.com` query, matching `sources.md` to four decimals. Do not re-test queue reordering here by any key. If a source key is ever wired in, fit it within the RDAP stratum. The one real lever left on this queue is the 979,647 uncovered `.de` names at ~0.121 EE/query, which need a non-Verisign liveness oracle and not an ordering. |
| **CDX bare-gTLD keyless/pagination shapes, refuted by the server README (2026-08-31)** | **CLOSED at 0 EE, zero CDX requests.** All three proposed bypass shapes (`matchType=range`, `resumeKey` alone, `page`/`pageSize` alone) require the `url` key per `wayback-cdx-server/README.md` (line 73), so the bare-TLD 403 is unavoidable; what dates one item is the existing `cdx_timestamp` master class, unaffected by this closure. https://raw.githubusercontent.com/internetarchive/wayback/master/wayback-cdx-server/README.md |
| **CDX suffix-weighted pseudo-registry namespaces (`uk.com`, `eu.org`, etc.), priced offline against the canonicalizer (2026-08-31)** | **CLOSED at 1.09 EE ceiling on the 19 bare pseudo-suffix domains (17 of 19 already complete 1996-2001), zero requests.** `to_registrable` collapses all 1,578 PSL-private gTLD-rightmost suffixes to their bare suffix name since `include_psl_private_domains` is off in `src/ark/canonical.py:42`, so no third-level name under them reaches the scorer; what would date one item is `cdx_timestamp`, unreachable under the current pipeline. No artifact fetched. |
| **A sibling bulk domain-year projection of the Wayback index, hunted through the release that produced `dartmouth_nber/domain-year-captures.txt` (2026-08-31)** | **CLOSED at 0 EE: the sibling does not exist.** `https://archive.org/metadata/DARTMOUTH-NBER-RESEARCH-2017-metadata`, 13 files, `domain-year-captures.txt` 227,919,677 B byte-identical to the on-disk copy. What dates one item is sound and unchanged: a row is the archive's own count of captures of that host inside that calendar year, machine-written by the crawler, so it self-dates as `cdx_timestamp` with no split. The family dies on existence. Zero-fetch on the local copy (9,227,380 rows, 840,963 distinct hosts, 765,194 in-window rows over 315,258 in-window hosts, 309,721 at 2001, 95.85% `.com`) plus 7 requests, ~130 KB. Held fractions are **0 net-new by construction** and are not quoted as a measurement, since the file is fully ingested as `dartmouth_nber_captures` (142,084 EE banked) and `sources.md:875`'s zero on the siblings is the same by-construction zero. New here: the CONTAINING COLLECTION was enumerated whole rather than the item's file list. `collection:webdataservices` 797 items split mechanically on `(ARCS-|WARCS-|_arc$|_warc$|-ARC_|-WARC_)` into payload and **126 readable items, all accounted for and not one a second in-window domain-year projection** (ParaCrawl, scholar_corpus, sandcrawler, web_pdf_grobid, Europat all 2019-2022; NHK a 2020 `.jp` extraction; the `*-cdx*` items index the ARCS closed at `:786`; `Dartmouth_10KwebURLs..._BFS_4-lvls` already banked as `dartmouth_bfs_seed`; `earlygovweb` closed at `:891`; the rest meeting recordings and LOCKSS). `CorporationWebsitesCollection` 659 items, and `collection:webarchivedatasets` re-verified at 8 through `advancedsearch.php`, so the 2026-08-08 count is a real zero and not a `scrape` lie. `Poland_pl-ccTLD_2001-12-31` is `access-restricted-item: true` with a 44,678 B shell and no payload. Third-party projections re-confirmed closed: Common Crawl earliest 2008-05, SNAP anonymised, WebBase 2001 at 603,245 domains all held and all already carrying 2001, academictorrents 0, Alexa/IA donated CDX HTTP 401. **The finding is a correction**: `sources.md:209` called the file a census of "every host the Wayback Machine then held". It is not. The item's own title is "CorporationWebsitesCollection Manifest and Metadata" and it ships its seed files (`dartmouth_research_companies.txt` 8,546 B), so 840,963 hosts at 95.85% `.com` is a US corporate-website SEED sample, three orders short of a census. That changes the ceiling from arithmetic to existence: a different-seed sibling of the same size is worth **~12,700 EE** at the head-type rate of 0.041 EE per name and ~119,500 EE at the population rate, so this is a five-figure family that was simply never published. Method: **enumerate the containing collection, not the item's file list, and split payload from projection mechanically** (one `advancedsearch.php` call at `rows=1000` plus one regex cut 797 items to 126), and **read the item's `title` and `collection` rather than its filenames to recover the SEED**, one free field that refuted a two-week-old description and decides whether a sibling can exist at all. archive.org robots.txt read whole at 238 B, `Disallow: /control/` and `/report/` only, no by-name group. Do not re-enumerate these three collections, the Dartmouth item or its ARCS/WARCS/CDX children, and do not re-probe Common Crawl, SNAP, WebBase, academictorrents or the donated CDX. Two things reopen it: a bulk host-year table whose seed is demonstrably disjoint from a US corporate list, or an access grant on the Alexa/IA donated crawl indexes, the one place a different seed is known to exist and is refused rather than absent. |
| **A whole-tree truncation audit of `data/raw`: which banked artifact was ingested at a fraction of its published size (2026-08-31)** | **CLOSED at 6.87 EE that exists right now in bytes already on disk, ~41 EE projected for the whole gap and ~99 EE as a generous ceiling. The hypothesised failure mode is REAL, three items wide and wholly unread; it is simply worth nothing.** Audit, no requests for three of four arms, over 161 GB: **gz** 9,104 files / 21.79 GB `gzip -t`, 68 fail and **zero are new** (64 are our own journals and probes, the other four are the known UKWA 2 GiB ceiling, the known truncated `odp/c2000.gz`, one "trailing garbage ignored" maillist whose payload decompresses whole, and a 172 B error page saved as `alexa/*.cdx.gz`); **plain files** every byte count `sources.md` quotes matches to the byte, with one bad QUOTE not a bad file (`ukwa/york-ac-uk-linkage.tsv.gz` 2,243,171 on disk against 2,244,274 written down, `gzip -t` passes) and 234 of 235 `checksums.sha256` verifying, the miss being a deleted file; **zip** 16,886 files / 113.13 GB, central directory plus a declared-extent test (`header_offset + 30 + len(name) + compress_size > filesize`), **zero extent overruns**, 81 fail to open, 4 probe stubs and 77 Usenet mboxes. **The find:** 13 `archive.org/metadata/<item>` calls give the publisher's own byte count for all 23,885 Usenet archives held, and **77 files are short by 677,197,467 bytes total** (`usenet-microsoft` 55 / 625.4 MB, `usenet-bit` 6 / 35.2 MB, `usenet-free` 16 / 16.6 MB; every other item matches file for file). 62 of the 77 are **170-byte HTML error pages saved under the archive's name**, the largest standing in for 123,533,643 bytes, curl reported 200 and nothing downstream ever looked, and not one of the 77 groups has a row in `evidence`. **Why it dies:** the affected corpus is `microsoft.public.*`, 92.4% of the shortfall, and it fails both screens at once. The seven recovered partials raw-inflate to 514,117,377 B of mbox, 204,463 messages, of which 17,105 (8.4%) are dated 1996-2001 and all 17,105 come from ONE group; five of six microsoft partials hold zero in-window messages. Those give 5,537 gross pairs over 4,602 domains, **99.41% already in `domain` and 94.35% independently dated, so 13 net-new pairs = 6.87 EE**, 0.000402 EE per in-window message. Dating was never the problem: the `Date:` header of the carrying message, `dated_directory` after the corroboration split, identical to `usenet_announce`. **Control, which is what makes 6.87 trustworthy:** the same extractor over 12 randomly sampled INTACT `microsoft.public` archives (144,645,021 B, 6,924 gross pairs, 5,673 domains, 100.00% held) returns **1 pair / 0.13 EE**, and ten of the twelve already carry evidence rows, so ~0 is the right answer and the extractor reproduces it rather than inventing headroom. **Sampling note, a 1,900x error if you get it wrong:** that same sample reads **433 pairs / 251.5 EE** when "held" means a row in `domain`, against **1 pair / 0.13 EE** when it correctly means an existing `domain_year` row; `domain` is full of candidate-pool names discovered and never dated. **False lead killed in passing:** the metadata diff also shows 7,200 archives / 211.2 GB published and absent from disk (`usenet-alt` 6,013, `usenet-comp` 1,187). Do NOT re-download it: 370 of 400 sampled absent `usenet-comp` groups and 336 of 400 `usenet-alt` groups already carry evidence rows, so the pool was processed and reclaimed exactly as the `uucp` entry says. Absence from disk is not absence from the store. **Method, and it is the point of the run.** (1) A whole-tree ZIP audit is two seeks per file: 16,886 archives and 113 GB finished in under two minutes; add the declared-extent test to catch a surviving central directory over missing member data. (2) **A publisher's own file listing is one request and it is the complete truncation audit.** `archive.org/metadata/<item>` versus `os.path.getsize` is the only instrument that sees a 170-byte error page written where a 123 MB archive should be, because that file is internally consistent and reported 200; `ls -l` against quoted sizes, which is what this hypothesis proposed as its kill screen, cannot find it. (3) **A truncated ZIP is readable with no refetch:** `zlib.decompressobj(-15)` on the bytes after `30 + name_len + extra_len` recovered 514 MB from 114 MB of stubs in six seconds, same reason the CRC-truncated `.gz` journals recovered. (4) **Price a truncation by inflating what is on disk before deciding to refetch**; six seconds here replaced a 677 MB download with a measured 41 EE. (5) For any Usenet or mailing-list corpus the pre-download discriminator is the in-window message share, not the byte count. `archive.org/robots.txt` read in full first, 238 B / 12 lines, restricting only `/control/` and `/report/`; `web.archive.org/cdx` was not touched and the 677 MB was deliberately not fetched. Closed. Two threads left, neither worth an hour: wiring raw-inflate into the Usenet reader banks the 6.87 EE for zero requests and un-breaks the 62 stubs on any future refetch, and if anyone wants bytes back, take only `usenet-bit`, 6 files and 35.2 MB of BITNET listserv groups whose era is the window, and leave the 625.4 MB of post-2001 `microsoft.public` where it is. |
| **The 2001 ODP full content dump on the off-hosts the original CDX key could not match, `rdf.dmoz.org` / `ftp.dmoz.org` / `download.dmoz.org` / `dirt.dmoz.org` (2026-08-31)** | **CLOSED at 0 EE on CONTENT and independently on EXISTENCE.** The premise was that `url=dmoz.org/rdf/*` was the wrong key; it was the right key. `dmoz.org/rdf.html` captured `20011130144434` links every dump as `/rdf/*.u8.gz` on dmoz.org itself, names no mirror and asks nobody to mirror. **Existence:** Memento timemaps (2 requests) show `content.rdf.u8.gz` at 2000-08-15 then 2017+, and `content.rdf.gz` at 2000-03-03, 03-04, 08-15 then 2021+, so **no 2001 capture of a full dump exists under either name**; `rdf.dmoz.org` first capture is **2005-09-07**, and `ftp.dmoz.org`, `download.dmoz.org`, `rdf.dmoz.com` and `dmoz.org/rdf-data/` have zero captures at any path over 54 URL x pin probes. The real off-host `dirt.dmoz.org`, found because the March 2000 records are 193-byte 302s pointing there and the file stamps itself "on dirt", has an **empty timemap on all three paths** against a same-session control that returned rows. And the absence is documented rather than merely unobserved: `dmoz.org/rdf/` captured `20011124192030` is an Apache index listing `content.rdf.u8.gz 19-Nov-2001 22:53 166M` and `content.rdf.gz 11-Jan-2001 22:38 131M`, so **IA crawled the index and never the files**. **Content, on the one genuinely new artifact recovered:** `content.rdf.gz` @ `20000815092656` is a different edition from the held `c2000.gz` and was never ingested, stamping itself `<!-- Generated at 2000-07-18 20:18:21 GMT on dirt -->` against the held file's `2000-08-07 08:00:40 GMT`. 17,472 `<ExternalPage>` elements, all in window, sampled as DISTINCT DOMAINS: 11,497 domains, **11,493 held (99.97%)**, and **held AND missing the artifact's own year 2000 = 0 of 11,493 = 0.00%**, so net-new post-split is **0 pairs / 0.0 EE** (pre-split 4 pairs / 1.1 EE, do not quote; raw overstates 10,672x). ODP's head is exactly what the store already covers at 2000. Dating is sound and not why it dies: the dump's own machine-written `<!-- Generated at YYYY-MM-DD HH:MM:SS GMT on <host> -->` in the first 200 bytes, class `artifact_listing`, no split. Consistent with two prior negatives from other directions, gwdg's 8.8 GB locatedb and discmaster's 1.72 billion indexed files both returning 0 for the `content.rdf` signature. **Method.** (1) **A Wayback `Content-Length` is not evidence of retrievability**: this record advertises 120,173,315 B and delivers 1,048,293. Two different ODP editions truncate at the identical 1,048,293 B, which identifies a **~1 MiB payload cap in the 2000-era crawler**, so any pre-2001 capture of a multi-megabyte file is a 1 MiB prefix and a range request past the cap returns nothing; treat an exact 1,048,293 as the cap's fingerprint, not a failed download. (2) **`web.archive.org/web/timemap/link/<url>` is a complete capture enumeration in one request and is not the metered `/cdx` path**, while `archive.org/wayback/available` returns only a closest snapshot and so turns silence into a false proved-absence; timemap listed the March 2000 records that `available` hid, and those exposed `dirt.dmoz.org`. Screen with `available`, decide with timemap, keep a known-captured control in the same session. (3) **Read a dead host's archived Apache `Index of /` before concluding a file never existed**: one request converts "we cannot find it" into the operator's own filename, size and Last-Modified. (4) **When a recipe names one of several sibling filenames, price the siblings**; the `.u8` and raw `.gz` twins are different captures on different days with different generation stamps and only one was ever read. Closed: do not re-test any ODP or DMOZ dump route, on-host or off, raw or `.u8`, and not the 2001 full dumps. One residual belongs to another lens: the 1 MiB cap means every large in-window file held may be a prefix, so `data/raw` is worth sweeping for artifacts of exactly 1,048,293 bytes. |
| **A RIPE database edition dated 2000-2003 carrying forward `.de` `domain:` objects (2026-08-31)** | **CLOSED at 0 EE on CONTENT and again on EXISTENCE, both settled for zero downloads.** The prize was priced first, from the 1999-08-04 `ripe.db.gz` already on disk: 1,195,658 distinct registrable non-`arpa` names, 741,726 held at 2000, 565,264 of those missing 2001, so a 2001-dated edition of the same object set would be **78,277.48 EE gross, 52,937.09 of it `.de`** (771,892 distinct `.de` names, 399,827 held at 2000 and missing 2001, at weight 0.1324 read from `ark.english_share`). Sampling was on DISTINCT DOMAINS throughout, never `domain_year` rows, against `data/exports/*.txt` written 2026-08-31 01:20. What dates one item is the dump's own machine-written cut stamp on line 2 of the payload, `# 990804 00:07:01`, class `artifact_listing`, no split; dating was never the problem with this family. **Content kill, in the registry's own words.** DENIC's operations manager Sabine Dolderer, on RIPE's own db-wg list, 2000-06-30: "it was a dicission made by the RIPE db-wg in Amsterdam (February?) that there should be no domainobject in the database after June 30th" (`https://www.ripe.net/ripe/mail/archives/db-wg/2000-June/001353.html`, thread 001329/001344/001349/001381 is providers complaining the deletion had already happened). The DB-WG chair confirms the wider exodus was staggered and underway 2000-08-17 (`.../db-wg//2000-August/001405.html`). So `.de` left the database before any 2001 edition could carry it, and the 2004-11-09 FUNET split is that exodus's end state: 162,408 `domain:` objects, 6,155 of them forward, and **exactly 0 ending in `.de`** (top forward TLDs `fo` 1,318, `sm` 1,245, `bg` 1,036, `mc` 693). **Existence kill, independent of the above.** No RIPE edition dated 2000-2003 survives on any reachable host. `ftp.funet.fi/pub/netinfo/RIPE/dbase/` lists only `ripe.db.gz` 1999-08-03 and `split/` 2004-11-09 with nothing between; its `archive/reimp/` is 19 RIPE database *software* tarballs; its 351,368 B `ls-lR.test` has zero `ripe.db` anywhere in the tree; `ftp.ripe.net/ripe/archives/` and the live uni-erlangen mirror are one 28 MB `enumarchive.zip` each; RIPE's NRTM serves only the previous 14 days of serials, so the registry itself keeps no history. Twelve further European archives were robots-screened in full before any other request: `ftp.tu-chemnitz.de` and `ftp.fu-berlin.de` are `Disallow: /` and `ftp.uni-stuttgart.de` is `Disallow: *`, so all three were left alone; `ftp.heanet.ie` prints "This site has stopped operations"; `ftp.cesnet.cz`, `ftp.uninett.no`, `ftp.chg.ru`, `ftp.dfn.de` do not answer; the four that allow crawling (`ftp.arnes.si`, `ftp.man.poznan.pl`, `ftp.task.gda.pl`, `ftp.gwdg.de`) 404 on all six `ripe`/`netinfo` path shapes against a same-session control where FUNET and uni-erlangen returned 200 on exactly those shapes. `www.nic.funet.fi/index/RIPE/dbase/`, surfaced by a search engine as a second FUNET view, is closed by terms: `Disallow: /index/` under `User-agent: *` in its 208 B robots.txt, read in full before the request. **Method, reusable.** When a hypothesis assumes a corpus still contained something at a later date, the registry's OWN working-group mailing-list archive prices that assumption for the cost of one search, and it is a machine-dated primary source: the `.de` question was settled by a post written by the registry that did the deleting, six weeks after it did it, against a 78,277 EE claim, for two searches and five page fetches. Second, an artifact already on disk can run a hypothesis's kill screen for a DIFFERENT artifact in the same family for free. Operational trap: on macOS `zcat` looks for `.Z` and fails with a `stat` error that reads exactly like an empty file, so use `gzip -dc` or a false zero is manufactured. **Left open, and it is the largest lead this run produced.** The hypothesis had the right idea and the wrong direction on the calendar: the live window is 1999-08-04 to 2000-06-30, where `.de` is still present by DENIC's own timeline, and priced the same way it is **64,425.85 EE gross net-new post-split**, 453,541 pairs held at 1999 and missing 2000, 294,955 of them `.de` worth 39,052.04. That is the same letter to the same RIPE NCC address that granted the 1999 file on 2026-08-26, asking for a 2000-H1 cut instead of a 1998 one, and at 7.4x the return it supersedes the 8,712 EE pre-1999 request left open by `ripe_pre1999_dbase_splits`. Do not re-test the 2001 arm, which is dead on content whatever turns up, and do not re-walk the mirror layer. |
| **The unreachable 18.9 GB tail of the UKWA/JISC `host-linkage.tsv.gz` host link graph (2026-08-31)** | **CLOSED at 0 EE, refuted offline for zero requests, and the one sub-claim the earlier closure left standing is now dead in principle.** The family was already closed at the byte on 2026-08-24 (ranged GETs succeed to `2147479552-2147483647` and the next 4 KB fails five times running; `timemap/link` returns two captures and the 2022 one fails at byte 0, curl exit 56; the `id_` capture reports `content-range: bytes 0-0/20928588915`), and that closure IS this hypothesis's own stated kill screen. The untested sub-claim was that "a later shard may also be a separate gzip member and inflatable from an offset". A full scan of the 2,147,483,648-byte local prefix for `1f 8b 08` finds exactly TWO candidates: offset 0 (FLG=0, XFL=2, OS=3, the real header) and offset 245,572,142, which fails with "header crc mismatch" under `zlib.decompressobj(16+MAX_WBITS)` and is a random byte coincidence. **The artifact is ONE gzip member, not 15 concatenated ones**; the "15 shards" of the source entry are logical sorted runs inside a single DEFLATE stream, whose byte N needs every byte before it, so partial recovery from an offset is impossible in principle and 0 EE by every route. What dates one item is field 1, the crawl year the archive's link-graph extractor wrote for the row, class `link_source`, master-eligible and unchanged; irrelevant here because no new row is obtainable. There is no unread remainder on disk either: `gzip -dc` yields 168,942,587 lines / 15,213,592,576 bytes before the truncation error, and that whole extent was ingested (2,468,674 in-window rows, source_id 8 `ukwa_link_source`, 116,477 assigned pairs worth 114,185.7 EE, TLD split `uk` 116,190 / `com` 224 / `net` 16 / `org` 13 / `de` 10). The remaining 18,781,105,267 bytes (89.74%) are unreachable: Wayback replays at most 2 GiB, only two captures exist, and the publisher host serves the same 159 B "400 Redirect" stub for the entire `/datasets/` tree, measured 2026-08-05, 08-15 and 08-24. **Ceiling on the tail, for the Autumn 2026 decision.** The tail is the same `.uk`-weighted crawl population, so it is bounded not by 9.74x the prefix but by remaining `.uk` headroom on the adjacent-year screen (held Y-1, missing Y) over 538,058 distinct in-window `.uk` domains: 1997 2,991; 1998 51,040; 1999 6,314; 2000 16,141; 2001 121,388, total 197,874 pairs, about **194,174 EE** at 0.9813. That independently corroborates the 200,574.8 EE register ceiling, so the tail remains the largest single known unclaimed source and is simply not retrievable by any route that exists today. **Method, reusable.** Before proposing to resume a truncated gzip from an offset, scan the held prefix for real member headers: search `1f 8b 08`, require FLG, XFL and OS to be plausible, then confirm by attempting `zlib.decompressobj(16+MAX_WBITS)` at that offset, where a header CRC mismatch means coincidence. One gzip member means partial recovery is impossible, and that is cheaper to establish than any network probe. Second, when a hypothesis proposes a kill screen, grep `sources.md` for the SCREEN and not only for the source name: this screen had already been run a week earlier under a different heading than the source entry. **Not reopenable by fetching.** The only live route is logged already: the British Library replied 2026-07-22 that bulk access returns Autumn 2026, first stage a per-URL lookup. Deliberately not re-probed today, one week after the last check, since "Autumn 2026" has not elapsed and the stub is a tree-level property measured three times. When that access opens, request this artifact and the `ukwa.ds.2/cdx/` tree together and price them against the ~194,174 EE ceiling above. |
| **The InterNIC `netinfo` periodic register, hunted inside archive.org mirror tarballs of `ftp.rs.internic.net` and `ftp.internic.net` (2026-08-31)** | **CLOSED at 0.0 net-new EE, measured on the WHOLE artifact rather than a sample, and the mirror trees the hypothesis predicted do not exist.** The prize file is reachable without any tarball: `https://www.statdns.com/files/domain-info.19960614.txt`, 9,918,513 B, line 1 `[ netinfo/domain-info.txt ]`. What dates it is the machine-written stamp `DDN NIC DOMAIN SUMMARY   14-Jun-96` on line 3 of the file itself, class `artifact_listing`, no split; dating was never the problem. 417,108 distinct registrable domains (com 372,383 / org 26,252 / net 15,364 / edu 2,659 / gov 450), **417,103 already held (99.9988%)**, and held AND missing the artifact's own year 1996: **0**. Under 200 EE, so no mirror tarball was opened. Existence, `advancedsearch.php` only: `identifier:(*internic*) OR title:(internic)` returns **16 items**, none a mirror tree (five Joe Internicola videos, four Usenet dumps, two favourites collections, a 1994 ISP list, a Swedish novel, an art item); the `rs.`/`ds.`/`internic.net` variant returns **1**, a 14,468 B Usenet mbox. All **974** members of `ftpsites`, the collection holding the `2015.04.ftp.isc.org.tar` that paid 15,173 EE, were enumerated: **no internic host**, and the only registry host in it is `ftp.arin.net`. The 1,123 `title:("FTP Site")` items outside `ftpsites` are 2015-2016 `archiveteam_ftp` WARCs of hosts still live a decade after internic.net died. Three independent preservation hosts stop on the same edition: `rscott.org/OldInternetFiles/` (18 file families, last `domain-info` 19960614), `www.statdns.com/files/` (7 editions, 19871215 to 19960614), and `emaillab.jp/dns/hosts/`, which tops out at 1995, is HOSTS.TXT only, and sits under the `/pub/` its 4-line robots.txt disallows. **`ftp.arin.net` cleared for free and proved empty**, a registry FTP host the 105-host sweep of 2026-08-29 did not cover: `https://archive.org/download/ftp.arin.net/2014.05.ftp.arin.net.tar.txt`, 19,483,705 B, ONE request, **186,105 paths** indexing a 43,351,132,160 B tar; `netinfo` 0 hits, `internic` 0 hits, `domain` a symlink to `inaddr`, and only 123 in-window mtimes, all rwhois source, RFCs or NANOG decks. ARIN inherited InterNIC's number registry, not its name registry. **The population is not saturated, only its year is**, swept on distinct domains over all 417,108: gross EE a perfectly dated edition of this exact population would pay is 1996 **0.0**, 1997 47,982.8, 1998 88,177.7, 1999 71,542.9, 2000 63,620.4, 2001 **118,438.7**; adjacent-year (held Y-1, missing Y) 1997 47,982.8, 1998 49,720.7, 1999 8,849.5, 2000 8,696.1, 2001 60,701.0. So the hypothesis was right about the value and wrong about availability, and wrong by an EDITION, not by a host. **Three methods, and they outrank the negative.** (a) **An `ftpsites` item publishes its own complete tar listing as a sibling `<id>.tar.txt`, so a 43 GB host costs one 19.5 MB request**: `GET /metadata/<id>` and look for it before thinking about the tar. This is the `ls-lR` rule extended to the archive.org collection that has already paid this project 15,173 EE, and unlike a real `ls-lR` it cannot be a stale index of a removed tree, because it was generated from the tar it ships with. (b) **When a corpus prices at 0.0 in its own year, run the six-year held-and-missing sweep before writing the family off**: it separates "this population is saturated" from "saturated in this one year", which look identical from a single-year price and imply opposite next moves. Here it inverts the reading and sends the hunt after a second edition, where it dies on existence instead of on value, a different and more useful negative. (c) **To settle whether a series has more editions, enumerate the preservation host's index page and group links by (prefix, suffix) with the date pulled out of the middle**: one request per host renders every file family and the edition each stops at, and three hosts terminating on the same edition is a far stronger existence negative than three hosts each probed for one guessed filename. **Do not re-test** rscott.org, statdns.com, emaillab.jp, FUNET's netinfo tree, `ftp.arin.net`, or any 1996-dated edition of this register, whose 0.0 EE price is now measured twice. Live residue, one question and not a family: no post-1996 edition of the InterNIC name register is known to survive, and a 1998 or 2001 edition of the identical population is worth 88,178 to 118,439 EE gross, so (1) finish method (a) across the other 973 `ftpsites` members, grepping the free listings for `netinfo`, `domain-info`, `domain-contacts`, `com.zone`, `net.zone`, a free existence layer over ~2 TB of preserved FTP hosts that generalises to every registry-file signature this project has hunted, and (2) NSI's registrar-era successors 1997-1998, a different publisher and a different file name. |
| **FidoNet weekly nodelists as a bulk self-dating corpus, the `INA` internet-address flag on the node records rather than the BBS phone numbers (2026-08-31)** | **CLOSED at 0 EE on a family already closed twice by measurement, for zero requests and about ten seconds.** `docs/sources.md` already holds "`textfiles.com` and FidoNet nodelists (2026-08-23)": FidoNet nodelists have the ideal weekly self-dating shape and give **under 500 EE across the whole family**, a whole-family figure that is already below the 1,000 EE bar before it is divided across six years of weekly editions. Corroborated from the other direction by the IRC-logs-and-web-forums closure (2026-08-27), which measured the FidoNet message base at **0.0155 net-new post-split pairs per listed domain**, 127.3 EE over 303,879 message bodies and 7.6 EE over 277,168 tearlines, against 0.386 EE for one random already-held `.com` missing 2001. The store has grown from merged260823 to merged260827-2 since, which moves an unbanked source down and never up, so the closure holds a fortiori. Dating is sound and is not why it dies: `Day number NNN : Friday, May 3, 1996` plus the CRC is a genuine machine-written stamp. Nothing fetched, `filegate.net` and the `nodehist` weekly runs never requested, no robots.txt read, because the family closure precedes the first request; and no nodelist artifact exists under `data/raw/`, so there was no free read to take. **Method: when a hypothesis block supplies a "nearest closed" reference, treat it as a claim to check, not as the grep.** This one named "Non-web service ping directories (2026-08-28)" and argued correctly that artifact size and retrievability differ, and both halves were beside the point, because the family was already closed under its own name on YIELD; a bulk weekly file instead of a 13-row delta does not move the constant that closed it. Same failure mode as the guestbook entry, where a fill-rate mechanism improved the numerator of a ratio nobody was measuring. The family name was verbatim in the hypothesis title, so the check was one command against a projected hour of fetching. **Maintenance note**: the 2026-08-27 entry cites this closure by LINE NUMBER, which has drifted from 784 to 866 as the file grew, so cross-references between entries should cite the entry title and date, never the line. |
| **The RDAP 404 verdict as a LIVENESS PRIOR on the CDX gap queue: does a name that is unregistered today answer a 2001 capture query less often than one that is still registered (2026-08-31)** | **FIND, and the mechanism is confirmed at z = 53.6 while the economics miss the hypothesis' own 900 EE/hour floor by 6x to 9x. Measured +0.0805 EE per `.com` query, +17.56%, or +97 to +161 EE per collector-hour. ZERO requests: no host contacted, no robots applies.** **Not a source and must never be read as one.** An RDAP 404 in 2026 says the name is unregistered today and evidences no year; it changes WHICH name the CDX engine asks about, and the pair that results is still `cdx_timestamp`, exactly as `link_target` is already used as a ranking signal. No new class, no approval needed. **Method, and it is the result worth keeping: a hypothesis whose kill screen needs live requests should first be checked for whether the experiment has already been run and journalled.** The designed A/B was 1,000 live queries; the retrospective join of `data/raw/cdx/*.jsonl.gz` (1,112 files, 2,952,695 rows of `{domain, status, years}`) against `data/raw/rdap*/**/*.jsonl.gz` (416 files, 6.5 GB, 33,778,743 rows of `{domain, status}`) gave 278,678 paired observations in 75 s. **Our own journals are a paired observational dataset**: any collector writing `{key, status}` and any other writing `{key, outcome}` join into a free experiment on whichever ordering is in question, and nothing is fetched, so no rate change can confound it, which is the trap that produced the earlier wrong law about ranking inside the Verisign clamp. **Measurement**, distinct domains not `domain_year` rows, restricted to walked names held at 2000 in the gap-queue families: RDAP-200 n = 70,982 P(walk returned 2001) = **0.8529**, RDAP-404 n = 6,731 **0.5191**, difference 0.3338, se 0.00623, lift 1.643x. Premise verified first: 18,593,773 rows at 200 and 15,029,917 at 404 against the 18,587,509 / 15,025,140 claimed, correct to 0.03%. **Not composition**: the live arm wins in every TLD stratum with both arms above 200 (com 0.8434/0.5821, org 0.7917/0.6216, ca 0.7002/0.5177, net 0.7870/0.6937, co.uk 0.3700/0.3149), no reversal, so no Simpson's paradox. **Not an existing ranker in disguise**: within `.com` held at 2000, `cdx_gtail` 0.8288/0.4284, `cdx_vedge` 0.8169/0.3253, `cdx_gap` 0.9235/0.7728. **The honest boundary**: `cdx_linkhint` is flat at 0.7150/0.7196, because a link-hint queue has already selected for survival, so do not apply it there. **The counterweight the hypothesis did not anticipate, and it is the transferable law: for any ranking signal measure BOTH the hit-rate lift AND the saturation of the arm it favours, because they point opposite ways.** The dead names are the residue: P(store lacks 2001 | held 2000) is 0.5948 for live `.com` and 0.8027 for dead. Quoting the hit rate alone overstated the value 3.7x. Priced against the queue as it stands (live share 0.6183), mixed order pays 0.4586 EE/query and live-first 0.5391. **Prove the ranker is not already sorted before pricing a reordering**: live fraction by position quintile is noisy and non-monotone in all three live queue files (`gap_ranked_20260824` 0.853/0.878/0.911/0.965/0.845, `queue_edge_local` 0.618/0.619/0.607/0.549/0.758, `queue_unasked_ranked` 0.133/0.192/0.195/0.158/0.129), so the gain is real and not already banked. **It is free where the money is**: 3,425,055 of 3,429,775 `.com` gap names already carry an RDAP verdict, 99.86%, so the `.com` reordering costs no Verisign quota; the uncovered 40.4% is almost entirely `.de` (980,570 gap names, 923 verdicts) and should not be chased. Rate taken from journal timestamps rather than assumed: ~2,000 q/h across the two collectors. **Operational**: the store was locked by a live writer throughout and `data/exports/*.txt` reproduced the held sets to 0.007% (6,562,432 vs 6,562,859 held-at-2000-missing-2001), so reporting work need not wait 900 s on the lock. **Apply as a tiebreaker sort key, never a filter** (dead names still pay 0.328 EE), and not on `cdx_linkhint`. **What this run could not settle**: every walked journal is already ingested, so "would have been net-new at query time" needs a live interleaved run; the direction and the ratio are settled without it. |
| **A pre-1999 RIPE database split, an edition of `ripe.db` dated 1996, 1997 or 1998 (2026-08-31)** | **The hypothesis' artifact does not exist on any reachable host, and a 1996 or 1997 edition would have been worth 123 and 502 EE anyway, priced free off the 1999 file already on disk. The FIND is a file the hypothesis did not name: `https://ftp.funet.fi/pub/netinfo/RIPE/dbase/split/ripe.db.domain.gz`, 5,452,546 B, `Last-Modified: Tue, 09 Nov 2004 23:31:00 GMT`.** What dates one item is the object's own `changed:` transaction line, `changed:      ovema@a.sol.no 19971128` under `domain:      hasselblad.gm`, the database's record of an update applied to that object. **Banked 913.84 EE over 1,510 pairs** as `ripe_dbase_split_2004`, 1998 4 / 1999 253 / 2000 792 / 2001 461, admitted under the standing rule; see `docs/approved-sources-list.md`. `docs/sources.md` line 597 is corrected in place: FUNET's `split/` is a 2004 edition, not 15 same-1999-edition subsets, and the claim that the 2000-and-2001 `changed:` route does not exist was wrong. **Do not re-test the existence hunt**: `ftp.ripe.net/ripe/dbase/` is the current edition only, FUNET `archive/` is one 1998 incident log (`981027-incident.New.gz`, 6,130 transaction lines, 6,125 `[person]`, **0 `[domain]`**) plus nineteen server-source tarballs, uni-erlangen is a live 2026 mirror, `ftp.icm.edu.pl` has no RIPE tree, and archive.org `advancedsearch.php` returns numFound 0 for `ripe.db`, `ripe.db.gz` and `ripe dbase` against a same-session control `ripe.net` = 19. **Method: price an earlier snapshot of a database you already hold, for free, off the snapshot you hold.** The `changed:` history inside one dated dump says which years each object was alive and which are already banked, so the whole time axis prices before a request; here it gives the general law that **going BACKWARDS from a churned registry dump is worth almost nothing, because 84% of objects alive in year Y already carry a `changed:` line in Y, and going FORWARDS is where the value is**. Also: **a mirror's `split/` can be a different edition from the same mirror's whole-database file**, five years apart in one directory, so read mtimes per file and never trust a previous sweep's one-line summary of a directory you can list in one request. And **a ccTLD register can sit inside an RIR database belonging to another continent**: `.gm` (Gambia, weight 0.9969) was run out of Norway and its 558 pairs outweigh `.bg` and `.mc` together. **Two open leads, in order**: a late-1998 edition projects **8,712 EE** and only RIPE NCC can supply it, so the route is a letter to the address that granted the 1999 file on 2026-08-26; and a **2000-2003 frozen mirror holding a whole `ripe.db.gz`** is worth thousands, because RIPE deleted the forward ccTLD objects between the two editions (1.23M forward names in 1999, 6,160 in 2004). |
| **The Wayback `__wb/sparkline` year histogram as an all-years dating oracle, plus its sibling `__wb/search/host` (2026-08-31)** | **CLOSED at 0 EE on ECONOMICS, not on evidence: 212 EE/hour against a 1,008 floor and against the 1,494 EE/hour the availability arm already earns.** `https://web.archive.org/__wb/sparkline?output=json&url=<host>&collection=web`, 1,308 B for `demon.co.uk`, no robots restriction (`web.archive.org/robots.txt` is a 404 with no rules). **What dates one item is IA's own per-year capture count, machine-written, the same claim `cdx_timestamp` makes, so the class is master-eligible and takes no split; the yield is what fails.** 200 distinct domains sampled from the 6,562,859 held at 2000 and missing 2001 (`com`/`net`/`org`/`uk` subset 4,252,193), all 200 answered 200: 128 net-new pairs, **81.23 EE, 0.4061 EE per request**, and the years-added distribution is exactly {0: 72, 1: 128}. **The all-years premise measures 1.00x, not the 2-6x it was bought for**: over the first 60 domains sparkline made 47 year-assertions in 1996-2000 and **0 were net-new**, while missing 68 years the store already holds, because the baseline is IA-derived and has already absorbed everything IA can say before 2001. It is also strictly narrower than `ark.cdx.cdx_url`, which queries `url=*.domain` across subdomains and paths where sparkline resolves one Wayback URL key. **Method: the throttle rate identifies which budget an endpoint sits on and it is free.** 82 refusals in 282 attempts, **29.1%**, is the CDX arm's 27.3% and not the availability arm's 0.67%, so this is the contended budget; two workers bought +6% throughput for +66% refusals, which is a quota signature and not latency. Positive control proves the negative against a known positive: 25 domains held at 2001 returned 2001 on **23 of 25**, so the 36.5% miss on the gap population is real absence. The sibling `__wb/search/host` is dead on evidence, returning `display_name` and nothing else, capped at 5 results, so its names take the corroboration split and earn no year. Reopens only if web.archive.org ever throttles us less than archive.org, the reverse of today, and then only as a fallback for the availability arm. |
| **The Memento TimeMap aggregator as a federated non-IA lens, `timetravel.mementoweb.org` and the 13 upstreams behind it (2026-08-31)** | **CLOSED at 0 EE against a floor needing P > 0.033, with no bulk fetch and 24 requests, ~130 KB total.** `timetravel.mementoweb.org`, `labs.mementoweb.org` and `aggregator.mementoweb.org` all still return no A record, re-confirming the 2026-08-18 entry. **Method (1), and the transferable law: the hypothesis's founding claim inverts before 2002.** National web archives were founded 2002-2013, and the handful showing 1996-2001 content have it because the Internet Archive donated a back-file: Arquivo.pt's own in-window index is literally named `IA.cdxj`, and the Australian Web Archive is IA-hosted. So {non-IA} and {holds 1996-2001} are very nearly disjoint and their intersection is IA-derived anyway. Non-IA AND in-window AND permitted measured **0 of 14** upstreams, so P = 0 and the projection is 0 EE. **Pre-download question for any federate-N-archives lens: what year was each archive founded? Founding dates are free and published.** **Method (2): a dead aggregator and an aggregator returning zero are indistinguishable, and this one was the second.** The one live substitute, `memgator.cs.odu.edu` (robots.txt 404, unrestricted), parses the Memento path grammar and prints a 13-archive `/about` page, yet returns HTTP 404 at 19 bytes for **every** URI including `http://example.com/`; anything trusting it would have banked fourteen false negatives from one host. Screen an aggregator with a URI the whole world holds before reading anything into a miss. Robots census, each file read in full before any other request to that host: **`webarchive.nationalarchives.gov.uk` (UKGWA) is now 55 bytes of `User-agent: * / Disallow: /`, a flip since sources.md recorded it working on 2026-08-06**, which is the host-survival and robots-refusal correlation playing out on a government host. **`archive.ph` allows `/` but disallows `/timemap/` and `/timegate/` by name**, so an archive can permit crawling and still refuse the Memento API specifically, and a `Disallow: /` grep is not enough for this lens. `arquivo.pt` puts `/wayback`, `/cdxj`, `/services` and `/datasets` in Disallow lines at 744-753 of a 753-line file, after roughly 725 `Allow:` lines. `webarchive.parliament.uk` and `waext.banq.qc.ca` are 26-byte `Disallow: /`; NDL Japan closes `/web/` and `/search`; `vefsafn.is` does not respond. `webarchiveweb.bac-lac.canada.ca` is the only host in the set that permits us, and 9 probes across three endpoint spellings and three URIs, honouring its `Crawl-delay: 10`, all returned 404 at an **identical 179 bytes**, a no-such-route 404 rather than an empty TimeMap. Identical byte counts across different URIs identify a routing 404; differing ones identify a real empty result. Only loose thread, not worth a request: `webarchive.nrscotland.gov.uk` returns 403 on robots.txt itself, so its terms are unknown rather than refused, and its in-window ceiling is well under 200 EE. |
| **Our own CDX journals' degraded tier: 315,432 domains whose years were built by `by_host` or `by_root`, the tiers that see only apex plus www (2026-08-30)** | **CLOSED at 1,683 to 4,800 net-new EE for 315,432 wildcard queries, on COST, with ZERO requests made.** Not a source: `data/raw/cdx/*.jsonl.gz`, 1,110 files, 656 MB, 2,951,495 rows already on disk; what dates one item is the Internet Archive capture timestamp (`cdx_timestamp`), master-eligible and unchanged, so the evidence class is sound and only the yield fails. The hypothesis's mechanism was wrong and its population right: tier 1 is `by_host`, and the `*.domain` wildcard that sees subdomains is tier 3, reached only when tier 1 answered 200 with no years. **Method (1): a journal that stamps WHICH query answered is its own control group, so re-querying to find out is the mistake.** The kill screen asked for 200 fresh CDX queries; the journals already held 15,608 domains answered by both a narrow and a wide tier, a 78x larger sample, free. On them the wide query adds an in-window year on 5.43% of domains (847 domains, 1,401 pairs, 0.0898 per domain) and LOSES one on 0.06%, and that asymmetry is what makes the two readings comparable as tier reach. **Method (2), and the number that kills it: P(store lacks the year) has a conditional and an unconditional form that differ by 7.7x.** Measured against `merged260821`, the reviewer file cut before our own sweeps and therefore uncontaminated, P(net-new | the wide query gained this year) = 122/1,371 = **0.0885**, 6.8x below the assumed 0.6; the `*.domain` scan agrees at 0.091. This is the first quantification of discovery law 1 as a conditional: an IA-derived gain is ~91% already inside an IA-derived baseline, because subdomain activity marks a live well-linked domain and those are the ones every other source already dates (the same baseline lacks 31.3% of the suffix sweep's pairs overall but only 8.9% of the subdomain-gained ones). The unconditional 0.687 gives 11,937 EE and reproduces the hypothesis's own 11,037 EE floor almost exactly, which is how the trap reads as a confirmation. Priced two ways from the same 124 observed net-new pairs: TLD stratification 1,683 EE (com 689, org 250, us 193, uk 176, au 140, ca 98), year-count stratification 4,791 EE, so the honest read is 1,700 to 4,800 EE and **one stratification is a point estimate pretending to be a measurement**. The verdict is cost: 315,432 queries on the one metered CDX channel, the wildcard tier at ~33 s median against ~2 s for the host query, 8.31 to 10.03 domains/min, so **350 to 584 hours returning 2.9 to 13.7 EE/hour against the ~3,000 EE/hour the gap queue pays, ~300x misallocated**. **Method (3): price against the PREVIOUS reviewer baseline when the corpus under test is already banked**, since these extras read 1.4% against `merged260827-2` (which contains them) and 8.9% against `merged260821`, with the corpus-coverage jump 68.7% to 81.7% as the check that the right file was picked. Two residues survive and neither is urgent: the 31,924 rows that returned HTTP 200 and no in-window year at all are 10.1% of the population carrying ~74% of the value at 0.111 EE/query (~3,560 EE, 35 to 66 hours, selected as `status==200 and years==[]`), and one 500-name `.com` wildcard probe would close the only unmeasured cell, since `.com` is 43.5% of the target and rests on 52 overlap observations with 0 net-new. Do not re-derive this from the strategy column. |
| **Our own fetch journals' non-200 rows, the names the instrument asked about and never got an answer for, CDX and RDAP (2026-08-30)** | **CLOSED at 0.0 net-new EE on the rows that actually exist, with ~1,724 EE of retry headroom for ~24,200 queries (0.071 EE per query), and ZERO requests made.** Not a source: `data/raw/cdx/*.jsonl.gz` plus `data/raw/rdap*/**/*.jsonl.gz`, already on disk; what dates one item is the CDX row's own 14-digit capture timestamp, but only for rows that exist, and a never-answered row carries no timestamp and dates nothing. **Method (1): a hypothesis that ships its own kill screen should have the kill screen run first.** It cost 0.9 s and zero requests and refuted the floor by 13x: 53,992 non-200 CDX rows are only 40,462 distinct domains, **37,990 of them (93.9%) already carry a status-200 row in the same journal set**, and the residue is **2,472 names against the floor's assumed 32,000**. Held 87.99%, mean 3.655 of 6 in-window years already held, optimistic ceiling 1,180.7 net-new pairs = **878.0 EE**, split 1,962 tried once (733.6 EE) and 510 tried twice or more (144.4 EE, low P(answer) since repeat failure is evidence of permanence). Generalises: for any "our instrument lost data" claim, join the loss against the successes in the same store before pricing the loss, because self-healing retry loops make most recorded errors historical rather than outstanding. RDAP at larger scale says the same: 65,123 unsettled distinct domains, 66.7% settled later, residue 21,691, and **method (2), condition a retry's yield on P(in-window | settled), not P(in-window | 200)**, which overstated the pool 2.6x (9,483 naive pairs against a true 3,581 gross = 1,059.7 gross EE, **845.7 EE net-new after rule 6**). Registries that publish no creation date are visible in the journals for free: `rdap.cctld.au` and the `.tw` endpoint measure P(in-window) = 0.0000 over every settled row, killing 1,652 of the 21,691 before any request. **Method (3), reusable and it silently corrupts any journal census: `gzip -dc` and `gzip.open()` return ZERO rows for a gz whose CRC trailer is missing, while `zlib.decompressobj(zlib.MAX_WBITS|16).decompress(raw)` returns the whole payload.** A per-file try/except discarding partial results on EOFError, which is the obvious way to write the reader, turns a readable file into a false zero: the local form of the archive.org-scrape trap. Screen with `for f in *.gz; do gzip -t "$f" || echo BAD $f; done` before any journal-wide count. Here 11 corrupt journals recovered completely, 2,796 rows, 2,209 at status 200, 1,993 distinct domains carrying 4,881 in-window pairs, and priced against the store **1,993 of 1,993 are already held (100.00%), 0 net-new pairs, 0.0 EE**, verified rather than assumed: every one reappears at status 200 in a readable journal, because the failed write never marked them done and the loop repaired itself. Nothing here is ingestible; the two retry pools are collector-priority decisions and neither should displace gap-queue work. Do not re-test: the residue only shrinks. |
| **The commercial fetch-log holders, the survey row itself rather than a page their crawler left behind: Netcraft, DomainTools, SecurityTrails, WhoisXML and WhoisFreaks (2026-08-30)** | **CLOSED at 3.16 net-new post-split EE over 80 queries, on ARTIFACT EXISTENCE.** Netcraft Site Report, `https://sitereport.netcraft.com/?url=http://<domain>`; **what dates one item is Netcraft's machine-written `Date first seen` month, stamped by their survey when it first got an HTTP response from that host**, so `artifact_listing` with no human in the loop and no split, dating its own year only. Measured on two stratified samples of DISTINCT DOMAINS, n=40 each: `.com` held at 2000 and missing 2001 paid 2 net-new pairs (1.264 EE), `.com` held at 2001 and missing 1996-1999, the population a first-seen instrument suits best, paid 3 (1.896 EE); pooled 0.0395 EE per query, 95% CI 0.013 to 0.088. No bulk artifact exists: Netcraft's TSV export is inside a paid platform contract, DomainTools is per-domain only and stops at 2001, WhoisXML begins 2010 and SecurityTrails 2002. **Method (1), and it kills the whole whois-history-holder family at once so no vendor needs re-testing: an advertised coverage year is a `creation_date` MINIMUM, not an earliest COLLECTION stamp, and the two differ by about twenty years.** WhoisFreaks advertises "snapshots dating back to 1986" and three free requests (`cisco.com` 41 records, `well.com` 58, `apple.com` 157) showed every visible snapshot stamped 2022. **Method (2): a vendor's live per-domain product prices its own historical retention for free**, since a first-seen LATER than a year we hold is positive evidence of no row, so 80 metered lookups measured the bulk corpus a permission letter would ask for: P(any in-window Netcraft record | store holds the domain in-window) = 25/80 = 31.3%, capping a granted Netcraft licence at ~863,000 EE gross on the 6,564,133-domain held-2000-missing-2001 gap. Terms and `robots.txt` (499 B) read whole before the loop and both permit automated Site Report retrieval with an identifying UA; 86 requests at 3 s spacing. Residue is one RIPE-shaped letter to Netcraft, now priced rather than guessed, plus a permitted but 21x-dominated free query route on a 5.0M-name pool held as the fallback if the RDAP pool runs dry. |
| **The on-disk URL corpus as an artifact FINDER: which PATHS we already hold promise a machine dump (2026-08-30)** | **FIND, 157.8 net-new post-split EE over 250 pairs, every one at 2001**, parked on condition 2 of the standing rule. Eleven archived listing pages, largest `https://web.archive.org/web/20011023104545id_/http://www.domainsww.com/Domain_Listing.htm`; **what dates one item is the Wayback capture stamp on the page and nothing inside it**, and the names are human-typed, so they take the corroboration split. 1,245 distinct domains and only **32.0% held in any year**, because a for-sale listing prints speculator inventory that never resolved; the 250 that pay are 62.8% of the 398 held names, which is the population `com` 2001 threshold of 0.611, so these pages carry no year advantage of their own. **Method, and it costs zero requests: match the PATH, never the raw URL** (host-inclusive is 375x noisier, 117,912 hits against 314, over the 118,142,155 URLs of `data/raw/webbase/webbase-2001.urls.gz`), keep only the domain-specific signatures (`all_domains`, `domainlist`, `newdomains`, `whole_list`, `zone_file`, `deleted`, `expiring`) since the generic dump words produced no survivor, and settle each candidate in two requests with `archive.org/wayback/available` plus one `id_` fetch. 290 of the 314 candidates are unscreened and worth roughly 1,900 EE more at the measured 11 useful pages per 24. Detail in `docs/approved-sources-list.md`. |
| **Public Mailman subscriber rosters, the membership table rather than the messages (2026-08-30)** | **FIND, 89.4 net-new post-split EE over 179 pairs, 178 of them at 2001**, parked on condition 2 of the standing rule. Five in-window rosters, largest `https://web.archive.org/web/20010717203344id_/http://mail.python.org:80/mailman/roster/mailman-users`; **the roster page carries no internal date stamp, so the Wayback capture timestamp alone dates it**. 2,358 distinct registrable domains, **96.8% held** and 92.1% of those already carrying 2001, so a roster is a discovery loss and a year win: the 181 held-and-missing-2001 names are the whole payout, at **0.0379 EE per listed subscriber domain** and 17.9 EE per page. Saturation is visible at n=5 (2,921 per-page names against a union of 2,358, 19.3% duplication), so 5,000 EE needs ~280 more public in-window rosters against a measured 1.1% conversion of probed roster URLs. **Method: a mail archive already on disk is a free URL generator for a family it is not itself a member of**, since every Mailman message footer prints `/mailman/listinfo/<list>`, which turned 868 MB of on-disk archives into 463 host/list pairs at zero network cost, and `archive.org/wayback/available` prices a family when CDX is off limits. Mailman's members-only default killed 4 of the 9 in-window captures and each is recognisable as a 324-519 B stub without reading the body. Detail in `docs/approved-sources-list.md`. |
| **Period office proxy logs, the third and last client-side shape, on the lead the browser-cache closure parked in writing (discmaster item 14148, `squid/logs/access.log`) (2026-08-30)** | **CLOSED at 5.59 net-new post-split EE, and the family is closed by a sublinear-discovery law, not by sample size.** What dates one item is the row's own leading Unix timestamp to the millisecond, written by Squid at the instant of the fetch (`993248012.306 ... GET http://security.debian.org/...`), so a row is self-dating and takes no split; the media mtime 2001-06-22 agrees. Artifact: `https://discmaster.textfiles.com/file/14148/MacHack%202001.toast/pc/The%20Hacks/Network%20Alias/network-alias.tar.gz/network-alias.tar/network-alias/squid/logs/access.log`, 1,272,715 B, 8,660 rows, plus its sibling `store.log`; robots.txt read in full first (267 B, `Disallow: /` with the operator's written carve-out for limited targeted research automation), 9 requests spaced 2 s. Population ENUMERATED and counted AS DOMAINS: 8,660 rows to 551 hosts to **276 distinct registrable domains, 268 (97.10%) already held**, 9 held and missing the artifact's own 2001 (the same 9 on the adjacent-year screen), so **5.59 EE**, or 10.62 with the 8 novel names on a permissive ceiling. **Its own kill line is crossed**: P(store lacks 2001 | domain held) is **0.0336** against the 0.611 population control, an 18.2x shortfall, and 0.0350 over the 205 `.com`; the hypothesis said under 0.05 closes the family. Priced against `data/exports/*.txt` at 25,740,534 pairs, 0.003% behind merged260827. **Pooling browsers concentrates the head instead of widening the tail**: 5.5 hours of one conference LAN heads on `akamai.net` 784, `slashdot.org` 387, `techtracker.com` 353, `apple.com` 343, `doubleclick.net` 198, which is the browser-cache death on the corpus built to escape it. **Method, and it prices every proxy log in the family without fetching one: a Heaps curve.** Distinct domains against requests, measured in-file at 100/250/500/1k/2k/4k/6k/7,920, fits **k = 1.142 x n^0.6112**, so 0.102 new domains per request at 500 falls to 0.035 at 7,920 and 1e6 requests still yields 5,312 domains; at the measured **0.02025 EE per distinct domain**, 5,000 EE needs 246,869 domains and therefore **~534,000,000 requests in ONE in-window log**. Treating every in-window `access.log` in preserved media as a single log (677,090,428 distinct bytes at 160.7 B/request) gives 4.21M requests, 12,783 domains and a **258.9 EE ceiling for the whole family**, 168.9 EE once the already-closed server logs come out. Availability confirms it: the 74 in-window `access.log` rows at `sizeMin=100000` collapse by `b3sum` to **21 distinct contents**, of which 4 are the PARC/NASA server logs already closed under killer 6, 1 is an ISO matched on its name and the rest are shipped software furniture (an Apache 1.2.4 sample across 4 discs, a Wargasm InstallShield log across 30, SurfStats demo samples), so **item 14148 is the only live proxy cache in a 1.7-billion-file index**, and its `store.log` adds exactly 0 domains over its `access.log` (273 of the same 276). Two reusable bits: dexvert types every proxy log as plain `Text File`, so the format facet is useless here and only a filename union (`access.log`/`store.log`/`cache.log`) plus `sizeMin` screens the class; and `b3sum` dedup before fetching cut 74 rows to 21, the cover-disc republication effect again. **Do not re-test proxy, cache or store logs on any screen**, and do not widen to the `wwwoffle` spool (its 326 in-window contents are the SuSE RPM and the source tarball). With browser caches (2026-08-29), visitor logs and era research traces (2026-08-16, killer 6), all client-side shapes are now closed. **The transferable law: price a per-request traffic log by n^0.61, not by bytes**, and prefer an artifact that ENUMERATES a population (a register, a zone, a directory) over one that RECORDS activity across it, because discovery per artifact is the binding constraint. |
| **Structured database files on preserved media: a dBASE, Paradox, Clarion, FileMaker or Access table with a URL column beside a per-row DATE column, hunted across discmaster's format facet (2026-08-30)** | **CLOSED at 8.1 net-new post-split EE, and the class dies on record COUNTS while its premise holds.** What dates one item is the dBASE header's own last-update stamp (bytes 1 to 3, written by the database engine, not typed), which equalled the media mtime exactly on 92 of 120 sampled files and to within 400 days on 115 of 120, so the class is doubly dated and dating is NOT why it fails. Artifacts: 20 in-window dbf pulled member-by-member from ISOs via `https://discmaster.textfiles.com/file/<itemid>/<path>`, 12,767,550 B and 24,877 records, the largest being `1904TRBLSHTNTWKS.BIN/mtl/indexes/manufac.dbf` (`WWW:C40`, 3,057 rows, 1996), `The Best Shareware of the Year 1997 Winter.iso/approach/data/vendors.dbf` (`BBS_OR_WWW:C75` beside per-row `MODF_VEND:D8`, 3,457 rows) and three editions of the ASP author register `JCSDA.DBF` (`URL:C80` beside `LAST_MAINT` and `CNT_DATE`). Population from one facet request plus one count each: in-window `dbf` 152,554 files, `csv` 614,920, `paradoxDatabase` 32,405, `accessDatabase` 29,983, `fileMakerPro` 22,441; narrowed by content search, `q=www.&qfields=t&format=dbf` gives **3,099 in-window files, 876 of them 2001-dated**. A b3sum-random sample of **120 distinct files**, each screened on its header alone, finds **65 (54.2%) with a URL-ish column and 27 (22.5%) with a URL column AND a per-row date column**, so the premise is CONFIRMED at the schema level and the one-request kill does not fire. It dies on contents: **median record count in the URL-bearing set is 12** and the columns belong to shipped product templates. Priced with `scripts/pricing/price_items.py --all-tlds` against the live store at 15,624,381 domains / 25,742,394 pairs, sampling DISTINCT DOMAINS: 15,145 dotted rows give **2,131 distinct domains, 1,772 known (83.2%), 1,765 held in some year**, for **22 net-new post-split pairs / 8.1 EE** at mean weight 0.3672, under the 0.4 floor; the strict arm gives 3.9 EE and the permissive ceiling 220.3 EE, do not quote. **The kill is the 2001 screen, not novelty**: hold the population and move only the year to 2001 and it is 145 pairs / 47.0 EE, because P(store lacks 2001 | domain held) is **0.0822 overall and 0.0472 across the 975 held `.com`** against the 0.611 control, a 12.9x shortfall matching browser caches (34x) and hostname-shaped paths (16x); the only TLD that passes is `.br` at 0.3895 over 190 names off one Brazilian magazine disc, and `.br` is worth nothing. Rates: 0.141 domains per record, **0.0038 EE per listed domain at the artifact's own year and 0.0221 at the 2001 counterfactual**, both below the 0.013 to 0.024 curated-directory floor that already closed directories, and the family ceiling over all 3,099 files is ~198 EE (~1,150 EE at the unattainable 2001 rate) for ~3,100 downloads, overstated because the three `JCSDA.DBF` editions are one register republished and the second added 5 domains over the first. About 285 requests, 1.5 to 2 s apart, one in flight; robots.txt read in full first. **Method, four reusable pre-download pieces.** (1) `/file/<itemid>/<path>` answers **HTTP 206**, so `-r 0-8191` reads a 153 MB table's field list and its engine-written date stamp for 8 KB, a 20,000x saving, which turns "does this artifact have a URL column" into a query runnable over all 152,554 in-window dbf. (2) discmaster content search reaches INSIDE binary dbf but returns zero inside Access against a working control, so the 29,983 `.mdb` are filename-reachable only and their whole URL-named population is at most ~600 files. (3) A third confirmation of the shipped-not-used law, now generalised past `/etc` and browser profiles to APPLICATION DATA: **price "a real database survived on a disc" at the MEDIAN record count of a random header sample, not at the largest file's size.** (4) Sorting by size returns one product republished (40 stock-quote dumps off three discs; one Norwegian accounting package across six magazines), which b3sum does not collapse, so a b3sum-random sample is the only honest population estimate. **Do not re-test dBASE, FoxPro, Paradox, Clarion, FileMaker, Works or Access tables on preserved media on any lens, and do not re-test magazine cover-disc indexes or shareware vendor and author registers in any format.** One arm is unopened rather than closed: `.mdb` contents were never read (`mdbtools` absent) and cannot be Range-screened; if anyone spends time there the two worth downloading are `CP007 1999.02.iso/Soft/Internet/Bookmarks.bin/1stSource.exe/MAINDIR/WebSites.mdb` (804,864 B, 1998-12-16) and the two editions of `AstroBookmarks.mdb` (2000-07-21 and 2001-12-26), and the prior for both is the 8.1 EE measured here. |
| **A live web search engine used as an existence instrument, pointed at the file signatures of the families closed on ARTIFACT EXISTENCE (2026-08-30)** | **CLOSED at 0.0 net-new post-split EE, and the instrument itself is validated and works.** It surfaced an artifact six independent existence layers had missed: `https://rscott.org/OldInternetFiles/domain-info.19960614.txt`, 9,918,513 B, the complete InterNIC gTLD register at 14 June 1996 (COM 374,462 / NET 15,452 / ORG 26,331 / EDU 2,661 / GOV 451, 417,109 distinct names), held independently a second time at `https://www.statdns.com/files/`, against a register entry that had closed the series with "Every surviving copy is 1993". What dates one item is the machine-written stamp `DDN NIC DOMAIN SUMMARY   14-Jun-96` on line 3 of the file, class `artifact_listing`. Neither host serves a robots.txt (404 on both), so both are permitted; `emaillab.jp` disallows `/pub/` and every candidate file there sits under it, so it was not fetched. PRICE, `scripts/pricing/price_items.py --all-tlds` against merged260827: 417,109 items, all in window, 417,103 distinct pairs, 417,103 already held, **0 net-new pairs, 0.0 EE**, 0 names new to the candidate pool. An independent duckdb screen sampling DISTINCT DOMAINS reads 417,101 of 417,109 held at 1996; the 8 residuals are fixed-width column-wrap junk (`concept-.com`, `comicnet..com`) or third-level names, not registrations. The store's 1996 `com` baseline, 559,053 names, is already denser than the entire mid-1996 `.com` namespace of 374,462. Other in-window files on the host are zero too, and the negatives are proved against the files' own headers, not asserted: `hosts.19960614.txt` and `hosts.19970826.txt` give 103,945 and 143,493 lines, every one `NET : addr : NETNAME :`, matching the header "contains data about IP networks only"; `edu.19991120.zone` gives 5,850 distinct `.edu` delegations, 5,850 of 5,850 already held at 1999, and is terms-blocked independently of value because lines 2 to 5 paste Network Solutions' zone-file access agreement. A first parser returned 0 on the zone through a layout bug and was re-derived before being believed. **The number that matters is the edition that does NOT survive**: the same 417,109 names on the adjacent-year screen (held Y-1, missing Y), so nothing takes the split, are worth 47,982.6 EE at 1997, 49,729.0 at 1998, 8,874.1 at 1999, 8,710.0 at 2000 and **61,033.9 at 2001** (com 56,512.3, org 2,778.6, net 1,525.7, edu 161.3, gov 56.0). The series terminates at 1996-06-14 on BOTH holders, which is the strongest available evidence that no later edition survives. METHOD, and it is the transferable part. (1) **Search the machine HEADER, not the host**: `"DDN NIC DOMAIN SUMMARY"` returned both holders as results #1 and #2, while `"domain-info.txt"` returned neither and so did six existence layers. (2) **Always run the "engine quoted it back" control before recording a zero**: the three named validation positives all returned nothing, which read as a dead instrument and was not; searching a phrase the engine had itself just quoted returned its source page as #1, proving literal retrieval works. Without that control a semantic engine's topical fallback is indistinguishable from a proved negative. (3) The 0 of 3 has a structural cause: all three positives live inside a `.tar.gz` member or in Wayback replay, and **a live engine indexes SERVED pages only**, so it is blind to archive members and dead hosts by construction. Never validate it on an archive-only artifact. (4) A frozen personal "old internet files" collection skews pre-1996, because enthusiasts collect the hosts.txt era, so expect this class to keep landing just outside the window. (5) Price the edition that does not exist before hunting it: one adjacent-year query over a surviving edition's name list priced every missing edition. This is the sharpest confirmation yet of "aim at 2001, not 1996": the most complete registry artifact of the era, 417,109 names, machine-dated, master-eligible and freely served, adds exactly zero. The untried application is method 1 run systematically over the machine headers of every artifact already banked, filtered to 2001 headers only. Do not re-test rscott.org, statdns.com, `emaillab.jp` (robots-blocked payload), the InterNIC `domain-info` series, or DNS-OARC (members only). |
| **OpenPGP keyserver user-ID binding self-signatures, at open-keyserver scale rather than on the Debian keyrings (2026-08-30)** | **CLOSED at 0 EE on ARTIFACT EXISTENCE, and no host was contacted, because the family is already closed twice over.** `sources.md:775` closed the SKS and Hockeypuck bulk dumps on 2026-08-18 across nine hosts (dead, NXDOMAIN or 404, with `pgp.key-server.io/sks-dump/` serving a squatted 1,095-byte stub under HTTP 200 and `keys.openpgp.org` publishing no dump by design), and `sources.md:776` closed the only retrievable substitute, the curated distribution keyrings, on size at 69 net-new pairs and 44.4 EE over 4,096 items. The evidence class was never the problem: what would date one item is the binding self-signature's creation time, machine-written by the signing implementation, which `discovery.md:56-60` already separated from the key creation time over 4,225 signatures. What is new is a **zero-network pricing of what such a dump WOULD have paid**, measured read-only against the store's own era email-address population, `usenet_address`, 899,998 distinct domains sampled AS DOMAINS. Bracketed between two independently derived rates: FLOOR 0.01084 EE per binding (the Debian empirical), CEILING 0.0585 EE per binding (0.346 pairs per item x 0.898 held x 0.2979 lacks-own-year x 0.6321), so 5,000 EE needs between 85,460 and 461,261 in-window bindings. **So the family was not arithmetically dead; it dies entirely on the artifact not existing.** THE DECISIVE NUMBER, and it corrects a project-wide screen: P(store lacks 2001 | `com` held) is **0.286** on an email-address population against the store-wide 0.611 that CLAUDE.md's 2001-threshold rule assumes, a 2.1x shortfall, so an already-held `com` name dated 2001 is worth 0.181 EE here and "1,000 EE needs ~2,600 held com names" becomes ~5,500. This joins Backflip at 0.041 and the CACM/IEEE citation set at 0.044 as a third independent confirmation, with a different selector again, that **self-selected populations concentrate in the years the store already covers**. The mechanism is visible before fetching: era address populations peak 1999-2001 (355,046 / 340,409 / 336,075 in this one) and the store's `com` gap is narrowest exactly there (.209 / .093 / .286), so the years an address corpus supplies most are the years it can least add. METHOD: **price an unobtainable artifact against a proxy population already in the store**, at zero network cost, which converts "no dump exists" from a dead end into a stated size threshold the next proposer must beat, instead of a re-run of the host census. Do not re-test the nine dump hosts, `keys.openpgp.org`, archive.org or Zenodo, and do not re-price the Debian, GNU or Apache keyrings; only a previously unknown artifact holding ~100,000 in-window bindings reopens it, and it is worth one grep if one ever surfaces inside another lens's whole-host index. |
| **Letters to the ccTLD registries that still hold their own 1996-2001 registers: Nominet, auDA and CIRA (2026-08-30)** | **BLOCKED at 0 EE, and two of the three namespaces are closed permanently while the third is now sized.** No data artifact was fetched; this is a screen of the three registries' published data-access policies plus a repricing against the live store (merged260827, 15,624,381 domains, snapshot 14:42), every count taken as DISTINCT DOMAINS off a per-domain pivot. **The migration screen decides where a letter can go, and it runs before any policy is read.** Ask when the namespace last changed registry operator and whether the changeover re-created the licences, because a re-registration resets every creation date to the migration instant. `.au` dies outright: Kevin Robert Elz administered it from 1986, `com.au` was licensed to Melbourne IT in October 1996, and auDA's competition model plus the AusRegistry registry began 1 July 2002 with 282,632 names under management, so auDA never held the window and its published-policy archive begins in 2002 with no earlier document. `.ca` is halved: from 18 September 2000 every registrant had to transition to CIRA by RE-REGISTERING, CIRA became the registry on 1 December 2000, so only the 2000 and 2001 slices are CIRA's to give and the 1996-1999 headroom is not. `.uk` survives whole, and the **inverse test** proves it in one record: our own Nominet RDAP observation has `demon.co.uk` at `1996-05-05T21:08:48Z`, nineteen days before Nominet was incorporated on 24 May 1996 (company number 03203859, printed in its own policy PDF), so the UK Naming Committee register was imported with its dates intact rather than restamped. **Bidirectional headroom (held Y-1 or Y+1, missing Y), the correct screen for a register since a register attests membership at Y:** `.uk` 702,229 EE total (1996 58,978 / 1997 27,390 / 1998 121,088 / 1999 202,226 / 2000 173,429 / 2001 119,118), `.au` 167,587, `.ca` 126,063; adjacent-only `.uk` 194,173, `.au` 53,401, `.ca` 49,486. Held distinct in-window: `.uk` 538,057 of 589,786, `.au` 123,408 of 154,019, `.ca` 105,045 of 133,560. The naive held-any-year-missing-Y figures, which must not be quoted, are `.uk` 2,190,672, `.au` 440,660, `.ca` 313,748. **The hypothesis over-priced its own prize by about 1.9x** by quoting those naive per-name numbers as register value: it claimed `.uk` 1999 at 0.7546 EE per already-held name, `.au` 0.6309, `.ca` 0.5270; measured today the naive figures are 0.7333, 0.5418, 0.4433 and the defensible register figures are **0.3758, 0.3402, 0.2930**. Cross-check: `.uk` 1999 bidirectional measures 202,226 EE here against the 200,574.8 EE recorded on 2026-08-27, agreeing to 0.8% with the store having grown between; and the CIRA-reachable 2000+2001 slice at 50,656 EE, spread roughly evenly over 2000-2002 transitions, gives ~24,900 EE, independently reproducing the 25,377 EE this file derived for `.ca` by a different route. **The policy screen answers no in all three, so the RIPE precedent does not transfer as a channel.** RIPE worked because its own README named `hostcount@ripe.net` for exactly this ask; no ccTLD registry here names an equivalent address. Nominet's only published door is the Data Release Policy (`cdn.prod.website-files.com/.../DataReleasePolicy.pdf`, 170,892 B, 4 pages, read in full), exclusively about personal data on a NAMED domain for legal enforcement, its three worked examples a trade mark holder, solicitors enforcing IP rights and a law enforcement agency, with no bulk, research or historical clause; the registrar-facing `data.nominet.uk` is credentialed one login per accredited registrar and serves the current zone file and drop lists, so killer law 4 applies to it. auDA's complete 2002-to-date policy archive (521,566 B) holds a WHOIS Policy, a Privacy Policy and a Registrant Contact Information Policy and no data-access, research-access or bulk-release policy of any kind. CIRA's complete document index (read at `web.archive.org/web/20241214125556id_/https://www.cira.ca/en/resources/documents/`, 526,815 B, live host 403s behind Cloudflare) holds a DNSSEC Practice Statement, Dispute Resolution Rules, the Registrant Agreement, Professional Services terms and privacy documents, its only data publications being the aggregate Internet Factbook and `.CA` Insights Reports. Freedom of information reaches none of them: Nominet is a private company limited by guarantee, auDA a not-for-profit under a Commonwealth endorsement rather than a Commonwealth entity, CIRA a not-for-profit corporation. **Operational: a WAF is not a refusal and is never a reason to file a policy as unread**, since when a registry's live site 403s an honest User-Agent, its policy documents are usually on a CDN host that does not, or in Wayback replay at `/<ts>id_/`. Robots read whole before any request: `nominet.uk` and `www.nominet.uk` return a 103 B 403 to an honest User-Agent and to Claude-User alike including on `/robots.txt`, so no Nominet robots file could be read at all; `registrars.nominet.uk/robots.txt` 179 B allow-all; `www.cira.ca/robots.txt` 188 B blocks only `/wp-admin/`, `/?s=`, `/page/*/?s=`, `/search/`; `www.auda.org.au` and `files.auda.org.au` serve no robots.txt; `rbscarchives.library.ubc.ca/robots.txt` 26 B, both lines commented out. **Do not write to auDA and do not re-open `.au` on this shape.** Exactly one letter is worth Ivo's time and it is on the open decision sheet: Nominet, for a two-column extract of domain name and registration date for `.uk` names registered before 1 January 2002, no registrant data at all, which puts the ask outside the Data Release Policy's scope entirely, addressed through `registrars.nominet.uk` and NOT to `data-release@nominet.uk`, which is the personal-data door and will bounce. **The one genuinely untried thread is the third question the screen ends on: who held the window is routinely not who holds the namespace now.** The pre-December-2000 `.ca` register was maintained by hand at UBC Computer Science by John Demco and a team of volunteers on UBC facilities, more than 100,000 names before the handover, so the 75,407 EE of 1996-1999 `.ca` headroom that is not CIRA's to give belongs to a UNIVERSITY, which has an archive and a deposit policy where a registry has terms of use. UBC's archival catalogue at `rbscarchives.library.ubc.ca` has no robots rules but is a JavaScript-only AtoM instance returning a 292 B "Please enable JavaScript" shell to a plain client, so it could not be searched here; it needs a JS-capable fetch or one email to UBC Archives asking whether a CA Domain Registry fonds exists. |
| **Machine-written Usenet posting families: a whole-spool census of the on-disk 1996-2001 archives for host-dense and recurring robot postings, the class both five-figure Usenet wins belong to (2026-08-30)** | CLOSED at **780.1 net-new post-split EE**. No network used: the 16,849 `*.mbox.zip` already under `data/raw/usenet_*`, 456.82 GB expanded and 178,700,605 messages, read end to end on two gates; what dates one item is the message's own `Date:` header written by the posting agent, over host tokens a human typed or a spammer forged, so the split applies. 109,006 items, 357,859 pairs over 261,996 domains, 240,776 already held, **2,207 net-new pairs and 780.1 EE** (117,083 pairs and 71,311.2 EE pre-split, do not quote, 91.4x), mean weight 0.3535, typo bound 62.7%. The kill is one number: of 45,147 domains named at 2001 that the store already dates, 682 lack 2001, P = 0.0151 against store-wide `com` 0.611, a 40x shortfall holding in every year of the window, because four collectors have already read this spool whole and a new SELECTION over it recovers only what they missed. Census, not a sample, so it bounds the class: 0 of 65,101 families reach 10,000 distinct in-window domains. Positive control on `can.domain`, which the same screen ranks the known CA-Registrar family first from its signature alone. Method in `discovery.md`, the re-selection law. |
| **Software Heritage as the one un-robots-barred archive of 1996-2001 source trees, hunting the period DATA FILE inside a revision (`domains`, `blacklist`, `urls`, `hosts`, `sites`) (2026-08-30)** | **CLOSED at 0 EE on the archive itself, because nothing in it dates one item, and at 13.4 net-new post-split EE on the control corpus that does carry the stamp.** `softwareheritage.org` stores content addressed by hash and its public export factors the filename and the date into separate tables, so the blob is the only thing a file family can be asked for and the blob carries no stamp: 0 of 692 sampled from the exact name family carry a machine-written 1996-2001 generation stamp. That fails condition 2 of the standing rule at the CLASS level, so no artifact in the archive can be admitted whatever its contents, and the revision's committer timestamp dates the commit and not the list inside it. **Control arm, run so the zero could be told from pointed-wrong**: `https://archive.debian.org/debian/dists/{bo,hamm,slink,potato,woody}/`, the same file family in a container where the stamp does exist as the `.deb` `data.tar` member mtime. 2,124 in-window items give 1,758 pairs over 1,108 domains, 1,067 domains held (96.3%), **21 net-new pairs and 13.4 EE post-split** (68 pairs and 47.9 EE pre-split, do not quote, 3.6x), mean weight 0.6380, typo bound 66.1%, and 97 packages carry the family against 48 read, so the ceiling is ~27 EE. **The kill is the 2001 screen: of 552 domains named at 2001 and held, 14 lack 2001, P = 0.025 against store-wide `com` 0.611, 24x worse.** A hostname printed in a config file that a Unix distribution shipped is a public server the crawlers reached, so a package payload belongs with the operator and authority corpora and not with the blocklists. **Method worth keeping: `Contents-<release>.gz` is the `ls-lR` trick for a package archive.** Five requests indexed 1,695,996 paths and reduced the whole archive to 270 candidate paths in 97 packages before a single payload was fetched, and the whole measurement cost 18 MB. **Do not re-test** Software Heritage, `archive.debian.org` package payloads, or the period-data-file-inside-a-package shape anywhere. |
| **The other four regional registries, on the reading that just paid 90,770 EE at RIPE: the RPSL `changed:` attribute in the APNIC, ARIN, LACNIC and AFRINIC database dumps (2026-08-30)** | **CLOSED at 99.7 net-new post-split EE, and refuted on SCHEMA, not on availability: every dump in the family was reachable and read.** `changed:` no longer exists outside RIPE's frozen mirror. APNIC carries 0 `changed:` lines, bulk-replaced by `last-modified:` stamped `2008-09-04T06:40:26Z`; LACNIC has 533,380 objects all carrying `changed:` and 0 in window, over a schema with no hostname attribute at all; ARIN's IRR has 25 in-window `changed:` lines out of 25,663 (0.097%), the same shape the register already priced at 4.44 EE. The `*dn:` reading that gave RIPE 1,232,554 names has no counterpart either: APNIC's 304,694 `domain:` objects are 28 forward names and the rest in-addr.arpa, because RIPE NCC was a domain registry for European ccTLDs in 1999 and the other three are reverse-delegation registries that never held forward names. The three NIR mirrors APNIC carries (`jpnic`, `krnic`, `twnic`) are stripped identically: 0 `changed:`, 0 `nserver:`, 0 `domain:`. No registry publishes a history of dumps, proved rather than assumed: ArchiveTeam's `archive.org/download/ftp.arin.net/2014.05.ftp.arin.net.tar.txt`, 19,483,705 B, lists all 186,105 entries of the 43.4 GB tree and only 123 carry a 1996-2001 mtime, every one rwhois source, an RFC PostScript or a slide deck, while `web.archive.org/web/20000817130836id_/http://ftp.apnic.net:80/apnic/` shows no `whois/` directory in August 2000, so the bulk dumps postdate the window. AFRINIC not fetched: `ftp.afrinic.net/robots.txt` is 58 B and 4 lines, read whole, and `Disallow: /db` is a prefix covering both its database paths. **The one artifact that survives, priced in full against the live store: 99.7 EE.** `https://ftp.apnic.net/apnic/arin/arin.zones.tar.gz`, 405,696 B, `Last-Modified: Wed, 03 Feb 1999 08:50:39 GMT`, is APNIC's tar of its own BIND slave directory holding ARIN's twelve in-addr.arpa /8 zones. What dates one item is BIND 8's own transfer comment at the head of each zone file, `; from 192.149.252.21   at Thu Jan  7 12:22:21 1999`, written by `named` when the AXFR completed, backed by the SOA serial `1998111700` and the tar member mtimes. 194,400 NS records give 8,805 hostnames and 5,591 registrable domains at 1999, of which 5,406 already carry 1999 (96.69%): **157 net-new pairs and 99.7 EE post-split** (185 pairs and 116.2 EE pre-split, do not quote), mean weight 0.6352, read twice by independent code agreeing on every figure. Not banked: the tarball is ARIN's data on APNIC's host and carries no notice of its own, so its terms are the one condition of the standing rule left open, and 99.7 EE does not justify chasing them. The directory has been frozen since 1999, so there is no second edition to recover. **The letter route the hypothesis proposed was priced before writing and is not worth sending: 299.3 EE.** APNIC's 717,655 live `nserver:` lines give 10,423 registrable domains of which only 557 are held-and-missing-2001; the other 6,878 are modern nameservers, so the pre-split 4,305.4 EE overstates the source 14.4x. The ARIN region is bounded worse, since 99.607% of the 5,591 operators its own reverse tree names are already held, and the counterfactual caps even a perfect 2001 snapshot of the identical artifact at 372.2 EE. **Method, two reusable pieces.** (1) The index mtime is the screen: on a host with no whole-tree index, walk the HTTP autoindex and keep only rows whose own date column falls in 1996-2001, which is how the paying artifact was found, in a directory one registry keeps for another registry's operational files and stopped tidying. Where an ArchiveTeam grab exists its `.tar.txt` does the same job for the whole host in ONE request and also recovers what has since been deleted: 19 MB answered 43.4 GB. (2) Price the family's ceiling from the MODERN file before asking anyone for the old one: today's `nserver:` set re-dated to 2001 bounds what a 2001 snapshot could ever be worth, and it cost one download to learn the letter was not worth Ivo's time. Terms read whole first on all eight hosts; APNIC's bulk-access AUP does permit "Internet research and analysis", so the whois tree was usable and is simply worthless. The counterfactual re-dating the same 5,591 names to 2001 pays 372.2 EE, 3.7x the 1999 reading, so the aim-at-2001 law holds on this population too. |
| **An exhaustive sweep of the NAME space: a machine walking a wordlist or every three- and four-character string and recording taken or available at a stamped instant, hunted in the speculator Usenet groups, on archive.org, in discmaster and at the "N-letter domains" hosts (2026-08-30)** | **CLOSED at 22.1 net-new post-split EE on the one arm that exists, and the artifact the hypothesis describes does not survive at all: five independent existence layers returned zero against a working positive control in each.** The only arm with data is `https://archive.org/download/usenet-comp/comp.protocols.tcp-ip.domains.mbox.zip`, 24,402,629 B, 36,737 messages, 23,655 in window; what dates one item is the message's own `Date:` header in the Usenet spool, machine-written by the posting agent, over a name list a human pasted, so the names take the corroboration split. At 60+ distinct names only 121 in-window posts exist and at 200+ only 6: 3,835 pairs over 3,264 distinct domains, 2,986 already held, 849 net-new pre-split (601.8 EE, **do not quote, it overstates by 27.2x**), **49 net-new pairs and 22.1 EE post-split**, mean weight 0.4510, typo upper bound 43.7%. Reading them kills the shape independently: 100 of the 121 are automated AlterNIC and International Root Server Confederation "Root Sync Reports" listing 130 to 180 TLD **nameservers**, and the largest single post, 1,212 names, is a 1996 list of DNS servers with UDP checksums disabled, so the densest posts in the densest group are killer 3, authorities and not hosts. **The four other layers, each with its control.** (1) The three groups the hypothesis names are out of window: the four `alt.domain-names.*` mboxes are 9,023,811 B and 9,046 messages with **earliest post 2003 in every one of the four**, while the same screen finds 56 posts carrying 100+ host tokens, all 2003 or later. **Corrected 2026-08-30, verified twice: the reason is wrong and the closure still stands.** The two copies on disk are `alt.domain-names.forsale.mbox.zip` 9,642,746 B, 29,342 messages, 18,286 dated 1999-2001, and `alt.domain-names.registries.mbox.zip` 4,291,950 B, 9,680 messages, 5,885 in window, so the in-window population does exist; re-priced over every in-window post of both, 22,163 items and 41,327 distinct domains give **88 net-new pairs and 31.6 EE post-split**, typo bound 68.7%. (2) archive.org `advancedsearch.php`, never `scrape`, control `title:(squidguard)` returning 3 in the same batch: `"domain availability"` in window **0**, `"zone file"` **0**, `title:(domainsurfer OR namedroppers OR "domain name database")` **0**, `"registered domains"` **1**, a 2002 shareware checker. The two availability tools that do exist ship no data (`avdom102_zip` is 402,933 B of `setup.exe` and a readme; `tucows_220806_Check_Domains` is a 2002 installer). (3) discmaster over 1,722,834,293 indexed files, `tsMin=19960101&tsMax=20011231`, control `blacklist` at 1,290: `domains.txt` returns 196 matches and **all 100 shown are two programs**, VisualRoute's ccTLD lookup table and the Up Yours mailbomber's relay list; `fourletter` and `domainlist` return nothing. (4) `archive.org/wayback/available` at `timestamp=20010601` on the hosts whose names promise the artifact, control `namewinner.com/whole_list.php?del=tab` returning its known capture: all seven with 2001 captures are framesets, parking pages, registrar homepages or one person's hand-typed 40-name for-sale page, and three have no captures at all. **The prize is real, which is why the zero matters, and it was priced off the store with no network at all**: 3-letter `.com` is 17,576 of 17,576 held and all 17,576 carry 2000 but only 10,595 carry 2001; 4-letter `.com` is 152,659 held, 114,659 at 2000, 69,836 at 2001. One 2001-dated per-name sweep of six letter spaces is worth **73,321 EE gross over already-held names**, **42,300 EE** on the adjacent-year-clean subset, plus ~13,477 EE from pool names an `artifact_listing` would date without the split. **That is the largest measured ceiling this lens can reach and there is no artifact under it.** **Method, and the first two transfer.** **(a) Ask when a Usenet group was CREATED before pricing what is in it**: one Giganews item per hierarchy with a `/metadata/` file list costs one request, so a whole hierarchy's year histogram is two requests and a `From `-split, and it settles the entire speculator family in under 10 MB. **The domain aftermarket's published record starts in 2002-2003, after the window closes** (alt.domain-names 2003, DNForum 2002, dnjournal 2002), so any hypothesis of the form "the speculator trade published tables" is asking the trade for records from before it had a press and dies on that alone. **(b) Price the enumerable space against the store BEFORE hunting the artifact**: generating `aaa.com`..`zzz.com` and joining `domain_year` costs one query and no requests, and it turns "would a sweep be worth it" into 73,321 EE with a per-space breakdown. Do this for every "somebody enumerated X" hypothesis. (c) A 2001 capture of a promising HOST NAME is worth almost nothing on its own, and `wayback/available` plus one `id_` fetch settles each for two requests with no CDX. (d) Confirmed for a second lens: an availability-checking PROGRAM never ships the answers, so screen software items out unless the file list shows a data file larger than the binary. **Do not re-test** the `alt.domain-names.*` groups, `comp.protocols.tcp-ip.domains`, discmaster by filename for this signature, or the "N-letter domains" host family. The 73,321 EE ceiling stays open with no public route: a self-run 2026 sweep evidences current state and not 2001 (killer 4), the Verisign quota that would pay for it is spent, and the only remaining routes are the two open non-public letters that `era_bulk_whois` already closed on. |
| **The Linux Counter machine census, a row-per-machine registry with its own entry date (`counter.li.org` reports, plus `lugww.counter.li.org`) (2026-08-30)** | **CLOSED at 0 net-new post-split EE over 0 distinct domains, because the registry never collected a host name.** `https://web.archive.org/web/19991117060801id_/http://counter.li.org:80/machine-form.html` and 13 siblings, 189,831 B. What dates one item is the counter's own machine-written registration stamp, printed per row on `reports/recentusers.php3` as `2001-12-24 20:20:16`, on a row that carries no host name at all: 15 machine rows, 15 exact timestamps, 0 host names. The 1999 registration form's complete field list is key, name, country, purpose, cpu-count, cpu-maker, cpu-type, cpu-speed, cpu-comment, memory, disk, network, distribution, distribution-version, source-main, source-detail, users, accounts, with no hostname, FQDN or URL field, and every machine report (`machines.html` 1999, `machines.txt` 2001, `systemstats.php` over 1,856 machines, `uptimestats.php` over 2,315) is an aggregate histogram. The volume half was right and did not save it: 100,306 registrations by 2001. Two further independent kills, either sufficient: the operator's whole 186 B `robots.txt` disallows `/cgi-bin/`, the only per-record view that could have carried a domain, and `legal.html` forbids "bulk usage" under the Berne Convention and the Norwegian Privacy Act and says the database "has been seeded with addresses to detect such usage", so those are terms we do not hold. The sibling `lugww.counter.li.org` (489 groups, homepage URL and a per-record "Last changed on 2001-04-01") was dropped once `sources.md` was found to close it: at the 0.0553 EE per listed group measured on `linux.org/groups`, 489 groups price at ~27 EE. **Method, three reusable parts.** **(a) For a registry hypothesis, read the REGISTRATION FORM before the reports**: one 6,690 B `machine-form.html` settled a 100,306-row corpus in a single request, because a field the form never collected cannot appear in any listing, in any year, at any fill rate. That is strictly cheaper and strictly more decisive than the fill-rate screen, which only measures how often an existing field is populated. **(b) A dead origin host is not a dead robots.txt**: `counter.li.org` no longer resolves, but its archived robots and terms are retrievable at `web/<ts>if_/<host>/robots.txt`, and they are the operator's standing instruction; fetch both before walking an archived site. **(c) A host-name grep is not a family grep**: "counter.li.org" and "linux counter" both return nothing here, which reads as a clean lens, while the LUG register the same host serves is closed three lines inside the entry titled "Federation club registers". Navigation cost no CDX: `archive.org/wayback/available` then `web/<ts>id_/<url>` and the page's own hrefs, with a retry wrapper, charging the politeness gap only to requests that reached the server. **Do not re-test** `counter.li.org`, `lugww.counter.li.org`, or any Linux Counter report. **One residual the negative does not touch**: a census that recorded the machine's NAME, which this one deliberately did not. The era's public NTP stratum-1 and stratum-2 server lists and the public DNS-secondary and Usenet-feed offer lists are named-host censuses with maintenance stamps, and neither is a roster, a directory nor a club register, so neither collides with a closed family. |
| **Registry whois records transcribed into artifacts that stamp themselves: pasted whois blocks in the on-disk Usenet spool, and the registrar creation dates recited in WIPO and NAF decisions (2026-08-30)** | **FIND, BANKED at 30.4 EE on the Usenet arm** (see `usenet_whois_paste` below), the UDRP arm PARKED. The Usenet arm reads the registry's own `Record created on 20-Jul-2000.` line out of 16,849 archives already on disk at zero network cost: 769 in-window pairs, 92.1% already held, 50 net-new pairs. **The UDRP arm fails condition 2 of the standing rule and is not ingested**: the date reaches the artifact as a panellist's sentence, `The Whois record of the domain MUSICWEB was created on January 10, 1995` (WIPO D2000-0001), which is a human recitation of a machine stamp and not the stamp itself, and `adrforum.com/robots.txt` answers with a redirect body rather than the file, so the NAF arm never had its terms read either. Measured anyway at 1,125 WIPO and 550 NAF decisions fetched: WIPO 284 pairs, 73.6% held, 46.2 EE; NAF 70 pairs, 75.7% held, 10.4 EE. **The family ceiling is 21,284 EE only if every one of the 8,892 disputed names earned every missing year, and rule 6 gives a creation date its own year alone, so the real ceiling is a small fraction of that.** Two things worth carrying. **A whois block puts the name twenty to forty lines above its creation line, so the binding is the whole risk**: an earlier pass bound `openssl.org`'s 1998 date to `engelschall.com` because a message quoted the same block three times and the HTML-escaped copy defeated a start-anchored name pattern while the date pattern still matched, the same mis-binding that once overstated the Edelman transcriptions by 47%. Fix is one normalisation both patterns read, plus a 40-line look-back cap carried on every row so it can be re-argued against data. **And `whois_creation` rows are year-checked against the first four-digit run in their evidence value**, so a value naming a group and a Message-ID would be read as the year 2000 on every row out of `microsoft.public.win2000.dns`: put the registry's own date first. |
| **MTA rejection notices quoted into dated messages: `Final-Recipient`, `Diagnostic-Code` and `550` blocks across mailing-list archives, LISTSERV gateways and the Usenet net-abuse groups (2026-08-30)** | CLOSED at **0.8 net-new post-split EE**, priced entirely off disk over 8.5 GB in four corpora (`data/raw/maillists/python` 1,207 archives, `data/raw/maillists/gnome` 1,415, 61 net-abuse and mail groups in `data/raw/usenet_bulk`, and all 582 `bit.*`, `mailing.*` and `fa.*` LISTSERV gateways in `data/raw/usenet_new`), with nothing fetched. What dates one item is the `Date:` header of the message quoting the notice, machine-written by the sending MUA, over a DSN body machine-written by the rejecting MTA. **700 in-window bounce messages gave 337 pairs over 307 domains, 313 pairs already held, 2 pairs and 0.8 EE post-split.** **The reusable law, and it generalises past mail: a corpus that fires on LIVENESS cannot add the year in which the liveness was observed.** A bounce exists because somebody was actively mailing that domain that month, and a domain being actively mailed in year Y is exactly the population the store already dates at Y. The discriminator, sampled as DISTINCT DOMAINS: **285 of 307 domains held (92.8%), and of the 315 pairs whose domain is held the store lacks the artifact's own year for 2, 0.6%, against a 58.3% control drawn at the same TLDs and years.** 97x worse than the population, so this is pointed-wrong, not nothing-found, and the held fraction is the wrong screen for any activity-triggered artifact (bounce, referrer, DNS query log, mail log, ticket queue). Density kills it independently: a census of the 61 net-abuse groups found 436,003 messages, 1,530 (0.35%) carrying any bounce marker and 156 (0.036%) dated in window. The dead arm (`Host unknown`, `Unrouteable`) was priced separately at 0.5 EE and should not be counted at all, since a rejection for an unknown host is evidence of ABSENCE and the address was human-typed besides. Method worth keeping: split the recipients into a live arm (`550 user unknown`, `552`, `554`, `Remote-MTA: dns;`, which prove a mail exchanger answered) and a dead arm, because a naive grep counts the negative arm as existence evidence. Do not re-test DSN material in list archives, LISTSERV gateways or net-abuse groups. The one residual is a different hypothesis: a bounce corpus whose recipients were never subscribers, such as a bulk-mailer's return-path log or an ISP postmaster dump from 2001-2003, and nothing of that shape is on disk. |
| **Pasted `user:pass@host` paysite credential dumps, hunted as a target across the whole Usenet spool plus 51 credential-family groups fetched for it (2026-08-30)** | CLOSED at **6.7 net-new post-split EE** over both arms. `https://archive.org/download/usenet-<hierarchy>/<group>.mbox.zip`, 1,515,813,396 B newly fetched (all HTTP 200; archive.org robots.txt read in full first, 12 lines and 238 B, `Disallow: /control/` and `/report/` only) plus the 16,849 archives already on disk, of which the sweep read 16,500 (97.9%) before the session ended. What dates one item is the `Date:` header of the message carrying the dump, machine-written by the posting agent; the `http://user:pass@host/members/` line itself is pasted by a human, so the hostname takes the corroboration split and only already-held names earn a year. **Union of both arms: 13,234 pairs over 9,686 distinct domains, 96.2% of domains held in some year and 97.0% of pairs already held, 17 pairs and 6.7 EE post-split** (whole-spool arm alone 12,851 pairs, 2.3 EE; targeted arm 792 pairs, 4.4 EE). **The kill is the 2001 screen: of 1,748 domains named at 2001, 1,610 are held and 10 lack 2001, so P(lacks 2001 | held) = 0.006 against the store-wide `com` 0.611, 102x worse.** The premise was that a 2001 paysite is short-lived and thinly crawled; measured, a host that was worth scraping a login for was a host with traffic, and traffic is what the crawlers followed. **A dump is a popularity ranking, not a census**, which puts it beside the magazine archive and the operator corpus on the head-selection list, and it refutes the 7.6 EE per dump page rate the Something Awful closure left behind: that page was one lucky page, not a rate. 362 of the 372 pre-split net-new pairs are novel names that earn nothing. Do not re-test credential or password material anywhere in Usenet, IRC logs or the warez hierarchies. |
| **Resolver and zone-file fragments pasted into dated messages: `dig` and `nslookup` answer sections, `named.conf` and zone excerpts, across ten Usenet DNS-operations groups (2026-08-29)** | CLOSED at **15.6 net-new post-split EE**. `https://archive.org/download/usenet-comp/comp.protocols.dns.bind.mbox.zip` and nine siblings, 275,156,324 B, 199,227 in-window messages; what dates one item is the `Date:` header of the message carrying the paste, machine-written by the posting agent, over a resolver's own ANSWER section. 5,033 pairs over 4,391 distinct domains, **81.7% already held**, 43 pairs and **15.6 EE** post-split. **Two reusable pieces.** (1) For a corpus of a KNOWN OPERATOR population, screen on P(lacks 2001 | held), never on the held fraction: here it is `com` 0.128 against the store-wide 0.611, so a held `com` name is worth 0.081 EE and the hypothesis's own floor of ~2,600 held `com` names understates the requirement by 4.8x. Somebody who ran BIND in 2001 ran a server the crawlers reached. **Operator corpus belongs beside authority corpus on the pre-download list.** (2) Prove the negative against the COUNTERFACTUAL store: a store that had never read a Usenet body would still price this lens at 937 pairs and 543.1 EE pre-split, **26.6 EE post-split over 59 pairs**, far under the bar. That separates "pre-empted by our own earlier ingest" from "capped", and it is the ceiling for every DNS newsgroup that exists. **The first pass put that ceiling at 772.4 EE and was wrong by 29x, from a join defect worth carrying.** It asked which pairs are held only by `usenet_bare`/`usenet_announce`/`usenet_address` by joining `evidence ON evidence_id = domain_year.evidence_id`, which returns exactly ONE row, the one the assignment happens to point at, so its `having sum(...)=0` tested whose row won an arbitrary tie-break rather than whether any other evidence exists for the pair. Joining every evidence row for that `(domain, evidence_year)` instead takes the count from **1,253 pairs to 16**. Re-derived independently 2026-08-30, the live figures reproduce exactly (5,033 pairs, 81.7% held, 15.6 EE, provenance `prior_task` 2,530, `usenet_announce` 680, `usenet_bare` 476, `isc_survey` 189, `usenet_address` 97) and only the counterfactual moved. **A counterfactual store is the set of PAIRS that would disappear, never the set of assignments that would change hands.** Also measured: PTR answers are the LEAST held bucket at 65.8%, not the visitor-log killer the hypothesis predicted, and machine forward answers the MOST at 91.0%. Do not re-test in the abuse groups: those hostnames are forged and the ~5% held law kills them on the other screen. |
| **US patent non-patent-literature citations carrying a printed retrieval date, read off the Google Patents front page (2026-08-29)** | **CLOSED at 0.0 net-new post-split EE, and the ceiling for every US patent ever granted is 325 EE.** `https://patents.google.com/patent/US6167569/en` and 8,963 siblings; robots.txt is 6 lines and allows `/patent/` for `User-agent: *`. What dates one item is the retrieval date printed inside the citation string itself, `[retrieved on Apr. 30, 2001]`, never the patent's issue date, so a year counts only where it stands within 60 characters after a retrieval verb, and URLs are masked to a token first so a digit inside a path cannot pose as the date. **Measurement**, 8,964 utility patents sampled at random inside each grant year 1996-2004 (199/198/199/1025/1027/1023/2175/2123/995, seeded RNG over the first-number-of-year table): 36,815 NPL citations, **695 carry a hostname (1.89%)**, 159 carry a retrieval date inside the window, 87 distinct hosts, **80 distinct (domain, cited-year) pairs over 78 registrable domains**. 75 of 78 held (96.2%), **61 of 78 carry ALL SIX in-window years (78.2%)**, 76 of 80 pairs already held at that very year, **0 held-but-missing**, 4 novel and taking the split. **0 net-new post-split pairs, 0.0 EE.** **P(store lacks the cited year given the domain is held) = 0.0000** against the population 0.611 for `com`: the most extreme head-selection in this register, past gopher's 0.018 and the browser cache's 0.024. **Ceiling**: 0.008925 pairs per patent over the 1,356,390 utility patents granted 1996-2004 gives 12,105 pairs corpus-wide, the rule of three on 0 misses in 76 caps P(missing) at 0.0395, and the cited TLD mix (com 53, edu 8, org 4, gov 3, au 2, uk 2, net 2) means 0.6800 EE per pair, so **325 EE if every patent were read**, 15x under the bar. Both looser dating arms were priced and both also fail: any in-window year anywhere in a URL-bearing citation gives 9 pairs and 5.3 EE at a 72.2% typo bound, and the patent's own grant year gives 2 pairs and 0.8 EE at 66.7%. **Method 1, the OCR-novelty floor, and it is a pre-download screen for any scanned corpus: 3 of 3 novel names (100.0%) are one edit from a name already held** (`nearac.com` for `nerac.com`, `procucts.com` for `products.com`, `www.gamesdomain.comgdreview` where the OCR ate a space). An OCR'd corpus cannot contribute DISCOVERY at all, because every name in it you do not hold is a corruption of one you do; run the typo bound before believing any novelty figure taken off scanned text. **Method 2, and it is the transferable law: an examiner citation is created by an ADVERSARIAL process, so it is head-selected harder than a voluntary one.** To survive a validity challenge the cited page has to be findable and institutionally credible years after the fact, which selects exactly `proquest.umi.com`, `www3.ncbi.nlm.nih.gov`, `www.ietf.org`, `schedule.acs.org`, `www.research.ibm.com`, `www.santafe.edu` and `britannica.com`. That is the same effect measured on a different corpus in the Edelman row, where a blocklist filed as court evidence came out 78x worse than the robot-compiled one: **sort citation corpora by whether the citation had to withstand a challenge, not by how many citations they hold.** The grant years richest in retrieval-dated citations are 2002 and 2003, sampled hardest here at 4,298 patents and 116 in-window retrieval dates between them, and they contributed 0 held-but-missing pairs like every other year. The 2026-08-15 projection lower in this table got the size right (order 10^4 distinct domains, measured 13,000) and the value wrong, because it counted names and never asked which YEARS they were missing. Bulk routes all closed and none is worth retrying: `bulkdata.uspto.gov` does not resolve, `data.uspto.gov` serves a 20,666 B Angular shell and `api.uspto.gov` PTGRXML is 401 without a key, Reed Tech 403s, BigQuery `patents-public-data` is 401 without GCP auth, and the PatCit Zenodo release ships no WEBPAGE table. **`patentsview.org` and `relianceonscience.org` both name ClaudeBot with `Disallow: /`**, two more for the refusing list in `CLAUDE.md`. **Do not re-test** patent citations on a bulk route or at a second patent authority: the ceiling is a property of the citation population, not of the sample size, and the adversarial-selection argument carries to EPO and WIPO unchanged, though only the US corpus was measured. |
| **The `.biz` and `.info` launch registries of 2001: an archived Afilias or NeuLevel zone snapshot, the NeuLevel startup and randomisation output, the Afilias sunrise allocations and the STOP and RDRP dockets (2026-08-29)** | CLOSED at **0 net-new EE**, and the entire family is capped at **57.7 EE** whatever is found in it. What dates one item in the only master-eligible arm is a registry creation date, `whois_creation`, and both registries forbid compiling it: `https://rdap.identitydigital.services/rdap/help` says "(b) enable high volume, automated, electronic processes ... except as reasonably necessary to register domain names" and `https://rdap.nic.biz/` says "the copying, compilation, repackaging, dissemination or other use of the information provided by the Service is explicitly forbidden without prior and explicit permission". No launch-era bulk listing exists either: `advancedsearch.php` holds 310 items matching `title:("zone file") OR description:("zone file")` and every one is 2022 or later, `afilias` returns 6 items (four court dockets, two podcasts), and ten plausible early `.info` and `.biz` directory hosts have **zero 2001 captures** between them, nine first appearing in 2002 or later. Method, three pieces. (1) **RDAP terms can be read with ZERO data queries**, which satisfies rule 3 exactly: the RFC 7482 `/help` endpoint returns the full `notices` array (3,575 B for `.info`) and a malformed path returns the same notices on an HTTP 400 (3,342 B for `.biz`), so no record is ever fetched under unread terms, strictly better than the port-43 habit of reading past the record. (2) **Price the SPLIT CEILING of a whole family before hunting any artifact in it.** One query, sampled as distinct domains: 123 `.info` and 29 `.biz` names hold at least one in-window year and miss 2001, so every human-typed artifact the family could ever yield is worth 123 x 0.3648 + 29 x 0.4425 = 57.7 EE, and a docket of 300,000 names pays the same as one of 300. A family is worth hunting only where a master-eligible artifact is plausible. (3) **Check provenance-by-source of what the store already holds in the namespace before believing a hypothesis about what no compilation can see**: the premise was that landrush names are invisible to `domain_creation_bulk`, and one `source x evidence_type` count inverted it, since that source supplies **93.9% of our `.info` 2001 pairs** (20,435 of 21,766) and 59.0% of `.biz`. Negative worth keeping: a namespace opening in the last quarter of the window has no in-window periodic observation at all, so ISC-style surveys, dispute dockets and directories covering it are all first dated 2002. Do not re-test sunrise or landrush lists, STOP or RDRP dockets, or registry startup reports; reopen only on written permission of the RIPE kind from Identity Digital, and only if a bulk list of the ~279,000 launch-era names surfaces first. |
| **The `.edu` registry's own creation dates at `whois.educause.edu:43`, the only endpoint that carries them (2026-08-29)** | BLOCKED on terms at **0 net-new EE banked**, priced at 400 to 1,400 EE had they allowed it. `whois.educause.edu:43`; what dates one item is `Domain record activated:    23-May-1985`, a registry-written creation date, `whois_creation`, dating its own year only. One query made (`mit.edu`) and no more: the banner PRECEDING every record says "The use of electronic processes to harvest information from this server is generally prohibited except as reasonably necessary to register or modify .edu domain names", and a ~7,700-record sweep is not that. No RDAP, `.edu` is absent from `data.iana.org/rdap/dns.json`; the web front end is the same database behind a Cloudflare `cf-mitigated: challenge` 403. Method: **band the candidate population before pricing any single-year evidence class.** Sampled with a third-party name list (`raw.githubusercontent.com/Hipo/university-domains-list`, 2,448 distinct `.edu` names) so no term was touched: 51.39% of the live register holds all six in-window years at P(missing) 0.0000, so a creation date pays nothing there whatever year it names, and 23,680 of the store's 25,400 `.edu` names are not in the live register at all. **A live registry's creation dates are anti-correlated with the store's gaps**, because the names still queryable today are the ones an archive crawled every year, and the effect is strongest where the TLD is small, institutional and stable. Also: the terms are not always past the record, EDUCAUSE puts them ahead of it, so read the whole response both sides of the data. Do not re-test either host or `.edu` RDAP. |
| **Commercial filter category databases: the CyberNOT, N2H2 and Bess, SurfWatch, WebSENSE, SmartFilter, X-Stop, CYBERsitter and Net Nanny lists, and the censorware publication and litigation channels that were meant to carry them (2026-08-29)** | BLOCKED at **134.1 net-new post-split EE**. `https://cyber.harvard.edu/archived_content/people/edelman/mul-v-us/`, Benjamin Edelman's expert report for the CIPA case; what dates one item is the vendor classifier's own observation column, `Blocked On (dates) (all dates are in 2001)`, e.g. `http://www.0-roulette.com  Blocked by: N2H2 (Pornography - Jul 26, Sep 16), Websense (Sex - Jul 5, Aug 18, Sep 16)`. 7,641 dated records, 4,898 distinct domains, 4,867 held any year (99.4%), 4,639 already carrying 2001 (94.7%), 228 held-and-missing-2001 all adjacent, **134.1 EE**. BLOCKED on rule 3, not on evidence: the host page states "The data contained here is not intended for use for other purposes, and it should not be used for other purposes without first contacting the author", so nothing here may be ingested until Edelman answers. The product side is closed separately: the vendor database never shipped in readable form on four products and three delivery architectures (Bess holds it server-side, Net Nanny ships an empty starter list, SurfWatch for Windows ships it encrypted at 7.7 to 7.99 bits per byte, SurfWatch for Mac fetches it on subscription), so the only retrievable artifact in this family is somebody else's test output. Method: **a blocklist published as litigation EVIDENCE is head-selected and loses the blocklist discount** because proving overblocking requires exhibiting sites a court will accept as legitimate, so the discriminator for an adversary-derived list is not "did a robot compile it" but "was it selected to be recognised": squidGuard's robot-compiled 2001 list is 57.9% held at 2001 and paid 10,736 EE, Edelman's same-vendor 2001 list is 94.7% held at 2001 and pays 134, a 78x collapse at comparable size. |
| **Random-IP web censuses: OCLC's Web Characterization Project, the Internet Operating System Counter, RIPE's DNS Hostcount published tables and the 1998 Internet Auditing Project (2026-08-29)** | CLOSED at **2.1 net-new post-split EE**. `https://web.archive.org/web/20001109203600id_/http://wcp.oclc.org:80/` and 53 sibling artifacts, 237,326 B; every artifact is self-dating (the IOS Counter's own report generator writes `Date : April '99`, RIPE's writes `This Count : Fri Apr 14 2000`) but **not one names a host**, so there is no item to date: of 57 domains recovered, only 16 occur in a census output at all and all 16 are queso fingerprint credits or illustrative labels inside an OS string, never a scanned host. 3 net-new pairs, **2.1 EE**, all from the prose around the censuses. Method in `discovery.md`, the sampling-unit law. |
| **Wayback-captured registry lookup pages: the `domainsurfer.com` zone-file substring search, plus the per-record whois front ends `betterwhois.com`, `allwhois.com`, `geektools.com`, `checkdomain.com`, `whois.net` and the `domainwatch.com` change reports (2026-08-29)** | CLOSED at **0.0 net-new post-split EE**. `https://web.archive.org/web/20000118094612id_/http://www.domainsurfer.com:80/ssearch.cgi?dom=money`, the only in-window query-bearing capture found across both arms; what dates one item is the machine-written index build stamp the page prints above its hit list, `10,401,344 records / Last updated: 17 Jan. 2000`. 40 distinct domains, **40 of 40 already held and all 40 already dated at 2000**, 0 net-new pre-split, **0.0 EE**. Method in `discovery.md`, the form-endpoint law. |
| **Package-manager fetch endpoints: `MASTER_SITES`, `PATCH_SITES` and `WWW:` across the full CVS history of FreeBSD ports, NetBSD pkgsrc and OpenBSD ports, pulled as dated checkouts from `codeload.github.com` (2026-08-29)** | CLOSED at **40.8 net-new post-split EE**. `https://codeload.github.com/freebsd/freebsd-ports/tar.gz/<sha>` and the two sister trees, 22 annual and intra-year checkouts, 168 MB; what dates one item is the CVS-converted commit stamp of the checkout, `committer.date 2001-12-31T20:39:31Z`, machine-written and attesting the whole tree at that instant. 51,191 port directories, 4,044 distinct domains, **94.91% already held at 2001** and only 106 of 3,822 held at 2000 are missing 2001, 91 net-new post-split pairs, **40.8 EE**. Method in `discovery.md`, the fetch-endpoint law. |
| **Browser cache indexes, cookie files and history databases on preserved media (discmaster `msieCache`, `internetExplorerCache`, `mozillaMorkDatabase`, `netscapeCookies`, plus filename sweeps for `index.dat`, `fat.db` and `netscape.hst`) (2026-08-29)** | **CLOSED at 0.0 net-new post-split EE, and it dies twice over: the class is not preserved in a USED state, and where it is used the machine-written half of a cache is the most head-selected corpus yet measured.** What dates one item is sound and is not why this fails: the media mtime discmaster records per file (`artifact_listing`), and inside the file a per-entry machine-written stamp (IE5 index.dat FILETIME, Netscape `netscape.hst` last-visit record). **Measurement**: **0 net-new post-split pairs and 0.0 EE against merged260827; 99 of 99 pairs already held at the artifact's own year, and 85 of 85 distinct registrable domains (100.0%) already known.** The population was ENUMERATED by content hash, not sampled: **90 distinct in-window artifacts** across discmaster's 1,718,970,121 indexed files (31 `netscape.hst`, 27 `cookies.txt`, 10 `index.dat`, 9 `fat.db`, 6 `cookies`, 7 `MM2048/MM256.DAT`), 94 files and 1,547,518 B fetched, of which **45 held zero http hosts**; the 49 that held any gave 113 host strings and **85 registrable domains** sampled AS DOMAINS. The IE arm the hypothesis names is **6 `index.dat` files in a browser-profile path in the entire index**, all 16,384 or 32,768 B, from a Dr Watson diagnostic dump, two never-booted Virtual PC images and one warez release. Counterfactual with the population fixed and only the year moved to 2001: **2 pairs, 1.1 EE**, 0.0134 EE per domain. **The pre-fetch discriminator, and it is the finding: P(store lacks 2001 | domain held) is 0.024 over all 85 and 0.018 for `.com` (1 of 56), against the store-wide `.com` figure of 0.611. A 34x shortfall, worse than Backflip's 15x and the CACM citation set's 14x.** **Why, and both halves are measurable before a download.** (1) **Availability: what survives is a SHIPPED profile, never a used one.** All nine in-window `fat.db` are exactly 65,536 or 16,384 B, the empty Berkeley DB allocation, and every one held zero http hosts; the `netscape.hst` files on magazine cover discs (WPCDEC97 57,344 B, pcwaug97 49,152 B, pcwjul97 45,056 B) parse correctly and hold only `file:///D|/html/...`, the publisher's own QA pass over the CD, not the web. (2) **The hypothesis's own discriminator is refuted in the opposite direction to the one it predicted.** A cache does hold the ad servers, the image hosts and the one-visit tail exactly as claimed, and those turn out to be the MOST universal hosts of the period web: `doubleclick.net`, `ad.doubleclick.net`, `flycast.com`, `imgis.com`, `focalink.com`, `preferences.com`, `burstnet.com`, `mediaplex.com`, `realmedia.com`, `us.yimg.com`, `bfast.com`, beside `netscape.com`, `yahoo.com`, `altavista.com`, `infoseek.com`, `excite.com`, `hotbot.com`, `zdnet.com`, `tucows.com`. **An ad server is shared by every site that carries the ad, so a cache is head-selected HARDER than a bookmark, not more softly.** The Backflip closure's rule holds and tightens: a corpus dated by human ACTIVITY selects the year the store already covers, and the machine-written half of that activity selects the most-shared hosts on the web. **Method, five reusable and all pre-download.** **(a) Price a whole file CLASS's ceiling in one request by reading discmaster's own format facet**: `GET /search` returns a `<select name="format">` of **6,452 formats each carrying its all-time file count** (`msieCache (343)`, `mozillaMorkDatabase (423)`, `netscapeCookies (172)`, `iBrowseCookies (57)`, `konquerorCookie (34)`, `windowsURLShortcut (206,969)`), which is the availability screen for any signature before any query and before any filename guess, the same trick as reading a host's own `ls-lR` first. **(b) The filename query and the format query are each incomplete and must be UNIONED**: `q=index.dat&qfields=name&mode=deep` returned 973 in-window rows and MISSED the `MSHist01.../index.dat` that `format=msieCache` returned, while `format=internetExplorerCache` is a false-positive detector, 868 of its 1,000 in-window rows being static HTML from one item. **(c) Deduplicate by the `b3sum` the search result already carries, BEFORE fetching**: 372 in-window rows collapse to 84 distinct contents, a 4.4x request saving, because cover discs republish the same preinstalled profile every month. **(d) For a preallocated index, exact size IS the emptiness test**: IE5 `index.dat` allocates in 32 KB blocks and Netscape `fat.db` in 64 KB, so a size of exactly 16,384 / 32,768 / 65,536 is an untouched file, and here every such file held zero hosts, so a size histogram of the distinct set closes the class with no download. **(e) `/file/<itemid>/<url-encoded path>` serves the raw member from inside an ISO, sit, rar or zip**, so a member of a 673 MB image costs one small request. robots.txt read in full first (267 B, `Disallow: /` with the operator's written carve-out for limited targeted research automation); about 110 requests spaced 2.5 s. **Do not re-test** browser caches, cookie files, cache indexes or history databases on preserved media on any screen, and do not widen to `mozillaMorkDatabase`, whose 103 in-window rows are one CompuServe installer's `panacea.dat` republished across magazine discs. **The live adjacent lead belongs to a different hypothesis**: the client-side log that is NOT one user's head, a period PROXY log covering a whole office's long tail. discmaster in-window `access.log` at `sizeMin=100000` is 74 rows and the one genuine proxy log is `squid/logs/access.log`, 1,272,715 B, ts 2001-06-22, inside `network-alias.tar.gz` on item 14148; the 131 MB 1998 files at `ftp.parc.xerox.com/pub/uir/WWW-1999-data/logs_cleaned/` are SERVER logs and the directory name flags killer 6, so they are already covered by the visitor-log closure at 98.4% held. |
| **Gopher master server directories, the protocol's own machine-maintained register of every server that registered itself (UMN mother gopher `othergophers`, plus Floodgap, Veronica and the quux gopherspace mirror screened out with it) (2026-08-29)** | **CLOSED at 0.2 net-new post-split EE, and it dies on YIELD after passing the trust-selection screen honestly.** What dates one item is legitimate `artifact_listing`: the tar member mtime on the "Other Gophers" menu cache, 1996-04-13, stamps the whole edition at the instant the mother gopher rebuilt it, one date per edition rather than per entry. Cleared the whole host in one request under the ls-lR rule: `archive.org/download/Boombox_201511/Boombox.tar`, 211,814,400 B, is the UK Mirror Service copy of the entire `ftp://boombox.micro.umn.edu/pub/` tree of the site that invented gopher, greppable offline, and its `Boombox.README` dates the upstream disappearance without one request to the origin. The register is `Boombox/gopher/misc/othergophers.tar.Z`, 98,304 B, member `./all/.cache`, tab-separated, host in field 3, truncated upstream at exactly 96 KiB so 122,082 of 156,203 content bytes survive (78.2%, ends mid-entry at "Th"). **Measurement** on distinct registrable domains, not rows: 2,106 host fields, 1,782 distinct hostnames, **1,294 distinct registrable domains** (edu 432, com 162, org 112, ca 98, us 75, net 68, gov 45, au 38). **1,291 of the 1,294 are known to the store at all, 99.77%**, above the 87-99% authority-corpus band. `price_items.py` on the artifact's own year: 1,288 of 1,292 (domain, 1996) pairs already held, 4 net-new gross, **2 pairs and 0.2 EE net-new post-split** at mean weight 0.1002, and half the net-new names are one edit from a name already held. Independent SQL agreed on 1,288. The three names the store has never seen are `savba.cs`, `gopher.cr`, `inred.com.mx`. The number that closes the family is the six-year sweep over the same 1,294 domains, held-but-missing year Y, priced as the gross EE a perfectly dated edition of this population could pay in its best year: 1996 missing 3 (0.2 EE), 1997 missing 1 (0.0), 1998 missing 5 (2.3), 1999 missing 11 (6.9), 2000 missing 12 (7.6), **2001 missing 41 (23.3 EE gross)**. That is 0.018 EE per held name in the best year, against the 0.386 population average for `com` and the 0.041 of a 2001 magazine archive: **twenty-one times worse than population average and the worst head-selection yet measured.** Correcting the 78.2% truncation gives ~1,650 domains and ~30 EE; scaling to gopherspace at its ~5,500-server peak gives ~4,200 domains and ~76 EE. The hypothesis set its own floor at 2,000 names for 1,500 EE and the family misses it by 20x to 70x with every edition summed. Self-registration passed the trust screen honestly, but in 1996 self-registration still selected universities, libraries and government, and those are the names the baseline holds in every year of the window. **Method, and it generalises past gopher: sweep ALL SIX window years on the sampled domains, not just the artifact's own year.** The standing "held AND missing the artifact's year" screen answers only whether this edition pays; the same count across 1996-2001 answers whether ANY edition of that population could, and it costs one extra query on a sample already in hand. A population that is missing no year cannot be paid for at any date, so hunting a later or larger edition is provably wasted. Run it before any hunt for a second edition of anything. **Do not re-test** on a fuller `othergophers` copy, a Floodgap edition, a Veronica index or the 2006/2007 quux gopherspace mirror: all four draw the same saturated population and the 2001 ceiling of 23.3 EE bounds them together. By-product for the refusal list: **`www.floodgap.com` and `gopher.floodgap.com` both refuse ClaudeBot by name with `Disallow: /`**, and www.floodgap.com also names `anthropic-ai`, so the obvious host for any gopher or retro-internet lens is closed to us before it is proposed. |
| **Free-DNS hosted-zone inventories, the nameserver's own list of the zones it was configured to serve (Granite Canyon, secondary.com, zoneedit.com, xname.org, freedns.com) (2026-08-29)** | **FIND at 1,732.9 net-new post-split EE over 3,059 pairs from seven objects and 1,567,653 B, 0.102 EE per listed name, confirmed on one of five hosts and refuted on the other four.** What dates one item: the list stamps its own generation instant (`Rejected Zone List: 7-May-2001 22:11 GMT`; status.shtml's "29 November 1999 ... here is the list of pruned zones") and the IA capture fixes when it existed, so one row is Granite Canyon's nameserver holding that zone in its BIND configuration at that instant, class `artifact_listing`. The zone name was typed by the customer into a submission form, so the corroboration split applies and only already-held domains earn a year. **The artifacts**: `stale_30Nov1999.txt` (205,787 B, 14,522 zone names) plus the six in-window editions of `soa.granitecanyon.com/ZoneRejects/` (1,361,866 B, 2,948 to 4,097 forward zones each). **Measurement**, `scripts/pricing/price_items.py --all-tlds` over 18,797 items: 17,049 distinct pairs over 16,979 domains, 6,777 already held, pre-split 10,272 pairs and 5,813.2 EE (overstates 3.4x, do not quote), **post-split 3,059 pairs and 1,732.9 EE at mean weight 0.5665**, by year {1999: 2,001, 2001: 1,058}, by TLD {com 1,613, net 407, org 300, ch 58, de 51, nu 49}, 7,213 pairs and 6,828 names to the candidate pool, typo upper bound 57.1% (856 of 1,500). An independent duckdb screen sampling DISTINCT DOMAINS agrees to about 1%: stale_30Nov1999 13,199 distinct domains, 7,969 held-any (60.4%), 2,026 held-and-missing-1999 against price_items' 2,001; the 2001 ZoneRejects union 4,092 domains, 1,916 held-any (46.8%), 1,071 held-and-missing-2001 against 1,058. **The held-fraction is the finding**: 60.4% and 46.8% against 87 to 99% for authority corpora, ~50% for blocklists and 98.4 to 99.6% for visitor logs, because a zone is not a page, so no crawler reaches it through a link and the artifact is not head-selected. **And the population does not collapse on the 2001 threshold**: P(lacks 2001 | held) here is com 0.5745, net 0.6174, org 0.5113 against the store-wide law's 0.611 / 0.653 / 0.568, within 6%, and yield per held name at 2001 is 0.317 EE against the 0.386 the law predicts. At 1999 the same population gives com 0.2539, so a held name in a 2001 edition is worth 2.3x one in a 1999 edition; the 1999 list alone is 1,125.5 EE and the 2001 reject union alone 607.3 EE. **Refutations**: secondary.com (2001-05-16), zoneedit.com (2001-06-04) and xname.org (2001-10-27) put the whole zone inventory behind `/auth/`, `login.html` and a per-zone password, and freedns.com served an empty Apache index. **Method, and it is the transferable part.** (1) A free-DNS operator's CUSTOMER population is the tail and its OPERATOR population is not, and the two look identical until you ask who typed the name: the zone-file-RHS closure the same week measured nameserver operators at 99.3% held-at-year and concluded that any population defined by "this host answers DNS" is the ISC survey's own, but the same service read from the other end measures 46.8% held and 0.5745. The discriminator is whether the selection predicate is "runs a nameserver" or "asked somebody else to run one". (2) **When a service hides its inventory behind a login, look for its ERROR LOG.** All four operators refused to publish a customer list; the one that published a nightly list of the zones its BIND could not load gave away 4,369 names it never meant to. Reject lists, prune lists, lame-delegation reports and stale-zone reports are machine-generated, self-stamped and regenerated on a schedule, so every capture is a fresh dated edition and nobody thinks of them as a customer list. (3) **Enumerate a dead site's list files from its own dated changelog, not from an index**: four captures of `status.shtml` at 1999-02-22, 2000-03-11, 2001-06 and 2002-06-06 named every list file the site ever served and dated each, which a CDX prefix query would have cost many more requests to establish. **Exhaustion**: six ZoneRejects editions exist in 2001 and no more, fourteen probes across 2001-01 to 2002-04 collapsing onto those six timestamps; the 2002-05-26 edition is out of window and cannot date a year; and the predecessor `zoneRejects.txt` is 9 names at 2000-03-03 and HTTP 403 at every later capture. Not a by-construction zero: no `granitecanyon` directory exists under `data/raw` and this table records no ingest, and what already dates the held names on a 400-domain sample is `prior_task` 196, `usenet_announce` 48, `domain_creation_bulk` 33, `isc_survey` 33, `rdap_snapshot` 26. Prior art: the 2026-08-24 row below records this family at 1,881.1 EE against a 5,000 bar and kept no URL and no bytes, so it was not reproducible; 1,732.9 is that figure decayed by five days of ingest. Cost 40 archive.org fetches, no other host touched, zero CDX queries. **Density per LISTED name is 0.102 EE combined, 0.148 at 2001 and 0.085 at 1999, 6x to 30x the curated-directory floor of 0.005 to 0.017**, so the ~83,000-name floor that class demands becomes about 7,000 to 12,000 names here. Family CLOSED at those seven objects: `granitecanyon / artifact_listing` is in `approved-sources-list.md` awaiting a human `Decision:` line, the bytes are already fetched and nothing is ingested. |
| **Sweep for the registry FILE SIGNATURE rather than for the host that might hold it (whole-tree indexes from ftp.gwdg.de, ftp.ripe.net, ftp.zx.net.nz, gatekeeper.dec.com, FUNET and discmaster, plus 105 hosts robots-screened and 31 probed at 22 canonical index paths) (2026-08-29)** | **CLOSED at 1.0 net-new post-split EE, and the premise dies on availability, not on content: the whole-host index is nearly extinct.** What dates an item is the artifact's own stamp in its bytes (SOA serial, index header date), which the hypothesis assumed correctly and is not why it fails; the sweep found no in-window name-bearing artifact to date. **Measurement**: the only in-window name-bearing artifact the sweep surfaced was the Dec-1997 Walnut Creek Internet Info CD's `netinfo/`, priced with `scripts/pricing/price_items.py` against merged260827 at 399 pairs over **399 distinct domains** (single-year, so the per-row sampling trap cannot bite), **390 already held (97.7%)**, pre-split 9 pairs and 7.9 EE (overstates 8.1x, do not quote), **post-split 1 pair and 1.0 EE**, 1997 only, `.edu` only, 8 pairs to the candidate pool. Four orders of magnitude below the ~5,000-pair volume bar. **Availability**: 31 permitted hosts x 22 canonical paths (`ls-lR{,.gz,.Z,.bz2,.xz}`, `locatedb{,.gz}`, `INDEX.gz`, `FILES.gz`, `find.txt.gz`, `filelist.txt.gz`, `ALL`) = 682 requests, and **exactly one host served one** (`ftp.gwdg.de/pub/locatedb`). Three whole-host indexes exist in 32, and only one was findable by probing: `ftp.ripe.net/ls-lR.gz` came from a search result, and `ftp.zx.net.nz` publishes its index as a **27-part sitemap of 1,319,576 URLs** named on line 2 of its own robots.txt. `ls-lR.gz` was a mirror-protocol artifact and rsync killed the protocol, so the index now survives only at the RIR and the very large general archive, both current-state trees: **killer 4 one level up, a current-state index cannot index a past year's file.** **Signature counts, all zero against working controls**: gwdg's 8.8 GB mlocate tree (927,852,695 B compressed, 8,838,050,642 B raw) gave `content.rdf` 0, `ripe.db` 0, `internic` 0, `hostcount` 0, `com.zone` 0, `net.zone` 0, `.zone.gz` 0. `ftp.zx.net.nz`'s 1,319,576 URLs are the strongest single negative, because that host **is** the frozen-mirror population, 67 preserved FTP sites including `gatekeeper.dec.com`, `garbo.uwasa.fi`, `ftp.digital.com`, `simtel.net`, `ftp.netscape.com` and `ftp.microsoft.com`: every signature 0, and the two `netinfo` hits are a Novell utility and a Delphi component. gatekeeper's own 986,543-entry `Index-byname.Z`: zero registry artifacts. discmaster over **1,722,834,293 indexed files**: `content.rdf.u8` 0 rows, `ripe.db` 0 rows, and `com.zone`/`net.zone`/`org.zone`/`root.zone`/`whois.gz` return rows but **0 rows over 200 KB**, where a real gTLD zone is megabytes (the `com.zone` hit is the DOS game `ZONE.COM`), against same-session controls `resolv.conf` 100, `dmoz` 100 and `hostcount` 15, so the tokeniser works and the zeros are real. **Both openings the hypothesis named are individually shut.** `ftp.ripe.net/ls-lR.gz` (3,696,737 B, 870,126 lines) is a one-request decisive proof: `./ripe/hostcount` is one 5,257 B text plus four plots, `./ripe/hostcount/History` is only the 5,901-6,556 B country tables, and `./ripe/dbase` is `nrtmv4/` and `split/` alone, the current dummified edition, with no archived edition in 870,126 entries; FUNET agrees, `/pub/netinfo/RIPE/dbase/archive/` is one file. **Corrected 2026-08-31**: FUNET's `split/` is NOT a 1999 edition, it is stamped `Tue, 09 Nov 2004 23:31:00 GMT`, and the sentence once here saying the `changed:`-lines-dated-2000-and-2001 route does not exist was wrong. That route exists, it is `ripe.db.domain.gz` in that directory, and it banked 916.6 EE (see `ripe_dbase_split_2004`). No 2000 or 2001 whole `ripe.db` exists on any reachable mirror, which is the claim that survives. **Read mtimes per FILE, never per tree.** And the only third-party mirror of an ODP dump is `download.huihoo.com/dmoz/content.rdf.u8.gz`, 285,221,345 B, `Last-Modified: Wed, 13 Feb 2013`, eleven years late, which closes the opening the ODP record left. **The one real find is out of window and closes an open thread**: the signature `domain-contacts`/`domain-info` recovers InterNIC's own register on the Walnut Creek Internet Info CD **(1993)**, `domain-contacts.txt` 2,355,463 B ts 1993-10-30 and `domain-info.txt` 171,293 B ts 1993-06-30, the DDN NIC register entire and also on the March 1994 disc, the files the FUNET closure proved gone. Every surviving copy is 1993, and **the December 1997 edition of the same series dropped the register**: its `netinfo/` is 19 DoD NIC documents, its `00netinfo-index.txt` is stamped `[30-Apr-97]` and lists no register, and its largest file self-stamps `[14 June 1993]`. Rule 6 says the listing evidences the instant it stamps, and that instant is 1993, so it can date 1993 and nothing else. **Nobody should price it.** **Method, five reusable parts.** **(a) archive.org lists the complete filename tree inside an ISO or ZIP without downloading a byte**: `GET /download/<id>/<file>.iso/` returns an HTML index, and one request against a 673 MB ISO gave 29,333 paths; it is the `ls-lR` trick applied to a disc image, and needs no discmaster and no ISO9660 parsing. Match `<id>.iso/`-prefixed hrefs and `unquote` them; a `<td>` parse fails because the listing is not a table. **(b) Check the 8-byte magic before decoding any `locatedb`**: gwdg's is `\0mlocate\0\0`, not LOCATE02, and **a LOCATE02 decoder does not fail on mlocate, it runs to completion and emits zero hits**, which reads exactly like a proved negative. That cost one full pass and would have produced a confident false zero. **(c) discmaster is the largest filename index reachable, and one request settles a signature across CDs, disc images and FTP tarballs at once**: read the count from the `outputAs=json` row array, never from the HTML, whose "N results found" string is absent from the served markup and silently yields zero for everything; every zero needs a dotted-filename control in the same session. **(d) robots.txt's `Sitemap:` line names a whole-host index, and it is in the file you are already required to read in full**: read line 2 as a lead and not just as a permission, and it is the politest form of the trick, since the operator published it to be fetched whole. **(e) Ask for a host's own index once, and when it 404s, stop**, rather than building a sweep that assumes the index exists; a registry's own `ls-lR` is the cheapest existence proof and should precede any access request, since 3.7 MB and one request retired a question that a Wayback sweep, eleven European FTP hosts and a national-contributor hunt had taken a session to answer. **New robots refusals**: **`ftp.acc.umu.se` names ClaudeBot, Claude-User, Claude-Web, Claude-SearchBot and Claude-Code at lines 115-119 of a 6,238 B file whose FIRST group is a permissive `User-agent: *`**, exactly the trap the register warns about; blanket `Disallow: /` at `ftp.ulak.net.tr`, `ftp.tu-clausthal.de`, `ftp.linux.org.tr`, `mirror.ox.ac.uk`, `ftp.iinet.net.au`, `ftp.isc.org`, `ftp.freebsd.org` and `ftp.openbsd.org`; refusals in substance at `ftp.ntua.gr` and `ftp.grnet.gr` (`/pub/`) and `mirror.csclub.uwaterloo.ca` (`/pub` and every mirrored tree). Of 105 hosts screened: 38 open, 21 limited, 9 refusing, 37 unreachable. **Do not re-test**: gwdg locatedb, ripe.net ls-lR, the zx.net.nz sitemap, gatekeeper `Index-byname`, discmaster for any zone or RDF signature, or the Walnut Creek Internet Info series. **One residual**: the zx.net.nz 1,319,576-URL index is on disk and is the best surviving instance of the frozen-mirror population, so it is worth re-grepping for a DIFFERENT lens's signature at zero further request cost. |
| **RIPE Hostcount raw per-host output, the monthly European DNS walk's own AXFR transcript rather than its published aggregates (ftp.ripe.net History tree, FUNET, uni-erlangen, DENIC and eleven further European FTP archives, plus the national contributor layer) (2026-08-29)** | **BLOCKED at 0 EE banked, because no copy is reachable, and the family is worth 10x what it was parked at: 353,308 EE of adjacent-year headroom, of which 312,014 sits in the 2000 and 2001 editions the hypothesis did not ask for.** What dates one item is the walk's own AXFR transcript, a resolver's answer carrying the zone SOA serial at the instant of transfer, plus the edition stamp in the filename, so `artifact_listing` with no human in the loop and no corroboration split; the class was never the problem. **Artifact existence is.** The published series is aggregates only, verified at its finest granularity: `ftp.ripe.net/ripe/hostcount/History/RIPE-Hostcount.98-Dec-09` is 6,907 B of a 101-row country table with no names, and the per-TLD per-month pages `www-static.ripe.net/pub/info/stats/hostcount/2001/06/uk/{all,zones,axfr,hosts,ratio}.cmp.html` are 838-12,813 B of attribute counts. The raw data is not lost but withheld: McGregor, Alcock and Karrenberg, "The RIPE NCC Internet Measurement Data Repository", PAM 2010, section 2.5, "The raw data from between 1990 and 2007 has been archived and is currently in off-line storage. However, the present policy is to discard the raw data after statistics have been extracted. This could be reversed if there was sufficient need amongst the research community", and, on any release, "Some administrators permit the RIPE scan, but this is often under the condition that only statistics, rather than raw data, are published." **Measurement, priced against merged260827 over the 97 RIPE-scanned TLDs carrying non-zero weight and held domains (2,936,405 held domains, TLD list read off the artifact's own Dec-1998 table), sampling DISTINCT DOMAINS: adjacent-year headroom held Y-1 and missing Y is 1997 2,002 EE, 1998 32,997, 1999 6,295, 2000 70,909, 2001 241,105, total 353,308 EE.** The naive held-any-missing-Y ceiling is 2,062,037 EE and must not be quoted. Per TLD across 1997-2001 adjacent: `de` 173,592 EE (980,570 names held 2000 and missing 2001), `dk` 25,322, `nl` 24,400, `uk` 20,209, `ch` 19,649, `it` 13,772, `se` 11,823, `at` 8,571, `no` 7,941, `be` 6,099. The high-weight TLDs want different editions: `uk` at 0.9813 is worth 17,543 EE from a **1998** edition and only 1,591 EE at 2001 because we already hold 2001 for nearly all held `.uk`, while `de`, `nl`, `ch` and `it` want **2001**. Walk size is not the constraint: Dec-2001 zone counts are `uk` 1,652,982, `de` 3,093,019, `nl` 529,457, `it` 418,539, all larger than our held sets. **Existence sweep, all negative against working controls:** ftp.ripe.net `/ripe/hostcount/{output,error-files}/` and `/ripe/hostcount/History/output/` 404 while `/ripe/hostcount/METHOD` is 200; ftp.uni-erlangen.de mirrors the same aggregates; **FUNET is a proved zero**, its stale `/pub/netinfo/ls-lR.test` (351,368 B) still lists a `hostcount` directory but HTTP 404s it and the live FTP listing of `/pub/netinfo/RIPE/` has no `hostcount` entry at all, while `dbase/` in the same session lists `ripe.db.gz`; 11 further European FTP archives had `/pub` fully enumerated and carry no RIPE or netinfo tree; archive.org `advancedsearch.php` returns numFound 0 for `hostcount` and for `RIPE hostcount` against a control of 19 for `ripe.net`; Wayback has no capture of `www.ripe.net/ripe/hostcount/`. **The national-contributor layer is dead**: `ftp.heanet.ie` answers "This site has stopped operations", and `ftp.nikhef.nl`, `ftp.nic.nl`, `ftp.iedr.ie`, `ftp.nominet.org.uk`, `ftp.denic.de`, `ftp.nic.at`, `ftp.nic.dk`, `ftp.nic.ch` and `ftp.nask.pl` are all NXDOMAIN. **New refusals to record, robots read in full before every first request: `ftp.tudelft.nl` names ClaudeBot explicitly alongside GPTBot and DeepSeek, and blanket `Disallow: /` at `ftp.rediris.es`, `ftp.uninett.no`, `ftp.fu-berlin.de`, `ftp.tu-chemnitz.de`, `ftp.uni-stuttgart.de`.** **Method, three reusable pieces. First: the surviving copy of a restricted central corpus is at the national contributor, not at the document mirrors, and it is filed under the contributor's name.** Grepping mirror indexes for `hostcount` found nothing; enumerating `/pub/doc/` on a large national archive found `ftp://ftp.icm.edu.pl/pub/doc/nask-hostcount/`, the RIPE hostcount raw output for `.pl` kept by NASK after RIPE restricted the central copy: `pl.output.gz` 63,809,385 B plus `old/` with 44 dated editions and `old/bad/` with 7 more. A 200 kB range request on `old/bad/pl.output.200212.gz` proves the layout is a raw AXFR transcript ("Asking zone transfer for pl ...", then `pl. 86400 IN SOA bilbo.nask.org.pl. ... 2002122003 ;serial (version)`), so it names hosts and self-dates and is master-eligible; it pays 0 here only because its earliest edition is 2002-12-30, one year outside the window. **Second: read the pricing question off the artifact's own table before hunting the artifact** - the free 6,907 B Dec-1998 aggregate names exactly which TLDs the instrument covered and how many zones it found, so the whole family priced against the store in one query at zero fetch cost. **Third: a range request on one `.gz` settles record layout for a 64 MB file.** Next step, and the only route left because every public copy is proved gone: one letter to `hostcount@ripe.net`, the address RIPE's own README names for this ("If you wish to view/use the raw data, please contact <hostcount@ripe.net>"), citing the PAM 2010 sentence that the discard policy "could be reversed if there was sufficient need". Same channel that granted `ripe_dbase_1999` on 2026-08-26. Ask for **2000 and 2001 first** (312,014 of the 353,308 EE), `de`/`nl`/`ch`/`it`/`dk` at 2001 and `uk` at 1998; expect a partial answer, since the paper says some administrators permitted the scan only on condition that statistics rather than raw data be published. State plainly that the off-line-storage claim is from 2010 and the project is now filed under archived-projects, so the tapes may not exist. **If the reply is negative the family is CLOSED**: the mirror, archive and national-contributor layers are all proved empty. Unfinished and not worth much: `ftp.man.torun.pl` is the last live Polish MAN host and was not checked for an older `nask-hostcount` mirror, but `.pl` at 0.1070 caps that thread near 2,600 EE. |
| **Zone-file NS right-hand sides, the nameserver hostname a delegation we already hold points AT rather than the delegated name itself (InterNIC `org`/`edu`/`gov`/`mil`/`root`/`arpa` at 1997, tomocha `edu`/`gov` at 1999) (2026-08-29)** | **CLOSED at 70.4 net-new post-split EE, and it dies on the yield screen after PASSING its own volume screen by 5x, which is the whole lesson.** Zero fetches and zero permission: the bytes were on disk, `data/raw/internic_zones/*.zone.gz`, 1,637,073 B. What dates one item is the zone's own SOA serial (`1997041800`, `1999112000`, `1999111901`) plus the IA capture fixing when the file existed, so an NS record is the registry serving that nameserver for that delegation at a stamped instant: `artifact_listing`, no human in the loop, no split. The class dates the item fine; the item is worth nothing. **Measurement: 160,258 NS records across the eight zones, 22,409 distinct RHS hostnames at 1997 and 7,418 at 1999, collapsing to 14,573 distinct registrable domains at 1997 and 4,452 at 1999, 16,231 over both.** The hypothesis guessed the file dies under about 3,000 distinct domains and pays at 15,000; it measured 14,573, so volume passed. `scripts/pricing/price_items.py --all-tlds` over 19,025 items: 19,024 distinct pairs over 16,231 domains, **18,822 already held**, pre-split 202 pairs and 119.2 EE (overstates 1.7x, do not quote), **post-split 120 pairs and 70.4 EE at mean weight 0.5864**, by year {1997: 63, 1999: 57}, by TLD {com 59, net 43, org 7, us 5, gov 1, in 1}; 82 pairs and 69 names to the candidate pool; typo upper bound 79.1% (159 of 201), itself a symptom, since a novel `ns2.` operator name is usually one edit from the `ns1.` we hold. An independent duckdb screen sampling **DISTINCT DOMAINS** reproduces it exactly: 1997 held-any 14,531 of 14,573 (99.71%), held **at** 1997 14,468 (99.28%), held-and-missing-1997 = 63; 1999 held-any 4,412 of 4,452 (99.10%), held at 1999 4,355 (97.82%), held-and-missing-1999 = 57; 63+57 = the 120 post-split pairs, so the two routes agree to the pair. **Not a by-construction zero**: `parse_internic_zone` (src/ark/sources.py:316) reads only the owner and explicitly discards the target, and the RHS has never been ingested. Per-TLD **P(lacks 1997 given held)**: com **0.0063** (5,697 held, 5,661 at 1997), net 0.0065, org 0.0015, edu 0.0007, uk 0.0051, and exactly 0.0000 for ca, us, gov, de, fr, se, nl, es, ch, au. On a 300-domain sample what already dates them at 1997 is `prior_task` 201 and **`isc_survey` 87**. **Method, and the reusable part is the screen this refutes.** The hypothesis priced itself off the RSS closure's fraction-of-held-missing at 1997, 0.8423, and argued a 1997 artifact over held names beats a 2001 one by 1.4x per name. Measured here that fraction is **0.0043, a 196x miss**, and 0.0063 for `com` against the 0.611 the 2001-threshold law quotes, a **97x** miss. So **fraction-of-held-missing is a population average over the store's names and does not survive conditioning on the population an artifact selects.** The conditioning is nameable in advance and costs no fetch: **the ISC 1997 survey is a reverse-DNS walk, so any 1997 population defined by "this host answers DNS" IS the ISC survey's own population, and nameserver operators are the maximal such set.** Same shape as the visitor-log and ping-directory closures at 98.4%, 99.6% and 9-of-9, and it generalises past them: it is not that the name comes from reverse DNS, it is that the artifact's selection predicate is a subset of a walk we already ran. **Pre-download discriminator to add to the held-fraction list: infrastructure-operator corpora sit at 99.3% held-AT-YEAR, above the 87-99% quoted for authority corpora**, and held-at-the-year is the statistic, not held-any (here they differ by 0.43 points and hide nothing). Second lesson: volume and yield are independent screens and volume is the weaker one - 14,573 domains cleared the gate by 5x and returned **0.0048 EE per domain** against the 0.386 expected for a held `com` name at 2001, an 80x shortfall. **Never let a domain count stand in for a yield estimate when the corpus is infrastructure.** Cost of the whole test: eight `gzip -dc | awk` calls and two `price_items.py` runs, no network at all. Closed: the `A` glue records in the same files are the same hostnames again, and `arpa`/`in-addr` targets are already measured at 336 pairs (src/ark/sources.py:320). A zone file dated **2001-2003** would escape the argument, P(lacks year given held) being 0.611 for com rather than 0.0063, but the reason none has been found is a licence, not an absence, and that is a different lead. |
| **Uptime and availability monitor target lists, the machine's record that a named host answered at a stamped instant (Netcraft uptime tables, MIDS/Matrix, WebSitePulse, InternetSeer, NetWhistle, Red Alert, WatchDog, SiteUptime, NetMechanic, AlertSite, Big Brother and NetSaint status displays) (2026-08-29)** | **CLOSED at 4.0 net-new post-split EE, and the evidence class was never the problem: it is master-eligible and clean.** What dates one item is the monitor's own machine-written stamp on the table it generated, `Generated on  5-Apr-2001` on `uptime.netcraft.com/up/today/*.html` and `Big Brother - Network status @ Fri Feb 13 13:16:21 WET 1998`, plus the replay instant, so `artifact_listing` plus `cdx_timestamp`, no human in the loop and no corroboration split. **Measurement: six artifacts all self-dated 2001, 130 distinct registrable domains sampled AS DOMAINS not rows. 123 of 130 (94.6%) already known, 111 of 130 (85.4%) already held AT 2001, and held-2000-missing-2001, the adjacent figure, is 10 of 130 (7.7%). Pre-split 19 pairs and 7.6 EE (do not quote), post-split 12 pairs and 4.0 EE, mean weight 0.3329, all at 2001 (nl 5, com 3, jp 2, org 1, net 1)**, priced with `scripts/pricing/price_items.py --all-tlds` against the live store. Per listed name that is **0.031 EE**, against 0.386 for a held `com` name at the 2001 threshold and 0.041 for the 2001 magazine archive, so this is the worst head-selected artifact measured to date. The `.com` arm alone: 67 distinct names, 63 held, 60 held at 2001, so **P(store lacks 2001 given held com) = 0.048 against the store-wide 0.611, a 12.8x shortfall.** Volume kills it before quality does: **the largest single published monitor artifact anywhere in the window is 52 names.** Operator arm, nine services probed by replay with no CDX: internetseer.com, which claimed to be the largest such service in the world, has its full 2001 navigation enumerated off `/home/` and it is ten marketing sections with no directory of any kind; netwhistle, redalert, watchdog.mycomputer and uptimemonitor expose login or signup only; siteuptime is an Oingo parking page in window so the service post-dates it; netmechanic puts results behind a control panel CGI. Netcraft's two surfaces with real volume, `/up/graph?site=` and `/up/hosted?netname=`, both redirect in-archive to netcraft's own `error/blocked.html`, so what had volume was robots-blocked to the 2001 crawler and what survives is top-50 by construction. Status-page arm via arquivo.pt `/textsearch` (its robots blocks `/wayback`, `/services`, `/cdxj` and `/datasets` for `*`, so textsearch was the only legal instrument): in-window hits Big Brother 279 and 235, NetSaint 110, control Apache 329,817, so availability is real, but of 29 returned items exactly ONE is a live display and it prints 13 monitored hosts as **bare labels with zero fully-qualified names**. **METHOD, four transferable parts. (1) The screen is published-over-managed and it costs one request: the monitored list IS the customer list, so what gets published is the showcase. WebSitePulse prints both numbers in one table, 27 linked customer sites beside "4134 accounts currently managed", a published fraction of 0.65%. Ask what fraction of the managed population reaches the page, never whether a list was published, because every operator publishes something. (2) A free substitute for CDX path enumeration that never touches the metered endpoint: fetch `/web/YYYYMMDD000000/URL` at a stride and read `%{url_effective}`, since replay redirects to the nearest capture. Twelve strides from 1998-01 to 2001-11 all landed on two timestamps, proving the Netcraft dailies survive as two editions rather than four years of them, at zero CDX cost. (3) The law: a server-generated page can be perfectly self-dating and still yield nothing, because the machine writes the LABEL, not the NAME.** Big Brother prints `ayron`, not `ayron.idt.ipp.pt`. Ask what name FORM a machine-written artifact emits before asking whether it is dated. This is the exact complement of the `mod_info`/`mod_status` closure of 2026-08-28, which had the right name form and no availability; together they close server-status output from both ends. **(4) Public monitoring output is head-selected BY CONSTRUCTION and Netcraft says so on its own `poweredby.html`**: only the most frequently requested sites appear, and the uptime page is a top 50. Do not re-test any of the nine operators or widen the Netcraft stride. Two adjacent things this did NOT close: MRTG and Big Brother per-host trees put the FQDN in the URL path, which is the right form and dated, but each tree is one organisation, so that needs a full-text archive index that is not arquivo.pt to find thousands at once; and the dated-recheck-log request at `docs/sources.md:569` survives untouched, since no operator here published an outage log. |
| **Crawler-indexed host tables, the roster of servers a crawler actually reached with per-host timestamps (FTP Search at NTNU, FAST, ftpsearch.lycos.com, AllTheWeb FTP tab, Filez/Zco, and the news.answers ftp-list) (2026-08-29)** | **CLOSED at 0.0 net-new post-split EE, and the family dies on POPULATION, not on era or on evidence.** The premise is false for both named members: a full navigation sweep, 10 replays and 77,621 B with no CDX touched, over the FTP Search home page, its advanced form and its entire `search-info/` subtree, the Lycos and AllTheWeb successors, and Filez plus its about page, found not one link to a server list. The only host list in the whole FTP Search document tree is `search-info/mirrors.html`, which lists 8 MIRROR SEARCH ENGINES rather than indexed servers. **Filez publishes the count and withholds the list**: "75+ million files, 7000+ FTP servers, servers are indexed weekly". `advancedsearch.php`: `ftpsearch` 6 unrelated items, `"ftp site list"` 0, `title:("anonymous ftp" AND list)` 3 items all 1993. The roster was the product and the product was never given away. Priced instead against the one surviving artifact of exactly this shape, already on disk at zero network cost: Perry Rovers, Anonymous FTP Sites Listing v5.2, 23 parts, 1,371,026 B, posted 1997-11-13. **Measurement, sampled as DISTINCT DOMAINS: 2,758 site entries. Reading A, whole list attested at its posting year 1997, gives 1,496 pairs over 1,496 domains, 1,491 already held (99.67%), pre-split 5 pairs and 2.1 EE (do not quote, it overstates by 21,490x), post-split 0 pairs and 0.0 EE. Reading B, each entry at its own `Date` field, gives 282 pairs, all 282 held, 0.0 EE.** **The zero is not an artefact of prior ingest**: the counterfactual dropping the `rtfm_faq` lineage (source_id 29) finds 996 of 997 whitelisted domains already dated 1997 by some other timestamped source, leaving exactly 1 worth 1.0 EE gross. **The number that closes the family is the 2001 threshold on this population: 1,486 of 1,496 are held at 2000 and only 30 are missing 2001, that is 2.0% against the `.com` population average of 61.1%.** So a listed anonymous-FTP host is worth **0.0078 gross EE**, Filez's own claimed 7,000-server roster would have been about 55 gross EE had it survived dated 2001, and 5,000 EE would need roughly 640,000 FTP hosts. Law 3 is visible in the TLD mix: 851 `.edu`, 430 `.com`, 201 `.de`, 122 `.gov`, 120 `.net`, 119 `.nl`, 100 `.au`, 99 `.ca`, 80 `.uk`, 75 `.jp`, 69 `.org`, 17 `.mil`. **METHOD, four reusable parts. (1) Ask whether a crawler service published its roster before hunting for the roster, and answer it in about ten replays: read the complete link set of the home page plus the whole help and about subtree at one capture each. Coverage was a marketing COUNT for every one of these services, so the shape "crawler's own record that a named host answered" reliably has no public artifact. (2) The population screen that kills the whole family is cheap and should run BEFORE the fetch: anonymous-FTP hosts are 97.4% already dated at 2001 and only 2.0% held-2000-missing-2001. Long-lived institutional servers are precisely the names every other source also carries, so a host roster is the worst possible shape under the 2001 threshold no matter how well dated. (3) A "last verified" date COLUMN is not a per-item date.** Rovers' list was posted 1997-11-13 and 88.2% of its rows are stamped 1994 or 1995, because the column records the last EDIT to the entry: only 326 of 2,758 rows carry an in-window date at all. Check the distribution of a date column before pricing any maintained register. **(4) The already-ingested counterfactual costs one SQL query** and should run before blaming an ingest for a zero: here it moved the answer by 1.0 EE, proving the corpus was worthless rather than merely spent. Positive controls held: 10 of 10 requested pages returned parseable era HTML three levels deep, and `archive.org/wayback/available` lied again, returning `{}` for `www.filez.com` at 19990601 while returning a 1999-04-28 capture of the SAME url at 20010601, so its zeros were discarded. Do not re-test any named member, and do not re-open Archie on the wrong-member argument: the era objection was never the binding one. The only untested surface is FTP Search RESULT pages, which do carry per-host `Date` columns, but the server is dead, few query URLs were captured, and screen (2) prices that population at 0.0078 gross EE per host regardless. Any host-roster idea worth an hour must target a NON-institutional population, for instance virtual-host tables from a mass shared-hosting provider, which is a different family. |
| **Online bookmark save streams, the row a user wrote by saving a page rather than by publicising it (Backflip public member folders, HotLinks guide, blink.com, itList, Clip2) (2026-08-29)** | **CLOSED at 0.5 net-new post-split EE against merged260827, and the interesting part is that the evidence class was perfect and the population still paid nothing.** What dates one item is master-eligible and exactly what the hypothesis wanted: Backflip's server printed a per-row `<span class="date">(added 2000/08/17)</span>` beside each saved URL and the replay instant fixes when that page existed, so `artifact_listing` plus `cdx_timestamp`, machine-written, no corroboration split. **Measurement: 291 dated rows off 50 member-folder pages (19 of 60 stride-sampled members), 248 distinct pairs over 244 distinct domains sampled AS DOMAINS. 237 of 244 (97.1%) already known; 240 of 248 (96.8%) already held AT THE ROW'S OWN SAVE YEAR. Pre-split 8 pairs and 4.4 EE (do not quote), post-split 1 pair and 0.5 EE, a single `.hk` at 0.4784.** Save years 2000: 188, 2001: 60. The counterfactual at the best year is 11 of 237 and 5.7 EE. **P(store lacks 2001 | `com` held) is 0.041 on this population against the store-wide 0.611, a 15x shortfall**; net 0.080, org 0.125, uk 0.000. Rate: 0.0041 net-new post-split pairs per listed domain against the curated-directory floor of 0.013 to 0.024, so **a save stream is 3 to 6x WORSE than a curated directory**. The second arm agrees and is worse: HotLinks guide, 26 leaf pages captured 2001-11-09 to 2001-12-18, 606 links, 499 distinct domains all dated 2001, **498 of 499 (99.8%) already held AT 2001, 0 pairs, 0.0 EE**, and the counterfactual at both 2001 and 2000 is exactly 0.0. **METHOD, and it is a new screen: the hypothesis's own discriminator is measurable before any bulk fetch, and it is refuted. Two compounding reasons.** (1) **The registrable-domain collapse destroys precisely what a bookmark contributes**: 60.9% of saved URLs are a path on a host, the tail lives at `geocities.com/SoHo/1234`, and the metric keeps `geocities.com`; the commonest saved domains are cjb.net, geocities.com, crosswinds.net, homestead.com, aol.com, netscape.com, yahoo.com, about.com, citysearch.com. (2) **An ACTIVITY date self-selects the year the store already covers**: the day a user saved a page is a day that site was most visible and most crawled, so the save year is the year LEAST likely to be missing, and the counterfactual at the modal save year 2000 is 2 of 237. **The consequence is a pre-fetch rule: for any corpus dated by human activity, do not price it at the population 2001 threshold (`com` 0.611); measure the held-at-2001 rate on a 200-domain sample first.** Backflip returns 0.041 and the CACM/IEEE citation set returned 0.044 with a completely different selector, an editor rather than a user. **That agreement is the finding: the selection principle does not matter, the domain collapse does, so "who chose this URL" is the wrong pre-fetch question and "what fraction of these hosts do we already hold AT THE TARGET YEAR" is the right one.** Reusable positives: a redirector is not automatically a kill, since Backflip hid targets behind `perl/go.pl?url=<id>` but the URL survived as display text truncated from the RIGHT in `<span class="location">` and in the `onmouseover` `window.status` string, so the registrable domain always survives. Operational, and it cost two false diagnoses: python `urllib` raised `[Errno 50] Network is down` on about half of all requests here while `curl` to the same URL succeeded, and `curl` then failed to connect to `web.archive.org:443` on about 40% of attempts at ~3.2s while `archive.org` answered every time; **neither is archive.org refusing us, so log the client error text before recording a source as unreachable.** Sampled via the public directory `pub_dir.ihtml?start=35|65..90`, 27 letter blocks, ~200 members per page, about 2 MB fetched in total; priced with `scripts/pricing/price_items.py --all-tlds` on 2026-08-29. **Do not re-test at a larger sample: the reading is stable across n=26, n=94, n=255 and n=291 (98.8%, 96.4%, 96.8% held at the save year), and the best-case 0.0234 EE per distinct domain needs about 42,800 distinct domains, roughly 9,000 folder pages and 18,000 requests, against a namespace already 97.1% saturated.** Do not re-test blink.com (33,459 B, 2001-12-17), itlist.com (9,985 B, 2002-01-24) or clip2.com (2,290 B, 2002-01-21): none exposes a public-folder namespace at all. Still open and NOT closed by this: the dated recheck log this file asks for below, over a non-citation population; a bookmark service's own dead-link sweep would qualify, but Backflip's is not archived and its member pages carry no status column, so that lead needs a different host. |
| **Network Solutions' Dot Com Directory, a registry front end that should have rendered registry rows with creation dates in bulk (`dotcomdirectory.com`, `www.dotcomdirectory.net`, `kevdb.dotcomdirectory.net`, `www.dotcom.com`) (2026-08-29)** | **CLOSED at 2.3 net-new post-split EE against merged260827, and the family is settled by the operator's own FAQ before any question of enumeration.** What dates one item: nothing. The capture instant dates the page (`dated_directory`), but no row carries a registry date, and NSI states it itself: "No. The dot com directory database is completely separate from the WHOIS database." Listings are self-submitted by the business, so a novel name would take the corroboration split as well. **Measurement: 48 pages, 933,641 bytes, captures 2000-11-10 to 2001-12-01, 116 distinct registrable domains. `scripts/pricing/price_items.py --all-tlds`: 117 pairs over 117 domains, 113 already held, pre-split 4 pairs and 3.0 EE (overstates 1.3x, do not quote), post-split 3 pairs and 2.3 EE at mean weight 0.7775, com 1 / au 1 / org 1, typo upper bound 50.0% (2 of 4). An independent duckdb screen sampling DISTINCT DOMAINS agrees to 0.03 EE: 115 of 116 held any year (99.1%), 112 already carrying 2001 (96.6%), held-and-missing-2001 = 3, all 3 adjacent (held 2000).** The corpus is single-year so pairs and distinct domains are 1:1 and the per-row sampling trap cannot bite. This is a population ceiling and not just a sample: the only enumerable surface is 15 top business categories and the result pages behind them were never captured. **It dies three independent ways.** (1) **Not the registry.** The public Dot Com Directory is a white-labelled InfoSpace yellow pages (`infospace.com/v.w/yp.htm`, InfoSpace patent notice in the footer) keyed on BUSINESSES: company name, business type, city, state, ticker. Listings are self-submitted and processed "within 10 business days", and no creation date appears anywhere. The real registry data was a paid product: `dotcom.com/services/index.html` sells "over 15 million registered domain names ... organized into a customer database of over 5 million unique customers" as Data Services, never published. (2) **The namespace does not descend.** The 15 categories are real `<a href>` links and 1 of 3 tested replays (Automotive, 27,641 B, memento 2001-05-30), but the subcategory step is a `<select name=QPAR>` inside a GET form, not a link, so no crawler followed it: `www.dotcomdirectory.net/nsi.com/dc/result.htm` has **zero captures in any year** while `nsi.com/index.htm?nl=1` has **44 in 2001**, so the archive holds the search form 44 times over and the results never. (3) **A session token sits in the PATH**, `dotcomdirectory.net/_1_4JNMTU203YD2CNL__nsi.com/...`, differing per capture, so even a captured result page would sit at an unguessable path. The aggregate pages behave exactly as the dates-without-names law below predicts: `facts/quickstats.html` gives activation percentages by month 1998-2000 and no name at all, and `profiles/f1000.html` is the head-corpus law in one page (amazon.com, expedia.com, disney.com, petsmart.com). **METHOD.** (a) **The pre-download question for any "registry front end" is answered by the operator's own FAQ.** One static, always-crawled `help.html` (18,355 B at memento 2001-05-04, 18,812 B at 2001-02-02, the WHOIS-separation sentence identical in two independent captures) settled a family that looked like `whois_creation` and would otherwise have cost a crawl of the whole browse namespace. Ask "is this the registry database?" BEFORE "does it enumerate?"; the hypothesis's own discriminator was the second question and the first one killed it, since this namespace does enumerate, one level, and still pays nothing because the rows are self-submitted business listings. (b) **`archive.org/wayback/available` produces false zeros in two distinct ways and both bit here**: it returns only status-200 captures, so it reported the `kevdb` root as a 2015 capture while the sparkline proved five 2001 captures (3xx/4xx) existed, and it returned `{"archived_snapshots":{}}` for every query-string URL that direct replay then served. Two clean negatives would have been filed as "not captured". **Cross-check with `https://web.archive.org/__wb/sparkline?output=json&collection=web&url=<urlencoded>`, which returns per-year and per-month counts plus a per-month status string, then confirm with a direct `/web/<ts>id_/` replay; neither endpoint is `/cdx`, so both are free while collectors meter it.** Sparkline is exact-URL, so it screens a known path but cannot enumerate a host. (c) **A session token in the path is a namespace killer and it is visible for free**: fetch the entry point once and check whether its own links carry a per-visit token; if they do, stop, because the archive's copies are scattered across unguessable paths and only a full URL index can recover them. (d) `curl -sL` on `/web/<ts>id_/` follows archived 302s into the real captured session path, which is how the Automotive page was reached; read the final `Memento-Datetime`, never `%{url_effective}`. (e) **Watch for content belonging to another site**: the 2000-12-03 and 2000-12-06 captures of both `dotcomdirectory.com` and `www.dotcomdirectory.com` serve Vicinity Corporation's homepage (18,555 B), and a byte count alone would have scored those as live directory pages. **Do not re-test `dotcomdirectory.com`, `dotcomdirectory.net`, `kevdb.dotcomdirectory.net`, `dotcom.com`, or the browse namespace.** `dotcomdirectory.com` is a different business after mid-2001 (Qwest Dex, redirecting to `dotcomdirectory.i411.com`), also a yellow pages, also worth nothing, and the rest of the family is already closed below: NSI and InterNIC registration reports (dates without names) and `rs.internic.net` with the six InterNIC zones. The one thread left belongs to a different lens: NSI sold the registry as bulk **Data Services** to marketers, so the question is not where NSI published it but which BUYER republished an extract, which is a paper-supplement or marketing-dataset hunt, not a registry hunt. |
| **Search engine transaction logs, the row where the user typed the domain into the box (Excite 1997-09-16 / 1999-12-20 / 2001-05-04, AlltheWeb 2001, Fireball 1998) (2026-08-29)** | **BLOCKED on access, and priced anyway at roughly 700 to 2,100 EE for the single best file, so the whole family sits under the 5,000 EE bar and is worth exactly one email.** What dates one item is master-eligible and clean: the engine's own log line stamps the instant the string was submitted, so the artifact is `artifact_listing` at a fixed calendar date; the string is human-typed, so every domain takes the corroboration split and only names another source has already dated can carry a year. The four in-window logs exist and are listed at `faculty.ist.psu.edu/jjansen/academic/transaction_logs.html` (3,134 B, robots.txt 404), but the page's only access route is "Please email me, Jim Jansen (jjansen@acm.org), I will place the file(s) on an ftp site for you." **No public copy exists, by enumeration:** DataCite `query log AND (excite OR altavista OR search engine)` 26 records and none in-window; Zenodo 0; HuggingFace 0 for archive-query-log / excite query log / aql-22 / query log 1999; GitHub repository search `excite query log` 0; archive.org `advancedsearch.php` (not scrape, per the trap) numFound 4 for `excite query log`, 1 for `excite transaction log`, 0 for `title:(query logs) AND mediatype:(data)` against live controls `title:(excite) AND mediatype:(data)` 10 and `search engine query log` 556, no hit a log. The one large public sibling, the Webis Archive Query Log (1999-2022, 356M queries), **refuses us by name**: `webis.de/robots.txt` carries `User-agent: ClaudeBot` / `Disallow: /` and the data host `files.webis.de` is a 27 B `Disallow: /` for every agent. **THE FAME LAW IS THE RESULT, and it is a store-side screen that costs one query and no download.** P(a held `.com` has 2001) rises with the number of DISTINCT pre-2001 sources that saw it: nsrc=1 0.1733 (n=2,395,370), 2 0.3005, 3 0.2935, 4 0.5424, 5 0.6638, 6 0.7803, 7 0.8498, 8 0.9408, 9 0.9834, **10+ 0.9966 (n=8,329)**; same shape on years-held, `.com` held in all five of 1996-2000 has P(2001)=0.7395 against 0.1988 for one pre-year. **So the 0.611 miss rate is a population average that does not survive conditioning on fame, and a search box is a fame filter.** Head spot-check, 273 hand-listed domains a 1999-2001 user would actually type (portals, retailers, media, ISPs, games, `.co.uk`): 273 of 273 known, 273 of 273 attested in window, **271 of 273 already held AT 2001**, total post-split value 1.34 EE = **0.0049 EE per name**, 79x below the 0.386 the hypothesis assumed and 8x below the 0.041 the magazine archive measured; median distinct pre-2001 sources per name 12, with 87.2% in the nsrc>=10 bucket. Surrogate actually fetched and measured, the only free member of the family, AOL 2006 file 02 (`cim.mcgill.ca/~dudek/206/Logs/...`, 45,909,232 B, robots.txt 404): 3,515,952 queries, 592,697 distinct terms, **664,227 queries (18.89%) carry a hostname token and 192,948 distinct registrable domains** under a weighted TLD. The transfer is licensed on a published statistic, not asserted: AOL's top-100 share 18.2% and singleton rate 65.3% sit on Excite 2001's own published 22.0% and 61.7% (`ieee_computer.pdf` Table 2). Priced on DISTINCT DOMAINS against merged260827-2: 68,833 known (35.7%), 67,823 attested in window, 124,115 never seen and therefore candidate-pool only, earning no year; dated 2001 that is 20,724 post-split pairs and **13,267.4 EE (0.0688 EE per distinct domain, 0.01997 per URL-shaped query)** against a gross of 123,445.3, a **9.3x** overstatement, do not quote the gross. On the clean adjacent-year screen (held 2000, missing 2001): 17,530 pairs and **11,208.7 EE, 0.01687 EE per URL-shaped query**, with only 29.6% of held-2000 typed domains lacking 2001 against the 0.611 population figure, exactly the halving the fame law predicts. Value is flat across frequency bands (head >=10 0.0674 EE per name, middle 0.0866, tail 0.0604) because the tail is 73% unknown names that earn nothing. **Projection for the blocked artifact:** Excite 2001 is 1,025,910 queries (Jansen Table 1) at a URL-shaped rate of 4% to 12% (AOL's 18.89% is a ceiling, since navigation queries were still climbing through the window), so **700 to 2,100 EE, plausibly 1,000 to 4,000** once corrected for an in-window log's higher attested fraction. The 1999 log is worth an eighth (1998-to-1999 adjacent headroom on the same set is 1,296.0 EE) and the two 1997 logs less again. **The hypothesis's own kill criterion was the wrong one:** it said "under about 3,000 distinct domains it dies on volume", and volume is fine at 192,948 distinct domains from 3.5M queries; what caps the family is that a typed name is a famous name and a famous name already carries 2001. **ROBOTS SLIP, recorded so nobody repeats it:** one request went to `zenodo.org/api/records?q=...`, which `zenodo.org/robots.txt` disallows under `Disallow: /api` (only `/api/records/*/files` is allowed). It returned 0 hits and nothing was collected. Zenodo is effectively closed to programmatic search since `/search` is disallowed too: **use `api.datacite.org` instead, which serves no robots.txt at all and covers Zenodo's DOIs.** Not closed, but not worth agent time: the only remaining move is one email from a human to jjansen@acm.org asking for `Excite_2001` ALONE, since the other five files are an order of magnitude thinner, and the reply is an ftp drop. If it arrives, take hostname-shaped tokens from field 3, collapse to registrable domains, price with `scripts/pricing/price_items.py --all-tlds` at 2001, and expect the artifact-listing class to need a `Decision:` line before any year is banked. **Do not re-test the Webis Archive Query Log or any webis.de mirror (refused by name), nor AOL, MSN, Sogou or Yandex logs (all 2006 or later, out of window).** |
| **Distributed-computing client submission stats, the project's own accounting of which address sent work today (`stats.distributed.net`, RC5-56 / RC5-64 / OGR dailies) (2026-08-29)** | **BLOCKED at 0 net-new post-split EE, and the kill is documented by the operator's own file: `stats.distributed.net/robots.txt` carries `User-agent: *` / `Disallow: /` at 19981203123429, 19990124000825 and 20010531224428, and `rc5stats.distributed.net` the same at 20010602021116, so the archiver was forbidden across the whole target window.** `www.distributed.net/robots.txt` at 20010602051535 blocks only five paths, so the block was specific to the stats hosts, not the project. What could have dated an item was sound in shape, the daily table's own date over the project's machine record of blocks received that day (`dated_directory`), but no such table was ever captured, so the claim is untestable rather than refuted. Everything reachable was pulled and priced: 5 pages, 39,101 B, via `web.archive.org` `/<ts>id_/` replay and never the live host. `stats.distributed.net/` at 19981212021247 (8,674 B), 19990208020539 (8,671 B) and 20001109082400 (1,223 B, a maintenance page); `/teamlist.html` at 19990202042932 (7,931 B); `rc5stats.distributed.net/` at 20010331062838 (12,602 B). **Priced with `price_items.py --all-tlds` against merged260827: 5 distinct domains, 10 pairs, 10 already held (100%), net-new 0 pairs and 0.0 EE before AND after the split, and 0 names new even to the candidate pool.** A strong zero: the five are `best.com` (an ISP footer), `w3.org` (the DTD), `rsac.org` (the PICS label), `distributed.net` and `d.net`, page chrome only, not one participant address among them. Every page carrying participant identity is absent: `/emtop100.html`, `/emyst100.html`, `/tm_top100.html`, `/tm_yst100.html`, `/oslist.html`, `/cgi/emsearch.idc`, `/cgi/teamcatlist.idc?CAT=Company`, and on `rc5stats` the whole 2001 tree `/rc5-64/`, `/ogr-24/`, `/CSC/` plus `tpemail.php3`, `psummary.php3`, `top100.php3`, `summary.php3`, `tpdomain.php3`, `country.php3`. **22 URLs probed, 0 hits, and it is a PROVED zero because controls fired in the same minutes**: `/index.html` 19990129071440, `/teamlist.html` 19990202042932, `/cpulist.html` 19981201205349, `/robots.txt` 20010531224428, `rc5stats` 20010331062838, `desstats` 20010331061054, `n0cgi` 20010515214911, `www.distributed.net/` 20010529011833, `/rc5/` 20010603092029, `/statistics/` 19991022065859. **The volume premise is refuted at the source, not merely unmeasured:** the 1998-99 navigation shows the site only ever published "Top 100 Overall" and "Top 100 Yesterday" plus a one-field `.idc` search form, so a browsable list of "a few hundred thousand participants" was never a page. Ceiling in the counterfactual where the crawler had been let in: 100 rows per project per day, stable day to day, so two years of 2000-2001 dailies is low thousands of distinct addresses and they are the head, where 2001 coverage is already best; at the measured head-selected rate of 0.041 EE per name even a generous 10,000 addresses is ~410 EE, **12x under the bar before law 5 takes the split**. No item-side copy either: `advancedsearch.php` gives `distributed.net` 11 items, `distributed.net stats` 1, `RC5-64` 11, `"distributed computing" AND rc5` 0, `dnetc` 3, all client source, a conference talk or an unrelated disc, and the only in-window hit `tucows_205891_distributed.net_Client` (2001) is a client binary, the container family already closed above. **Method, three parts, and the first is the reusable one. (1) Fetch the TARGET HOST'S PERIOD `robots.txt` out of Wayback before concluding anything about capture coverage**: a dynamic site whose deep pages are missing looks identical to a site the crawler was told to skip, and the archived file separates them in one request, here turning a guess into a documented 1998-2001 operator refusal. Run it for every "Wayback only has the front page" family. (2) **`archive.org/wayback/available` enumerates capture coverage without touching `/cdx`**, so it composes with a metered cdx collector; probe URL shapes read off an early capture's own navigation bar rather than guessed shapes, and interleave known-positive controls so the zero is proved. (3) **Read the era's navigation to size the artifact before costing a crawl**: the nav said "Top 100" and "Search", which caps the family at 100 rows a day and refutes a six-figure population without fetching one data page. **Live-host refusal, recorded:** `stats.distributed.net/robots.txt` today names `ClaudeBot`, `Claude-User` and `Claude-SearchBot` with `Disallow: /`, plus `User-agent: *` / `Disallow: /` and `Content-Usage: ai=n`. **Zero requests were made to it**; everything came from `web.archive.org` and `archive.org`, no CDX spent. Closed: **do not re-test distributed.net, SETI@home or any volunteer-computing participant stats on any screen**, since all published head-truncated leaderboards behind a query form and all had reason to keep crawlers off a database-backed stats box. One adjacent thread is NOT this family and stays open: `www.distributed.net` itself was crawlable and its `/discussion/lists.html` archives are a raw-mbox population, the same unfollowed thread flagged elsewhere in this file, that a raw mbox keeps the `Received:` chain pipermail strips. |
| **Apache `mod_info` and `mod_status` vhost lists, a shared host's own config page naming every site on the box (2026-08-28)** | **CLOSED at 0 net-new post-split EE against merged260827, on measured in-window availability of 0: no rendered `/server-info` or `/server-status` page from 1996-2001 exists in any reachable archive.** Had one been captured the dating would have been sound and split-free (capture instant over a machine-written config dump, `cdx_timestamp` plus `artifact_listing`, no human in the loop), so this class dies on availability, not on dating. Instrument was arquivo.pt `/textsearch` plus `/textextracted`, because **CDX cannot search by path across hosts at all** (SURT-sorted by host, `url` is mandatory), so the hypothesis's own proposed screen is not a supported CDX operation on any archive. Control passed: in-window "Apache" 329,817 hits, "server-status" 1,475, "Total accesses" 7,771, and 4-word phrase queries return nonzero ("Apache Server Status for" = 2). Against that working control the strings emitted ONLY by rendered output score zero: "Configuration Phase Participation" 0, "Request Phase Participation" 0, "seconds since last request" 0; the 2 hits for "Apache Server Information" and all 477 for "server-info" are the shipped Apache manual or unrelated source and RFCs, never live output. Second instrument archive.org `advancedsearch.php` (not scrape, per the trap): "httpd.conf" 41 items, "server-status" 155, all 2017-2026 GitHub and perma.cc mirrors, zero in-window. Third instrument unavailable: `data.commoncrawl.org` and `vefsafn.is` are `Disallow: /` and the Bibliotheca Alexandrina IA mirror no longer resolves. Ceiling had it existed, sampled over DISTINCT DOMAINS (7,023,420 held `.com`): P(store lacks 2001 | held) = 0.5967, so 0.3772 EE per held name and 2,651 names per 1,000 EE (`.net` 0.6369 and 3,466), which at tens to low hundreds of vhosts per box is roughly 10 to 50 captured 2001 pages from large hosters. **The reusable part is the refutation, and it is a two-request screen to run BEFORE any "unprotected endpoint" hypothesis: read the version-contemporary manual and ask whether the default was opt-in and whether anything linked to it.** The in-window Apache 1.3 documentation refutes the premise in its own words. mod_info: "This module is not compiled into the server by default ... To enable it, add the following line to the server build Configuration file, and rebuild the server", so exposure required recompiling and the population is not shared hosters but the few admins who rebuilt and then declined the `<Limit>` clause the same page recommends. mod_status: compiled in, but `ExtendedStatus Off` is the documented default and the VHost column is available only with it On, and the enabling recipe the docs teach is itself `Order Deny,Allow / Deny from all / Allow from .foo.com`. On top of that both endpoints are unlinked and 1996-2001 crawls were link-driven, so even an exposed one was structurally unreachable by the instrument that would have preserved it. Evidence retrieved instead: `arquivo.pt/textextracted?m=http://math.ist.utl.pt/manual/mod/mod_info.html/19990427125624` (1,957 B, captured 1999-04-27) and `.../lur.df.fct.unl.pt/manual/mod/mod_status.html/20010228004951` (3,084 B, captured 2001-02-28). **arquivo.pt is itself the transferable tool**: a genuine full-text instrument over 1996-2001, robots-clean, no key, 5 s crawl-delay, and it does not touch the metered IA endpoints, so it is the right answer to any "does this page shape exist in the window" question. Its robots.txt is the CLAUDE.md trap in pure form, the `User-agent: *` Disallow lines sitting at lines 744-753 of 753 after about 726 `Allow:` lines, so reading the head would have shown only permissions. The one sibling the run proposed as untried, webserver statistics pages (Analog, Webalizer, pwebstats) whose referrer blocks are `link_source`, **is already closed the same day at 0 EE and 141 of 141 pairs held at the artifact's own year** (row above), so its 7,771 in-window hits are not a lead. Do not re-test either family. |
| **The TARGET column of a vanity-URL redirection table (V3.com, `come.to`, `i.am`, `go.to`, `cjb.net`, `shorturl.com`, OCLC PURL), where the directory turns out to enumerate handles and not targets (2026-08-28)** | **CLOSED at 0 net-new post-split EE. Harvested off an INTERIM measurement: the run pulled and read the artifacts but its pricing pass did not finish, so the 0 is the standing figure and the decisive result is the dating argument, not a priced yield.** What dates one item: only the Wayback capture instant over V3's own machine-written META-Refresh or frameset naming the target, **so a resolved handle page dates its target and a browsable directory page dates nothing but handles.** That is what kills the volume premise the hypothesis rested on. V3 advertised millions of registered handles, but the datable unit is one resolved handle capture naming one target hostname, which is one request per candidate name, the worst request-to-name ratio in the register and strictly worse than querying, which adds ~3,000 EE/hour. Artifact: V3's whole public surface, archived and pulled, 64 HTML objects and 773,206 B. `web.archive.org/web/20010204191900/http://www.v3.com/Search.asp` (14,324 B) carries the search schema and the only bulk enumeration V3 ever published, **its own 92-name domain roster**, which is not a target list at all but the operator's own portfolio, the identical shape closed the same day as the free-mail signup menus at 0.041 EE per listed domain, pricing 92 names at about 3.8 EE. `coolsites.asp` at 20000711034600 / 20010130110700 / 20010602022936 / 20011122075506 (20,847 / 18,942 / 23,000 / 22,180 B) is the other listing V3 published and it is a hand-picked cool-sites page, so it is head-selected and takes the corroboration split. Also pulled: `cjb.net` at 20011217200201 (12,828 B), `shorturl.com` at 20001206215900 (11,213 B), `purl.oclc.org/` at 19991103221055 (5,888 B) with `maint/display.html`, `maint/search_domain.html` and `maint/power.html`, plus 42 resolved handle captures. **Method, and it is the MARC 856 trap in a new costume: in a handle-to-target service the creation stamp on a row dates the HANDLE, never the target it points at, so the row's own date is worthless and the whole class collapses onto per-capture replay.** Generalised screen, two requests and no download: for any indirection service, fetch one captured directory page and ask whether it prints the target hostname or only the handle. V3, cjb.net and PURL all print handles, which is the answer for the family. The Bomis webring result (a hub prints the bare hostname as text even where the anchor goes through its own redirector) was the reason to test rather than assume, and it does not transfer: a webring hub lists members it describes, while a redirector lists handles it sells. Not re-testable on effort: the missing pricing pass could only have moved a figure bounded above by ~3.8 EE on the one enumeration that exists. |
| **Free-mail signup domain menus (Mail.com, iName, MyOwnEmail, EveryOne.net, Zzn, PoBox and seven more operators), where the dating is clean and the WHOLE FAMILY is 531 names (2026-08-28)** | **CLOSED at 22.0 net-new post-split EE over 50 Wayback captures 1998-2001 from 12 operators, so 0.041 EE per listed domain, which puts the 5,000 EE bar at ~120,000 listed domains in a family whose total measured population is 531.** What dates one item is master-eligible and needs no corroboration split: a signup `<option>` list is written from the operator's own live mail-domain table, so the capture timestamp fixes an instant when each name was accepting mail (`artifact_listing` on `cdx_timestamp`, machine-written). The dating is sound and the VOLUME and the YEAR SCREEN both kill it. Priced on DISTINCT DOMAINS against merged260827: 804 pairs over 531 distinct registrable domains, **518 held in some in-window year = 97.6%**, 51 pairs / 30.8 EE gross against 36 pairs / **22.0 EE** post-split (do not quote the gross), by year {1998: 5, 1999: 3, 2000: 2, 2001: 26}, by TLD {com 32, net 4}. **The 2001 threshold run backwards is the screen: 365 domains listed at 2001, 352 already held, held-AND-missing-2001 = 26, so 94.8% of the held names ALREADY CARRY the artifact's own year** against the 61.1% `com` population miss rate, and the typo upper bound is 26 of 48 sampled net-new names one edit from a held name, so even the 22.0 is soft. Largest single portfolio ever built is 241 (iName @19980111131221), hand-counted against the extractor, with MyOwnEmail 171 and Mail.com 92 also hand-checked; MyOwnEmail's advertised "HUNDREDS MORE" resolves to one register page holding all ten category selects at 171 names. **The partner-domain arm the hypothesis named as the source of volume is REFUTED, not untested**: EveryOne.net's white-label partners are enumerated nowhere, its 45,381 B signup page's 401 `<option>` tags are countries and company-size bands, and the two pages that looked like listings are a curated web-host link directory (88 domains, 72 held, 7.2 EE) which is the already-closed dated-directory family. **The general law, applicable BEFORE download to any "portfolio of a company that registered many names" idea: an operator's vanity-domain portfolio is a head-of-distribution population BY CONSTRUCTION,** because the words that make a saleable address (`doctor.com`, `lawyer.com`, `london.com`) are exactly the words registered earliest and held through the whole window, so the corpus scores 97.6% held and already carries its own year. Same shape as the magazine-article result. Method worth reusing: **(1) neither CDX nor `/wayback/available` is needed for a dated capture** since `web.archive.org/web/<ts>id_/<url>` 302s to the nearest one and `curl -L -w '%{url_effective}'` hands back the real timestamp, one request for lookup and fetch, which mattered because `/wayback/available` returned a hard 429 with `x-rl: 0` and no `Retry-After` for the whole run while the replay host answered on try 1 or 2 for all 50 fetches (it refuses the TCP connection about a third of the time, so a retry wrapper is mandatory); **(2) pre-screen any "the operator's own table" hypothesis by counting `<option>` tags before extracting anything**, one grep separates a domain menu from a country dropdown; **(3) `grep -a` or the screen is a lie**, half these 1998-2001 captures are Non-ISO extended ASCII and plain grep reports nothing on them, which is indistinguishable from a page with no links (caught on everyone.net, zzn.com and usa.net). Zero CDX spent; archive.org robots.txt read in full first, no Claude agent named. **Do not re-test freemail or vanity-address domain menus on any operator in any year, nor the white-label partner arm: those lists were never published.** |
| **Public bug trackers where the mail headers survive: `debian-bugs-dist` gone, and the continuous-list law (2026-08-28)** | **CLOSED at 6.5 net-new post-split EE over 3,655 messages from 17 weekly mbox files, so 0.00178 EE per message, which puts 5,000 EE at about 2.8M in-window messages against the 78,000 the hypothesis budgeted: 36x off.** The artifact survives and the POPULATION kills it. What dates one item is master-eligible and clean: the last-hop `Received:` line written by hub.freebsd.org, restated by the archive's own weekly filename, 4,858 of 4,858 messages agreeing. Priced by `scripts/pricing/price_items.py --all-tlds` against the live store on DISTINCT DOMAINS, re-derived from the streams rather than taken from the researcher's summary: 2001 (14 files, `freebsd-bugs`/`chat`/`current`/`hackers`, 2,036 msgs) 1,013 domains, **927 already held at the artifact's own year = 91.5%**, 8 pairs / **3.7 EE** post-split against 86 pairs / 43.7 EE gross, a **12.0x** overstatement, DO NOT QUOTE; 1996 (3 files, `freebsd-questions`, 1,619 msgs) 684 domains, 677 held = **99.0%**, 6 pairs / **2.8 EE**. The eight 2001 survivors are `au` 2, `il` 1, `dk` 1, `pl` 1, `ru` 1, `org` 1 at mean weight 0.4568, and the **typo upper bound is 51.2%**, so half of even those are junk. **THE 2001 THRESHOLD RUN BACKWARDS IS THE SCREEN.** CLAUDE.md prices a held `com` name in a 2001 artifact at 0.386 EE because P(store lacks 2001 | held) = 0.611; this corpus is **8.5% missing 2001 against that 61.1%, 7.2x worse**, and the 1996 arm is worse still at 99.0% held. **So the killer is CONTINUOUS OPERATION: a technical list that ran unbroken from 1994 dates its own population every year, names only the continuously-attested authority head, and leaves no year-gap to sell. The longer the list ran, the more completely its names are already dated.** Ask how long a mail corpus ran before pricing it; prefer one that STOPPED, or one whose senders are not its subscribers. The hypothesis's flagship is a **proved zero, not a wrong path**: `lists.debian.org/debian-bugs-dist/` answers HTTP 200 with "This mailing list has no archive" and no `.txt.gz`, while the positive control `/debian-devel/2001/01/` is a 75,727 B MHonArc index, so the scheme works and the archive is genuinely absent; `bugs.debian.org/robots.txt` (239 B whole) gives named crawlers `bugreport.cgi` then `User-agent: *` / `Disallow: /`, a blanket refusal; the only bulk capture anywhere, `warc_home-grab-grabs-_debian-bugs_20171114`, was settled by reading its **4.3 MB CDX instead of the 859 MB WARC** (99,908 captures of bugs 781,992-881,900, every one stamped 2017, out of window by sixteen years); KDE and GNOME Bugzilla store parsed comments, so no `Received:` chain and zero addresses to an anonymous reader. **Reusable and worth keeping although it paid nothing here:** `docs.freebsd.org/cgi/getmsg.cgi?fetch=0+0+archive/<year>/<list>/<YYYYMMDD>.<list>+archive` returns a whole weekly mbox as `text/plain` with headers intact, ~60 lists 1994-2026, one request per week-file and no crawling, and its robots.txt is 52 B read whole disallowing only `/statistic` and `/doc/`. Two corrections to the researcher's own log, both verified against the data on disk: the density is **0.498 domains per message at 2001 and 0.422 at 1996, not the 0.275 reported** (2.6x worse than the spam corpus's 1.30, not 4.7x), and these mboxes carry **two `^From ` lines per message**, so a naive `grep -c` doubles the message count. |
| **The hostnames a 2001 program was compiled to contact: 130 Tucows payloads opened, and the container law (2026-08-28)** | **CLOSED at 0.6 net-new post-split EE over 130 archives and 137 MB, so 0.0046 EE per archive; the whole in-window Tucows corpus of 11,499 items, ~12.1 GB and ~23,000 requests, projects to 53 EE on the admissible reading and 241 EE if you wrongly credit every build-written string, 94x and 20x under the 5,000 EE bar.** Every prior catalogue closure in this file measured the INDEX (Winsite's `INDEX.TXT`, SimTel's CD indexes, Download.com, all zero vendor domains); nobody had opened a payload, so the read was genuinely new. What dates one item: the archive's own catalogue release `date`, already master-eligible and banked as `tucows_catalogue`, which can date a string the BUILD wrote and cannot date the bundled documentation where 94% of the hostnames actually live. Four arms sampled as DISTINCT ITEMS then priced on DISTINCT DOMAINS against merged260827: random stratified 1998-2001 (20 items, 22 domains, 0.6 EE), title-matched net/web/ftp/mail/irc/server 2000-2001 (40 items, 554 domains, 7.3 EE), random all-2001 (40 items, 219 domains, 1.5 EE), titles that should SHIP a server list (30 items, 125 domains, 1.47 EE); union 130 items, **804 distinct domains, 791 held in some in-window year = 98.4%**, held-and-missing the artifact's own year 20 pairs / 10.90 EE gross, 12 of the first 15 with Y-1 held, only 8 domains absent from the store entirely. That reads as a small clean find and the attribution pass destroys it. **Bucketing every hostname by the MEMBER FILE it came from: binary (.exe/.dll/.prc/.scr) 47 distinct hostnames, installer and config (.ini/.conf/.inf) 22, bundled documentation (readme/changelog/credits/FAQ/configure) 824, other bundled text 1,016. Build-written 68 against 1,069 in text the build did not write, a factor of 15.7, and only 40 of 130 archives hold a single build-written hostname, i.e. 0.52 per archive, below the hypothesis's own stated kill line of one per archive.** And the bundled text cannot take the release date: 11 of the 15 held-missing pairs in the first 60 archives come from two files, `dpsftp-0.6.1/doc/X-FAQ` (a copy of the comp.windows.x FAQ shipped inside a 2000-dated Java FTP client, whose icomsim.com and sunbim.be the store's own record contradicts, both last held 1998) and PTlink 6.3.7's `doc/example.conf`, `src/version.c` and `src/test/data/match.data`, a shipped EXAMPLE config and a wildcard-matching UNIT TEST FIXTURE, so those hostnames were never contacted by anything. Killer 2 in its purest form. Priced on the admissible arm alone (binary plus installer and config strings, the 40 archives that have any): 66 distinct domains, 95.5% held, 3 net-new pairs / 2.1 EE, and after removing PTlink's example-config names and the `example.es` placeholder **exactly ONE admissible pair survives 130 archives**, `awesomescreens.com` 2001 at weight 0.6321, verified independently in the store as held 2000 and missing 2001, and it is the hyphen-free sibling of `awesome-screens.com` which the already-banked `creator` field carries at 2001. The ceiling is structural, not sampling: the 24 build-only hostnames over the first 100 archives are the vendor's own site, the four shareware payment processors (kagi, regsoft, shareit, handango), the platform giants, two freemail hosts, an InstallShield template placeholder (`www.mywebsite.com`, `www.server.com`, `www.different.com`) and one binary-corruption token (`www.anaja.comaxzb`); kagi.com, shareit.com, hotmail.com, yahoo.com, bigfoot.com, aladdinsys.com, xs4all.nl all carry the full 1996-2001 span. Two closed head-of-distribution populations: the vendor's own domain, which `tucows_catalogue` already banked at that very year, and a dozen processors shared across thousands of items. The designed best case returned the same rate, 20 build-written hostnames over 30 server-tool archives, and no FTP or IRC client in the sample shipped a site list. **Method, four parts, and the third is the reusable one.** (1) The payload of a dated archive is two requests (`/metadata/<id>` then `/download/<id>/<file>`) and a 7z pass gets inside self-extracting exes, cabs and nested tars, taking readable file count from 1 to 30 on an InstallShield exe and 3 to 45 on a Palm zip. (2) **Anchor the hostname regex on `http://`, `www.` or `@`, never on name shape, when the input is binary**: `price_items --all-tlds` over raw binary strings reported 178 "domains" from 20 archives of which 158 were byte noise (`2.fj`, `zy7.bh`) or filenames (`.zip`, `normal.dot`); anchored extraction gave 22. (3) **For any corpus made of CONTAINERS rather than RECORDS, the container's date dates only what the container's own build wrote, so bucket by member file BEFORE pricing and price the buckets apart.** Bundled documentation, example configs, autoconf boilerplate and test fixtures ride along at a 15.7x volume advantage and read as a find until you ask which file they came from. This applies unchanged to ISOs, distro packages, installer bundles and document attachments. (4) **When the corpus's index is already ingested, the payload's dominant yield is zero by construction**: check what an already-banked field covers before reading a second field off the same items. archive.org robots.txt read in full first (12 lines, `Disallow: /control/` and `/report/` only); the `/download/` redirect target `dn710805.ca.archive.org` serves no robots.txt (404); web.archive.org untouched; no CDX spent. Closed: do not re-test payload-embedded endpoints on another software archive, another collection or another decade. Both halves are dead for opposite reasons, commercial and shareware payloads carrying ~0.5 build-written head-of-distribution hostnames each while open-source payloads carry volume the release date cannot date. Scaling elsewhere is already shut anyway: `cdromsoftware`'s 12,845 in-window items are ISOs closed on fetch cost at line 704 of this file, and `open_source_software`'s 19,920 items are the bundled-documentation failure mode by definition. |
| **Abuse-contact registries (`abuse.net`, its `whois.abuse.net` and DNS TXT lookups, `network-abuse.org`, period anti-spam FAQ contact tables), where the only interface is a one-field form and the operator forbids the export (2026-08-28)** | **CLOSED at 0 net-new post-split EE over 5 distinct registrable domains, 5 of 5 already held and 5 of 5 already carrying 2001, so held-and-missing-2001 = 0; four requests, priced against merged260827.** The dating route is fine and is not why it dies: a browsable listing replayed at a Wayback instant would be clean `dated_directory`. There is no listing. `web/20010401id_/http://www.abuse.net/lookup.phtml` (1,001 B) is a single `<FORM ACTION="/lookup.phtml" METHOD=GET>` with one text input; `howwork.html` (5,168 B) says "You can check our current list" and the href on that phrase points at the form; the whole 2001 site is ten static pages (index, addnew, books, commercial, contact, howwork, lookup, register, spamtools, tools) and none is an index of names. The operator forecloses bulk access in writing on `using.phtml` (7,763 B, live): "We don't make copies of the abuse.net database available to build into tools, so don't ask... The database is not available via BIND zone transfer... there is no zone file", with a local MySQL mirror by invitation only above 100,000 queries/month. The population is the painful part and is stated on the form page itself: "Database updated February 5, 2002 09:41, current size 92725 entries", i.e. 92,725 rows of exactly the machine-maintained, non-promotional, really-registered names the hypothesis wanted, with no retrieval path to any of them. Everything actually retrievable (three in-window captures, 26,674 B concatenated, plus the form) yields **7 hostnames and 5 distinct registrable domains**: `abuse.net`, `gpo.gov`, `doshelp.com`, `iecc.com`, `spamfree.org`. Sampled as DISTINCT DOMAINS, not `domain_year` rows. `network-abuse.org` is NXDOMAIN. robots.txt read in full first (7 lines, 98 B; GPTBot disallowed, `Disallow: /cgi-bin` for all); nothing under /cgi-bin fetched. **Method, and it generalises: ask the OPERATOR, not the crawler, whether a bulk artifact exists.** The developer or usage page states the export policy in plain words, and one fetch settles what a CDX sweep would spend an hour guessing at. Second, **the phrase "our current list" is not evidence of a list; resolve the href** (here it went to a one-field form, and the anchor text alone would have justified a download with nothing to download). Third, a lookup form is often the cheapest place on a site to learn the DATABASE POPULATION, because such pages advertise their own size and update stamp: 92,725 entries for 1,001 bytes. This is the mailing-list-catalogue closure in a new costume, and the family now reads as one law: **a service endpoint is the worst possible target, and a per-domain query interface does not become a corpus however good the population behind it is.** Do not re-open. The residual (captured `lookup.phtml?DOMAIN=x` RESULT pages, which would be genuine per-item `dated_directory`) fails on volume before anything else: nothing on the site links to a populated query, so captures are single or double digits against the ~2,600 held `com` names 1,000 EE requires, and enumerating them needs the metered CDX endpoint. |
| **Guestbook homepage URL fields on the hosted services (Dreambook, Lpage, GuestWorld, Bravenet, HTMLGear), refuted on the family constant before the first fetch (2026-08-28)** | **CLOSED at 0 EE for zero requests, on the closure already recorded in this table as "IRC logs, web forums and signature URLs, closed on density at a scale that cannot be exceeded (2026-08-27)".** The hypothesis argued that a guestbook is a form with a dedicated homepage field rather than a conversation, so the fill rate is a property of the script; that is true and it moves the wrong ratio. The IRC, web forum and signature-URL closure of 2026-08-27 ends with the sentence "do not re-test IRC logs, web forums, BBS echomail, signature URLs or guestbooks on any screen", and it is measured, not asserted: four artifacts, 20,761 distinct domains, 26,360 of 28,348 pairs held (93.0%), mean weight 0.6388, **511.1 net-new post-split EE**. Its transferable constant is net-new post-split pairs per LISTED domain: 0.0480 (Something Awful), 0.0391 (CloneCD), 0.0203 (Table Talk), 0.0155 (FidoNet), against 0.386 for a random held `.com` missing 2001. At that constant a 2001-dated artifact needs about **21,000 distinct domains** for 1,000 EE. A replayed book carries 20 to 50 entries, and at any fill rate below 1.0 with signers repeating across books that is order **10 to 25 distinct domains per Wayback fetch**, so 1,000 EE needs roughly 1,000 to 2,000 separately replayed pages. Two further reasons the premise cannot rescue it: the URL field is HUMAN-TYPED, so law 5 applies unchanged and novel names take the split and earn no year, meaning a higher fill rate buys novelty, which is a COST; and signers are personal-site owners on the low-traffic tail, which is head-selection inverted but lands in the same place, since the closure measured 93.0% already held on a comparable population and the unheld remainder is exactly what the split refuses to date. **Method: the screen order itself, which is the trap CLAUDE.md names.** One grep of `docs/sources.md` for the family, before the first fetch, refuted a carefully argued per-fetch economics case (4.5 domains per fetch on AVS gateways against 20 to 50 entries per book) that was beside the point. **When a hypothesis distinguishes itself from a closed family by mechanism, check whether the mechanism moves the constant that closed the family or a different one.** Here it improved the numerator of a ratio nobody was measuring. Do not re-open on a fill-rate argument. Only a BULK guestbook artifact would reopen the shape: a hosted service's own entry database, or a mirrored book archive of order 21,000 distinct 2001-dated signer domains in one downloadable file. Nothing of that kind is named in the record, and per-page Wayback replay cannot assemble it at a sane request budget. |
| **Link-persistence and URL-decay study data (Spinellis's CACM and IEEE Computer set measured in full; Koehler, Markwell and Brooks, Bar-Ilan and Peritz, Rumsey closed with it), Ding's paper-supplements lens aimed at its one machine-written artifact (2026-08-28)** | **CLOSED at 0.7 net-new post-split EE over 1,437 distinct registrable domains, 1,436 of 1,437 already held at 2000; the counterfactual at the artifact's best possible year is 27.8 EE.** Not previously in `docs/sources.md` under link-rot, link persistence, decay, Koehler, Spinellis, Markwell, Bar-Ilan, Rumsey, citation, supplement or replication, so genuinely new. Fetched all of `https://www.spinellis.gr/sw/url-decay/`: `log.rl.20000530.gz` (185,773 B, 37,671 lines, 4,367 check blocks, the ONLY in-window pass), `log.ist.20020828.gz` (2002, out of window), `cacm.urls`, `computer.urls`. robots.txt read in full first (35 B, `Disallow: /cgi-bin`). The evidence class is as good as the hypothesis claimed: a machine-written `getting <url> <status>` line paired with the block's own `date Tue, 30 May 2000` stamp is a self-dating observation that the host resolved and served, no human typing and no corroboration split. Status mix: 3,031x 200, 686x 404, 662x 301, 567x 302, 267x 901 (webcopy's own failure code, no evidence), 83x 504, 52x 503, 31x 403, 15x 400, 5x 500, 2x 401, 1x 303. Live hosts 1,801 of 1,990, collapsing to 1,437 distinct domains (com 664, org 220, edu 156, uk 44, gov 34, net 33, ca 27, de 27, au 23, jp 22). Priced with `scripts/pricing/price_items.py --all-tlds` against the live store, sampling DISTINCT DOMAINS: **1 net-new pair, 0.7 EE** (one `.org`, weight 0.7101). Re-priced at 2001 the same 1,437 domains give 46 net-new pairs and **27.8 EE**, so the whole family at its ~2,500 distinct-domain ceiling cannot reach 50 EE. **Method, and it is a new pre-fetch discriminator for the whole supplements lens: the 2001 threshold does not transfer to citation-derived populations, and this measures the shortfall.** P(store lacks 2001 | `com` held) is 0.611 across the store; on hosts cited by CACM and IEEE Computer it is **4.4%** (29 of 664), a **14x** shortfall, and 3.2% across all TLDs, a 19x shortfall, worse even than the 2001 magazine archive's 9x, because a refereed CS citation selects a narrower head than a magazine does: standards bodies, universities and vendors, all carried in every year already. **So a corpus whose URLs were harvested from FORMAL CITATIONS is head-selected twice, once by the author and once by the venue, and must be priced at the adjacent-year held rate of an authority corpus (95 to 99%), never at the population average.** Second: **self-dating buys nothing when the population is one the store already holds in that very year.** Datability and novelty were not in tension here as law 5 usually forces; there is simply no headroom, so screen the POPULATION before valuing the evidence class. Third: check the pass DATES before downloading, since a recheck study is worth only the years of its passes, and Spinellis ran exactly two, one in and one out of window. Do not re-test the link-rot family on any screen: every member harvests from formal citations and so shares the 4.4% figure. The one live variant is a dated recheck log over a NON-citation population (a directory's own dead-link sweep, a bookmark service's link checker, an ISP hosting index's availability run), where no editor or referee filtered the URL list; that is a different lens and should be briefed as one. |
| **Non-web service ping directories (newzbot's NNTP scanner measured; The MUD Connector, SHOUTcast and Icecast YP, Hotline trackers, DC hublists, Z39.50 target tests closed with it), where escaping law 2 by CONNECTING is what destroys the population (2026-08-28)** | **CLOSED at 0 net-new post-split EE over 9 distinct registrable domains, 9 of 9 already held and held-and-missing-2001 = 0, 0 names new to the candidate pool; priced against merged260827.** The premise is sound and is not why it dies: a scanner row carries its own "last checked" stamp, so the row is self-dating `artifact_listing` and the liveness inference law 2 kills is genuinely never made. Artifact: `web/20011214022943id_/http://www.newzbot.com/serverlist.php` (7,089 B), machine-scanned, with its idle-scanner status page (20020202014558). The `since=ALL` full-database variants are the four sort links on that page and every one 404s in Wayback, since query-string URLs were never captured, so 13-row weekly deltas are the retrievable ceiling. The MUD Connector is worse: `mudlist.html` captures (19991127104923, 29,136 B) are "72 New Listings Found" pages carrying names and added-dates and **no telnet host at all**, the host living on `mud-bin/adv_search.cgi?Mode=MUD&mud=` pages that 404 in Wayback, so the cheapest screen cannot be run there without ~1,700 per-entry fetches. The screen the hypothesis named, run on the one retrievable edition: 13 rows all stamped 2001-12-06 or later; **4 of 13 (30.8%) are bare IPv4** and can never be a domain; the 9 remaining hostnames collapse to 9 distinct domains, all held, all at 2001. The raw rows show why: `ip-66-51-100-134.tera-byte.com`, `host213-123-247-107.in-addr.btopenworld.com`, `ilm25-40-080.ec.rr.com` and `aux-209-217-58-114.oklahoma.net` are **4 of the 9 (44%) ISP reverse-DNS names of a customer machine**, registering as `tera-byte.com`, `btopenworld.com`, `rr.com`, `oklahoma.net`; the other five are institution and ISP heads (`yorku.ca`, `chello.at`, `uni-kiel.de`, `linuxfr.org`, `infinity.net.pl`). Small sample, stated as such, but the hypothesis itself said this one ratio decides the family. **Method: a directory written by a machine that CONTACTED the host records the host's PTR name, and for the low-traffic tail that name belongs to the ISP.** This is the mechanism that put visitor logs at 98.4% and 99.6% held, and it is structural rather than incidental: escaping law 2 by connecting is exactly what forces the name to come from reverse DNS or from an IP literal instead of from the operator's own registration, so the family buys self-dating at the price of the only population that pays. **One-number screen for the whole family: bare-IP fraction plus ISP-reverse-DNS fraction of the non-IP rows** (here 30.8% plus 44% of the rest, 8 of 13 rows unusable before the store is asked). Applies unchanged to SHOUTcast and Icecast YP, Hotline trackers and DC hublists, which are keyed on IP and port and so are strictly worse, not even carrying a PTR lookup. |
| **B2B vertical marketplace self-registered supplier profiles (VerticalNet's 59 communities, priced on `solidwaste.com` and `plasticsnet.com`; Industry.net, `manufacturing.net`, Ariba and Commerce One killed by the same mechanism), where the fill rate is exactly 0 and the causation runs backwards (2026-08-28)** | **CLOSED at 0 net-new post-split EE, 0 distinct domains, for five in-window fetches; nothing was priced against merged260827 because there is nothing to price, held fraction undefined and held-and-missing-2001 = 0.** What would have dated one item is sound and irrelevant: a Buyer's Guide listing served from VerticalNet's live supplier database and replayed at a Wayback instant inside 2001 is clean `dated_directory` shape, but **no item carries a domain to date**. The self-registered profile has a postal address, a phone, a fax, a product list and keywords, and no URL and no email field at all. Artifacts, all reachable and in window: `Sponsors.asp?SType=Storefront` on `solidwaste.com` (65,470 B, capture 20010620113239, the page's own "Total=342, Pages=14") and on `plasticsnet.com` (63,643 B, 20010602032732, Total=142, Pages=6); `/BuyersGuide/Companies.asp?CID=4276&NR=48` (44,962 B, 20010620134931); storefront mini-sites `/storefronts/oneplus.html` (15,307 B, 20010603052022) and `/storefronts/agr.html` (15,745 B, 20010616212131). The whole 59-community roster is free off one page, `web/20010531142856id_/http://www.verticalnet.com/` (31,397 B), which prints all 59 community hostnames plus 14 `search_onlines.asp?group=` sector indexes. **The zero was measured three ways on two independent communities.** (1) Listings: 3 pages, 52 supplier rows each carrying a complete street address, phone and fax block, **0 supplier hostnames**, in the anchors or in the rendered text; the only external hosts are `akamaitech.net` image CDNs, `adsdirect.vertical.net`, `athoc.com`, `convertit.com` and sister communities. (2) Profiles: `CompanyProfile.asp?CoID=<n>`, one request per company, 24 linked off a single category page; 2 of 4 sampled returned 200 and 2 returned 404, and **the two with content have no 2001 capture at all**, the archive 302-ing `20010620000000id_/` to 20060312152417, out of window and footed "Copyright 1996-2006, VertMarkets". The full rendered text of CoID=10212 runs name, address, phone, fax, products, keywords and stops: **no web-site field exists in the template**. (3) Storefronts, the paid self-authored page the hypothesis actually predicted would carry a URL: 3 fetched across both communities, **0 external supplier hosts**, including `agr.html` at a genuine 20010616 capture. Extractor control on the same pipeline in the same session: the VerticalNet homepage yields **63 distinct external hosts**, so the zero is the artifact's and not the reader's. Method, two parts and the second is the general one. (1) **The fill-rate screen costs ONE page and must run before anything else**: strip tags on a single listing page and count external hostnames against contact rows; 52 fully-addressed rows at 0 hosts settled a 59-community, ~20,000-supplier corpus for five fetches. (2) **The hypothesis inverted the causation, and the inversion is the transferable law: a marketplace suppresses the supplier's URL precisely BECAUSE the supplier self-registered.** The argument was that a telco's yellow-pages row has an empty URL field from neglect while a self-registered profile carries a URL because the supplier wanted the traffic. The opposite holds. VerticalNet's business was renting the supplier a storefront ON `solidwaste.com` and selling the lead, so linking out would give away the only thing it sold, and the storefront slug is the company shortname (`accurateplastics`, `akrontool`) rather than the company domain. **The stronger the self-registration incentive, the stronger the operator's incentive to keep the visitor inside**, so B2B marketplaces, hosted-storefront exchanges and lead-generation directories are all WORSE bets than the neglected telco row, not better. That is the same sign the ad-network and comparison-engine routing results reached from a third direction. `web.archive.org` serves no robots.txt (404), so nothing there restricted the fetch; no CDX was spent. Closed: do not re-test VerticalNet, VertMarkets or its 59 communities, and do not fetch Industry.net, `manufacturing.net` or the Ariba and Commerce One supplier networks, which are the same hosted-profile business model and die on the same reasoning. The two unfetched variants are named only so nobody re-derives them: `/content/vertpages/ecommcenters.asp` (`SType=E-Commerce+Center`, the tier above storefronts) and `/buyersguide/onlinecompanyreg/` (the self-registration form) are the same template and the same business model, and the storefront reading already tests the hypothesis at its strongest point. The one data point worth having for the LAW rather than for EE is a marketplace that charged a LISTING fee but hosted nothing, where the operator has no click to protect. |
| **Adult verification service gateway member directories (Adult Check, CyberAge, MANcheck, NetVerifier, AVS Empire), closed on the field the row carries and on a Wayback host exclusion (2026-08-28)** | **CLOSED at 10.0 net-new post-split EE over 28 pages and 1,004,000 B, 0.36 EE per page and 0.021 EE per listed member row, so 5,000 EE needs ~240,000 listed members, 2.4x the largest AVS that ever existed.** What dates one item: the Wayback capture instant of a gateway directory page served from the live member database; no page in the family self-dates. `price_items.py --all-tlds` against merged260827: 28 items all in window, 125 distinct domains, 92 already held, pre-split 33 pairs / 19.3 EE (overstates 1.9x, do not quote), **post-split 17 pairs / 10.0 EE**, all 17 at 2001, mean weight 0.5904, typo upper bound 63.6%. An independent duckdb screen sampling DISTINCT DOMAINS agrees to 0.7%: 119 domains, 89.9% held, 91 already carrying 2001, held-and-missing-2001 = 16, 15 adjacent. **The family dies on the row, not on volume or dating.** All 376 counted CyberAge member rows are `aux.avsofchoice.com/lnkwarn?refws=<memberID>`, an ID and no domain; the only hostnames leak from a JavaScript thumbnail popup on ~40% of rows and usually name a shared content farm, so 517,000 B and 376 rows gave 49 distinct hosts, 0.13 per row. NetVerifier is the same shape (`reportSiteLite.asp?siteID=`), AVS Empire routes all 13 entries through `maximumcash.com/cgi-bin/maxcash.cgi`, and Adult Check Gold's own `inactive_links.htm` prints 100 of 3,251 Gold sites with name and description in 27,877 B and **zero hostnames and zero anchors**. MANcheck is the one arm printing bare member domains and fails instead on volume and window: its five in-window `/cats/` pages are hand-written weekly picks carrying 5 to 15 links against a claimed 9,503 to 10,342 sites, and its machine-generated roster was **first crawled in 2002**. **Adult Check itself, the 100,000-site target the hypothesis rests on, is EXCLUDED from the Wayback Machine**: 403 with an empty body on every path, `wayback/available` NONE at 2000 and 2001, against a control answering 200 in the same second. Method, and the first part generalises: **a roster prints the member's domain when the reader must go to the member AND the lister has no interest in the trip; when the lister's revenue IS the interception, the row degrades to an ID.** That is one mechanism covering AVS gateways, affiliate catalogues and provider member indexes, and it is checkable in ONE request by reading the anchor template. Second: **`wayback/available` returning NONE plus a replay 403 with an empty body, against a live control, is a host-scoped EXCLUSION rather than an absent capture**, so the sibling brand `adultcheckgold.com` was measured instead. Third: on a JavaScript-heavy 2001 directory the member hostname may exist only inside an `onclick` string, so scan for `https?://` anywhere in the document; a quoted-href regex read 0 hosts where a permissive scan read 12. No CDX used. `web.archive.org` refused about 5 of 6 TCP connections at a flat 3.4 s with no 429, and succeeded on retry every time, so use a flat 2 s retry and many attempts, not exponential backoff. Closed: do not re-test adult verification services, age verification gateways or paid-access member directories on any screen, measured on four independent networks. The only untested increment is MANcheck's browsable `/search/cat/` namespace, ~40 pages, every capture 2002 and therefore out of window. One thread belongs elsewhere: the CyberAge popups name shared CONTENT-FARM hosts (`hrescontent.com`, `teeniepornvideos.com`, `sexycyberwebsites.com`), a different population from a member roster. |
| **Broadcast and press licensee registers (NAA newspaperlinks.com, E&P MediaINFO Links, NewsLink, radio-locator, 100000watts), where the fill rate PASSES at 100% and the family dies on the year (2026-08-28)** | **CLOSED at 52.8 net-new post-split EE over 28 priced state pages and 394,001 B, 0.0648 EE per listed newspaper, so all 51 states are worth ~97 EE and E&P's 3,217 US records ~208 EE for 161 fetches; the whole press family, US, Canada, world and magazines, does not reach 1,000 EE and the bar needs 77,000 listed papers.** What dates one item: the register page's Wayback capture instant (2001-04-18) over a server-generated state query, corroborated on NewsLink by a per-page `META HTTP-EQUIV=Last-Modified` regeneration stamp that varies page to page. `price_items.py --all-tlds` against merged260827: 815 distinct domains, 729 already held at 2001, pre-split 86 pairs / 52.9 EE (do not quote), **post-split 85 pairs / 52.8 EE**, mean weight 0.6216, com 80 / net 5, 1 name new to the candidate pool, typo upper bound 27.9%. An independent duckdb screen on DISTINCT DOMAINS agrees: 811 domains, **811 held any year = 100.000%**, 726 already carrying 2001, **held-and-missing-2001 = 85, 10.481% of held**, all 85 adjacent. California, the head state, is worse still: 125 domains, 100% held, 96.8% at 2001, 2.5 EE. The hypothesis's own close criterion fires, 10.5% under its 15% line and six times under the 61.1% `.com` population average. The broadcast arm never gets priced for two separate reasons: (1) NewsLink's leaves are **redirect-obfuscated**, every entry `<A HREF=cgi/goto.cgi?2=002252>`, so canews.html carries 253 links and zero domain names and one key costs one Wayback request to resolve (tested; the archived 302 came back 20021010, out of window), ~12,000 requests for a population already shown 100% held; (2) radio-locator and 100000watts are **not archived in window at leaf level**, the availability API returning 2005 for every state finder whether asked 19990101 or 20011215. Method, three reusable parts. (1) **A media register dies on the YEAR, not the fill rate, and the two deaths need different screens.** RSGB-style registers were closed on `href="http` fill; these are 100% filled and still pay nothing, because a newspaper is head enough to already carry 2001. Measure `already carrying 2001 / held`, not fill, on any sector census whose members are businesses that advertise; 100.000% held over 811 domains is the sharpest no-discovery reading in the file. (2) **An opaque numeric redirect key is a harder death than an empty URL field and it is one request to detect**: fetch one leaf, grep for `://` inside hrefs, and `cgi/goto.cgi?2=NNNNNN` means the register never prints its own subject. (3) **The availability API is the cheap in-window test for a CGI namespace and it costs no CDX**, but URL-encode the query string (the raw `?` form returns NONE while the encoded form returns a capture) and reject any timestamp not starting 2001, since the API returns the CLOSEST. Also: the E&P template writes `HREF='http://...'` in single quotes inside latin-1 bytes, so a shell grep reported 0 URLs on a page holding 20; decode as latin-1 before counting. Closed: do not re-test US or Canadian newspaper, radio or TV registers on any screen. The two loose ends are bounded by the measured rate and not worth the requests: 21 remaining NAA state pages (~45 EE) and E&P's 161 US result pages plus world, Canada and magazines (~600 EE for ~400 fetches). Reopen only on a NON-US register of a sector whose members are not businesses. |
| **Price-comparison engine merchant registries (BizRate, and mySimon, DealTime, PriceSCAN, Shopper.com unreached), where the routing discriminator predicts SHAPE and not payout for the third time (2026-08-28)** | **CLOSED at 7.0 net-new post-split EE over 22 listing pages and 969,667 B, 0.318 EE per page and 0.0129 EE per listed store slot; the full roster is ~6,730 store slots over ~270 pages, worth ~86 EE, so 5,000 EE would need ~393,000 listed stores and the family's best member is short by 30x to 58x.** What dates one item: the listing page prints its own generation date in the header ("November 9, 2001") and the capture instant fixes when BizRate's live merchant database asserted that store set, so `dated_directory` and nothing a human typed. Artifact: `bizrate.com/ratings_guide/listing.xpml?cat_id=<C>[&start=<N>]`, captures 20010617 to 20011127, reached from `/ratings_guide/guide.xpml` which enumerates all 20 departments; there is no all-departments roster. 542 store links, 456 raw host strings, **357 distinct registrable domains**. Sampled as DISTINCT DOMAINS: **356 of 357 held (99.7%)**, 345 already carrying 2001 (96.9% of held), **held-and-missing-2001 = 11, 3.1% of held**, all adjacent. `price_items.py --all-tlds` against merged260827: pre-split 12 pairs / 7.6 EE (overstates 1.1x), **post-split 11 pairs / 7.0 EE**, all at 2001, all `com`, typo upper bound 50.0%, exactly 1 name new to the candidate pool; an independent duckdb screen agrees at 11 pairs / 6.95 EE. The premise is confirmed on shape and destroyed on population: 3.1% against the 61.1% `.com` average is a **20x head-selection penalty and the worst yet measured in this roster family** (COMDEX booths 9.7%, BURST! members 9.0%, Bomis rings 7.9%). **Head/tail gradient INSIDE the artifact, 4.7x**: the 14 first pages (default sort "Top-Rated Stores") give 229 domains, 100.0% held, 1.3% missing 2001, 0.136 EE per page; the 8 deep-pagination pages give 147 domains, 5.5% missing 2001, **0.633 EE per page**, and the two sets are near-disjoint. **URL fill, the screen the hypothesis did not name**: on the Apparel first page only 6 of 25 store anchors carry the store's own host, the other 18 being `linksynergy`, `cc-dt.com`, `bfast.com` and `qksrv.net` tracking URLs with no merchant hostname anywhere, with four more recoverable only from description prose, so union fill is ~40% on a head page and ~66% over 22 pages. Cost compounds it: 6 of 32 replay attempts refused or timed out and 22 pages cost ~25 minutes against the ~3,000 EE/hour querying alone pays. Method, three reusable parts. (1) **The routing discriminator is now measured on a THIRD population and it predicts shape, not payout**: a price engine does print a route to the store, but it prints a CPC tracking URL, so ask what fraction of anchors carry a host, on one page, before pricing. (2) **`wayback/available` canonicalises `www.` away and the replay path must use the host it RETURNS**: `web/<ts>id_/http://www.bizrate.com/marketplace/index.xpml` failed on six serial attempts while the API's own `http://bizrate.com:80/...` at the same timestamp returned 35,810 B first try, and six false zeros were produced before this was noticed. Also `curl -L` on an `id_` replay can silently deliver the archived `robots.txt` instead of the page, so never follow redirects on a replay fetch. (3) **A paginated roster's `start=` bar is a free denominator AND a free tail sample**: one first-page fetch per department gives the store count and the deep-page URLs, and pricing head and tail separately turned 0.136 into 0.633 EE per page on the same artifact. Closed: do not re-test BizRate or any shopping-bot merchant roster, because a rated store is a store customers bought from in 2001, exactly the population the store already covers at 2001. The one untested variant, not worth it at the 20x penalty, is a MERCHANT-SIDE artifact (`merchant.bizrate.com/oa/merchant_services/`) listing applicants rather than rated stores. |
| **Statutory tourism and accommodation registers (smoothhound.co.uk, Bord Failte, AA, NITB), where the fill rate PASSES at 86.4% and the artifact still cannot add its own year (2026-08-28)** | **CLOSED at 1.61 net-new post-split EE over 44 in-window premises pages, 0.0366 EE per fetched page and 0.0168 per LISTED premises; at a generous 15,000 premises the whole directory is 250 to 550 EE for ~17,000 Wayback requests, ~0.03 EE per request, below the curated-directory floor.** What dates one item: the Wayback capture instant of the register page, and that is exactly what kills it. Structure enumerates cleanly: town-letter index, town page, one page per premises; 16 of 26 letter indexes gave 829 distinct town pages (full alphabet ~1,400-1,800) and 6 town pages listed 303 distinct premises at 21 to 106 each. A domain-bearing field is present on 38 of 44 fetched premises pages = **86.4%, far above the 10% kill screen**: proprietor email in a hidden `mto.pl` form field on 37 of 45, an external website URL on 11 of 45. Pricing against merged260827, sampling DISTINCT DOMAINS: 34 domains, 33 already held (97.1%), 1 novel (earns nothing), **held AND missing 2001 = 2, i.e. 6.1% of held**. CONTROL measured the same day on 40,000-row store samples: P(missing 2001 | held) is 0.352 for `co.uk` and 0.637 for `com`, so this population reads **5.8x worse than its own TLD baseline**, which makes it a pointed-wrong reading and not a nothing-found one; 17 of the first 18 per-domain year lists already carry 2001. The statutory boards are worse on retrievability, not better: Bord Failte's register is ASP query-string driven with no in-window capture of `/reservations/`, `/seeireland/` or `gettingaround/search.asp`, and the AA hotel guide index is an empty 78-byte capture. **THE LAW, and it generalises past this family: a directory whose date comes from its own Wayback capture cannot add that year to the domains it links with direct hrefs, because the crawl that dated the artifact also dated its targets.** That is law 1 sharpened from the NAME to the YEAR: the names are not merely held, they are held at precisely the one year the artifact could ever evidence. So the pre-download screen for any Wayback-dated listing is neither the fill rate nor the held fraction but "were these links reachable by the crawler that captured this page". The only escaping channel here was the crawler-invisible one, emails inside hidden form fields, and public-suffix collapse destroys it: those fields reduce to `freeserve.co.uk`, `fsnet.co.uk`, `demon.co.uk`, `btinternet.com`, `aol.com`, `clara.co.uk`, every one held in all six years. Reusable screen, three requests: fetch one listing page, take 20 linked entries, ask the store what years those 20 carry, and stop if they carry the artifact's own year. Also reusable: `archive.org/wayback/available` plus `web/<ts>id_/` costs one request per page and needs no CDX, so a family can be priced without touching the metered index. Closed: do not re-test the accommodation sector. The residual worth one hour, and only one, is a listing whose outbound links are NOT crawler-reachable (POST forms, redirector scripts, plain-text URLs in print-derived pages), since that is the single condition under which a Wayback-dated directory can still add its own year. |
| **Free-for-all (FFA) link pages, ffanet.com and the networks around it, where the self-dating premise is REFUTED and the family pays anyway on the 2001 year screen (2026-08-28)** | **FIND at 25.2 net-new post-split EE over 41 pairs from 9 pages and 178,235 B, 0.0726 EE per listed distinct domain, effectively all of it from ONE page.** What dates one item: the Wayback capture instant of a member FFA page, which displays the posted link as live text at that instant. Class `dated_directory`, master-eligible; approval request filed, not ingested. **The hypothesis's own premise dies first.** It says an FFA page stamps every entry with its own date, so no capture is needed. Counting date literals against link count across all 9 pages: `iwv2000.htm` 506 links / 0 stamps, `bds.htm` 307 / 0, `list.pl` 123 / 0, `linkstoyou.com` 38 / 0. The only page with per-entry dates is `freeforall.net`, 90 links / 63 stamps, and it is a hand-curated "free stuff" table (Title / Info / Date, MMDDYY) and not an FFA page at all. So this is an ordinary `dated_directory` and the date is in the capture. **The 4.56% question the brief said decides the family is answered 20x the other way**: 347 distinct 2001 domains, 317 held (91.35%), against the remailer corpus's 4.56%, because an FFA submitter is advertising and must post a URL that resolves. **And the whole yield is the year, exactly as squidGuard.** 2001 captures: 347 distinct, 91.35% held, 41 held-and-missing-2001, **25.1997 EE**, all 41 `com` 37 / `net` 4, mean weight 0.6146, and all 41 are held at 2000 so the adjacent-year figure equals the raw one. 2000 captures: 175 distinct, **175 of 175 held and all 175 already carrying 2000, 0.0000 EE**. Gross pre-split is 71 pairs / 43.4 EE and overstates 1.7x, do not quote. Typo upper bound 40.8%; 21 of 347 novel to the store. The redirector killer that closed WebRing and `free_host_member_indexes` does NOT fire: FFA entries are plain `href`s to the submitter's own URL. What erodes the count instead is submitters without a domain posting on a free host, so the registrable name collapses onto one certainly held that year (`roibot.com` x8, `hotyellow98.com` x7, `angelfire.com` x6, `homestead.com` x5, `geocities.com` x4), which is why held-at-2001 is 87.1% where the `.com` population predicts 38.9%. Artifacts: `web.archive.org/web/<ts>id_/http://pages.ffanet.com:80/links/<member>.htm`, 47,810 B (`bds.htm` 20000706234213) and 71,305 B (`iwv2000.htm` 20010307072648); the 110-member roster is `ffanet.com:80/links/list.pl?` at 20000304021731, 14,590 B; plus `freeforall.net` 20010722003910, `linkstoyou.com` 20010630123832, `ffanet.com` 20010709121131, `ffanetwork.com` 20010721153909, `freeffa.com` 20010720133803, `ffapages.com` 20010302105939, `1-2-free.com` 20010622230517. Not archived: `ffanet.com/ffalist.htm`, `ffa.net`, `ffaking.com`, `1st-in-links.com`. Out of window and NOT to be fetched: `addfreelinks.com` 2010, `ffagold.com` 2003, `ffamaster.com` 2002, `ffa-links.com` 2002. Method, three parts. (1) **Probe availability before replay**: `archive.org/wayback/available?url=&timestamp=` answers in ~1.5 s where `web/<ts>id_/` took 25 to 120 s under contention, so one cheap probe per URL turns every miss into a 1.5 s miss and keeps off `/cdx` entirely. (2) **Audit the date claim before pricing the corpus**: date literals against link count is one pass and it refuted the premise outright; a hypothesis resting on "the artifact carries a per-entry date" should be tested by that ratio on page one. (3) **A curated page inside a family is not an instance of it**: `freeforall.net` looks like the hypothesis confirmed and is the one page that is not an FFA page; pricing it alone would have reported a self-dating family that does not exist. Tools at `scratchpad/ffa/`. Continuation, ONLY at 2001: ~25 EE per 2001-captured member page means ~40 pages for 1,000 EE and ~200 for 5,000; pull `list.pl` at several captures since the roster rotates, availability-probe every member URL at a 2001 pin, discard 2000 captures unmeasured, replay the survivors. Page size predicts yield in this sample but is confounded with year and wants separating on the next 20 pages. Rights: `list.pl` asserts sole intellectual property over the ROSTER, member pages carry a normal copyright line, measurement unaffected. `web.archive.org` serves no robots.txt (404, verified); `archive.org` disallows only `/control/` and `/report/`. |
| **Online yellow pages 2001, nine publishers (Yell, Switchboard, SuperPages, Telstra, Canada), where the artifact is in the archive and the DATA IS NOT IN THE ARTIFACT (2026-08-28)** | **CLOSED at 4.9 EE, which should be read as zero.** Nothing dates an item, because no item exists: across nine publishers and twelve archived files, 261,588 B, business rows captured is **0**. The hypothesis's first kill condition fires but its reason is wrong, and the correction is the reusable part. It predicted "a result surface living in a query string Wayback never captured". Query strings ARE captured: `cgidir.dll?LNK=3:2&MEM=1` has 20010615143522, `cgidir.dll?func=catlist&mem=1` 20021030004718, `cgiqa.dll?LNK=3:1&MEM=1` 20010714010256, `cgidir.dll?PR=105&mem=1&...&N=1` 20020613132717. **What is never captured is a URL that was never an `href`.** The result URL is assembled client-side: `sp_listings.phtml` carries eleven occurrences of a JavaScript `buildURL(stype)` concatenating `?SRC=&STYPE=&C=&N=&K=` from form fields and `switchboard.com` two more, so a 2001 crawler had nothing to follow and the result space has zero captures by construction. Availability probed on 81 URLs through `archive.org/wayback/available`, never `/cdx`: publisher HOMEPAGES 29 of 29 have in-window captures, so the family is not missing from the archive; parameterised RESULT URLs with real values **0 of 11**. The controlled six are the strongest number: they are the exact `results_categories.jhtml;$sessionid$...&_DAV=browse` anchors printed on the yellowpages.com.au page the crawler itself captured at 20010627194118, and **0 of 6 were followed**, with the parameterless `/yp/search/results_categories.jhtml` (20010208093117) and `/yp/search/results.jhtml` (20010608181603) both answering 200 as controls. Fill rate, the second kill condition, cannot be measured because the denominator is zero. Priced with `scripts/pricing/price_items.py --all-tlds` against merged260827 on DISTINCT DOMAINS: 59 distinct, 45 held (76.3%), pre-split 14 pairs / 10.9 EE (do not quote), **post-split 6 pairs / 4.9 EE**, and the net-new TLDs are `ng` 4, `select` 1, `com` 1, where the `.ng` names are `click.ng`, `html.ng`, `js.ng`, `image.ng` off an ad server path and `this.select` is CSS. Every genuine name of the 59 is the publisher, a sister property, an ad network or a homepage banner advertiser; **not one is a directory listing**. Per flagship, measured not assumed: **Yell** POSTs to `search.yell.com/search/DoSearch` in May 2001, GETs it by August, and `yell.co.uk` and `yellowpages.co.uk` are 266-byte meta-refresh stubs; **Telstra** POSTs to `results.jhtml` and puts `;$sessionid$` in the path, making every URL single-use; **Switchboard** is GET-native but its captured pages are the empty form and an A-Z jump list with `C=` empty; **SuperPages** `listings.phtml` renders "Try Again"; **canadayellowpages.com** is a 1,798 B splash; **ukyellowpages.com** is an SEO doorway with a 70-term keyword meta tag. The volume argument in the hypothesis (11M Switchboard businesses, 2M Yell) is real and irrelevant, because none of it was ever crawlable. Adjacent to the printed-directory row below, which killed Thomas Register, Kompass and D&B on digitisation reach; the online editions are a different artifact and fail on different and more complete grounds. Method, three pieces. (1) **The pre-download screen for any database-backed 1996-2001 site is `grep -c buildURL` on its archived homepage**, or any JS concatenating a query string from form fields: if the search URL is assembled in script it was never an `href`, no crawler emitted it, and the result space has zero captures no matter how large the database was. This is sharper than "it has a query string", which is wrong. One fetch, before pricing anything. (2) **The controlled follow test**: take the anchors off a page Wayback itself captured and ask the availability API whether each was followed, so zeros are about the crawler and not about your URL guessing, paired with a parameterless control on the same path. (3) **A session ID in the path (`;$sessionid$`, `;jsessionid=`) is a hard zero**, since every capture is a distinct URL nothing else links to. Also confirmed: `archive.org/wayback/available` echoes your requested `timestamp` at the top level, so a `grep -oE '"timestamp"'` taking the last match reads your own input as the snapshot date; parse `archived_snapshots.closest.timestamp` or every probe reports a perfect hit. Do not re-test Yell, Yell.co.uk, yellowpages.co.uk, Scoot, Switchboard, SuperPages, BigYellow, InfoSpace, SmartPages, AnyWho, Canada411, canadayellowpages.com, ukyellowpages.com, yellowpages.ca or Telstra Yellow Pages Online on any screen. Left open and belonging elsewhere: Switchboard sold hosted business pages at `ypc.switchboard.com/bin/cgicontent.dll?BUS=<id>` with sequential IDs, but the one specimen has no capture at any date; and a yellow-pages publisher that printed a STATIC "new web sites this month" page would be an href and therefore crawlable, though none of the nine had one. |
| **Dated job postings, 49 Usenet job groups already on disk, where the ad names the RE-POSTER and not the employer (2026-08-28)** | **CLOSED at 158.2 net-new post-split EE counterfactual (8.6 EE against the live store), 0.000797 EE per in-window posting and 0.00932 EE per distinct advertiser domain, so 1,000 EE needs ~1,254,000 postings and 5,000 EE needs ~6,270,000.** What dates one item: the `Date:` header of the posting, machine-stamped by the posting agent, in the same record as the advertiser's address, master-eligible as `usenet_announce`; but the body domains are human-typed and take the corroboration split, which is where 98.0% of the gross goes. **All three groups the hypothesis names were already ingested**, so the live-store figure is 0 by construction and the honest number is the counterfactual: dropping source_ids 22, 23, 31, 32, 39, 40 and asking whether any of the 39 remaining timestamped sources dated the pair gives 21,727 of 23,370 held (93.0%), pre-split 1,643 pairs / 993.2 EE, **post-split 270 pairs / 158.2 EE**, by year {1996:7, 1997:21, 1998:50, 1999:71, 2000:13, 2001:108}. Against the live store it reads 94.1% held and 8.6 EE; the 1.1-point gap IS the whole contribution of the usenet lineage. Corpus: 198,397 in-window postings parsed out of 49 job-group mboxes already at `data/raw/usenet_bulk/*.mbox.zip`, 552,123,385 B, apply-to channel only. **Zero network requests; `web.archive.org` was never touched.** Positive control in the same code path, because nothing-found and pointed-wrong look identical: 200 `.com` names the counterfactual store holds at 2000 and lacks at 2001, written as synthetic "Contact: jobs@X / Apply: http://www.X/careers" ads through the SAME extractor and the SAME pricer, returned **200/200 and 126.4 EE**, so 198,397 real ads are worth 1.25x what 200 hand-picked names are, and the pipeline is sound. The 2001 threshold on distinct domains: of 15,215 advertiser domains held at 2000, only **2,153 are missing 2001 (14.2%)** against the `.com` population average of 61.1%; the 2000 adjacent figure is 1.1%. Head/tail gradient is 1.5x, not BURST!'s 17x: 100+ mentions 507 domains 98.7% held 6.4 EE, 2 to 99 mentions 8,969 domains 95.3% held 82.4 EE, exactly once 7,504 domains 87.4% held 69.4 EE, so even the singleton tail is 4x worse than the population. **The hypothesis's own discriminating variable, firm size, is refuted directly**: `alt.bestjobsusa.denver.jobs` 1,138 postings 0.000 EE, `.la.jobs` 1,669 0.000, `.sandiego.jobs` 1,001 0.000, `.texas.jobs` 334 0.000, `.illinois.jobs` 117 0.000, against `comp.jobs.contract` 67,497 postings at 0.265 EE per 1,000. **The mechanism**: 2.71 distinct domains per posting and the top 25 domains carry 40.6% of all mentions, being dice.com 51,014, selectjobs.com 38,607, gate.net 20,884, aol.com 13,817, usenetserver.com 7,286, netcom.com 7,079, recruitersonline.com 5,426. Those are job boards, news providers and ISPs, and "Company Name: Technology Services, LLC" is printed as plain text with no domain at all. The employer's domain is not in the ad. **The web-board arm is closed with ZERO archive requests**, because the boards syndicated to Usenet so their own output is on disk: selectjobs.com 22,314 postings / 782 domains / 97.1% held / **5.3 EE** (0.239 per 1,000), medzilla.com 3,878 / 825 / 96.5% / 4.4 EE, gojobs.com 3,785 / 235 / 91.2% / 2.4 EE, careerspan.com 2,328 / 510 / 97.4% / 2.6 EE, dice.com 251 / 60 / 96.7% / 0.6 EE. Recovering a whole 2001 board archive out of Wayback would need roughly 1M to 4M per-posting captures to reach 1,000 EE. Robots read whole before any request: `www.jobserve.com` 1,265 B / 70 lines blocks eleven bots, none ours; `www.jobs.ac.uk` 120 B; `www.dice.com` 1,167 B / 50 lines disallows `/job` and `/jobsearch/`, exactly the per-posting path. Method, three reusable parts. (1) **When a corpus is already ingested, do not report the 0 it returns; rebuild the pre-ingest store by dropping its lineage.** `SELECT DISTINCT domain, evidence_year FROM evidence WHERE source_id IN (<timestamped sources MINUS this lineage>)` restores what the store knew before the ingest for exactly the pairs in question, and it moved the answer from 8.6 EE to 158.2 EE. Script at `scratchpad/counterfactual.py`, takes any `price_items.py` item stream. (2) **A syndicating aggregator can be priced without touching its host**: dice, selectjobs, medzilla and gojobs all gatewayed to Usenet, so grouping on-disk postings by the `From:` domain prices each named web lead offline, at full corpus size, with no crawl and no false zero. Group by poster before proposing to fetch a board. (3) **The screen for any advertisement corpus is POSTING CONCENTRATION, not firm size**: postings per distinct domain predicts the rate directly, `alt.jobs.overseas` at 3.3:1 paying 2.142 EE per 1,000 against `comp.jobs.contract` at 32.1:1 paying 0.265, an 8x spread on the same shape. Count distinct posters against distinct ads on a sample before parsing; a corpus where a few hundred agencies file everything is a people population and dies to the ChangeLog inversion regardless of how small the advertised firms are. Do not re-test Usenet job, resume, employment or careers hierarchies, and do not fetch jobserve.com, jobs.ac.uk, monster.com or dice.com per-posting pages. General result: any classified-advertising population, jobs, for-sale, personals or property, names the venue and the agency rather than the subject. Not disproved: a classified corpus where the ADVERTISER is the domain holder by definition, such as a web-hosting or domain-for-sale group, but that is a different family. |
| **Ad network publisher rosters (BURST! Media and eleven rivals), where the shape prediction is CONFIRMED and the family dies on MEMBERSHIP SIZE, plus the head/tail gradient measured inside a single directory (2026-08-28)** | **CLOSED at 19.4 net-new post-split EE over 32 pairs from 13 pages and 178,349 B, 0.0533 EE per listed member and 1.49 EE per page, with the entire BURST! gallery capped at ~280 EE against a 5,000 EE bar.** What dates one item: the Wayback capture instant of a BURST! gallery page, which is served from BURST!'s live membership database and so asserts the named site is a paying, ad-carrying member at that instant; the page's own "(c) 2001 BURST! Media" gives a year but no instant, so nothing dates an item without the capture. Machine-generated from a database, so no corroboration split on the listing itself. Artifacts: `web.archive.org/web/<ts>id_/http://www.burstmedia.com/release/gallery/cat_list.asp` with `?q=<cat>&p=<n>&s=trafficup|trafficdown&d=0`, captures 20010610 to 20010701, 13,070 to 16,208 B per page, 13 pages; category index at 20010618120800 is 8,855 B; contrast artifact `www.phase2media.com/advertising_opps/byalpha.html` at 20010610081925, 15,735 B. **The hypothesis's own discriminator is confirmed exactly as written**: BURST! anchors each member's title straight at the member's own server (`http://www.coolquiz.com`, `http://www.eeggs.com`) with no redirector, ~37 bare hostnames per page, 40 members per page, sorted by traffic, over 20 top categories and ~200 subcategories. Priced with `scripts/pricing/price_items.py --all-tlds` against merged260827: 13 items, all in window, 364 distinct registrable domains, 364 pairs, 325 already held, pre-split 39 pairs / 23.7 EE (overstates 1.2x, do not quote), **post-split 32 pairs / 19.4 EE**, all 32 at 2001, mean weight 0.6065 (com 29, net 2, uz 1), 7 novel names to the candidate pool only, typo upper bound 64.1%. Year screen on DISTINCT DOMAINS, not rows: 364 listed, 356 held in any year (97.8%), 324 already carrying 2001, **held-and-missing-2001 = 32, i.e. 9.0% of held** against the 61.1% `.com` population average. **The head/tail gradient INSIDE the one artifact is the real result and it is 17x.** Sorted `trafficup` (largest members): 3 pages, 108 domains, 98.1% held, 105 of 106 held names already carry 2001, held-and-missing-2001 = 1 (0.9%), 1 pair / 0.6 EE, **0.2 EE per page**. Sorted `trafficdown` (smallest members): 2 pages, 61 domains, 95.1% held, held-and-missing-2001 = 9 (15.5%), 9 pairs / 5.5 EE, **2.75 EE per page**. The two sets are disjoint (overlap 1 of 38 against 31). So the network's head is already covered at 2001 and only its tail pays, which is the magazine-archive law reproduced inside one directory rather than across artifacts. **Volume ceiling, measured not assumed, off the "Page 1 of N" counter every response carries**: Arts & Entertainment 34 pages, Computers & Internet 9, and 4, 4, 4, 6, 5 for five more, so 66 pages over 7 of 20 categories, mean 9.43, hence ~189 pages and **~7,200 member entries for the whole gallery, capping it at ~280 EE** (~380 EE by the per-listed-domain route, before the ~15% cross-category duplication). 1,000 EE would need 18,760 listed members and 5,000 EE would need 93,800, so the family's best member is **short by 13x**. Throughput compounds it: web.archive.org refused roughly half of all connections and 13 pages cost ~45 minutes wall clock with retries, so ~189 pages is several hours for under 400 EE against the ~3,000 EE/hour querying alone pays. **The rest of the family is thinner, not thicker.** Phase2Media's `byalpha.html` is the head-selected variant the affiliate closure predicted: 28 distinct domains for the whole artifact, 26 already carrying 2001, 2 post-split pairs / 1.3 EE, and it prints brand names as plain text with ZERO anchors ("BabyZone", "Boating Online", "Maxim Online" carry no domain at all) while naming AmericanExpress.com, Barbie.com, Britannica.com and FOXSports.com. Eleven other networks checked at their 2001 homepages expose no member roster to follow: valueclick.com (8,963 B, publisher recruitment and press only), engage.com (17,458 B, solutions pages), fastclick.com (12,167 B, media kit), burstnet.com (1,453 B redirect stub), flycast.com (66 B), l90.com (255 B), targetnet.com (4,244 B), plus adsmart.net, doubleclick.net, smartclicks.com and adorigin.com, whose 2001 captures exist but whose fetches failed on transport rather than absence. **Two false-zero controls, both honoured**: `archive.org/wayback/available` returned NONE for `goldenwebawards.com`, a 2001 capture this file already records, so every NONE here is a lower bound and not an absence, invalidating the zeros for 247media.com and for deep pagination `q=2&p=17` and `q=2&p=30`; the ~280 EE ceiling deliberately does not depend on those, being computed from the membership TOTAL rather than from reachable pages, which is the generous direction. Positive control that the fetch path works: `q=10&p=2` found and fetched at 20010618183013, 14,346 B, so shallow pagination is archived and reachable. **Method, three reusable parts.** (1) **When a directory offers a sort order, sort by SMALLEST and sample there, then price head and tail separately**: page 1 of the traffic-sorted roster reads 0.9% held-and-missing-2001 and would have closed this family at 0.2 EE per page, while the same artifact's reverse sort reads 15.5% and 2.75 EE per page. One extra request per category converts a head sample into a tail sample, and the head of any 2001-dated directory is worthless by construction. (2) **A paginated directory prints "Page 1 of N", so the volume ceiling costs ZERO extra requests** if you read the counter off the page you were fetching anyway; combined with the reverse sort, one request per category yields the tail sample, the category size and the subcategory list at once. (3) **The roster discriminator predicts SHAPE reliably and PAYOUT not at all.** A roster prints the member's bare domain exactly when the reader must GO to the member, and BURST! is the positive instance the affiliate catalogues lacked, so the rule is now measured on both sides; what kills a roster is that one ad network has 10^3 to 10^4 members while 10^4 to 10^5 listed names are needed. **Price the next roster on membership size first, never on whether it prints domains.** Do not re-test ad network, banner network or ad rep firm member rosters: the shape works, the population does not. The one thing not disproved is a MULTI-YEAR walk of this same gallery, since the members lacking 2001 are a different set from those lacking 2000 or 1999 and the gallery has earlier captures, but at 1.49 EE per page that is a rate argument the family already loses, so revisit only if a bulk copy is ever found that does not cost one request per 38 names. |
| **Federation club registers (RSGB, linux.org), closed on the URL FILL RATE, a one-request screen the family had never been given (2026-08-28)** | **CLOSED at 27.6 net-new post-split EE over two arms, 0.0553 EE per listed club entry, and closed for every sport, hobby and user-group register on either screen.** What dates one item: the register page's own regeneration stamp ("This page was last updated on 16 March 2001", varying across 66 of 68 RSGB county files, so a real monthly regeneration), with the Wayback capture instant as the second leg. On linux.org the same footer is a template constant, 44 of 45 pages identical to the minute, so only the capture instant dates it. Artifacts, 1,283,000 B: RSGB affiliated-societies register, 68 of 75 county files under `web.archive.org/web/20010418144102id_/http://www.rsgb.org/memb/clubs/LCLB*.HTM` (538,733 B) plus two index pages; positive control linux.org user-group register, 45 country and US-state pages under `.../20011115235923id_/http://www.linux.org/groups/` (708,157 B) plus its index. **RSGB, the hypothesis's own named target, is killed by the hypothesis's own stated discriminator**: 68 county files, 493 club entries, exactly ONE club web URL in the register (`www.sadars.org`), a **fill rate of 0.20%**; every other entry gives a callsign, a meeting night, a named secretary and a phone number. Priced with `price_items.py --all-tlds`: 68 items, 57 distinct registrable domains, 51 held, pre-split 6 pairs / 5.2 EE (do not quote), **post-split 4 pairs / 3.6 EE**, mean weight 0.8940, uk 3 / com 1. Those 57 are not club sites but the mailto hosts of secretaries, so the population is ISPs (freeserve.co.uk, ntlworld.com, compuserve.com, aol.com, talk21.com) with about twenty club-owned names among them. Independent duckdb screen on DISTINCT DOMAINS agrees: 49 of 51 held (96.078%), 46 already carrying 2001, **held-and-missing-2001 = 3**, all adjacent, 2.94 EE gross. **The positive control proves the zero belongs to the register and not to the instrument**: linux.org is the same shape over a web-native population, 434 of 1,346 groups read, 379 carrying a URL, **fill rate 87.3% against RSGB's 0.20%**; 492 distinct domains, 415 held, pre-split 77 pairs / 32.9 EE (do not quote), **post-split 58 pairs / 24.0 EE**, mean weight 0.4135, de 18 / org 12 / com 11 / net 6 / ar 4 / br 3, typo bound 66.2%, duckdb agreeing at 473 of 492 held (96.138%) and 57 of 58 adjacent. Projected over the full 1,346-group register: linear 74, saturation 74, power law 69, so **quote 69 EE for the whole artifact**. **So the family fails both independent screens, one target each**: RSGB fails DENSITY at 0.20% fill over a population the federation itself puts at "more than 700" clubs, and linux.org passes density and fails AUTHORITY and volume at 96.138% held, 83.5% already carrying 2001, 0.0553 EE per listed group. The 5,000 EE bar then needs about **90,000 club entries in one register**, and no 1996-2001 federation register is within two orders of magnitude. The premise, that a reader has to GO to the club so the register must print its site, is simply false for 2001: the register prints a person and a phone number, because the club's contact was a volunteer secretary and not a server. Method, two reusable parts. (1) **THE URL FILL RATE IS THE WHOLE SCREEN FOR A CLUB REGISTER AND IT COSTS ONE REQUEST**: a 2001 federation register was compiled from paper affiliation forms, so its URL field is optional and empty, while a register compiled by a web portal from web submissions is 87% full. Ask who typed the entry, not how many clubs the body has; fetch one county index and count `href="http` against club entries before pricing anything. (2) **A COUNT PRINTED NEXT TO EVERY LINK IS A FREE DENOMINATOR AND A FREE EXTRACTOR TEST**: linux.org's index prints "Australia (17)", summing to 1,346 over 102 countries, which gave the projection base without fetching the register and caught an off-by-one in the block splitter, since the "more information" anchor sits at the END of a row so the text BEFORE anchor i belongs to group i. Also: a portal footer stamp can be a template constant, so vary-check it across pages before letting it date anything. Navigation used no `cdx` at all: `archive.org/wayback/available?url=...&timestamp=...` to find a capture, then `web.archive.org/web/<ts>id_/<url>` and the page's own hrefs to walk down, with a six-attempt retry wrapper because `web.archive.org` refuses the connection intermittently and succeeds on retry. Unreachable and why: ARRL's `FandES/field/club/biglist.html` is a GET form and the availability API returns NONE for its `?section=AL` form, `clubsearch.phtml` being a search endpoint; ScoutBase UK had moved to `sid.sbuk.org.uk/ukemail/` PHP forms by 2001 and its static Jumpstation A page carries 7 external links; the-fa.org at 2001 is a frameset over a JavaScript preloader with no reachable club directory. Two loose ends left deliberately, bounded by the 0.0553 EE per entry rate at under 1 EE and 45 EE: RSGB's 7 remaining county files plus `memb/nat-club.htm`, and the 912 linux.org groups not yet read. Do not re-test sport, hobby or user-group club registers on any screen. |
| **GNU-style ChangeLog author lines, and the saturation screen that replaces address density (2026-08-28)** | **CLOSED at 6.0 net-new post-split EE over 13 pairs from 141.4 MB, 0.0069 EE per registrable author domain, and closed for every per-contributor author population in any project or distribution.** What dates one item is real: the ISO date and the address are the same field (`2001-03-14  Name  <user@domain>`), so it escapes the mutable-column trap that killed MARC 856 and the NSF `piEmail` column, but it is human-typed and takes the corroboration split. Artifacts, 148,234,136 B pulled: 46 `download.gnome.org/sources/` module tarballs (99,742,459 B), 13 `ftp.gwdg.de/pub/linux/kde/Attic/2.2.2/src/` modules (47,115,650 B), 9 `archive.debian.org` `Sources.gz` over slink/potato/woody (1,376,027 B), plus `squidguard-1.2.0/CHANGELOG` already on disk. 837,232 ChangeLog lines yield 54,620 in-window author lines which collapse to **1,040 distinct email hosts and 871 registrable domains, a 52:1 collapse**. Priced with `price_items.py --all-tlds` against merged260827 sampling DISTINCT DOMAINS: 1,360 pairs, **1,310 already held (96.3%)**, pre-split 50 pairs / 25.1 EE (do not quote), **post-split 13 pairs / 6.0 EE**, mean weight 0.4589, typo bound 62.5%, 19 names new to the candidate pool. Per-arm: GNOME 49 modules 51,355 lines / 466 domains / 703 of 730 pairs held / 4.2 EE; GNOME restricted to 2001 20,215 lines / 205 domains / **193 held AT 2001 (94.1%)** / 2.2 EE; KDE 2.2.2 2,592 lines / 65 domains / 80 of 82 held / 0.2 EE at mean weight 0.1711, under the 0.4 floor; Debian maintainer and uploader fields over 10,431 source packages (slink 1,882, potato 2,964, woody 5,585) / 321 pairs / **320 held (99.7%)** / 0.7 EE. **The 2001 threshold runs BACKWARDS on this population**: P(store lacks Y | domain held) is 1996 0.394, 1997 0.298, 1998 0.217, 1999 0.120, 2000 0.037, **2001 0.031**, and `.com` at 2001 is **0.043 against the store-wide 0.611, 14x worse**. Positive control in the same code path, because nothing-found and pointed-wrong look identical: 200 `.com` names held at 2000 and missing 2001, written as synthetic author lines, ran through the SAME parser and pricer and returned 200/200 and **126.4 EE post-split**, so the whole 54,620-line corpus is worth less than ten such names and the pipeline was sound. **Method, and it is the result: the hypothesis's own cheap screen is the wrong screen, and it passes.** Address density is 7.8 email hosts per 1,000 lines on gnome-core, 8x the proposed floor, and the family still dies; the binding constraint is distinct-domain saturation, fitting **hosts = 6.87 x lines^0.406** (exponent measured twice independently at 0.414 and 0.399 over 3,565 / 12,183 / 51,355 lines giving 190 / 316 / 561 hosts). At 0.0069 EE per registrable domain, 1,000 EE needs ~145,000 author domains, and the fit puts 5,000 hosts alone at 11.3M author lines, 219x this corpus, while the entire Debian woody source archive has 310. **So screen an author corpus on distinct-domain saturation, never on address density.** The mechanism is the mirror image of the blocklist law: a blocklist pays because its held names LACK the year, while a contributor writes from a long-lived employer, university or ISP domain the store already carries in EVERY year. **Any corpus whose population is PEOPLE rather than SITES shows this inversion**, which reaches the mailing-list closure from a second independent direction. Three reusable operational notes: (1) **`download.gnome.org` mtimes are not release dates** (gtk+ re-stamped 2013-Feb-22), so pick tarballs by version series, and since ChangeLogs are cumulative one late tarball carries the whole 1996-2001 history at one fetch per module; (2) **one `Sources.gz` bounds a whole distribution's maintainer-domain universe in a single request**, the `ls-lR` trick applied to an author population; (3) `download.kde.org` is Mirrorbits and 302s with `link: rel=duplicate` headers naming every mirror, so read those headers to pick an allowed mirror rather than following the redirect blind. Robots read whole before the first request: **`www.winehq.org` disallows ClaudeBot** in its Cloudflare-managed block so Wine was never fetched; `mirrors.dotsrc.org` names Claude and was avoided; `download.gnome.org`, `download.kde.org` and `archive.debian.org` 404 on robots.txt; `ftp.gwdg.de` is empty `Disallow:`; `ftp.icm.edu.pl` disallows two paths, neither ours. web.archive.org was not touched. **Do not re-test GNU-style ChangeLogs, AUTHORS or CREDITS files, Debian maintainer or uploader fields, or any other per-contributor author population.** KDE also shows the convention is narrow: 13 KDE modules hold 23,447 ChangeLog lines against 813,785 for 49 GNOME modules, so outside the GNU orbit there is not even a corpus to price. |
| **Mailing-list catalogues (PAML, Liszt, tile.net, CataList), and a fourth screen: a SERVICE endpoint is the worst possible target under the 2001 threshold (2026-08-28)** | **CLOSED at 11.2 net-new post-split EE over 16 pairs from 66,245,691 B of Usenet mbox; counterfactual re-dating of the same 2,151 hosts to the only year that pays gives 61.5 EE, 0.0286 EE per distinct host, and the largest artifact in the family (Liszt, ~90,000 lists) tops out at 228 EE against a 5,000 EE bar.** The hypothesis is right that a catalogue of LISTS enumerates mail HOSTS in bulk and wrong about what a mail host is worth. What dates one item is the `Date:` header of a PAML Usenet part, identical to `usenet_announce`, but **11,381 of 18,258 entries in the 1996 parts carry a per-entry `Last change: <month> 95`, so the posting date dates the CATALOGUE and not the entry.** Artifacts pulled and read offline: `archive.org/download/usenet-news/news.lists.mbox.zip` 29,483,704 B and `news.lists.misc.mbox.zip` 36,761,987 B. The 2001 web editions (`paml.net` 7,363 lists built 2001-05-02, `tile.net/lists` about 52,000, CataList 48,424 lists / 2,104 sites stamped `4 Jun 2001 04:00 -0400`) are all archived and all sit behind web.archive.org, which refused TCP for most of the session after about 15 replay fetches; `paml.net` is now a GoDaddy lander, `tile.net` redirects to ChrisKnight.com and `www.neosoft.com` 403s to riseinternet.com, so there is no live route. Priced 495 in-window PAML parts (1996: 235, 1997: 240; the 20 dated 2001 are 115-byte stubs, not editions) with `price_items.py --all-tlds`: 3,342 distinct pairs over **2,151 distinct domains, 3,233 already held (96.7%)**, pre-split 109 pairs / 73.9 EE, **post-split 16 pairs / 11.2 EE**, and the surviving TLDs are `dance`, `tv`, `baseball`, `recipes`, so the true figure is lower. Whole message bodies were fed, so owner addresses and `http://` homepage URLs are already inside that number and it is generous. The decisive test re-dated the same 2,151 hosts to 2001 and re-priced: **100 net-new post-split pairs, 61.5 EE**. One number explains it: of the 2,079 hosts the store holds, **1,979 already carry 2001, so only 4.81% are missing it** (`.com` alone 864 held, 59 missing, 6.83%; adjacent held-2000-missing-2001 5.86%). Proved against a known positive in the same code path and session: 2,058 random `.com` domains drawn from the store read **59.48% missing 2001** (adjacent 66.69%), reproducing the register's 0.611, so the query is pointed right and the negative is real. **Mailing-list mail hosts are 8.7x LESS likely to be missing 2001 than a random held `.com`.** Cohort check against the objection that 1996-97 hosts are unusually established: the 1997-new cohort reads 6.08% missing 2001 and 0.0368 EE per host against the 1996 cohort's 4.26% and 0.0252, same order, so a later edition changes nothing. Sizing the whole family from the concentration ratio: PAML prints one `Contact:` line per list and 19,175 entries in 1996 collapse to 1,328 distinct hosts, 22,269 in 1997 to 1,515, so **14.4 to 14.7 lists per host**, while CataList self-reports 48,424 lists across 2,104 sites, 23.0 to 1. Top hosts `netcom.com` 730, `world.std.com` 424, `xmission.com`, `fatcity.com`, `lists.best.com`. At the most favourable measured rate Liszt's 90,000 lists give ~6,200 hosts and **228 EE**, tile.net ~3,600 hosts and 132 EE, CataList's 2,104 sites 77 EE; even the impossible bound of one host per list puts Liszt at 3,312 EE, still under the bar, so **the family cannot reach it arithmetically**. tile.net separately fails on retrievability: browse pages print a bare list name and the host lives only behind `showlists.php?list_id=N` over ~52,000 ids (known positive `?domain=A` returns 200 and 485,867 B; the one `list_id` probed returned 404, which is one probe and not a rate, but 52,000 replay fetches is unaffordable regardless). **Method: counterfactual re-dating is the cheap way to price a corpus whose good edition is unreachable.** Take the host population from any edition you CAN get, rewrite every item's year to the target year and re-price; that isolates what the population is worth in the year that pays from what the particular artifact asserts, and it costs no network. Pair it with a control drawn from the store by the same code path, because a held-fraction that extreme is otherwise indistinguishable from a broken query. Second reusable rule: **ask for the artifact's own lists-per-host ratio before pricing any directory of SERVICES rather than SITES** (CataList prints its ratio on the front page and PAML's is recoverable from one edition; either would have killed this in one fetch). Third: when web.archive.org is refusing connections, check whether the artifact was ALSO posted to Usenet, since `archive.org/download` stayed up throughout while `web.archive.org` was dead. **The register gains a fourth screen alongside density and authority: a mail host, a name server or any other SERVICE endpoint is the worst possible target under the 2001 threshold, because the very durability that let it host other people's lists is what makes the store already hold it at 2001.** One unspent thread, not for this family: `news.answers.mbox.zip` (3,370,920,579 B) carries the 1998-2000 PAML editions missing from `news.lists`, and `rtfm.mit.edu`'s mirror does NOT contain PAML (only `mail/mailing-lists/providers`), so the two are not duplicates. |
| **Pay-per-click bid tables (GoTo/Overture and ten rivals), closed on what the crawler LINKED rather than on what the engine sold (2026-08-28)** | **CLOSED at 22.2 net-new post-split EE over 36 pairs from 22 captures, 0.083 EE per listed domain; GoTo itself has 0 of 70 crawler-seen query URLs in window, and everything still reachable in the family caps under 50 EE.** What dates one item is clean: the Wayback capture instant of a pay-per-click result page asserts the advertiser is buying that keyword at that moment, so it is a live-site observation and machine-generated with no human-typed split; the page carries no date of its own, so nothing dates an item without the capture. **The named artifact does not exist in window** - no GoTo or Overture bid page has a 1996-2001 capture, and `inventory.overture.com` is 2002. The surviving sibling is `partner.ah-ha.com/1000.asp?query=<kw>`, 19 captures at 2000-12-12, 2001-04-13 and 2001-06-03 (~33 KB each; `web/20010603133655id_/` for `query=tickets` is 32,986 B), plus 3 `www.bay9.com/?page=<country>` captures at 2001-04-04. Rank and bid price sit in each row's redirector (`tools/redirect_nk.asp?...&bid=0%2E05&...&r=18`) and the destination domain is plain text, so it is a genuine bid table, exactly the shape proposed. 70 query-string URLs were lifted verbatim from two captured goto.com pages (20001210082400, 20010830140743), so they are URLs the crawler itself saw: 67 have no capture and 3 are captured only at 2004-2005. 8 guessed peek and bidtool URLs return nothing. Pointed-wrong is excluded by four positive controls answering in the same batches (`www.isi.edu/in-notes/us-domain-delegated.txt` 20010606153725, `www.goto.com/d/about/advertisers/` 19990221101215, `www.goto.com/d/search/` 20001210082400, `www.goto.com/` 20010830140743), and the oracle is exact-match not fuzzy: the same URL with two fabricated `;$sessionid$` tokens returns nothing while the real token returns 20040528190918. **The family is alive on the second-tier engines and dead only on the market leader**: 10 of 11 PPC engines have 2001 homepage captures (7search.com none), and of 45 crawler-seen query URLs ah-ha is 19/20, kanoodle 8/15, bay9 4. Priced with `price_items.py --all-tlds` against merged260827 sampling DISTINCT DOMAINS: 22 captures give 268 in-window pairs over **266 distinct domains, 212 already held AT the artifact's own year (79.1%)**, pre-split 56 pairs / 35.0 EE, **post-split 36 pairs / 22.2 EE**, all at 2001, com 33 / net 3, mean weight 0.6172, typo bound 28.6%. That is **0.083 net-new post-split EE per listed domain, 5x worse than the 0.386 a held `.com` is worth at 2001 population-wide** and close to the 0.041 measured on a 2001 magazine archive, because an advertiser roster is the commercial head and we already cover the head at 2001. The 8 kanoodle and 1 bay9 pages not fetched are another ~150 domains, under 15 EE. Five method results: (1) **never guess a query-string URL; take the exact hrefs out of a captured page of the same host and probe those** - guessed URLs manufacture false zeros, and that one switch turned a flat zero into 19/20 on ah-ha; (2) `archive.org/wayback/available` is a per-URL existence oracle that never touches the metered CDX and is exact on the whole query string including odd path params like `;$sessionid$X`; (3) **but it emits REPRODUCIBLE false zeros, so it cannot close a family on its own** - `partner.ah-ha.com/1000.asp?query=tickets` was reported captured at 20010603133655 in one batch, then returned no snapshot on three consecutive retries while a known-positive control answered correctly in the same batch, and replay then served that exact capture in full at 32,986 B, so **confirm every zero that matters against the replay channel** and read a batch of zeros as an upper bound on the negative (this extends the documented `services/search/v1/scrape` lie to `wayback/available`); (4) **a PPC engine's reachable corpus equals the keyword set LINKED from its crawled homepage and nothing more** - 19 of 19 linked keywords are captured, 0 of 63 unlinked common commercial keywords are, and pagination (`&P=2`) is not captured either, so size this family by counting homepage links and never by the engine's advertiser count, GoTo having had 45,000 advertisers in Q2 2001 and zero reachable pages; (5) **new pricing law: a roster of PAYING commercial sites already carries its own year at ~79%**, the squidGuard 2000-10-18 death rather than the 2001-12-18 win, so screen any advertiser roster at ~0.08 EE per listed name before downloading it. Closed: the named artifact has no in-window capture and the siblings that do are capped by what was crawled, so neither more keywords nor more engines can move it. The 22 priced captures are already on disk and worth 22.2 EE for zero further collection. |
| **2001 nonprofit portals, a FIND that closes its own family, and the head-selection law operating INSIDE one directory (2026-08-28)** | **FIND at 202.2 net-new post-split EE over 289 pairs, all at 2001, mean weight 0.6996 (org 257 / com 29 / net 3), from 19 dated pages and 601,956 B; and the family is CLOSED at that figure, because its entire enumerable population is 1,843 names against the 9,116 that 1,000 EE needs.** Class `dated_directory`, master-eligible, approval request filed with `Decision: pending`; NOT ingested. Dating rests on the Wayback capture instant alone, since no page in the family carries a usable self-date (the Foundation Center list pages print none), so the corroboration split is taken. The paying seven, all `web/<ts>id_/`: `fdncenter.org/funders/grantmaker/gws_pubch/pubch_list.html` at 20011102033013 (740 domains), `gws_priv/priv_list.html` at 20011024182703 (548), `gws_priv/priv2.html` at 20011004165617 (546, a Netscape-4 duplicate that checks priv_list is complete), `gws_corp/corp_list.html` at 20010806091046 (376), `interaction.org/members/` at 20011101195341 (149), `foundations.org/grantmakers.html` at 20011015131123 (99), `igc.org/igc/gateway/index.html` at 20011011002103 (36). Priced with `scripts/pricing/price_items.py --all-tlds` against merged260827: 1,843 distinct pairs over 1,843 domains, 1,523 already held AT 2001 (82.6%), only 29 names never held anywhere (98.43% held), pre-split 320 pairs and 223.6 EE (overstates 1.11x, do not quote), **post-split 289 pairs and 202.2 EE**, typo upper bound 37.8%. An independent duckdb screen sampling DISTINCT DOMAINS over the three fdncenter `_list` pages agrees to 0.36% (1,648 listed, 1,619 held, 1,349 carrying 2001, 268 held-and-missing at 187.43 EE against price_items' 269 at 188.1). **279 of the 289 gain domains (96.5%) are held at 2000**, so this is adjacent-year headroom and not a death gap. Rates: **0.1097 EE per listed domain, 10.6 EE per dated page fetched.** Head-selection penalty measured at 4.34x: only 15.93% of held names here lack 2001 (289/1,814) against the `.org` population's 69.15%; `.org` at weight 0.7101 makes one already-held name dated 2001 worth 0.491 EE, above `.com`'s 0.386. **The method, and it is the result: within one nonprofit directory the discriminator is whether the listed entity's website is a CORPORATE domain or a standalone nonprofit domain, and it is readable off the section name before any fetch.** The Foundation Center's four sections are one artifact family, one crawl, one page shape, one capture window, and they spread 10.1x: priv_list 548 listed / 532 held / 375 carrying 2001 / 157 gain / 109.98 EE = **0.2007 EE per listed name**; pubch_list 740 / 728 / 625 / 103 / 72.08 = 0.0974; interaction 149 / 149 / 134 / 15 / 10.65 = 0.0715; foundations.org 99 / 99 / 94 / 5 / 3.47 = 0.0351; **corp_list 376 / 373 / 362 / 11 / 7.50 = 0.0199**. The reason is visible in the names: corporate giving programs live at `abbott.com`, `aetna.com`, `adobe.com`, `agilent.com`, so 97.05% of the held ones already carry 2001, while private foundations live on their own small `.org` domains and only 70.5% do. So split a directory by listed-entity type and price only the nonprofit-domain sections; **per-arm attribution before quoting any per-item rate.** Second method find: **on any alphabet-indexed directory look for the single-page variant FIRST.** `priv1.html` offers A-to-Z browse plus one link labelled only "related document", and that link is `priv_list.html`, the complete unannotated roster of all 26 letters in one file, both cheaper (1 request against 26) and the higher-coverage surface. **Why the family dies, and it is the pre-download screen: volume and browsability are anticorrelated across this whole sector.** Every 2001 nonprofit portal with five-figure record counts put them behind a search form and has no captured multi-record surface, GuideStar ~640,000 orgs and Idealist ~20,000 being one record per request, while CharityChoice states "more than 7,000 entries in 27 categories" in its own meta description and archives only the FORM (`searchtdframe.htm` at 20011020104216, whose only captured frame is `searchtdpage.htm`). The rosters that ARE browsable are the small accreditation-shaped bodies. Measured zeros, each fetched and parsed rather than assumed: `netministries.org/see/churches/` at 20000920005932 is 58 B of "*Sorry* cannot find that church"; `idealist.org/orgs/` at 19991002025040 yields 0 external hosts; `give.org/reports/index.asp` at 20011002001937 yields 2, both `bbb.org` and `adobe.com`, because a BBB charity report links to its own internal report page and never to the charity; `oneworld.org/partners/`, `charitynet.org/` and `wango.org/` yield 0. Genuine 404s against a passing control: the whole `gws_comm` community-foundations section and `helping.org/nonprofit/`. archive.org's item store holds no bulk artifact for the family: `advancedsearch.php` on guidestar / charity directory / nonprofit directory / grantmaker web sites / foundation center, each AND `date:[1996-01-01 TO 2001-12-31]`, returns numFound 3, 11, 27, 0 and 2,500, and every in-window hit is a printed fundraising handbook or an ERIC paper. Two traps. **`archive.org/wayback/available` returned NONE for `priv_a.html`, which the replay endpoint's own 302 then resolved to 20011024145446**, reproducing the false zero recorded on 2026-08-27; the replay 302 got the same four probes right including two genuine 404s, so re-test every `available` zero against `/web/<ts>/URL` with a known-good control before recording it. And **a sub-1KB 2001 page is often a `navigator.appVersion` browser fork, not the content**: `gws_priv/priv.html` is 849 B of JavaScript branching to `priv1.html` or `priv2.html`, and a reader that took it as the section would have filed the largest arm in the family as empty. Operationally, **`web.archive.org` and `archive.org` fail independently, so use the healthy one for the question it can answer**: replay refused roughly half of all TCP connections through this run (4 of 6, 3 of 6, 4 of 6 in three timed probes, a flat ~3.4 s connect failure, no 429, no 503, no `Retry-After`) while `archive.org/wayback/available` answered 200 continuously, so existence and URL-variant mapping were done entirely on archive.org at ~1 q/s and the expensive `id_` fetches restricted to confirmed timestamps. That refusal is capacity and wants a flat 8 s retry, not exponential backoff; treating it as absence would have filed four live pages as unreachable. Do not re-test the Foundation Center Grantmaker Web Sites (`gws_comm` was never captured, and its A-Z letter pages are the same names annotated, so 78 further fetches add nothing), helping.org, GuideStar, Idealist, CharityChoice, netministries, give.org, oneworld, wango, charitynet or foundations.org. **The one thread that belongs to a different family:** `igc.org/igc/gateway/index.html` names 36 hosts of which several are `*.igc.apc.org` subdomains, the signature of a **nonprofit ISP hosting its members**, and a member list from IGC, EcoNet, PeaceNet or an ISP association is a different population from a directory of organisations that already had their own domain. |
| **Registry MEMBER rosters, and the third question the seal-roster discriminator was missing (2026-08-28)** | **CLOSED at 17.7 net-new post-split EE over 44 pairs, 0.0294 EE per listed domain, against a family whose total enumerable domain-bearing population is about 600 names.** 11 priced pages, 471,998 B, all fetched as `http://web.archive.org/web/<ts>id_/...`: Nominet's `taglist.html` at 20001206101400 (194,247 B, 3,355 tag holders), five ICANN `registrars/accredited-list.html` editions spanning 20000303 to 20011201, InterNIC `alpha.html` at 20010803025008, `corenic.org/comnetorg.htm` at 20011020044926, `centr.org/members.html` at 20011211193538, and DENIC `mitglieder.shtml` plus `mitglieder2.shtml` at 20010807. Dating is `dated_directory` and rests on the capture instant alone: no member page carries a usable self-date. Priced with `scripts/pricing/price_items.py --all-tlds` against merged260827, 742 pairs over 602 domains, 674 already held, pre-split 68 pairs and 28.1 EE (overstates 1.6x, do not quote), **post-split 44 pairs and 17.7 EE**, mean weight 0.4024, com 19 / de 14 / net 5 / cn 2 / am 2 / io 1, typo upper bound 74.6%. An independent duckdb screen sampling DISTINCT DOMAINS agrees to 0.06%: at 2001, 430 listed distinct, 416 held (96.744%), 375 already carrying 2001 (87.209%), held-and-missing-2001 = 41 and **all 41 adjacent** (held 2000), 15.99 EE; at 2000, 312 listed, 302 held, 299 already at 2000, 3 pairs and 1.72 EE. The head-selection penalty reproduces exactly: 12.8% of held names lack 2001 against the population's 61.1% for `.com`, a 4.8x penalty, and the five ICANN editions show the multi-edition lever fails too. **The method, and it is the result: the seal-roster discriminator needs a third question, and it is what the roster is FOR.** A roster prints the member's own domain only when the reader has to GO to the member. ICANN, InterNIC, CORE and CENTR exist to route a registrant to a registrar, so every row is a link (109 to 159, 83, 48 and 42 absolute member hrefs). Nominet's tag list, DENIC's Mitgliederuebersicht and SIDN's deelnemers exist to record who holds a vote or a co-op share, and those rows are a legal name and a postcode. **Routing rosters carry domains, membership rosters carry legal names.** Nominet, the only member of the family with volume at 3,355 tag holders, pays 3 pairs and 1.7 EE, **0.0005 EE per member**, because it prints TAG plus REGISTERED COMPANY NAME: 141 domain-shaped tokens in 194 kB (4.2% of rows), several of them CSS class names (`a.menu`, `b.head`, `c.dp`, `g.triad`) and the rest company names that happen to contain a domain (`101cd.com Ltd`, `4u-uk.com Limited`). DENIC's second page is the one genuine domain-bearing registry roster, 149 member `www.<member>.de` URLs in one request, and prices at 177 domains, 157 held, 14 pairs and 2.2 EE at mean weight 0.1553, below the 0.4 floor. **The corollary bounds the family before any download: every roster that does print domains is an accreditation list, and accreditation is by construction small.** gTLD accreditation was 109 to 159 companies across the whole window, against the ~2,600 held `com` names that 1,000 EE requires. Two operational finds. **`archive.org/wayback/available` at a pinned `timestamp=` separates wrong-path from never-captured without touching the metered CDX**: 22 calls in 1.2 s, with `www.isi.edu/in-notes/us-domain-delegated.txt` as a positive control returning 20010606153725 in the same batch, so the four `none` rows are the hosts and not the paths. And a dating trap: **DENIC's `.shtml` pages print a server-side timestamp that is STALE.** `mitglieder.shtml` captured 20010807122242 prints `09.03.2000, 15:55:19 Uhr` and `mitglieder2.shtml` captured 20010807123326 prints `08.02.2000, 11:49:21 Uhr`, a 17-month lag, while the home page captured 20010712011748 prints `09.04.2001`. Dating that roster by its own printed date would have moved it to 2000, where it pays 0, and would have recorded a false observation; only the capture is defensible. Dead or walled, recorded so nobody probes them again: `auda.org.au` has NO in-window capture and `/registrars/` first appears 20020802093824; `cira.ca/cira/rr_choose_en`, the registrar chooser its 2001 register page links to, returns no capture at any timestamp; `nominet.org.uk/members/membership.html` at 20010815060741 says "over 2000 members" and puts them behind a password-protected member area; `sidn.nl/deelnemers.html` at 20010309022335 puts its deelnemers behind an SSL login and names none. Do not re-test Nominet's tag list or member area, DENIC's Mitgliederuebersicht, SIDN's deelnemers, CIRA's registrar chooser, auDA, CENTR, CORE, or the ICANN and InterNIC accredited-registrar directories. **The one thread this leaves open belongs to a different family:** `denic.de/doc/DENIC/mitglieder2.shtml` is the working shape of a NATIONAL ISP ASSOCIATION roster (name, postal address, phone, e-mail and website in one page), which is the same artifact an ISPA or an ISOC chapter publishes with a larger membership and a higher-weight TLD, and no such roster has been tested here. |
| **Affiliate-network merchant catalogues, closed on the field the row actually carries (2026-08-28)** | **CLOSED, and closed on the reading rather than on a completed price: the researcher was stopped with `measurement: IN PROGRESS`, so the finding file's header figure of 101.0 EE has NOTHING behind it and must not be quoted.** What was established is the part that decides the family, and it is a clean negative. The dating argument is sound and is `dated_directory` shape: the merchant row is a PROGRAM record served from the aggregator's live program database and replayed at a Wayback timestamp inside 2001, with all 59 catalogue pages resolving to a single capture at 20010602, so the artifact does assert a state at an instant the archive fixes. **It dies one step later, on what the row contains: the listing names the affiliate PROGRAM's brand, not the merchant's site.** That is the same death as a provider-run member index and the same death the hypothesis itself predicted for the redirector case, arrived at by a different route: here the outbound link is not swallowed by `bfast.com` or `linksynergy.com`, it is simply absent, because a program directory is written for an affiliate who will be handed a tracking URL and never needs the merchant's bare hostname. **The screen that generalises: ask what the directory's READER is being sent to.** A routing roster prints a domain because the reader must go to the member; an affiliate catalogue routes through the network by design, so the domain is structurally missing from the record even when the record is perfectly dated. This is the same law the registry member rosters measured on the same day, reached from the opposite side. Do not re-test Commission Junction, LinkShare, BeFree, ClickTrade or `refer-it.com` merchant catalogues. If anything here is ever worth re-opening it is the pricing that never ran, and it should be re-opened only against an artifact class that prints bare hostnames as TEXT beside the redirector anchor, which is the Bomis shape and is not what these 59 pages do. |
| **Channel-partner and reseller locator databases, closed on artifact existence in one screen (2026-08-28)** | **CLOSED at 0 EE, on the cheapest screen the hypothesis named and before any bulk fetch: nothing reaches an item, because the roster is a QUERY RESULT and no parameterised result page is in the archive.** The hypothesis was the seal-roster shape at a much larger scale, Microsoft Solution Provider and Certified Partner, Cisco, Novell, Sun, Intel and Compaq partner locators, tens of thousands of small IT firms weighted to `uk`, `au`, `ca` and `com`, and it wrote its own death test: whether the 2001 capture exposes an enumerable namespace the way BBBOnLine exposed 36 letter pages, or only a form POSTing to a CGI. It is the form. **This is the CDNnet death and it is now the third family to die of it** (CDNnet, PICS label lookup, and now partner locators), which makes it the reliable pre-download screen rather than a coincidence: a crawler cannot guess a postcode, a product code or a country code, so a search-only surface leaves no archived artifact to date, no matter how large or how well dated the database behind it was. The record here is interim in its numbers and complete in its verdict: there is no EE to measure because there is no item. **Test enumerability before value on any locator, directory or find-a-partner surface**, since it costs one capture and it is decided by the URL shape alone. Do not re-test Microsoft, Cisco, Novell, Sun, Intel or Compaq partner and reseller locators. |
| **RSS and RDF channel registries, and the head-selection penalty that INVERTS the aim-at-2001 rule (2026-08-28)** | **Closed at 47.9 net-new post-split EE over 83 pairs, all at 2001, mean weight 0.5775 (com 54, org 10, net 9, de 3, is 2, pl 1), against a whole-family ceiling under 250 EE.** Dating was never the problem: the registry dump stamps itself (`dateCreated 2001-11-29`, `updated="Fri, 09 Nov 2001 22:34:00 GMT"`) and the Wayback capture pins it, a `dated_directory`, and Syndic8's `feedlist.php` prints a per-feed Created and Approved date. The family dies on population. Syndic8, the only member that works: `web/20011112122642id_/http://www.syndic8.com/genfeed.php?Format=opml`, 861,293 B in ONE request, parsed 3,479 of 3,479 outlines (`stats.php` at 2001-11-25 corroborates 5,718 total feeds). Sampled as DISTINCT DOMAINS: 1,610 distinct registrable, 1,527 held in some year (94.8%), 1,453 held AT 2001 (90.2%), held-and-missing-2001 74, held-2000-missing-2001 71, never held 83; post-split 74 pairs and 42.3 EE at **0.0263 EE per listed domain** (pre-split 157 pairs and 88.9 EE, overstates 2.1x, do not quote). weblogs.com's `changes.xml` ping registry rates 2.5x better per domain, 106 registrable from one capture, 87 held at 2001, 11 post-split pairs and 6.9 EE at 0.0651 EE per domain, but it is killed on volume: `__wb/sparkline` reports **2 in-window captures** of `changes.xml` and 1 of `newhome.weblogs.com/`, so the union ceiling is ~250 domains and ~16 EE and the 22,490-member roster the file self-reports was never archived as a listing. Union of the two, priced against merged260827: 1,681 domains, 1,507 already held at 2001, **83 post-split pairs and 47.9 EE**. xmlTree is 1999-dated and unextractable: the page self-dates "Tuesday, 12 October 1999" and counts 1,318 leaves over 10 Dewey branches, but branch pages carry 0 external hrefs and 0 leaf links and the ColdFusion `resource/detail.cfm/ResourceID/N` pages returned the not-archived calendar for 6 of 6 sampled ids against a passing control, so 0 extractable domains. newsisfree is the right shape at the wrong cost: 41 in-window captures of `/sources/byname/`, self-dated "Thu, 11/08/01", but the letter pages print 31 ids and titles per page and **no hostnames**; `/sources/info/<id>/` does print the site URL, so it is 1 datable domain per request over ~2,700 ids, a ~175 EE ceiling for ~2,700 fetches, and the headline URLs on those pages are `link_target` and never date a year. `my.netscape.com/publish/` is a publisher onboarding form with 1 external href: the hypothesis's premise that Netscape ran a browsable RSS 0.91 channel directory is wrong for this surface, the channel list lived behind the personalised My Netscape page and was never public. **The transferable law qualifies "Aim at 2001, not 1996": head selection is a 2001 problem, not a general one, and the fix is to aim the artifact EARLY.** A control of 20,000 random DISTINCT held `.com` reproduces the register's own 0.611 at 0.5980, validating the measurement, and gives fraction-of-held-missing by year: 1996 0.9231, 1997 0.8423, 1998 0.7933, 1999 0.6048, 2000 0.2605, 2001 0.5980. The same table over Syndic8's 1,527 held publishers: 1999 0.3556, 2000 0.1179, 2001 0.0485. So the head-selection penalty against the population is **12.3x at 2001 and only 1.7x at 1999**, because the store covers long-lived publishers almost completely at 2000 and 2001 and thinly before. The same 1,610 names in an artifact dated 1999 would show 543 held-and-missing instead of 74, a **7.3x swing**. For a curated corpus of survivors, prefer the OLDEST edition; the population rule inverts. The caveat that keeps it honest: missing-1999 partly means "did not exist in 1999", so the headroom is real only for names a genuinely 1999-dated artifact lists, which is why xmlTree was the decisive test and why its unextractability closes the family rather than leaving it open. Two operational finds. **A directory's own machine-readable export clears a whole site in one request**, the `ls-lR` trick applied to a directory: 861,293 B and 3,479 dated rows for one fetch against newsisfree's 1 domain per fetch over the same population, so look for `genfeed`-style OPML or OCS on the front page before crawling any directory tree. And **a Wayback not-archived answer is HTTP 200 with ~4,679 B of calendar HTML**, so status and size cannot separate a hit from a miss and a size-only check silently manufactures a corpus: grep the body for `web-static.archive.org/_static`, which caught 6 false positives on xmlTree that would have read as archived leaf pages. `__wb/sparkline?output=json` retired weblogs.com in a single call with no CDX. Operationally, web.archive.org replay refused connections on ports 80 and 443 repeatedly while `archive.org` answered in 0.65s and a sibling fan-out agent was fetching replay concurrently: **the fan-out competes with itself for one IP's replay budget**, the refusal is connection-level with no `Retry-After`, and a retry-every-25s loop got through where rest-and-double backoff did not. Do not re-test Syndic8, xmlTree, newsisfree, weblogs.com or `my.netscape.com/publish`. |
| **Public Analog and Webalizer reports, the referrer section: the third and last death of traffic as a selection principle (2026-08-28)** | **Zero, and a strong zero: 0 net-new pairs and 0.0 EE before AND after the split, 0 names new even to the candidate pool, from 55 in-window dated reports over 6 live hosts, 7,629,595 B fetched.** The shape is master-eligible and takes no split: the analyser stamps its own period ("Summary Period: March 2001", "Analysed requests from Fri-01-Oct-1999") and a referrer row is a machine observation that the named site sent a real click inside that period, so it is `link_source`. Dating is sound; the value is nil. 61 reports from 9 hosts, 55 dated in window, spanning 1998 16, 1999 17, 2000 16, 2001 6, over US academic, UK and Swedish personal, and an ibiblio-hosted news mirror; largest runs `www.weewave.mer.utexas.edu/stats/logs_{98,99,00}/` 36 monthlies and `space.physics.uiowa.edu/pi/previous/erebus/analog/` 16 quarterlies. Host Reports of client hostnames were excluded by section, being the reverse-DNS visitor-log population already closed at 98.4% and 99.6% held. Referrer sections give **108 distinct registrable domains under a weighted TLD and 141 (domain, year) pairs; 108 of 108 domains already held (100%) and 141 of 141 pairs already held AT the artifact's own year (100%), so held-and-missing-that-year = 0**. `scripts/pricing/price_items.py --all-tlds` against merged260827 and an independent duckdb per-domain screen agree exactly at 141/141. Sampled as DISTINCT DOMAINS. TLD mix com 48, edu 20, org 9, jp 5, net 5, au 4, uk 2, gov 2, ca 2, cn 2, so the zero is not an artifact of US academia. **The 2001-threshold law predicts ~84.8 of the 141 pairs should be missing their year (28.1 of the 47 at 2001); observed 0, and P(141/141) under that law is ~1e-56, so this is a different population and not a small sample.** The population, measured: **75.0% of referrer domains are held in ALL six years 1996-2001 against a store baseline of 2.6% on 3,408 random held domains, a 29x enrichment in the longest-lived names**, and reading the list says the same (altavista, aol, google, excite, hotbot, dogpile, goto, alltheweb, geocities, compuserve, earthlink, cnet, adobe, go.com). Three structural facts cap the ceiling independently: (1) **two thirds of reports carry no referrer data at all**, 39 of 58 files never contain the string "referrer" because referrer reporting needs a combined log format, so only 18 of 55 dated reports yield one referrer host; (2) **the report is a truncation that prints what it withheld**, ccic shows 20 of 1,001 referring URLs and says "[not listed: 981 URLs]", chiark 10 of 248, purdue "Top 81 of 125 Total Referrers", so 35% to 98% of the population is suppressed and what is printed is by construction the head; (3) **the class is nearly absent from Wayback**, `www.asi.org/usage/2001-07.html` is a hard 404 in the replay index while `www.asi.org/` has captures, `h-net.org/stats/` redirects to 2012, and `advancedsearch.php` on titles (usage statistics OR webalizer OR web server statistics OR referrer report) AND date:[1996-01-01 TO 2002-12-31] returns numFound 3, being a tucows copy of the Webalizer binary and two ERIC papers. Rule of three on 0 in 141: net-new rate <= 2.13%, so <= 0.0345 EE per dated report and <= 0.1053 EE per referrer-bearing report even if every net-new pair were `.com`; **1,000 EE needs >= 29,004 dated reports (9,492 referrer-bearing) and 5,000 EE needs >= 145,019**, against six searches that named 11 candidate hosts of which 3 no longer resolve and no host serving a directory index. **The transferable law: a referrer is by construction a page that was live, linked, clicked and therefore crawled, so a referrer table is drawn from the crawled population, which is the baseline. Traffic-sending is crawl-correlated, and the correlation is measurable before any download as the fraction of names held in all six years, 75.0% here against a 2.6% baseline.** This kills the referrer field the way 2026-08-25 killed the visitor log and 2026-08-27 killed the counter directory, completing the family from its third and last direction: whichever end of the traffic pipe you read, traffic as a selection principle fails the authority screen. Do not re-test any log-analyser output. Cheaper than sampling: **a log analyser prints the size of the tail it refused to print**, so grep `[not listed:` or `Top N of M` and read the suppression ratio straight off the artifact, and check the Go To list for a Referrer Report at all. New trap: **`archive.org/wayback/available` returns HTTP 200 with `archived_snapshots: {}` for `http://www.yahoo.com/` at timestamp 20010901**, a false zero on a URL Wayback holds thousands of times, so the CDX-free finder recommended on 2026-08-27 needs a control; the replay endpoint's own 302 gets the control right (`web/20010901000000/http://www.yahoo.com/` to `20010901004632`), carries the chosen stamp in `Location`, and needs about 5 retries because web.archive.org drops roughly half of first connections. Search-engine indexes of this class are stale, so treat a hit as a lead and not an artifact (3 of 11 named hosts dead, psubs.org's whole 2001 stats directory 404). robots.txt read in full on every host before any request: ibiblio Crawl-delay 2 with `/ccic/` allowed, uiowa disallows `*/webalizer/` and `*/archive/` but not the `/analog/` path used, `h-net.org` answers 403 on robots.txt and was left alone. |
| **PICS self-rating label registries, closed on the MISSING HALF of the seal-roster discriminator (2026-08-28)** | **Closed at 3.9 net-new post-split EE over 6 pairs, all at 2001, mean weight 0.6451, against a claimed population of order 130,000 self-rated sites.** The family census is complete and it is 11 rating services and 10 label bureaus, taken in TWO requests from `w3.org/PICS/raters.htm` (4,270 B) and `w3.org/PICS/bureaus.htm` (2,939 B) at a 2000-06 capture, which named six services the hypothesis had not: RSACi, SafeSurf, Net Shepherd, SurfWatch, CyberPatrol/Microsystems, EvaluWeb, ORC, Adequate.com, Weburbia, Vancouver Webpages VWP1.0, CSR Bologna, plus IBM, Jigsaw, NewView, Source Pacific and AT&T as bureaus. 47 distinct replay paths fetched, 10 of them 404. **EVERY service is a questionnaire-and-`.rat`-file generator with no directory.** ICRA's entire 2001 site is five nav entries and its only enumeration is `members.html` at 21 corporate hosts (aol, microsoft, bt, novell, uunet, networksolutions); its own About page states "ICRA does not rate internet content, the content providers do that", so the label lives on the labelled site and ICRA holds no roster. `rsac.org` by 2000-06 is a 2,010 B frameset forwarding to ICRA and `ratingsv01.html` is the system definition, not a list. Net Shepherd is a corporate brochure at 1999-04 (4,992 B, 0 external hosts) and 2000-03, by which date it had pivoted to community software. Vancouver Webpages and Weburbia ship a `.rat` file and a header generator, and Weburbia's only site list is a Site Of The Week. `pics.microsys.com` at 1999-06 is a 1,695 B Cyber Patrol shell whose data is the CyberNOT blocklist, the already-closed blocklist lens. So SafeSurf is the only member publishing any site list and those are editorial picks, of the pick shape already closed at line 564. Artifact: 6 pages, 72,309 B, `web/<ts>id_/` at 2001-03-08 to 2001-04-13 over `safesurf.com/kids{1,2,3}.htm`, `safesurf.com/mirror/`, `ufcws.org/menu/index.html` and `child-safe.com/`. Priced by `scripts/pricing/price_items.py --all-tlds` against merged260827: 151 distinct pairs over 151 domains, 144 already held, pre-split 7 pairs and 4.9 EE (overstates 1.3x, do not quote), **post-split 6 pairs and 3.9 EE**, com 5 org 1, typo upper bound 85.7%. Sampled as DISTINCT DOMAINS with the six lister domains removed, 147 listed: 146 held any year (99.3%), **141 already carrying 2001 (95.9%)**, so held-and-missing-2001 is 5, 3.24 EE gross, all 5 adjacent (held 2000), agreeing with the split figure to 0.7 EE. **The gap between claimed and enumerable is the whole result: RSACi claimed order-100,000 self-rated sites and SafeSurf order-30,000, and the total enumerable population across the entire family is 147 domains.** **The transferable law: the seal-roster discriminator is necessary but not sufficient, and the missing half is where verification happens.** BBBOnLine and TRUSTe publish the member's own domain because a seal is worth nothing unless a visitor can check it against the issuer, so the issuer MUST run a public directory. A PICS label is verified by the browser parsing the label out of the page itself, so the registry never needs a directory and none of the eleven ever built one. Pre-download question for any "who is certified" corpus: **does the checker go to the ISSUER or to the ARTIFACT? If the artifact carries its own proof, there is no roster, whatever the population size.** Operational, and it changed a number here: **`Memento-Datetime` is what tells you which capture you got, and `%{url_effective}` is not.** `/web/20010401000000id_/http://www.safesurf.com/safesurfing/` returned HTTP 200 with no redirect and served a **2004-03-17** capture while `url_effective` still read `20010401000000id_`; one `curl -I` per page kept a 2004 listing out of a 2001 measurement. Do not re-test ICRA, RSAC, SafeSurf, Net Shepherd, Weburbia, Vancouver Webpages, EvaluWeb, Adequate.com or the Cyber Patrol PICS bureau. Two threads named so nobody chases them: `adequate.com`'s `/cgi-bin/adequate/ratings.cgi` participant list is the only true roster the family ever had and it returns 404 at 1998, 1999, 2000 and 2001 targets, so it exists nowhere; `evaluweb.ptloma.edu` never answered inside the replay budget and is a 1999-era head-selected university service, ceiling well under 10 EE. |
| **2001 trade-show exhibitor rosters, closed: a BOOTH is a head selector (2026-08-28)** | **CLOSED at 7.1 net-new post-split EE over the 214 of 1,103 COMDEX Fall 2001 booth records that were fetched, 0.0438 EE per listed domain. This row supersedes the INCOMPLETE entry filed earlier the same day, whose header figure of 25 had nothing behind it; the pricing has now run.** Dating is unchanged and sound, `dated_directory` shape: the booth record is served from the show's live exhibitor database and replayed at a Wayback timestamp inside 2001, so the artifact asserts the exhibitor's URL as current at an instant the archive fixes, and COMDEX Fall 2001 (12 to 16 November 2001) fixes what the database is a roster OF. Artifact: `http://marketplace.key3media.com/exhibitor/PreviewVirtualBooth.jsp?dept=Attendee&event_id=507&row_id=<opaque Oracle ROWID>`, about 17 kB per record, reached one request deeper than the 27 `ExhibitorList.jsp` pages (1,076,449 B, 1,550 rows) that enumerate the show. **Measurement, sampled as DISTINCT DOMAINS: 214 records, 162 distinct registrable domains, 144 held in some year (88.9%), 130 of those 144 already carry 2001 (90.3% of held), so held-and-missing-2001 is 14, or 9.7% of held, and the post-split total is 7.1 EE.** The premise is what failed, and it failed on the second screen and not the first: the held fraction of 88.9% is authority-corpus grade and fine, but only 9.7% of held names lack 2001 against the population's 61.1% for `.com`, a 6.3x head-selection penalty derived from those two figures. **A paid booth is a head selector.** The hypothesis argued the opposite, that a COMDEX exhibitor list is the B2B long tail and should behave like BBBOnLine's air-conditioning contractors rather than TRUSTe's `about.com`; a company that can fund a stand at the largest trade show in the industry is a company the store already covers at 2001. Cost settles it. At 0.0332 EE per record fetched, the remaining 889 records are worth about 30 EE for about 15 MB, and the whole 1,103-record database about 37 EE, one request per domain, well under the 1 EE per 100 fetches that closed the download catalogues, so the other 889 were not fetched. **The transferable screen is that price of admission predicts the head-selection penalty**: any roster whose entry condition is a payment large enough to notice selects the same population a magazine cites, and the aim-at-2001 rule then pays nothing, exactly as the RSS registries measured on the same day. Do not re-test COMDEX, Internet World, CeBIT, E3, SIGGRAPH or Networld Interop exhibitor directories. |
| **Frozen ccTLD register listings, closed on artifact existence and not on value (2026-08-27)** | **CLOSED at 0 EE, against a 223,193.8 EE ceiling, so the family died on artifact existence and not on weight or value.** All seven named ccTLDs were already answered in this file before the run: `uk` line 593, `au` lines 604 and 779, `nz`, `sg` and `ca` line 604, `ie` line 525 and `us` line 1013 both banked masters. `.ca` was re-tested independently as the largest unbanked ceiling, and closed a second time on the mechanism rather than on a yield: CDNnet exposed the register three ways and none is a listing. (1) `/regs/` and `/search/index.html` are forms POSTing to `/cgi-bin/ca-w-search`, so the register is not enumerable even in principle from a capture. (2) Per-domain static files at `http://www.cdnnet.ca/regs-text/ca.<prov>.<name>`, one URL per domain, and the availability API returns no capture for the directory, its index, or any of three sampled members. (3) `/info/statistics` (19980506062029, 2,326 B) is counts only, zero hostname-shaped tokens, and it hands over a hard volume ceiling: `30,123` registered organizations in the entire `.ca` register at 5 May 1998, against 103,715 distinct in-window `.ca` domains already held, 3.44x, so even a perfect 1998 `.ca` register file is a subset of the store. `.za` third-party academic page closed the same death as the AUNIC page: `www2.frd.ac.za/uninet/zadomains.html` (1999-05-03, 6,066 B), the UNINET manager's own `.ZA` page, is policy prose, 24 distinct `.za` strings of which 17 are second-level labels (`ac.za`, `co.za`, `tm.za`) and the rest worked examples. Ceilings priced against merged260827 sampling DISTINCT DOMAINS via `count(distinct domain)`: held-2000-and-missing-2001 sums to **223,193.8 EE over twelve ccTLDs** (`uk` 117,686.3, `au` 34,991.8, `ca` 29,294.2, `nz` 17,054.0, `za` 12,598.2); bidirectional (held Y-1 or Y+1, missing Y), the correct screen for a register since it attests membership at Y, `uk` 1999 alone is 200,574.8 EE against the 1,623.0 EE that `.jp` dated 1999 actually paid. Of the era's registries only four published bulk at all, IEDR, JPNIC, MYNIC and co.za, and all four are already found. **Method, the one-fetch discriminator that kills a whole namespace: fetch the registry's `reg-services` or how-to-register page, not its front page, and read how it says to retrieve an existing registration. A form POST or a per-domain URL template means the register was never a listing and no capture can make it one; a path with a letter or a scope in it is the `.ie` shape and worth the sweep.** CDNnet handed over both facts in 2,954 bytes, the cheap version of the 170-URL and 1,605-capture enumerations that closed `.nz` and `.au`. Second method piece: `archive.org/wayback/available?url=...&timestamp=YYYYMMDD` separates wrong-path from never-captured WITHOUT touching the metered CDX, because an absent path returns an empty `archived_snapshots` regardless of the timestamp asked for; carry a positive control in the same batch, here `www.isi.edu/in-notes/us-domain-delegated.txt` returning 20010606153725, so the sixteen `none` rows are the host and not the tool (1.2 s spacing, four retries, zero errors in 34 calls). Third: replay returned 0 bytes on the first attempt for two of five paths and the full body on retry, so a zero-byte read is not a missing capture. Do not re-open this on a new ccTLD unless that registry is shown to have published bulk, and test that with the discriminator first; the residual English ccTLDs are under the volume floor at `ph` 4,096 and `in` 2,237 held domains for the whole window, 976.7 and 549.3 EE ceilings even from a perfect 2001 register. Two live threads belong elsewhere: the `.ca` `can.domain` classification ruling at line 745, about 10,482 pairs and 8,768 EE for zero further collection, needing only a human reading of whether a registry-printed `Date-Approved:` field is the registry self-dating; and `.uk`, the largest number here, reachable only through Nominet and closed on their RDAP terms and on the absence of any bulk publication. |
| **Academic and security DNS snapshots that are not ISC, killed by the record layout (2026-08-27)** | **CLOSED at 0 EE, killed by derivation and not by sampling: every non-ISC 1996-2001 DNS and topology instrument records an ADDRESS and not a NAME, so there is no item to date.** (1) Internet Mapping Project (`https://www.cheswick.com/ches/map/` 7,033 B and its format spec `datadoc.html` 5,923 B, robots.txt 99 B allows `/ches/`): the published record format is one line per CIDR block with `Probe=`, `Target=`, `Path=` hop lists, `R`/`S`/`T`/`I` timing and TTL vectors, `Whiner`, `Asnpath`, `Complete`, `Pathdate`, and the only name-bearing label is documented verbatim as "name of the network. Not implemented yet."; the scope paragraph reads "We have no interest in the specific endpoints or network services on those endpoints, just the topology of the 'center' of the Internet." So zero extractable domains from ~90,000 networks probed daily 1998-2011, 0 pairs at any volume, and `dbs/index.html` is 404 with the database "available via bittorent somewhere". (2) Mercator (Govindan and Tangmunarunkit, INFOCOM 2000): hop-limited plus informed random address probing and router-alias resolution, no name field, no release located, and a full release would be router interfaces, the reverse-DNS ISP-infrastructure population already measured at 98.4% and 99.6% held. (3) WAND/WITS (Waikato and Auckland traces from early 1999, the one in-window member): the sanitisation paragraph was obtained before any fetch and is fatal twice, IP addresses anonymised with Crypto-Pan prefix-preserving AES into 10.0.0.0 **and payload removed**, so no DNS question section survives; law 6 kills the addresses and payload stripping kills the names, and the host resolves to 130.217.250.15 but refuses connection on 80 and 443, so nothing was requested. (4) Early passive DNS does not reach the window at all: Weimer's passive DNS replication gathers from 2004 and the earliest commercial product, Sandstorm NetIntercept, is 2002 and proprietary, so no pre-2002 passive DNS corpus exists to test. (5) Archive route, proved against a working control: `advancedsearch.php` on `(title:(dns) OR title:(domain survey) OR title:(hostname) OR title:(host list)) AND date:[1996-01-01 TO 2002-12-31]` returns numFound 92 and 92 docs, classified in full as 41 RFCs, 22 DTIC reports, 7 NASA reports, 3 tucows shareware lookup tools and 19 other (10 O'Reilly/Sybex DNS books, a 1996 Australian statistics capture, a Hungarian audio item, a journal article, a workshop, two hacking texts). **Zero data artifacts**, with the non-zero count as the control that the engine answered. Also reached: MIT/KAIST DNS traces, project page only at `web.archive.org/web/20030429101600id_/http://nms.lcs.mit.edu/dns/` 7,065 B with no data release named, and `nms.lcs.mit.edu` and `nms.csail.mit.edu` no longer resolve. **Method, the pre-fetch discriminator for the whole network-measurement family, one question: does the instrument record a NAME or an ADDRESS? Read the dataset's own format spec or sanitisation paragraph and look for a hostname field; if the record is IP paths, packet headers or anonymised prefixes, the yield is exactly zero regardless of corpus size, era or licence, and no download, no permission request and no sampling is warranted.** It screens out in one pass every 1996-2001 topology and traffic instrument (traceroute maps, skitter, Mercator, Internet Mapping Project, WITS/NLANR PMA, MAWI, DEC-PKT) and it explains why ISC was the only member that ever paid: ISC did zone transfers, and a zone transfer returns names. It is cheaper than law 6, which needs the sanitisation paragraph, because this needs only the record layout, usually one static HTML page on the same host. Second reusable piece: an archive-side zero is a proved zero only if you classify all N returned identifiers rather than counting them, since 92 hits that are 41 RFCs and 10 books read as a live population in a summary and as an empty one in a listing. Do not re-test Internet Mapping Project, Lumeta, Mercator, WAND/WITS, NLANR PMA, MAWI, MIT/KAIST DNS traces or pre-2002 passive DNS on any screen. The one member that could still pay is a pre-February-1999 mirror of RIPE Hostcount host-output files, since RIPE's own README dates the access restriction to 01/02/1999 and a mirror copied before then would hold names; deprioritised rather than untried, because its target years are 1996-1998 and that headroom is 103,953 pairs against 6,708,320 at 2000-to-2001, a 64x penalty. |
| **Seal and certification rosters, the one shape of "customer showcase" that pays (2026-08-27)** | **FIND, 1,581.7 net-new post-split EE over 2,554 pairs, and a discriminator that splits the family in two.** BBBOnLine's Reliability participant directory is an alphabetically enumerable 36-page namespace, `http://www.bbbonline.org/search/Relresult.asp?letter=<@,0-9,A-Z>`, linked from `search/Relbrowse.asp`; 33 letters have a 2001 capture, 3,574,800 bytes, 7,605 participant rows, 9,019 distinct registrable domains under a weighted TLD. Sampled DISTINCT DOMAINS: 8,286 held any year (91.9%), 5,911 already carrying 2001, **2,375 held-and-missing-2001, of which 2,222 (93.6%) are adjacent held-2000-and-missing-2001** rather than death-gap. `scripts/pricing/price_items.py --all-tlds` against merged260827: 3,109 net-new pairs and 1,919.7 EE pre-split (do not quote), **2,376 pairs and 1,470.1 EE post-split**, mean weight 0.6187, `com` 2,163 / `net` 175 / `org` 23, typo upper bound 46.3%; an independent duckdb year screen gives 2,375 pairs and 1,469.5 EE, agreeing to 0.04%. TRUSTe's licensee roster (`truste.org/users/users_lookup.html` at `20010603230742`) adds 1,522 domains, 99.0% held, 1,323 already at 2001, **184 held-and-missing-2001 and 115.1 EE**, which is 0.076 EE per listed name against BBBOnLine's 0.163: the head-corpus law exactly, since TRUSTe licensed `abc.com`, `about.com` and `expedia.com` while BBBOnLine listed air-conditioning contractors and local ISPs. **The reusable discriminator: a provider-run member index links the member THROUGH the provider and so cannot name the member's own domain (measured at 0.0 EE the same day, `free_host_member_indexes`), while a seal or certification roster MUST print the member's own domain, because the domain is what is being certified.** So the pre-download question is whether the listing's subject is the customer's SITE or the customer's ACCOUNT, and the whole 7,600-row database comes out in 36 requests with no CDX query anywhere. Two traps paid for here: `archive.org/wayback/available` returns the CLOSEST capture, which for a `20011215` target lands in 2002-01 for a third of the letters and would silently import a 2002 roster as a 2001 observation, so target `20010901` and reject any timestamp not starting `2001`; and this template writes `HREF=http://...` UNQUOTED, so a quoted-href regex reports 0 absolute links on a 347,822-byte page holding 1,013 of them, which is how a real source gets buried. Approval requests filed as `bbbonline_reliability_roster / artifact_listing` at potential 87 and `truste_licensee_roster / artifact_listing` at potential 55, both Decision pending, neither ingested; both readings are on the sheet, since the master reading (a roster generated from a participant database is not something a human typed, the `namewinner_expiring` argument) is 1,919.7 EE against 1,470.1 EE split. Three cheap increments left: the Privacy programme's separate roster at `search/Pribrowse.asp`, the three letters (`L`, `V`, `6`) with no 2001 capture, and the same namespace at 1999 and 2000 captures, where the adjacent-year law says price one letter page before fetching 36. The family is otherwise thin: 12 payment-gateway, storefront and certificate-issuer hosts were screened at 2001 and none carries a merchant listing (`cybercash.com` an 11,247 B shell, `ccbill.com` 1,910 B, `shopnow.com` 983 B, `ibill.com` only a `/Members/` login, `authorizenet.com` no directory link, `miva.com` and `mercantec.com` link a storefront), and `dotcomdirectory.com` delegates every category listing to `dotcomdirectory.net`, which has NO capture at all. |
| **IRC logs, web forums and signature URLs, closed on density at a scale that cannot be exceeded (2026-08-27)** | **511.1 net-new post-split EE over 800 pairs, and the family is closed because conversation is the wrong density and scale does not fix it.** Four artifacts, all fetched and all priced against merged260827: the Archive Team grab of Salon Table Talk (`archiveteam-tabletalk-panic/tabletalk.final.tar.bz2`, 923,956,129 B, 156,894 HTML pages), an Offline Explorer mirror of `forums.somethingawful.com` taken 2001-04-30 to 2001-05-01 (`something-awful-forums-2001/SA2001.zip`, 103,117,451 B), 303,879 FidoNet echomail messages (`fidonet_combined_execpc_starfleet_messages_1995_1999`, 530,898,445 B raw), and `nirvs-clone-cd-irc-logs` (#clonecd on EFnet, 2000-12-29 to 2003-07-03). Family union 20,761 distinct domains, 26,360 of 28,348 pairs held (93.0%), mean weight 0.6388, 530 of the 800 net-new pairs at 2001; gross is 1,988 pairs and 1,300.6 EE, do not quote. **Table Talk is the decisive test because nothing else in this family reaches its scale: 7,195,164 posts carry a WebX machine stamp, 2,330,700 of them in window (32.4%) and 885,916 at 2001, and it paid 224.5 EE over 355 pairs, so 0.0000963 EE per in-window post and 1,000 EE needs 10.4 million dated posts.** Of the 10,501 domains it names that the store holds at some in-window year, only 713 (6.8%) lack 2001, against the 61.1% `.com` population average, and those 713 spread over 697 distinct posts with at most four from any single post: evenly distributed, millions of items, and it still prices here. Something Awful looks 20x better and the reason is not the forum: 167 held-and-missing-2001 pairs over its page text, but **121 of them (72.5%) come from ONE page**, `showthread.php@threadid=26538`, a 616,004 B pasted adult-paysite login list, and the other 7,713 pages of genuine 2001 conversation give 46 pairs and 28.1 EE: **0.0036 EE per page against 7.6 EE per page for the ten dump pages, a 2,100x gap.** FidoNet is the same lens in the wrong years (85,980 / 113,037 / 79,469 messages at 1996 / 1997 / 1998, and 2 after), paying 127.3 EE on message bodies and 7.6 EE over 277,168 tearlines, because a 1996-1998 `* Origin:` line carries a node number and a phone number and almost never a hostname; its 0.0418 pairs per item lands exactly on the 0.042 prose ceiling while net-new post-split is 61x below it, so that ceiling describes extraction and not yield. CloneCD refutes the hypothesis's own premise, since the machine channel outpaid the typed one: hostmask `***` lines (reverse DNS) 25 pairs and 15.4 EE against typed `<nick>` lines 20 pairs and 12.1 EE. Per listed domain the family lands on the head-selection constant and then falls below it: SA conversation 0.0480, CloneCD 0.0391, **Table Talk 0.0203**, FidoNet 0.0155, against Bomis rings 0.0481, WinNetMagCD 0.041 and 0.386 for a random held `.com` missing 2001. So 1,000 EE needs about 21,000 distinct domains in one 2001-dated artifact and the two 2001 artifacts here hold 2,341 and 11,081. **Negatives, each against a passing control.** The Archive Team Yahoo! Groups API grab is the one lead in the family with the volume to matter (hundreds of items at 30 to 45 GB, per-message dates back to eGroups in 1998) and it is DARK rather than missing: three megawarcs return 401, 401 and 403 on HEAD, the item CDX derivative returns 401, and a Range request 302s into the same wall, while the control `googlegroups-part2-S` in a sibling Archive Team collection lists a downloadable 45,043,937,280 B tar. `slashdot.org` robots.txt disallows `/comments.pl`, `/stories/`, `/journal` and `/search`; `kuro5hin.org` returns an empty body; `advogato.org` 302s its own robots.txt to `web.archive.org`. **No other in-window IRC log corpus exists** (`title:(irc log)` 37 items, `identifier:irclog*` 23, every one 2003 or later bar CloneCD and a 1995 item; `logbot.info` begins 2004 by its own description) and **no other in-window forum mirror exists** (`("Offline Explorer" OR "HTTrack" OR "wget mirror") AND date:[1996 TO 2002]` is 26 items of which exactly one is a forum, the rest magazine cover CDs; `mediatype:web AND date:[1996 TO 2002]` is 4,476 items that die on law 1, and excluding `title:(Crawldata)` leaves 256 Archive-It crawls run in 2021 and 2022). FidoNet NODELISTS were closed on date at line 784 of this file; this measures the MESSAGE BASE and confirms the same sub-500 EE family ceiling from the other side. Method, five reusable pieces: (1) **split one corpus by WHO WROTE THE HOSTNAME before pricing, not after**, which is what refuted the premise here; (2) **attribute yield to items before quoting a per-item rate**, one `Counter(first_item[d] for d in net_new)` turning a 104 EE forum into 28.1 EE of forum plus 76 EE of one pasted list, and running the same screen on Table Talk proved the opposite there, which is how two anecdotes became one law; (3) an Offline Explorer or HTTrack zip preserves the downloader's per-file mtimes, so a saved response body stamped 2001 is a CAPTURE rather than a `link_target` and needs no corroboration from the page, and `unzip -l` alone prices a 103 MB artifact without extracting it; (4) **a prefix of a hash-ordered tar is not a sample**, since the first 80,000 of Table Talk's 156,894 pages read 0.6% in-window against 32.4% for the whole corpus because the old forums cluster under their own hash prefixes, so stream the whole `r|bz2` member by member and drop every post with no hostname, turning 7.2 GB into a 166,047-item file in one pass; (5) HEAD the largest file of an `archiveteam` item before budgeting its download, with a sibling collection as the 200 control, because privacy-sensitive grabs go dark while their metadata stays public and searchable. Closed: do not re-test IRC logs, web forums, BBS echomail, signature URLs or guestbooks on any screen. The 511.1 EE is real and sits in four named, licence-clear, correctly dated artifacts if the banking process wants it, Table Talk and Something Awful carrying 355 and 210 of the 800 pairs. |
| Award galleries and pick-of-the-day archives, re-aimed at 2001 and closed a third time (2026-08-27) | **Closed at 3.5 net-new post-split EE over 5 pairs, reproducing the 2026-08-08 reading to within 0.34 EE on a disjoint set of pages.** The class was already closed twice in this file ("Award galleries and cool-site lists", 206 domains over 7 pages, 3.16 EE; and "Dated directories and navigation sites", 2026-08-24), and it was re-tested anyway because both closures predate the 2001-threshold law and every artifact behind them was dated in a year already well covered. The loophole does not open. 8 dated editions over 6 programs, 197,041 B, fetched raw as `web.archive.org/web/<ts>id_/`: `coolsiteoftheday.com` 2001-05-22, `goldenwebawards.com` 2001-06-01, `usatoday.com/life/cyber/ch.htm` 2001-05-30, `projectcool.com/sightings/` 2001-05-19, `links2go.com` 2001-05-24 and `yahoo.com/new/` plus `/picks/` at 1997 and 1998. Dating is fine and is a `dated_directory` of the `ncsa_whats_new` shape, the edition date being printed in body (`05/30/2001 - Updated 02:56 PM ET`) or carried in the path (`/life/cyber/2001-05-30-free-e-mail.htm`). The population is not: priced by `scripts/pricing/price_items.py --all-tlds`, 135 distinct pairs over 128 distinct domains, 111 already held (86.7%), net-new pre-split 24 pairs and 13.2 EE (do not quote, overstates 3.8x), **post-split 5 pairs and 3.5 EE**, all 5 at 2001, mean weight 0.7011, typo upper bound 37.5%. **That is 0.44 EE per dated edition, and the mechanism is that the family's two halves fail on OPPOSITE screens.** Sampled as distinct domains: the head half (portal and newspaper editorial picks, 43 distinct) is 42 held at 97.7% and **42 of 42 held names already carry 2001, so held-AND-missing-2001 is 0**; the tail half (award-program winners off the 2001-06-01 Golden Web Awards page, 28 distinct) is 71.4% held with 28.6% novel names that take the corroboration split and earn no year, leaving 4 held-and-missing-2001. What the head lists is already dated at 2001, what the tail lists is novel and undatable. **Volume ceiling, measured rather than assumed, from `__wb/sparkline?output=json` in ONE request per host and no CDX at all**: in-window captures are 345 for `usatoday.com/life/cyber/ch.htm` (303 of them 2001), 58 for `projectcool.com/sightings/` and 48 for `goldenwebawards.com`, so a page rewritten daily for six years survives as 58 distinct editions, about one per 38 days. Granting the whole family 8 programs at 150 in-window editions each is 1,200 pages and about 530 EE against a 5,000 EE bar, before marginal novelty collapses on the affiliate plumbing every edition repeats (`bfast.com`, `linksynergy.com`, `commission-junction.com`, `flycast.com`; `links2go.com` yielded exactly 1 registrable domain and it was an ad network). Reaching 5,000 EE would need about 11,400 dated editions. **The reusable discriminator is entries per edition and who chose them, not how many years the program ran**: `ncsa_whats_new` is banked at 4,916 names for 1996 because it is an ANNOUNCEMENT REGISTER enumerating a population at about 400 entries per issue, while an editorial PICK archive is 1 to 33 registrable domains per edition with a third of those its own ad plumbing, and on the best member the picked URL is not on the pick page at all but one level deeper in a dated review article, so the real cost is about 1 request per 1 to 3 domains, the same structure that made `point.lycos.com` measure 1 outbound domain per 484-href page. **Two false zeros caught, both by positive control**: `archive.org/wayback/available` began answering `{"archived_snapshots": {}}` for a URL whose 2001 capture had already been fetched and parsed, which also invalidates the two zeros recorded for `/life/cyber/arch.htm` in that same window; and `yahoo.com/new/` first read as 0 external domains under an `href="http..."` regex because Yahoo wrapped every outbound link as `href="/new/*http://..."`, where a permissive scan for `https?://` anywhere in the document gave 19 domains. Closed: do not re-test picks, galleries or award programs. If anything in this direction is worth an hour it is the REGISTER variant rather than the pick variant, a 1999-2001 successor to NCSA What's New listing all newly launched sites rather than the chosen few, which is a different population. |
| Download-site catalogues with per-entry dates, closed on fetch cost rather than yield (2026-08-27) | **Closed at 1.264 net-new post-split EE over 13 leaf pages, 0.0972 EE per fetch and 1.00 vendor domain per fetch.** Half the family was already closed at line 837 of this file (Winsite `INDEX.TXT`, 7,057 entries and zero vendor domains; CNET Download.com, zero vendor URLs; SimTel, no author domains). Of the rest, WinFiles is dead as an independent site by 2001, its 2001-04-05 capture being a 1,810-byte CNET shell that links only download.com, so it collapses into the closed Download.com entry; and Jumbo, FileMine, SoftSeek, DaveCentral, freewarehome and completelyfreesoftware were all captured in 2001 and all fail the density screen. **No site in this family prints the vendor homepage on a multi-entry page**: a SoftSeek category page is 24,499 B with 8 distinct external hosts, all zdnet and gamespot furniture; freewarehome's front page is a 716-byte frameset; completelyfreesoftware's `index_all.html` is 47,112 B with 14 external hosts, 12 of them ads and webrings; a 32bit.com category page is 45,143 B with 7 external hosts, all its own. Only the per-program LEAF prints the vendor domain, and it prints exactly one. 32bit.com is the one surviving member, dated the Bomis way with `Date Added: MM/DD/YY` beside `Home Page: <vendor url>` and the Wayback capture pinning the page, a `dated_directory`; its tree is 9 category pages, 190 subcategories, 19 leaf IDs per subcategory, IDs running 491 to 27,449. 13 leaf pages priced against merged260827, sampling distinct domains: 13 of 13 carry both fields, 13 of 13 domains already held (100%), 12 of 13 already held AT 2001 (92.3%), held-and-missing-the-catalogue's-own-year 2 (`mvsoft-comp.com` 2000, `fileportal.com` 2001), **net-new post-split 1.264 EE**. The `Date Added` years scatter 1999 x5, 2000 x4, 2001 x4, so only a third of the corpus can even offer the one year with headroom. n=13 is small and quoted as such, but it lands with two independent head-selected measurements (Bomis rings 0.0481 EE per name, WinNetMagCD 0.041) against 0.386 for a random held `.com`: a software catalogue lists the vendor still worth listing, and the store covers those at 2001. At 0.0972 EE and one domain per fetch, 1,000 EE needs about 10,300 leaf fetches, 20x the fetch cost of a list artifact of the same value. Method: **read the rendered text of the LEAF, but screen on the LIST**, one request per site counting distinct external hosts on a category page, which disqualified six sites in seven requests. Two operational findings. **web.archive.org replay refuses concurrency**: six parallel urllib workers returned 9 non-429 errors and zero content, six parallel curl workers got `Failed to connect ... port 80` on 25 of 37, while a single serial curl to the same path returned 200 and 33,697 bytes in the same minute. It is a connection refusal, not a rate limit that answers `Retry-After`, so budget replay as ONE serial client at about 4s per page (~900/hour), not as a pool; serial urllib is a further 6x slower than serial curl on these redirecting `id_` paths, so use `curl -L`. And `/web/<ts>id_/` on this host 302s to the nearest capture, so a 404 means the path was never archived while a 302 means it was, separating "not captured" from "not on the site" without touching CDX. Closed: do not re-test Download.com, SimTel, Winsite, WinFiles, Jumbo, FileMine, SoftSeek, DaveCentral, freewarehome or completelyfreesoftware. The only unfinished thread is 32bit.com's `/software/listings/Authors/`, which would be the dense artifact if it exists but 404s at both 2001-06-01 and 2001-07-20 and appears only inside an HTML comment on the pages naming it, so it was probably never live; one CDX filename query settles it. If anyone does spend the fetches on the leaf tree, rank the queue by `Date Added` year, since the 1999 and 2000 two-thirds is worth roughly nothing. |
| **Bruce Guenter's spam archive re-priced: the advertised URL is refuted and the RECIPIENT header is the find (2026-08-27)** | **1,288.1 net-new post-split EE over 3,053 pairs, 6.6x the 2026-08-15 reading of the same bytes, and the hypothesis that produced it is refuted with the sign reversed.** The lens was the URL a spam ADVERTISES rather than the sender it forges, on the argument that a spammer paying for traffic must name a real live site. Five populations were cut out of the same 20,010 messages (`untroubled.org/spam/1998.7z` through `2001.7z`, 9,312,329 B, plus two 1997-1998 header dumps) and priced separately by `scripts/pricing/price_items.py --all-tlds`, sampling DISTINCT DOMAINS: recipient (To/Cc/Bcc/Delivered-To) 10,313 domains, **91.1% held**, mean weight 0.549, 566.2 EE; sender (asserted plus all relays) 15,379, 82.7%, 680.9 EE; observed (last-hop `Received:` only) 8,999, 82.6%, 485.5 EE; asserted (From/Return-Path/Message-ID) 7,486, 83.3%, 219.7 EE; **body (advertised URLs) 4,820 domains, 77.3% held, the WORST of the five, 139.6 EE**; union 26,112 domains, 29,356 pairs, 83.9% held, **1,288.1 EE**. The advertised domain is disposable, registered days before the campaign, so it is exactly the name the store does not hold, and under the split not holding it earns nothing: 678 of its 4,820 names are new even to the candidate pool. **Two laws come out of this.** (1) **Forgery does not predict the held fraction at all.** What the receiver observed on the wire (peer hostname, HELO, IP, not under the spammer's control) reads 82.6% and what the sender asserted reads 83.3%, indistinguishable. So the register's 4.56% figure from the Lazarus remailer logs is not a law about forged headers, it is a property of remailer nym addresses; direct-to-MX bulk mail forges plausible real ISP domains because it wants to look deliverable, and its forged senders are 18x more likely to be held. **Ask which FIELD, not whether it is forged**: established business or ISP (recipient, pays), Asian open relay (observed, weight 0.313), disposable landing site (body, 77.3%). (2) **The harvested recipient list is the highest-value field in a message**, 91.1% held at weight 0.549, the only population high on both screens, because a harvested address sits at an established English-language business that survived, and 2001 spam BCCs an alphabetical walk of somebody's zone rather than human-typed names. Dating: the qmail maildir filename is a unix epoch written by the RECEIVING MTA and the last-hop `Received:` line restates it; 20,007 of 20,010 filename epochs agree with the archive's own directory year, and the spammer's forgeable `Date:` header is ignored. 2001 carries 2,794 of the 3,053 pairs (1998 115, 2000 91, 1999 50, 1997 3) because 14,732 of the 20,010 messages are 2001; by TLD com 1,353, jp 245, net 194, kr 193, tw 131, de 102. Adjacent-year check: 1,106.8 EE, 85.9%, sits on a domain held at Y-1 or Y+1, so it is not the contaminated held-any-year shape. Gross before the split is 7,325 pairs and 3,470.5 EE: do not quote. Ten random scoring pairs were traced to the byte that dates them and all ten confirmed. **Why the 2026-08-15 row read 195.5 EE over 4,793 domains and these bytes hold 26,112: nothing was MIME-decoded.** 2001 spam hides its URL in base64 or quoted-printable HTML, and an `email` walk with `get_payload(decode=True)` over every text/* part yields 78,753,891 bytes of body and a URL in 11,734 of 20,010 messages where a raw grep finds almost none. **A filename epoch is a delivery stamp only in a maildir**: all 401 files in `1997-1998-headers.tar.bz2` carry epochs inside 89 seconds of 1998-03-26, which is when the mailbox was dumped, and only the mbox `From ` separator in the sibling reaches 1997. Licence: "Permission is hereby granted to use this archive without restriction", robots.txt 50 B denying only `/stats/` and `/lists/`. **The family is exhausted**: the `untroubled_spam_archive` item on archive.org is byte-identical file for file, `spamarchive.org` is now a Syracuse window-replacement business and so is a proved zero rather than a refusal, and Ling-Spam is 481 messages and body-only. Sizing for a future trap: 0.0644 EE and 1.30 distinct domains per message, so 5,000 EE needs about 78,000 in-window trapped messages. Raised for approval as `untroubled_spam_headers / artifact_listing`. **Re-run the MIME and recipient laws over every mail-shaped corpus already ingested** (`data/raw/enron`, `data/raw/maillists`, `data/raw/usenet`): anything ingested from HTML hypermail never saw a header at all. |
| **Bomis rings, and the redirector law that does not generalise (2026-08-27)** | **19.1 net-new post-split EE over 16 archived ring pages, and the finding is that a redirector kill is a claim about one hub rather than about a shape.** `docs/sources.md` rejected webring member lists on 2026-08-05 because every WebRing member is linked through `go.webring.org`. Bomis routes its members through its own `/rings/<ring>/<n>` pages too, but it ALSO prints the bare hostname as text beside each title (`3. The Rotherwick Firewall Resource   www.zeuros.co.uk`), so the domain is extractable with no second request and the redirector is irrelevant. 20 rings drawn with a fixed seed from the 83 named on the 14 top-level `/tree/` pages, fetched as `http://web.archive.org/web/20010701000000id_/http://www.bomis.com/ring_home.fcgi?ring=<ring>`, 6-8 KB each: 16 returned content, all captured 2001-04 to 2001-07, so **in-window archival coverage is 16/20 = 80%**. 500 host strings gave **396 distinct registrable domains, 392 held at some year (98.99%), 361 already held AT 2001, held-and-missing-2001 = 31, only 4 names never held, 19.1 EE post-split** (re-priced against the live store on harvest: 405 domains, 35 raw pairs, 31 after the split, mean weight 0.615, all at 2001, com 28 and net 3). Rates: **0.0481 EE per listed domain, 0.95 EE per ring attempted, 1.19 per productive ring, 24.8 domains per ring**, and five of the first six rings paid exactly zero while one shopping ring carried 15.27 of the 19.06. **It is thin for the head-selection reason and the figure reproduces one the register already has**: only 7.9% of held names lack 2001 here (31/392) against the population average of 61.1% for `.com`, which is the `WinNetMagCD` result from another direction (0.0481 EE per name against that magazine's 0.041, against 0.386 for a random held `.com`). A curated ring lists the site still worth listing, and the store covers those at 2001. Negatives measured on the way, each against a passing control: **RingSurf dies on retrieval, not on shape**, its 2001 front page claiming 4,300 rings and 22,000 sites and its directory naming rings as `/netring?ring=X;action=info`, yet **0 of 16 sampled ring pages are archived at all**, with the control being the WebRing URL the register already records (`www.webring.com/cgi-bin/webring?ring=railring&list`), returned at its exact recorded capture `20000422003921` by the same API; **LinkExchange** has no 2001 surface, closest capture 1999-08-30. Method, three reusable pieces: (1) before writing off any member list, **read the rendered TEXT and not only the `href`s**, because hubs printed the hostname for the reader even where the anchor pointed at their own counter, and that one check reopened a family closed for three weeks; (2) **`archive.org/wayback/available` substitutes for CDX** when CDX is metered by other collectors, since it accepts a full query string and honours a `timestamp=` pin, but validate it against a capture the register already records before trusting a zero; (3) **pin the replay timestamp explicitly**, because `/web/2001id_/` resolved to 2002-01-22 for a page that has a 2001-06-01 capture and would have silently bought an out-of-window corpus, while `/web/20010701000000id_/` returned 20 of 20 captures inside 2001-04 to 2001-07. Volume is the one thing unmeasured: at 0.95 EE per ring, 1,000 EE needs ~1,050 rings and 5,000 EE needs ~5,250, and a BFS of `/tree/` had 164 unvisited category pages queued after 4 fetches, with several rings auto-imported ODP mirrors, so the inventory is plausibly in the thousands. `www.bomis.com/rings/<r>/` carries the member count as `max=N` in its frame src, so ring SIZE is readable one request before the member list and the queue can be ranked biggest-first, which matters because the yield is concentrated in the few large rings. The blocker is IA replay throughput, not permission. Raised for approval as `bomis_ring_member_lists / dated_directory`. Do not spend more time on RingSurf, LinkExchange or WebRing itself. |
| **Free-hosting and ISP member indexes, re-tested at 2001 on the right screen (2026-08-27)** | **Still 0.0 EE, and now for the right reason, plus a mechanism that generalises.** The 2026-08-08 closure used the RETIRED novelty screen ("617 domains at 97.4% already held"), written 17 days before the 2001-threshold law, which is the same error corrected for Stanford WebBase, so it was worth one re-test. Four fresh artifacts plus three host homepages as controls, all reachable: Tripod's member index at `20010515062824` (173 B), GeoCities' neighbourhoods at `20000901091820` (567 B), Bigstep's example sites at `20010603211725` (17,627 B), NetNation's `/customer/` at `20010601115834` (15,861 B), and `pair.com`, `he.net`, `netnation.com` at 2001. `scripts/pricing/price_items.py` over all seven against the live store: **7 items, all in window, 15 distinct registrable domains, 16 pairs, 16 of 16 already held, 0 net-new before AND after the split, 0.0 EE**. Year screen on distinct domains: 15/15 held, **15 already carrying 2001, held-and-missing-2001 = 0**. Every one of the 15 is a provider or ad-network name (`tripod.com`, `geocities.com`, `bigstep.com`, `netnation.com`, `pair.com`, `he.net`, `bfast.com`, `yimg.com`), 12 of them held across all six years. **The mechanism is the transferable part: a provider-run member index links members through the PROVIDER, so it cannot name a member's own domain even when the member has one.** Tripod's index is a 301 stub naming `tripod.com`; GeoCities' is a meta-refresh into `cgi-bin/hood/geo`, so there is no bulk index artifact at all. **The vanity-domain carve-out was tested on its best instance and refuted**: Bigstep hosted small BUSINESSES with their own domains and showcases 13 members in 17,627 bytes while naming **zero** of their domains, because each member is a thumbnail (`ss_bookbouquet.gif`) linked through bigstep.com; exactly one member name was recoverable anywhere on the page, from a caption, and the store holds it at 2000 and 2001. NetNation's `/customer/` is a login portal with four hostnames, all NetNation's own, and `pair.com` and `he.net` carry no customer-list navigation at 2001. This is the same death WebRing took, reached on a host whose members were businesses rather than hobbyists, which is the strongest form of the carve-out. Volume kills what the mechanism leaves: at 1 recoverable domain per 13 showcased members, the ~2,590 held `com` names 1,000 EE needs at 2001 would take about 33,600 members over roughly 2,600 pages. Ten guessed customer-list paths returned NONE from the availability API, but a positive control proved that was wrong paths and not missing captures: the same API returns 2001 captures for all three bare hosts. Method worth keeping: **`archive.org/wayback/available?url=...&timestamp=YYYYMMDD` finds captures without touching a metered CDX** (`web.archive.org` returns 404 for robots.txt so carries no file; `archive.org`'s is 12 lines, `/control/` and `/report/` only, naming no Claude agent), and **fetch the `id_` replay form** or Wayback's link rewriting turns every outbound href into `web.archive.org` and the extraction measures the archive instead of the artifact. A retry wrapper is required: `web.archive.org` refused the connection on 6 of 14 attempts and succeeded on retry every time. The one population left in this family is NameZero, which registered real `.com` domains for over 400,000 free members by May 2000 and was then the largest single registrant in the world; its 2001 site publishes no member directory, so it is unreachable through this lens and would have to come from registry or WHOIS data, where the Verisign pool is already spent. Do not re-test free-hosting or ISP member indexes on any screen. |
| **Site Meter and the counter-service lens, re-proposed and closed without a fetch (2026-08-27)** | **0 EE, duplicate lens.** The hypothesis named theCounter, NedStat, eXTReMe Tracking and Site Meter; the counter-directory row above sizes the first three by name in the same sweep (`nedstat.com` 3,392 in-window captures, `extreme-dm.com` 7, `thecounter.com` 0) and closes the family on measurement: `hitbox.com`'s head page at 1998-02-05 gives 48 registrable domains, **48 of 48 already held AT 1998, 0.0 EE**, the deep rank pages carry 0 member rows, and the whole in-window list-page universe is 483 captures with **none at 2001**. Site Meter is the only member never named in `docs/sources.md` (grepped `sitemeter|site meter|statcounter|webtrends|hitbox|websidestory` across `docs/`, one hit). It is not an exception: sitemeter.com launched 1999 and its public surface is per-member stats pages plus a ranked top-sites list, the identical shape, and the closure's mechanism is structural rather than host-specific. No new measurement was taken and none is warranted. **The cheap pre-download test this leaves behind: a service's public list is not its database, it is the marketing head of it.** The only version of this family that could ever pass the authority screen is the inverse shape, a public artifact that is NOT rank-ordered so that publication is not head-selection: a full alphabetical member index, an embedder manifest, or a billing list. Do not re-test traffic or counter services. |
| **The "Wayback refused this IP" hour, measured: it was not us, not a block, and not that hour (2026-08-27)** | **The refusal is real but it is REPLAY ONLY, and the concurrency hypothesis is refuted three ways.** The flag said `web.archive.org` refused this IP at the TCP layer through most of the 15:00 UTC hour while `ark cdx` ran two clients. Four corrections, all measured. **(1) The hour is wrong by two.** Every log this project writes is local CEST, so the window was **13:05 to 13:35 UTC**, and anyone correlating it against the archive's own status history would have read the wrong hour. **(2) There was one `ark cdx` client, not two**: `cdx_pool` stopped at 08:02Z and only `cdx_gtail` was running; the second client was the iteration's OWN replay fetcher, sequential, one socket, started 13:10:28Z. **(3) Throughput is invariant to our concurrency.** Six batches today, worker count 8, 8, 3, 3, 3, 8: rates 8.93, 10.03, 8.90, 9.05, 7.16 and 8.31 domains/min. Cutting workers from 8 to 3 at 10:46Z changed the rate by 0.3% and the failure rate went the WRONG way, 3.67% to 6.17% to 7.83% to 18.45%, then FELL to 8.62% when workers went back to 8. A 2.7x change in our own in-flight requests moves neither number, because `throttles` ran at roughly one per query all day and `final_delay_ms` was pinned at its 3.0s ceiling in every completed batch: the bottleneck is server-side per-query latency shared across workers, so we were never the limiting party. **(4) It was not an IP-level block, because the index kept answering.** In the incident batch the CDX index still completed 233 queries and answered **81.5% of them 200**. A block on this IP would take both services; instead, in the same minutes, `web.archive.org/cdx/search/cdx` ran at ~8.3 domains/min while `web.archive.org/web/<ts>id_/` produced **21 successes in 26.9 minutes, then zero in the 5m10s to 13:42:32Z**. The two are different tiers and only the replay tier fell over. **It also did not end at 13:35Z**: the replay client was still alive and still producing nothing seven minutes later, so "most of that hour" understates the duration and misattributes the boundary to the supervisor restart that happened to fall there. **The reusable finding is the forensic gap: a per-minute series cannot be reconstructed from the cdx journals at all.** Journal records carry `domain`, `status`, `strategy`, `truncated` and `years` and **no timestamp**; the only per-second stream is the tqdm progress bar on stderr, which `supervise_cdx_pool.sh` strips with `grep -v "domain/s]"` before appending to the shared log, and `BATCH_OUT` is truncated on every dispatch. So the finest retrospective resolution this project can ever reach on a past window is one batch, about an hour. The supervisor already computes `journal_bytes()` every `CHECK` and logs it only on a stall; logging it unconditionally would give a 15-minute series for free. **Do not make that edit while a supervisor is running**: bash reads a script lazily by byte offset, so editing a live `supervise_cdx_pool.sh` can corrupt the loop mid-flight. **The operational rule: when the archive slows, do not throttle ourselves further. Check the index separately from replay first, because only one of them is usually down, and our worker count is not what either responds to.** |
| **Traffic-ranked counter directories, and the pagination that is a template (2026-08-27)** | **Closed at a measured 0.0 EE on the head and 0 member rows in the tail, and the mechanism is a new law.** The lens was hit-counter and traffic-measurement services, on the argument that their public member directories are machine-generated from the counter database and assert live traffic at the instant the page is stamped, so they would take no corroboration split and would not be a human-curated list. `hitbox.com` (WebSideStory) is the largest such artifact and it is real: 20,000 in-window captures, and `www.hitbox.com/wc/world.<rank>.<Category>.html` in steps of 100 up to 1000 across about 35 categories, each row carrying the member's own URL and an average-hits-per-day figure. **The apparent volume is 35 categories x 1,000 ranks and the real volume is the head page.** `world.100.PersonalHomepage.html` at 1998-02-05, 54,527 B, holds 100 rows and 48 registrable domains under a weighted TLD, and **48 of 48 are already held AT 1998, so it prices at exactly 0.0 EE net-new**, with 22 further hostname-shaped names dropped by the TLD whitelist. Four `world.700` through `world.1000` pages in two categories are 6,796 to 6,982 B and carry **0 member rows and 0 member URLs**: the ranks past the populated head render the navigation template and nothing else, because those categories never had 1,000 members. So the family's populated surface is about 35 head pages per crawl, roughly 1,680 domain slots, at a measured net-new rate of zero, and the whole in-window capture universe is **483 list-page captures, 368 at 1998, 62 at 1999, 53 at 2000 and none at 2001**, so it cannot pay in the only year with real headroom. **The transferable law: a traffic-ranked directory paginates its TEMPLATE, not its membership, so rank depth is not volume, and the part that is populated is by construction the head, which the store already covers at the artifact's own year.** Sizing a ranked list by its deepest URL overstates it here by more than 20x. This is the visitor-log result of 2026-08-25 reached by a different route: the reciprocal-traffic row measured 98.4% and 99.6% already held on reverse-DNS hostnames, and a counter directory measures 100% on real member URLs, so traffic as a selection principle fails the authority screen whichever end of the pipe it is read from. The siblings were sized in the same sweep and are smaller: `nedstat.com` 3,392 in-window captures, `extreme-dm.com` 7, `thecounter.com` 0. **Do not re-test traffic or counter services.** |
| **`rfc-ignorant.org`, the one domain-based blocklist the IP-based closure did not cover (2026-08-27)** | **Closed at about 6 usable domains, and the closure it was meant to reopen turns out to be right for the wrong reason.** The 2026-08-15 row closed anti-spam blocklists on the grounds that "every in-window blocklist is IP-based, not domain-based", naming MAPS RBL, ORBS, the Dial-Up List and SPEWS. `rfc-ignorant.org` is the counterexample to that sentence: its `whois`, `postmaster`, `dsn` and `abuse` tables list DOMAINS, tested by robot rather than typed, which is the shape that paid on squidGuard. It fails on era and on archiving instead. Of 3,096 captures across the host 1999-2003 only **36 are in 2001** and 2,830 are 2003, there is no bulk list artifact of any kind (`rhsbl.m4` is a 1,055 B sendmail macro, not a list), and the site's own listing pages are per-domain query forms. The one dating route it does offer is unusually clean and still empty: `tools/detail.php?domain=<D>&submitted=<epoch>&table=<t>` carries the listing's submission time as a Unix epoch **in the URL itself**, so the CDX index alone dates every listing with no fetch at all. There are 173 such captures, 122 of them the `ipwhois` table and therefore netblocks rather than names, and **12 rows fall inside 1996-2001, collapsing to about 6 registrable domains** (`aol.com` four times, two Turkish and two Chinese ISPs, and two bare ccTLD strings). The epoch parser was verified against real in-window dates rather than assumed, so this is a proved zero and not a pointed-wrong one. |
| **The independent 2001 blacklists, and an era closure that was scoped to a mirror (2026-08-27)** | **The upstream squidGuard contrib list pays 1,506.4 EE and every other named alternative is dead, three of them on dates outside the window.** The transferable finding is the closure error: `content_filter_blacklists` was rejected on 2026-08-25 partly because "the squidGuard half of this entry was already closed on era (2026-08-24, artifact is 2003)", but that era test was measured on `ftp.teledanmark.no`, a Danish MIRROR whose earliest capture is 2003-12-11. The ORIGIN, `ftp.ost.eltele.no`, has two in-window captures, and the rejected entry's own surviving bullet quotes one of their headers verbatim (`compiled in 33:22:40 on 2001.09.09`). **A mirror is the worst possible place to run an era test, because a mirror starts later than the thing it mirrors; date the origin or record the closure as host-scoped.** Priced separately and measured, so nobody re-tests them: **MESD**, `squidguard.mesd.k12.or.us`, five `blacklists.tgz` captures running 2002-08-03 to 2004-06-07 and **none in window**, the site index first captured 2001-06-03 and no robots.txt at any capture before 2004; its earliest edition, 1,570,913 B and 55 members, has 26 members stamped inside 2001 and they are all exclusion diffs and stub categories, **588 hostnames, 466 registrable domains, 444 held, 10.5 EE**, while everything large is 2002 (`porn/domains` 2,838,103 B at Aug 3 2002, `adult/domains` 2,501,089 B at Jul 29 2002). **urlblacklist.com**: zero 200-captures across 1996-2003. **Linugen**, the first of the two alternatives squidGuard itself named: 15 captures of `www.linugen.com/contentfilter/` from 2001-08-01 on, all 3.3 KB PHP pages, and `list.php` is a 2,617 to 2,641 byte per-URL query form, so **there is no bulk artifact to date**. **Universite Toulouse**, the other one and the university-proxy leg: its era distribution is named in `cache.univ-tlse1.fr/documentations/cache/squidguard_en.html` (earliest capture 1999-08-23) as `ftp://ftp.univ-tlse1.fr/pub/reseau/cache/squidguard_contrib/` holding `adult`, `forums`, `publicite` and `warez` tarballs, and **Wayback holds 133 rows over 31 distinct paths on that host, not one of them under `/pub/`**, so the era tarballs were never archived; `cache.univ-tlse1.fr` has 1,280 rows, no capture before 2002 and no archive file of any kind. The live successor `dsi.ut-capitole.fr/blacklists/` publishes ONE `blacklists.tar.gz` of today's contents (adult at 4,600,370 entries) with no dated historical editions, so it dies on law 4 and not on access: its robots.txt is 505 bytes read whole, disallows `/cgi-bin`, `/proxy`, `/ut1`, the stats trees and `/private`, names no Claude agent and **does not disallow `/blacklists`**. The only 2001 Toulouse content that survives anywhere reaches us secondhand, as the `forums` and `publicite` categories MESD carried under their French names at a 2001-12-04 stamp, and they are inside that 10.5 EE. **shallalist has no ancestor in the window and this was proved rather than assumed**: 1996-2004 returned zero for both hosts, so a positive control widened to 2010 and gives a first capture of `shalla.de` at 2006-02-02 and of `shallalist.de` at 2007-10-11, five and six years late. |
| **`chastity-list`'s own release history, and the adjacent-year law on a fresh population (2026-08-27)** | **Closed at an 11.0 EE ceiling, because the project is nine days old: the whole SourceForge release history is `0.2` 2001-12-06, `0.4` 2001-12-10 and `0.5` 2001-12-14.** There is no 1999-2000 edition to find, so the premise that earlier releases would date the thin years is refuted by the listing itself. Read from `sourceforge.net/projects/chastity/files/` and its three subdirectories, one request each: `chastity-list/` holds the three versions, `squidGuard/` holds only the 1.2.0 already banked, and `OldFiles/` is three squidGuard 1.2.0-0.4 RPMs of 2001-12-10, not old blacklists. **The tarball agrees from the inside**: all 258 members of `chastity-list_0.5.orig.tar.gz` carry one flat mtime of 2001-12-14, and the per-date diffs span 2001-08-13 to 2001-12-03, so the list did not exist before August 2001. **The residual was then bounded offline with no fetch at all.** What an earlier edition could hold and 0.5 could not is exactly the names 0.5 deleted, and those are recoverable from its own diff removal lanes: 542 removed hostnames, 385 never present in any base or added lane, collapsing to 113 registrable domains, 90 held, **22 net-new pairs and 11.0 EE**. So both editions were priced without downloading either. **This matters because the download hosts refuse us:** `downloads.sourceforge.net` and `prdownloads.sourceforge.net` serve the same 26-line file ending `User-agent: *` / `Disallow: /`, and no release tarball can be fetched from either. The `sourceforge.net` 196-line robots.txt names no Claude agent and allows `/projects/*/files/` listings (only `stats/`, `latest/download` and similar are excluded), but it disallows `/rest/`, so the file-listing API is off-limits and the HTML listing is the route. `chastity.sourceforge.net` has no robots.txt at all, a 404, so it is open, and empty of releases. **The second and transferable finding is worth more than the source was.** The 97,943 registrable domains of chastity 0.5 were screened against the store both ways, and the contaminated screen and the adjacent one disagree by two orders of magnitude: held-in-any-year-and-missing-Y gives 1997 81,171, 1998 68,410, 1999 45,506, 2000 12,699, 2001 24,927, while held-in-Y-1-and-missing-Y gives **1997 239, 1998 639, 1999 323, 2000 329, 2001 24,754**. The naive figure overstates the 1999 headroom **141x** and the 2000 headroom **39x**, and at 2001 it is honest to within 0.7%. Gross EE on the adjacent sets: 1999 197.1, 2000 182.2, **2001 14,138.8**. Priced naively the same population reads 26,664.0 EE at 1999 and 7,578.1 at 2000, which is the trap in one number. **The mechanism is that a blacklist's population was registered in the years just before the compile**, so an artifact of year Y pays at Y and cannot pay at Y-2: the store holds these names from 2000 onwards because that is when they came into existence, and the 1999 gap is non-existence, not missing data. **So never carry a store-wide adjacent-gap figure onto a head-of-2001 population, and never price a blacklist at a year earlier than its own compile.** The 2001 control reproduced the banked reading exactly, 14,229.7 against the register's 14,229.0, which is what makes the two negatives trustworthy. |
| **The blocklist seam in the four OTHER distro archives (2026-08-27)** | **Closed at 25.7 EE total, and the Debian find does not generalise: it was one package in one release, not a seam.** The one-request-per-release trick from `chastity-list` was applied to every distribution with 1996-2001 releases that will serve us. **Debian `contrib` and `non-free`**, the two sections the first pass never fetched, across all five in-window releases (10 `Packages.gz`, 8.7 KB to 70 KB): **0** blocklist packages, the only near-miss being `muffin`, a Java web proxy that filters by regex and ships no domain list. **Slackware** 3.3 to 8.0 at `ftp6.gwdg.de`, nine in-window releases via `FILELIST.TXT` (whole-tree, 143 KB to 796 KB) plus `PACKAGES.TXT` where it exists (six releases, descriptions): **0** hits on any blocklist filename or description; 3.1 has no `FILELIST.TXT` and its 255-entry `contents/` is also 0. **SuSE**, one request for `suse/ftp.suse.com/ls-laR.gz`, 772,244 lines indexing the whole mirror: **0** blocklist filenames, and the mirror holds only about 250 in-window-dated files in total, so it is not an archive of its own past. **Red Hat** 4.2 to 7.2 at `ftp.icm.edu.pl`, ten `RedHat/base/hdlist` indexes (247 KB to 10.0 MB, every package description in one request, screened with a binary-safe `strings` grep) plus six `powertools` listings: the only blocklist package in twelve years of Red Hat is **`junkbuster` 2.0.2**, whose family this register already closed at 4 non-comment lines. `blacklist_recipients.m4` is a sendmail macro and carries no domains. 4.0 and 4.1 are skeleton directories at ICM with no packages, which costs nothing: both predate squidGuard and chastity. **Mandrake** 5.1 to 8.1, seven in-window releases at ICM and gwdg, which needed direct listings because the mirrors' own `ls-lR.gz` is a byte-identical stale upstream file (`e827549f`) whose oldest entry is 7.2. **The one live lead in the sweep was `squidGuard-1.1.4`**, shipped in Mandrake 8.0 (May 2001) and 8.1 (Sep 2001), and its source tarball holds two separately dated artifacts that the banked 1.2.0 edition does not. Both were priced and both are dead. `samples/dest/blacklists.tar.gz`, 1,053,139 B, is a genuine robot compile carrying its own header `# This list was compiled in 0:24:15 on 2000.04.03 14:46:46.` from `squidGuardRobot-2.0.8` with `92663 tested successfully`, so it asserts liveness and would take no split: 28,659 domains, **28,554 already held AT 2000**, **17.2 EE**. `test/blacklist/domains` and `urls`, tar-stamped `Sep 13 1999`, are the first 1999 artifact this family has offered, and they carry **no compile header at all**, so they are a listing and not a liveness assertion: 7,302 domains, 7,269 held, **8.5 EE**. **The reason the whole seam is empty is one mechanism: squidGuard's bundled sample blacklist is a rolling artifact recompiled at each release, so every distribution ships the SAME upstream list at a different compile date, and editions within one year collapse to one edition.** That gives the family a measured year profile from three independent copies: **1999 = 8.5 EE, 2000 = 17.2 EE (confirming the period-CD 2000.10.18 reading of 18.2 independently), 2001 = 10,736 EE.** A **1,263x** spread across one artifact family whose contents barely change, which is the adjacent-year law in its purest form: the list is not what varies, the store's coverage of the year is. **Do not re-test distro archives for blocklists.** Refusing us, recorded so nobody probes them again: `archive.kernel.org` and `mirror.nsc.liu.se` (`User-agent: *` / `Disallow: /`), `archives.fedoraproject.org` (names ClaudeBot, Claude-User, Claude-Web and Claude-SearchBot in a 97-line block AND `Disallow: /` for `*` at line 97), `mirrors.dotsrc.org` (names Claude). `archive.download.redhat.com` is NOT a refusal: its Akamai edge fails the TLS handshake against LibreSSL 3.3.6 and against Python's OpenSSL alike, so it was reached through ICM instead. Allowing us and worth keeping: `ftp.gwdg.de` and `ftp6.gwdg.de` (empty `Disallow:`), `ftp.icm.edu.pl` (two narrow paths, neither ours), `mirrors.slackware.com` (`Crawl-delay: 5`, redirects to gwdg). |
| **The Debian package archive as a blocklist seam, and the one find in it (2026-08-27)** | **14,229.0 EE found, and the method is the reusable part: ONE request per release indexes every package in it.** `archive.debian.org` has no robots.txt, and `dists/<rel>/main/binary-i386/Packages.gz` carries every package name, version, size and description for that release. Fetched for all five in-window releases: bo 213,586 B, hamm 331,271, slink 493,436, potato 823,298, woody 1,773,087. That is the `ls-lR` trick applied to a package archive, and it turns a whole distribution into an offline grep. Narrow blocklist regex over the five: **bo 0 hits, hamm 2, slink 2, potato 1, woody 4**. **The find is `chastity-list 0.5`**, 701,038 B in woody and described as "blacklists for SquidGuard", whose source tarball is tar-stamped `Dec 14 2001` with per-date diffs inside the window: 97,937 distinct domains, **94.0% held**, 24,927 held-and-missing-2001, **14,229.0 EE**, GPL v2. Filed as `chastity_list_blacklist / dated_directory`, pending, ranked first in the triage queue. **`junkbuster` is the seam's other member and is worthless**: present in hamm, slink, potato and woody at 78 to 104 KB, but `/etc/junkbuster/blockfile` is 2,108 bytes in 1998 and 2,058 in 2000 and is headed "Illustrative Blockfile", with **4 non-comment lines** in every one of the three in-window versions. Closed. `chastity-list_0.5.20020928` is the out-of-window sibling, stamped `Sep 28 2002`, recorded so nobody fetches it twice. |
| The `tomocha.net` refusal, applied to one file and not the other (2026-08-27) | **An inconsistency in this register, found by the same consistency check that caught the RDAP terms, and it currently costs nothing.** `tomocha.net/robots.txt` carries `User-agent: ClaudeBot` / `Disallow: /` at lines 51-52, and on 2026-08-25 `jpnic_register` was withdrawn for it with the note that its 1,623 EE "stands and must not be used". **But 179.8 EE from the same host on the same day was banked**: the 1999 InterNIC `edu` and `gov` zones at `tomocha.net/files/dns/`, ledgered as `edu.zone.19991120.gz` at 13,822 rows and `gov.zone.19991119.gz` at 1,306. There is no principled difference between them on content, since tomocha mirrors somebody else's register in both cases, so either the refusal covers both files or it covers neither. **Measured cost of resolving it either way is zero for this round**: `internic_zone` at 1999 is 0 pairs and 0.0 EE net-new against `merged260827`, all of it already merged into his baseline, so nothing is withdrawable and only the register's wording is at stake. Raised for Ivo rather than decided. |
| The expansion corpus's unpromoted half (2026-08-27) | **A real gap in the promotion tool worth about 3 EE.** `build_promotion_journals.py` covers eight mention sources and `page_expansion` is not one of them, so its candidate half had never been re-promoted. By the re-split law that should pay; it does not, because the corpus is tiny: **696 evidence rows over 551 distinct domains**, of which 695 have a domain some other source now dates and only **5 (domain, year) pairs are held-but-missing-that-year**. Adding `page_expansion` to the promotion tool is correct and is not worth doing for the yield. |
| Ranking the candidate pool by corroboration instead of by a modelled hit rate (2026-08-27) | **Built, measured, and deliberately NOT switched into a running engine.** The modelled ranking's failure mode is fabricated names, so the fix is a filter the model cannot express: how many INDEPENDENT sources name the string at all. Over the whole pool, **86.32% of 2,376,036 names are named by exactly one source**, which is where a generator's output lands; keeping the multi-source slice leaves **325,127 names worth 209,036 EE** if each gained one year, led by `.au` 0.9904, `.gov` 0.9825 and `.uk` 0.9813 instead of by a fabricated `.ca` block. `scripts/engines/build_multisrc_queue.py` rebuilds it. **Status is honest: unmeasured against the gap population**, which cannot contain a fabricated name at all since a gap target is a name already held, and the engine that would have been switched was measuring 1.67 years per query at the time. A working engine is not a test bed. |
| **Re-splitting a mention corpus against a grown store, measured twice (2026-08-27)** | **The largest lever found in this round, and it reads nothing new.** The corroboration split promotes a mention to a dated record only when some OTHER source already places that domain in a year, and that test is re-evaluated on every split run. So the same journals are worth more as the store grows. Measured on two corpora on one morning: **addresses**, 60 new archives plus a re-split of the 2026-08-08 journals gave 51,235 pairs and 30,645.6 EE, of which roughly 700 pairs are attributable to the new archives; **bare hostnames**, one bank re-splitting 601,738 recovered rows gave 16,114 pairs and 11,447.7 EE against 188 pairs and 128.17 EE for the 400 new archives that triggered it. **So the ratio of re-split to new-reading is about 40:1 and 90:1.** It also corrects a projection quoted earlier the same day: `project_usenet_bare.py` priced the seam at 514 to 1,007 EE, which is the right answer to "what do new archives add" and the wrong answer to "what does running the bank step add". Both figures are correct about different questions. **The announce corpus has no cheap equivalent**: `split_usenet.py` takes archives rather than journals, so its re-split is the promotion tranche, measured the same morning at 5,779 pairs and 1,824.4 EE. **Run the split, not the reader, first, and re-run it after any large ingest.** |
| FUNET's frozen `netinfo` mirror, swept by its own index (2026-08-27) | **The one-request `ls-lR` trick worked and the prize it named is gone, but the lead it leaves is worth finishing.** FUNET is the host that supplied `ripe_dbase_1999` and has NO robots.txt at all, so nothing there forbids us. `/pub/netinfo/ls-lR.test`, one request, 351,368 bytes, 5,795 lines, indexes the whole subtree offline: 563 files dated 1996-2001 over 62,188,621 bytes. **The two largest are a registry dump**, in `/pub/netinfo/FUNET/history/a.dump.of.funet.fi-ftp.archive.1996-10-25/netinfo/netinfo/`: `domain-contacts.txt.Z` at **35,571,481 bytes** and `domain-info.txt` at **9,918,513 bytes**, both stamped `Jun 23 1996`, which is InterNIC's own domain register frozen inside a 1996 FTP dump. **Both are 404 and the negative is proved against a known positive**: in the same minute and over the same protocol, `/pub/netinfo/RIPE/dbase/ripe.db.gz` still answers HTTP 200 with 71,919,736 bytes and `Last-Modified: Tue, 03 Aug 1999 21:27:00 GMT`, so the server serves deep paths under that tree and the files are genuinely gone. `ls-lR.test` is a stale index of a tree since removed, and `/pub/netinfo/FUNET/history/` now lists only a `README.FTP-1997-and-older` where the dump stood. **The upstream was then checked and is empty too, so this is CLOSED.** The index named what to look for, `domain-info.txt` and `domain-contacts.txt` under InterNIC's own `netinfo`. First attempt failed at transport level and was recorded as unfinished rather than zero; retried with `--retry-all-errors`, `rs.internic.net/netinfo` returns HTTP 200 and empty while the `nw.com/zone/` control returns rows in the same session. A filename filter over the whole `internic.net` domain then settles it: `original:.*domain-contacts.*` returns **zero rows**, `.*netinfo.*` returns only 404s and referrer noise, and `.*domain-info.*` returns 12 rows which are the HTML `/domain-info/` FAQ section rather than the file, so the filter mechanism demonstrably works and the two zeros are real. Note the year: a 1996-dated register dates 1996, our thinnest year, where the whole 1996-to-1997 adjacent gap is 109,796 domains and 66,701.7 EE gross, so the ceiling is real but bounded. |
| dailychanges.com, the whole archived parameter space (2026-08-27) | **Closed: the one page already priced IS the source, and there is no corpus behind it.** The endpoint is parameterised by nameserver, date and lane, so it looked like C-38's "one endpoint enumerates a namespace" shape. A CDX domain query returns **4,428 captures at HTTP 200, 4,423 of them `/detail/` pages**, across lanes `a` 1,115, `d` 1,048, `l` 1,137, `n` 1,120, and dated `date=2003` 3,667 against `date=2002` 743. Only the `act=d` pages dated 2002 can reach 2001, and only through the pending one-year-term inference: 195 pages, 611,538 bytes. **A byte-scaled ceiling put that at 14,220 EE and a measured sample refuted it the same hour.** Calibrating on the single page already measured gave 0.02325 EE per byte, but bytes are HTML furniture rather than domains: a 43,309-byte page carries 278 names. Size-spread sample of four pages, 4,845 names: `LAME-DELEGATION.ORG` 4,512 names at **66.7% held**, 2,592 held-and-missing-2001, 1,548.3 EE, which reproduces the earlier reading of 4,511 names and 66.7% exactly; `UGAAIS.COM` 278 names at **0.4% held**, 0.0 EE; `SOHU-INC.COM` 48 at 6.2%, 1.1 EE; `JOMAX.NET` 7 at 14.3%, 0.0 EE. So the whole value sits in one aggregate pseudo-nameserver page and every ordinary registrar page is worthless, which is the register's own earlier note that held-fraction tracks the age of the nameserver's population, now confirmed on a spread sample rather than on the head. **Do not byte-scale an HTML corpus.** |
| The bare-hostname Usenet seam, priced over the pools still on disk (2026-08-27) | **128.17 EE over 400 archives, projecting to 514-1,007 EE over all 16,797, so worth free CPU and nothing more.** `collect_usenet_bare.py` reads the plain `foo.com` in running prose that no other extractor sees, and it had the same empty-directory bug as the address extractor. Spread sample of 400 `usenet_bulk` archives, 3,922,752 messages, 1,956,416 in window: 13,685 distinct pairs gross, of which 9,964 were already asserted by `usenet_announce` or `usenet_address`, 11,135 already assigned by some source, 2,362 uncorroborated, and **188 marginal net-new**. **Gross would have read 8,748.19 EE against a marginal 128.17, a 68x overstatement**, which is the clearest single illustration of why this project quotes net-new post-split. `project_usenet_bare.py`'s three fits: linear 5,382 EE, the shape already known to overstate by 24x; saturation 514 EE with half-yield at 1,402 archives; power law 1,007 EE at exponent 0.60. The sample's own curve rises 48, 57, 128 EE at 110, 210 and 400 archives, so saturation is real but not yet biting. Needs no decision: `usenet_bare / dated_directory` is already master. |
| The rebuilt candidate-pool CDX queue, an 80x collapse and a measured revert (2026-08-27) | **Recorded as a measurement, deliberately NOT as a law about ranking, because that mistake has already been made once here.** Rebuilding `queue_pool_local.txt` against the new baseline took the local engine from 1.15-1.66 years per query, over five finished journals on 26 August, to **0.014 and then 0.020**. What rules out throttling, which is what produced the withdrawn sixtyfold claim on 2026-08-26, is that failures FELL from 58% to 9% and 312 of 342 requests were answered: the archive replied and the names simply had no in-window capture, which is a population property and not a rate property. The cause is visible in the queue head: **18,184 of the first 20,000 lines are `.ca`**, and they are fabricated pool strings such as `afakeaddress.ca`, `lgffu.ca` and `doodoo.cg`. This is the `.mil` failure the queue builder's own docstring records from 11 August, recurring under a different TLD once `.ca` gained enough dated rows for `pool_plausibility` to rank it while its pool stayed fabricated. **No re-tuning was done under time pressure.** The gap population cannot have this defect, since a gap target is a name already held, so the local engine was moved to the unreached tail of the gap ranking, positions 200,001 to 409,075, 209,075 targets and all `.com` at the head. First reading there: **15 queried, 15 answered, 32 year-records, 0% failures, 2.13 years per query.** |
| Stanford WebBase 2001, re-tested on the RIGHT screen (2026-08-27) | **Zero, and the re-test was worth running because the original closure used the wrong screen.** It was retired at "99.99% already held", which is the novelty screen, and the doctrine says novelty is only half the test: what pays is *held AND missing this year*. Re-measured over the 720 MB of URLs and the 14 MB host list already on disk, so no download and no permission: 603,245 distinct registrable domains, of which **603,245 are held and 603,245 already carry 2001**. Held-but-missing-2001 is exactly **0**, so the gross ceiling is 0.0 EE. The original retirement was right for the wrong reason and is now right for the right one. |
| The unbanked squidGuard 2001 dated diffs (2026-08-27) | **Zero, from 175 per-date diff files nothing had ingested.** `data/raw/squidguard2001/bl/blacklists/*/domains.YYYYMMDD.diff`, every stamp inside 2001, sitting in a directory the residual audit flags as `unreferenced`. Added names extracted from the `+`/`>` lanes: **36,573 distinct registrable domains, 100.0% held, and 0 of them missing the diff's own year**. The banked `squidguard_2001_blacklist` edition of 2001-12-18 had already dated all of them at 2001, and a second artifact from the same year cannot add a year the first one gave. **Editions within one year are worth one edition.** |
| The ISC Internet Domain Survey for 1998-2001 (2026-08-27) | **Closed twice over, and the second reason explains the first.** The store holds ISC/Network Wizards walks for 9507, 9601, 9607, 9701 and 9707 only, so 1998-2001 looked like the one non-crawl bulk artifact that could attest 2001. `ftp.isc.org/robots.txt` is four lines and the whole file reads `User-agent: *`, `Disallow: /pub/usenet`, `Disallow: /usenet`, `Disallow: /`, so the live host refuses us outright. **And the archive holds nothing either, for the same reason**: a CDX prefix query over `ftp.isc.org/www/survey` returns HTTP 200 and empty, while `nw.com/zone/` in the same session returns 40 rows, which proves the negative against a known positive. A host that has told crawlers to stay out was never crawled, so its refusal closes the archive route as well as the direct one. `www.isc.org` allows everything but `/thankyou-contact/` and holds no survey data. |
| The whole RDAP query route, closed on the registries' own terms (2026-08-27) | **The terms were inside every response the entire time, in the `notices` block, so this cost nothing to find and three days of engine time not to.** Read out of our own journals for Verisign, PIR and Nominet, and from the page Verisign's notice links to. **Verisign** (`verisign.com/legal-center/rdap-terms/`): you will not "enable high volume, automated, electronic processes that send queries or data to the systems of Verisign or an ICANN-accredited registrar, except as reasonably necessary to register domain names or modify existing registrations". **PIR** the same clause and carve-out, plus "Abuse of the RDAP system through data mining is mitigated by detecting and limiting bulk query access". **Nominet** the same clause with NO carve-out, and separately "You are explicitly prohibited from extracting, copying and/or using or re-using in any form and by any means (electronically or not) all or part (quantitatively or qualitatively) of the contents of the RDAP database without prior and explicit permission". **CIRA** the same, rejected the same morning. So the engine pointed at Nominet on 2026-08-24 "needing no approval" was right about the evidence class and wrong about the terms, three days after `CLAUDE.md` recorded that `.uk` says the same thing. Both engines stopped 2026-08-27 07:47 and 07:51; `ark rdap` now refuses `com`, `net`, `org`, `uk`, `ca` and `nz` in code and needs a named written permission to send one query. Exposure: the class holds 748,099 pairs and 459,792.0 EE, of which only **1,615 pairs and 851.0 EE are unshipped**, because the rest is already merged into his baseline. Only Nominet's clause reaches USE as well as collection, so only its 121 unshipped pairs and 118.7 EE are a withdrawal question. **The route reopens on a written permission of the RIPE kind and on nothing else.** |
| The VPS RDAP journals nobody had banked, priced at zero (2026-08-27) | **A collector alive for 45 hours writing nothing, and 85 MB of journal that paid 0.00 EE.** The VPS sibling sweep showed `up 2-16:29` in the process table while its newest journal had last been written at 2026-08-25 08:51:28 UTC. Six `.part` files, 85,373,766 bytes, were snapshotted and ingested: 701,225 journal lines, 112,114 records with in-window creation dates, **0 evidence rows and 0 year rows**, because earlier snapshots of the same files had already been banked and the queries since had added nothing. Two lessons, both already in `CLAUDE.md` and both re-earned: presence, progress and yield are three questions, and a `.part` is worth snapshotting the moment its writer stops rather than when someone notices. |
| The residual audit's `unread` flag, priced (2026-08-27) | **Worth 22.2 EE, not the "cheapest yield in the project" the audit calls it, because it counts FILES and not value.** Four files matched a documented ingest glob and no ingest had read them. Three are `us_domain_delegated` captures at 2000-12-06, 2001-02-01 and 2001-04-11, and all three are **byte-identical to each other** at 435,847 bytes, one md5: the same delegated-zone list fetched at three instants. The already-ingested 20000815 and 20010606 editions cover those names at 2000 and at 2001, so the trio adds 24 pairs and 22.2 EE, all from the 2000 capture, and exactly zero from either 2001 capture. Predicted 24 net-new before ingesting and the ingest wrote 24 year_rows, so the pricing method is sound and the population is spent. The fourth file, one `cdx_pool` journal of 9,381 bytes, paid 175 pairs. **Rule: price an `unread` count by content, not by file count. Duplicate captures of one artifact inflate it, and a second capture of an unchanged list can only add a year the store may already hold.** |
| Every RDAP-served TLD ranked by headroom, the family closed on measurement (2026-08-27) | **Systematic rather than guessed, and `.ca` is the only one worth a conversation.** The IANA bootstrap was joined against our own holdings and each TLD priced at held-domains x weight x the 29.3% in-window rate measured on Nominet. Ceilings: `.ca` **25,377 EE**, `.nl` 9,200, `.sg` 4,233, `.br` 3,934, `.no` 3,105, `.cc` 2,443, `.info` 2,322, `.pl` 2,158, `.tw` 2,057, `.fi` 1,968, `.gov` 1,942, `.to` 1,908, `.cz` 1,371, `.ar` 1,260. Everything below `.ca` is under 10,000 and mostly low-weight, so **the family is closed except for `.ca`**: no further endpoint is worth a licence question. `.gov` publishes no creation date and is already closed. Both live probes work and both bind use to terms: `.sg` serves `nus.edu.sg` at `('registration', '1996-09-02T16:00:00Z')` and prints "This data is provided for information purposes only" with its policy documents linked but unread, so it is filed pending rather than queried, at a ceiling too small to justify the reading. **`.ca` is live, serves an in-window creation date, and its Terms of Use forbid the query.** The IANA bootstrap gives endpoints for `ca`, `au`, `in`, `uk` and `sg`; `.au`, `.in`, `.nz`, `.za`, `.ie` and `.us` were already closed on an earlier probe. `.ca` was not. One query to `https://rdap.ca.fury.ca/rdap/domain/rita.ca` returns `('registration', '2001-02-01T17:11:06Z')`, the same `whois_creation` semantics already approved. Ceiling **~25,377 EE**, from 103,541 held in-window `.ca` domains at `.ca`'s 0.8365 and the 29.3% in-window rate measured on Nominet. **It died on terms, not on evidence.** The record carries a Legal Notice binding use to CIRA's Terms of Use; that page answers HTTP 403 behind a Cloudflare challenge, so Ivo fetched it from a browser on 2026-08-27 and it forbids this on four separate grounds: s.10(c) bars any robot retrieving the site "to collect information about other users or domain names"; s.11 permits WHOIS use "solely" to check availability, identify a holder or contact a holder; s.11 lists "unauthorised aggregation or collection of information from the WHOIS database" as prohibited; and s.11 bars "automated processes that send multiple queries". s.4 licenses content for non-commercial use only, and this work is paid. **Closed. Reopen only on written CIRA permission of the RIPE kind.** `robots.txt` is 8 lines, names no agent and disallows only `/wp-admin/`, `/?s=` and `/search/`, so robots is not the obstacle. Filed as `cira_ca_rdap / whois_creation`, pending, needing either a human reading of the Terms or a letter of the RIPE kind. `.sg` is untried and worth 31,793 held pairs, far less. |
| The VPS journal backlog, and why the cycle undercounted it (2026-08-27) | **125 journals had never come home and they were worth 40,893.6 EE, which is more than everything else this night produced combined.** `just cycle` reported "rsync 2 VPS journals home"; the real diff between the two machines was **125 files and 416 MB**, 122 of them `cdx_suffix` sweeps and 3 `cdx_vedge`. Converted with `cdx_suffix_convert.py`, which collapses capture rows into per-domain year sets and enters as the already-approved `cdx_snapshot / cdx_timestamp`, they gave 141,013 domains with in-window captures, 122,842 evidence rows and 48,056 year rows. Measured against a pre-ingest snapshot: **50,102 net-new pairs, 40,893.6 EE**, taking the store from 5.3405% to 5.6514%. **The lesson is about the counter, not the VPS**: the cycle counts what a documented glob matches on THIS machine, so work finished on another one is invisible to it until it is copied. Diff the two file lists directly rather than trusting the count, and do it before spending a single new query, because these were queries already paid for. |
| `data/raw/usenet_bulk`, a second 53 GB Usenet pool nothing had ever read (2026-08-27) | **9,266 archives, ZERO filename overlap with the 7,531 in `usenet_new`, no journals, no progress marker and no script referencing it.** Found by listing every `data/raw` directory against the ingest ledger rather than against a documented glob, which is what the residual audit checks and therefore what it cannot see. **It is also far denser than the pool that was worked.** Two disjoint stratified samples over its non-`alt.sex` 2-60 MB stratum: 6 archives / 71.6 MB gave 178 net-new pairs and 99.88 EE (1.3950 per MB), a second 6 / 63.5 MB gave 53 and 30.75 (0.4842). Pooled **231 pairs, 130.63 EE over 135.1 MB, 0.9669 EE per MB**, against the **0.0088** measured on `usenet_new`. The samples are 2.9x apart so the rate is noisy and only the pooled figure should be quoted. Projected over the SAMPLED STRATUM alone, 34,084 MB of the 53,431 MB directory, that is **roughly 33,000 EE**; the `alt.sex` archives, which are the largest files, and everything outside 2-60 MB were deliberately not sampled and are not in that projection. **The likely reason for the 110x gap is population**: `usenet_new` was bit, linux, microsoft and gov, whose domains the store already holds, and this is consumer `alt.*` naming small businesses, fan sites and ISPs it does not. Needs no decision and no queries: `usenet_dated` is already master and `usenet_candidates` already candidate-only, and the cost is CPU on bytes already downloaded. Running via `ARK_USENET_SRC=data/raw/usenet_bulk scripts/sources/usenet/work_usenet_new.sh`. **A third pool turned up on the same check and is now finished**: `usenet_probe5`, 48 archives and 2,298 MB, again zero overlap with either other pool, all 48 processed. `usenet_msft` (3 archives, 488 MB) is left alone because the register already measured the microsoft hierarchy at 63 pairs over 1,979 MB. The other large unbanked directories are NOT this shape and were checked: `rtfm`, `maillists` and `probes` are raw inputs whose derived journals are ledgered, and `ccgraph`, `webbase` and `nypw` are closed families. **Archive SIZE anticorrelates with in-window share and that is the sampling rule to carry forward**: a group that stayed busy into the 2000s produces a large file mostly outside 1996-2001. `alt.sex.anal.mbox.zip`, 974.7 MB compressed and 2.18 GB of readable Usenet whose headers show `g2news2.google.com` throughout, split to **0 records and 0.00 EE**, against 0.9669 EE per MB for the 2-60 MB stratum. `ARK_USENET_MAX_MB` now skips the tail so a deadline is spent on the part that pays |
| Scholarly-index sweep for deposited early-web data (2026-08-24) | Failed positive control: OpenAlex `early web` returns 314 works and not one is the UMN DRUM dataset already ingested here. `type:dataset` 1996-2005 with web/URL/domain returns 3,363 works and no URL corpus; `domain` in scholarly search means protein domain. Route shut. |
| The pre-Nominet and Nominet `.uk` register (2026-08-24) | The file never existed. 12,491 captures over 2,710 URLs of `nic.uk`, `nominet.org.uk`, `nominet.net`; largest object ever served is a 94,785-byte membership list, worth 2 net-new pairs, 1.96 EE. Register exposed only per-name; `members-private/expanded-whois/` is HTTP 401. |
| `uk.*` registration announcements as a `can.domain` analogue (2026-08-24) | Zero: every named file is already in `.processed`. The `uk.net.news.*` subtree measured 1,031 gross pairs against 69,609 for `can.domain` on the same pipeline. Unfetched IA `usenethistorical` is 270.81 GB across 1,007 items, honest band 5,000 to 45,000 EE. |
| `.us` locality registers, Granite Canyon, and a 20,000 EE wildcard (2026-08-24) | `.us` locality registers 39.6 EE, 0 novel names on all four states tested. Granite Canyon secondary-DNS artifacts 1,881.1 EE post-split against a 5,000 bar. Best wildcard candidate, Nominet's member list, 507.96 EE. |
| `ark gaps` queue ranking (2026-08-24) | Not a source: the bracketed-gap queue, 451,490 domains at a 264,814 EE ceiling, ranks by weight and returns 31 years from 600 queries (5.2%) against 673 years from 600 on the `.com`-heavy file it replaced. Reordered to put `.com`/`.net`/`.org`/`.uk` first. |
| Not Your Parents' Web TimeMaps, deferral converted to REJECT (2026-08-24) | Tested at `1996/..._deeplinks_part00o.tar.gz`, 5,641,617 bytes: 17,035 in-window pairs, 17,006 already held, 29 net-new, 14.2 EE. Folder year is year of first archive, not of content, so the 1996 folder's net-new pairs land in 1998, 1999 and 2001. |
| Wayback `__wb/sparkline` endpoint (2026-08-24) | Right shape, same rate limiter. Head to head over 80 gap-queue domains: sparkline 8 of 80 at 1.93 q/s, CDX 7 of 80 at 0.93 q/s. Twice as fast per attempt and no less refused. |
| The re-registration rule, re-measured (2026-08-24) | Of 370 answering RDAP records over 472 seeded-random capture-dated domains, 59.7% still carry an in-window creation date. Transfer, bankruptcy and change of owner never reset it (EFF 1990-10-10, Pets 1994-11-21, Napster 1999-02-20). |
| Arquivo.pt live CDX as a dating engine (2026-08-24) | Answers 17.3 q/s, 250 of 250 HTTP 200, and holds nothing needed: 0 in-window 200s over 250 candidate-pool names and 0 over 157 domains our store already dates in window. |
| RDAP over the candidate pool (2026-08-24) | The 2,395,205 undated names would be worth 1,658,653 EE, and two pilots of 3,000 returned 0 in-window creation dates (336 answers, 234 of them 404; 266 answers, 195 of them 404). |
| squidGuard robot-compiled blacklists (2026-08-24) | Closed on era: `ftp.teledanmark.no/pub/www/proxy/squidGuard/contrib/blacklists.tar.gz`, 429,365 bytes, earliest Wayback capture 2003-12-11, two years outside the window. |
| Registry change reports across five regions (2026-08-24) | Paid about 7,500 EE over eight small artifacts (TWNIC 1,275.0, SaudiNIC 1,506.4, NIC Malta 1,470.5, NIC Venezuela 1,131.3, IDNIC 872.6, RESTENA 708.5, ISOC-IL 375.0, `.nu` 144.1). gTLD side empty: the only in-window listing is `greatdomains.com`, 2,466 owner-submitted records, about 104 EE after the split. |
| National register listings, the `.ie` shape across nine namespaces (2026-08-24) | Two paid (`.my` MYNIC, `co.za`), six empty on measurement: `.nz` whole Domainz site is 170 URLs yielding 5 and 1 names, `.au` largest AUNIC page yields 10 names all worked examples, `.ca` counts only, `.sg` and `.hk` no listing, `.ph` a rolling 30-day expiry window. |
| Pricing on parser `raw` rather than canonical form (2026-08-24) | `ukwa_geoindex` priced at 4,509.1 EE over 4,595 pairs, admitted at 4,493.0 over 4,591. Joining `BulkRecord.raw` URLs against `domain_year` finds zero held and returns top TLDs `htm` 2,106,483 and `html` 2,055,761. After canonicalisation 17,912,511 rows collapse to 289,857 pairs, 285,262 already held. |
| DataCite sweep for deposited early-web datasets (2026-08-24) | Eight query shapes against `api.datacite.org/dois` surface nothing not already held: link list/graph plus web gives 21 hits, the only in-window one the UKWA host link graph; `early web` gives 19 hits whose only deposits are UMN DRUM and the Zenodo banner ads; `web crawl` 1997-2006 zero. |
| Nominet RDAP over held `.uk`, banked (2026-08-24) | Banked, no approval needed. `rdap.nominet.uk` publishes a machine-written `registration` event with a full timestamp, verified `demon.co.uk` `1996-05-05T21:08:48Z`, reached through the IANA bootstrap by `ark rdap`. Evidence type `rdap_snapshot / whois_creation`, `Decision: master` since phase 4. On 400 seeded held `.uk` names read at 157 answers: 29.3% in-window, 19 of 46 pairs net-new, 118.8 EE per 1,000 queries. |
| UCSF Industry Documents Library (2026-08-24) | 3,826,999 in-window documents with per-document `documentdate`, and 6,000 fetched give 216 pairs for 146.6 EE after the corroboration split, because 89% of the net-new names are dated nowhere else. Whole-population projection about 730 EE post-split. |
| Small-organisation open data with a per-row date (2026-08-24) | All four zero. DOL Form 5500, 740,473 dated 1999 rows and zero hostnames; CRA T3010, 441,785 in-window rows, zero; EPA TRI Form R, 2,400 rows, zero against a same-minute control; Canada Gazette Part 1, 50 to 100 EE for the whole run. The era's forms had no web-address field. |
| Uncrawled mailing-list subscriber populations (2026-08-24) | RootsWeb 26.47 GB of WARC measures effectively 0 EE because the capture is of the modern interface; ArchiveTeam Yahoo eGroups 8,271 items, zero net-new by access; `bit.listserv` 1,100 to 1,600 EE for the whole hierarchy. |
| Organisational mail releases beyond Enron (2026-08-24) | One real member, 67x short: `jeb_bush_gubernatorial_email`, 411,928,998 bytes, 626 born-digital files, 519,581 in-window `Sent:`/`Date:` headers, 4,011 EE over 6,412 net-new post-split pairs of which only 1,607.7 EE comes from a `To:`/`Cc:` line. |
| Generated RDAP target populations, and what ORDER to query them in (2026-08-24, extended 2026-08-27) | Four populations of 1,500 to 3,000 names queried direct to Verisign: English dictionary words 13.5 EE per 1,000 queries (28.00% in-window) but finite at ~235,000 words and 92.4% already held, sibling TLDs of held names 9.7 (5.64%), random four-character strings 6.3, invented two-word compounds **0.0** over 859 queries. Siblings won on material: 14,080,169 names against a dictionary that exhausts in an hour. **The pilot rate does not survive contact with the whole queue, and the reason is the transferable part.** Measured 47,164 queries into a later run: **8.2 EE per 1,000**, with the in-window hit rate splitting **7.3-fold** by how many of the six years the store holds the sibling's BASE label, 7.84% at six years against 1.08% at one, and **54% of an unranked queue hangs off one-year labels**. A label the archive sees across all six years belonged to a going concern, and a going concern of that era defensively registered the other two gTLDs in that era; a label seen once is as likely to be a typo or a parked name that never had a sibling. `scripts/engines/rank_sibling_queue.py` sorts on that. **And the registry, not the ordering, is what governs the rate.** Per-minute counts from the journals: the unranked run held **65 queries a second flat for seventeen minutes** with no decay, then every later run collapsed to about 1 q/s whatever order it used, including a shuffled run over the same population. Verisign served **64,568 queries and then clamped for at least twenty-five minutes across three restarts**, so all three ordering experiments ran inside one quota and none of them measured ordering. A first reading of this as "ranking loses sixtyfold" was wrong and is withdrawn. What ranking demonstrably does, being a property of the population rather than the limiter, is raise the answered-200 share from 18.7% to 74.4% and the in-window rate from 1.80% to 4.02%. The queue ships shuffled (`--shuffle`), deterministically, and settling whether ranking pays needs a rested registry. **Rule: a generated population needs an ordering as much as it needs a generator, and the ordering is measurable from the engine's own answers after an hour.** |
| UKWA host link graph, 2 GiB replay cap re-probed (2026-08-24) | Closed at the byte. `timemap/link` returns two captures only; the 2022 one fails at byte 0 with curl exit 56. On the 2020 capture ranged GETs work up to the cap (`2147479552-2147483647` returns 206 with 4,096 bytes) and the next 4K fails five consecutive attempts. |
| Internet Archive bulk CDX / ZipNum index (2026-08-16) | Not public. `archive.org/metadata/wayback-cdx-index` returns `{}` and `cdx/search/cdx?url=*.com&from=1999&to=1999` returns HTTP 403. The 403 is policy, not an outage: do not re-probe. |
| Usenet `Message-ID` posting hosts (2026-08-16) | Over 73,751 in-window messages carrying both a `Message-ID` and a `Date`: 1,405 distinct domains, 2,056 pairs, 51 net-new pairs and zero domains never seen before. Top hosts `wisc.edu` 22,380, `gi.net` 20,962. |
| UKWA ds.1 classification list (2026-08-16) | Recovered from Wayback at `opendata/ukwa.ds.1/classification/classification.tsv`, 3,011,797 bytes over 26,910 rows, and deliberately not ingested: columns are category, title and URL with no date field of any kind, so it is candidate-pool only, and UKWA's selective archive began after 2001. |
| Cybermetrics (Wolverhampton) crawl databases (2026-08-16) | Closed on the authority rule, and the filenames lie about the year: `stats/data/UK_2001.txt` opens with its own header "UK 2002 database crawled July 2002". Read the file's own header before trusting its name. |
| Era web traces and proxy logs, the whole family (2026-08-16) | Closed by design: DEC/Compaq 1996 states "it should not be possible to discover the actual identity of any host or URL in these traces", BU 1998 hashes the Host field, UC Berkeley Home IP 1996 anonymises URLs. Ask any era-trace proposal for the sanitisation paragraph before fetching a byte. |
| Library catalogue records with a MARC 856 URL, measured (2026-08-16) | 47 qualifying records in 48.2 MB of Scriblio give 13 domains, 12 already held, one net-new and that one a public-suffix subdomain. Dating and URL-bearing are anticorrelated: LC books carry an in-window MARC 005 on 28.25% of records and hold 67 hosts in 72,588; LC serials hold 3,492 hosts and carry one on 0.34%. |
| Search engine indexes 1996-2001, the whole family (2026-08-16) | Not one machine-readable dated hostname list survives. AltaVista's May 1999 crawl of 203M URLs was never published and Yahoo Webscope no longer resolves; six archive.org sweeps over Lycos, Excite, HotBot/Inktomi, Infoseek, Northern Light and WebCrawler return zero index artifacts. All three surviving in-window Open Directory dumps are already held. |
| IPEDS institutional characteristics (2026-08-16) | Of 3,251 domains in `IC99_HD`, 2,946 are already dated 1999, the exact year the file attests, leaving 147 post-split pairs and 100.8 EE. The web-address column exists for one in-window year only. `.edu` is 95.5% saturated at the year an institutional directory attests. |
| Unheld Usenet hierarchies, IA `usenethistorical` (2026-08-16) | Deferred. Of the unheld remainder only about 40 GB is English-facing (`microsoft` 26.6 GB) and about 135 GB is national hierarchies an English-weighted metric discounts to near nothing. |
| Not Your Parents' Web TimeMaps, IA `nypw_timemaps` (2026-08-16) | Deferred on cost: in-window folders total 19,350,762,163 bytes, field 3 of a TimeMap line is a 14-digit capture timestamp so the year is per-record, but the methodology paper (arXiv:2507.14752) documents downsampling and the sibling `nypw_firstcdx` is already rejected at 53 net-new domains over 6.28M lines. |
| Parallel Language Records of the Early Web (2026-08-16) | No date of any kind in a record: README plus shard 00 (42,290 lines) confirm SURT pattern then `<lang> <url>`, the only date being collection-level "captured before year 2000". Its 1,164,183 records also select for multilingual mirrors, top tuples `ca-sg` 134,941, `de-en-fr` 89,557. |
| Netcraft Web Server Survey `/domains/cache/` listings (2026-08-12) | Candidate-only: 0 pairs as master, 13,078 names stay in the pool. The pages are machine-generated alphabetical dumps with no per-item date, so they died on contemporaneity rather than on the corroboration split. |
| INET conference proceedings 1996 to 2001 (2026-08-11) | 460 in-window pairs, 416 already held, 19 net-new after the split worth 12.7 EE over 223 pages; whole-corpus estimate 116 EE. `isoc.org` 301-redirects every proceedings path to web.archive.org, so the source is IA-only. |
| Debian package changelogs and upstream homepage fields (2026-08-11) | 803 in-window pairs, 762 already held, 21 net-new after the split worth 14.4 EE. The named mechanism does not exist in window: grep for `Homepage:` returns 0 across all 36 in-window index files, because it entered Debian policy around 2007. |
| W3C technical reports index (2026-08-11) | Census, not sample: 626 in-window reports yield 1,225 pairs, 1,078 already held, 56 net-new after the split worth 36.1 EE, 87x below the bar. Trap: W3C retrofits post-window status banners into archived recommendations. |
| RFC and Internet-Draft documents (2026-08-11) | Complete RFC population plus a 12.2% draft sample: 3,605 in-window pairs, 3,151 already held, 140 net-new after the split worth 88.2 EE. The split does not protect against fictional hostnames, and this corpus is full of them (`acmecorp.com`, `bigco.com`, `widgetco.com`). |
| Microsoft Bookshelf Internet Directory, 1996 CD-ROM (2026-08-11) | 7 net-new pairs and 4.7 EE after the split. The 99 MB ISO yields 2,020 distinct (domain, 1996) pairs of which 1,863, or 92.2%, are already held. |
| Web defacement mirrors other than attrition.org (2026-08-10) | Closed on availability: archive.org holds 0 items for `alldas` and 0 for `safemode defaced`, and its 212 hits for `defacement` are unrelated. |
| Linux Software Map, ibiblio LSM snapshots (2026-08-10) | 86 net-new pairs and 37.3 EE after the split. Each `Begin3 ... End` block carries its own `Entered-date` beside `Primary-site`, so the dating is ideal and the population is wrong: 3,743 of 3,951 in-window pairs, 94.7%, are already held. |
| Printed Internet directory books, the whole named family (2026-08-08) | Closed on reach AND on yield, and both halves matter: a title query over `mediatype:texts AND year:[1994 TO 2002]` returns 34 volumes, every one `inlibrary`/`printdisabled`, and `_djvu.txt` and `_hocr_searchtext.txt.gz` both return HTTP 401, verified on four volumes. The HathiTrust Extracted Features route is legitimate and would not have paid: measured at 15.7 net-new pairs per volume over 69 in-window volumes, 74 net-new pairs and 49.4 equivalent-English after the split against a ~5,000-pair bar. Do not re-probe on availability alone. |
| SEC EDGAR filings 1996-2001 (2026-08-08) | 150 filings stratified across six years, 150 of 150 reachable, 61.1 MB, 46 pairs in total, 4 net-new and 1.9 EE, or 0.01 EE per filing. Filers that print URLs are the large public companies the baseline holds first. |
| InterNIC public zone files via Wayback (2026-08-08) | Absent: `internic.net` under `matchType=domain` holds 8,001 captures of which 16 resemble data and those are single-domain whois lookups; `ftp.internic.net/domain` captures are 435-byte stubs. Trap: `url=host/path/*` with `matchType=prefix` returns zero even for known captures, so drop the `*`. |
| Usenet `Path:` relay chains (2026-08-08) | 7.1M accepted hops over a 400-archive sample collapse to 4,736 domains and 7,201 pairs, of which 49 are net-new after the split: 13.89 EE. A relay is a large ISP or university, so 99.32% of sampled pairs are already held or uncorroborated. |
| Other national web archives, non-Nordic (2026-08-08) | Australia's AWA is the only open in-window index and it is IA data: 13 of 13 cross-checked domains return identical year sets from AWA and the IA CDX, 0 AWA-only pairs, every in-window row from `NLA-EXTRACTION-1996-2004-ARCS-PART-*`. Japan, Austria, Catalonia, Slovenia, Croatia, Netherlands, Singapore, Estonia, Switzerland, Germany, Spain and Italy all postdate the window. |
| Nordic and Baltic national web archives (2026-08-08) | Seven of eight have no public in-window index. Iceland's `vefsafn.is` pywb CDX serves in-window captures but cannot be enumerated, capping the addressable set at 2,540 known `.is` names: 66 lookups, 0 unknown domains, 867 projected EE. Sweden's Kulturarw3 is reading-room terminal only. |
| Shareware and CD-ROM catalogues beyond Tucows (2026-08-08) | Info-Mac worked to exhaustion: 2,604 domains, 2,477 already held, 234 pairs, 134.15 EE. garbo.uwasa.fi's master index contains one domain, its own. Trap: an archive.org software scrape reports 682 net-new domains and all are spurious, 15,399 of 15,521 hits coming from modern uploader prose. |
| Free-hosting member indexes: GeoCities, Tripod, Angelfire, Xoom, FortuneCity, Homestead (2026-08-08) | Collapses architecturally: every member URL is a path or subdomain under the provider's own domain and all ten provider domains are held. 0 member-owned registered domains from 4 index pages; the member-links fallback gives 617 domains at 97.4% already held. |
| Award galleries and cool-site lists (2026-08-08) | 206 domains across 7 dated award pages: 2 net-new domains (0.97%), 5 net-new pairs, 3.16 EE. `point.lycos.com` gives 1 outbound domain in a 90 KB, 484-href 1996 listing page. |
| Institutional link directories: university, library, government, museum (2026-08-08) | 386 of 388 domains across 11 archived BUBL LINK pages are already held: 2 net-new domains, 5 pairs, 1.96 EE. The best-case page, a worldwide museums directory with 192 external links, gave 0. |
| Research crawl datasets, remaining angles (2026-08-08) | academictorrents 2,851 items with 0 in-window web crawls, `collection:webarchivedatasets` exactly 8 items, LAW/UNIMI 2 in-window graphs (`cnr-2000` is 325,557 URLs to one domain), CAIDA no hostname inventory, RIPE Hostcount aggregates only. The parallel-language salvage nets +374 EE and scores negative on the project's own estimator. |
| Search-engine and portal directory trees (2026-08-08, rejected) | The 1996-1997 Yahoo catalogue does live under `www.yahoo.com/<Category>/` and pays nothing: 55 requests, 7.7295 EE. The zero-domain result from `dir.yahoo.com` was an `unwrap_redirect` bug, now fixed, turning one 20000817191821 capture from 0 domains into 3. |
| Non-English regional portals (2026-08-08) | Deferred: 10 archived catalogue pages give 445 EE measured after the split, about 27 EE per request, but 97.4% comes from one Indian portal (Khoj), Seznam's 1,723 domains gave 0, the Brazilian pages gave 0, and everything lands in 2001. |
| Stanford WebBase 2001 (via LAW) | 118M URLs to 603,245 registered domains, 99.99% already held. Retired as a growth source. |
| `deduplicated_urls_*` (supplied seeds) | Exhausted: 200k lines probed yielded 3 domains not in the baseline. |
| Common Crawl | Earliest collection is 2008-05; capture timestamps fail the in-window evidence bar. |
| Arquivo.pt bulk `AWP*` collections | 214 files, sampled slices all 2008, out of window (`Roteiro` and `IA.cdxj` are the in-window exceptions). |
| UKWA per-year bulk CDX | Not publicly retrievable in 2026: dead host, soft-404 successor, 404 DOI, never Wayback-captured. Probe the data paths, not the repository front page: `https://www.webarchive.org.uk/datasets/ukwa.ds.2/cdx/1999.cdx.gz`, with `linkage/host-linkage.tsv.gz` as the positive control, a file we hold 2 GiB of that returns the same 159-byte stub. Access requested. |
| ODP full 2001 content dumps | Unavailable in 2026: the URL serves a "Page Has Moved" stub. |
| ODP full Aug-2000 content dump | Unrecoverable; only `structure.rdf` was archived, which has no external links. |
| Public 1996-2001 zone files | Some do survive: an intact April 1997 InterNIC `.org` zone was found at `nic.mil` on 2026-08-18. |
| Historical zone files and bulk registry snapshots (2026-08-08) | Closed for 1998-2001. archive.org holds no in-window zone file (`title:(zone file)` 303 items all 2009 or later, `"com.zone"` zero); the CD-ROM route holds shareware discs, not registry snapshots; and `wuarchive.wustl.edu`, `ftp.uu.net`, `ftp.cdrom.com` and `ftp.funet.fi` return zero Wayback captures. |
| Australian Web Archive (PANDORA/Trove) | Superseded: the operative verdict is redundancy with the Internet Archive, zero AWA-only pairs. `web.archive.org.au/awa/cdx` answers normally. |
| Other ccTLD registry open data | Nothing free reaches 1996-2001. CENTR aggregates only, OpenINTEL starts 2015, commercial WHOIS is paid. Re-checked for a per-domain file carrying both a creation and a withdrawal date: Nominet, auDA, InternetNZ, CIRA, SGNIC, IEDR, SWITCH and SIDN publish daily feeds, dashboards or top-N rankings, none per-domain lifecycle. AFNIC `.fr` is the only one and is already banked. |
| SNAP web graphs | Nodes are anonymised integers with no URL mapping. |
| Yahoo! Webscope AltaVista graph | Permanent: `webscope.sandbox.yahoo.com` has no DNS record. Does not want re-probing. |
| TREC WT10g / VLC2 / WT2g / .GOV (2026-08-15) | Closed on measurement. Glasgow sells WT2g at 350 GBP, WT10g and .GOV at 500, DVD only behind a signed agreement. The two free files, `wt10g_inlinks.gz` and `wt2g_inlinks.gz`, contain only opaque docids (`WTX001-B01-1`, 8,063,026 lines) with the docid-to-URL table on the paid media. |
| Yahoo! Directory | No machine-readable dump was ever published. Not a re-probe candidate: the artefact never existed. |
| GeoCities derivatives, DNS Census | 2009 and 2013 respectively, out of window. |
| Post-July-1997 ISC `.domains` lists | Do not exist; later editions publish aggregate counts only. Confirmed from two independent live directory listings, so an absence rather than an outage. |
| ISC January 1997 file | Corrupt in every known copy. Permanent gap. |
| Internet Archive Alexa crawls (`alexacrawls`, `webwidecrawl`) | 226,901 items from 1996 with per-item CDX, but every payload returns HTTP 401; only `_meta.xml` is public. |
| UKWA per-year bulk CDX (2026 recheck) | Docs survive at `ukwa.github.io/opendata/ukwa.ds.2/cdx/`; the download host serves the same 159-byte stub and the DOI 403s behind Cloudflare. Wayback captured the directory listing but never the `.gz` files. In-window size would have been about 13.4 GB. |
| New Zealand (National Library) | `webarchive.natlib.govt.nz` and `natlib.govt.nz` return an Imperva bot interstitial; NLNZ's archive.org CDX items are 2025-2026 crawls. Keep in rotation: harvesting began in window, `.nz` weighs 0.9895, and a bot interstitial can change. |
| Canada (Library and Archives Canada) | Federal web harvesting began December 2005 on their own statement; `open.canada.ca` returns zero web-archive index datasets. |
| Ireland (National Library) | Archives via Archive-It, 138 collections, earliest captures 2011. |
| `early-web_parallel-language-urls` | 1,164,183 pre-2000 multilingual URL patterns with no timestamps, so no per-year evidence. Seed-only at best. |
| OCLC Web Characterization Project | Only aggregate statistics were published; the host is gone. |
| Mailing-list archives (2026-08-01) | Population is wrong. archive.org's in-window holdings are hobbyist digests (`sf-lovers`, `GLOWBUGS`); the W3C public lists are small and technical, `www-announce` running for 3 archive periods, `www-talk` 121, `www-html` 246. A 1997 `www-announce` month carries 53 messages against the 20,000-plus domains one Usenet commerce group yields. |
| archive.org books, three collections (2026-08-05) | `subject:(internet)`: 57 of 60 sampled in-window items publish no downloadable `_djvu.txt`, 2 net-new pairs. `collection:folkscanomy_computer`: 36 of 40 unreachable, 2 net-new pairs from 40 items. In-window book scans largely carry no OCR text layer. |
| archive.org `magazine_rack` at large (2026-08-05) | 34,279 in-window items at 0.4 net-new pairs per reachable item, against 10.5 for the computing trade press measured the same way. In-window holdings are user-group zines and lab newsletters. |
| Boardwatch ISP Directory volumes (2026-08-05) | The monthly issues carry `_djvu.txt`; the directory volumes do not. `boardwatch-directory-of-internet-service-providers-july-august-1997_djvu.txt` returns a 146-byte stub. |
| IRCache / NLANR proxy traces (2026-08-06) | Gone. `ircache.net` serves a squatted blog, `ftp://ircache.nlanr.net/Traces/` is dead, `(ircache OR nlanr) AND trace` returns zero archive.org items, and `web-caching.com` now answers with a consent-manager parking page. All three hosts are squatted or parked. |
| Internet Traffic Archive web traces (2026-08-06) | `ita.ee.lbl.gov` is alive and the ideal dataset is unusable: UC Berkeley Home IP 1996, 9,244,728 requests, has anonymised URLs, its own format example being `GET 9168504434183313441..gif`. `BU-Web-Client` has clear URLs and runs 1994-1995, out of window. |
| Shareware CD-ROM catalogues on archive.org (2026-08-06) | archive.org cannot list inside an ISO: `/download/<item>/<file>.ISO/` ends "failed to obtain file list", so measuring density costs a full ISO download per item, 127 MB to 1,300 MB. The 3,578 `cdbbsarchive` items also carry no `date` or `year` metadata. |
| DMOZ / ODP pre-2002 dumps on archive.org (2026-08-06) | archive.org holds exactly one ODP RDF item, `dmoz-rdf-20150327`, 29.8 GB, 2015. No pre-2002 dump exists there. |
| InterNIC / NSI zone or WHOIS snapshots on archive.org (2026-08-06) | 8 hits for `internic AND (zone OR whois OR domain)` and none is data: two Tucows programs, an RFC, two videos, two GitHub mirrors. |
| Other released email corpora (2026-08-06) | Enron is the only released corpus in window. |
| faqs.org as a route to the Usenet FAQs (2026-08-06) | Moot rather than closed: `http://www.faqs.org/faqs/` returned HTTP 429 twice an hour apart and its TLS is too old for the local LibreSSL. The same FAQs were taken through the rtfm.mit.edu mirror. |
| UK Government Web Archive (2026-08-06) | Not rejected. It works and it is tiny: real coverage from 1996-11-11, government-only, 250 addressable domains. |
| `nav.webring.yahoo.com` (2026-08-05) | Zero in-window captures for the entire host prefix. Wrong hostname for the period. |
| WebRing member lists (2026-08-05) | Reject as a bulk source: the largest real page, `www.webring.com/cgi-bin/webring?ring=railring&list` at 20000422003921, is 14,154 bytes, lists 20 member sites and contains 2 member URLs, because every member is linked through a `go.webring.org` redirector. **Narrowed 2026-08-27: this is a fact about WebRing's hub, not about ring member lists.** Bomis prints the bare hostname as text beside each member title and prices at 19.1 EE on 16 rings. See the Bomis row at the top of this table. |
| Bibliotheca Alexandrina IA mirror (2026-08-05) | `web.archive.bibalex.org` and `web.archive.org.bibalex.org` both fail to resolve; only the institutional landing page answers. |
| US trademark filings for domain-name marks, 1998-2001 (2026-08-15) | Closed on two reasoned grounds: a filing costs money and legal work, so the population is businesses an 8.26M-domain store already holds; and an intent-to-use filing evidences an intention, not a live domain, so only a use-in-commerce filing with a specimen attests the site existed. |
| URLs cited in US patents, 1996-2001 (2026-08-15) | A projection, not a measurement: a cited reference is the definition of an authority-selected population, and even at 3% of the roughly 1.0M US patents granted 1996-2001 citing a URL the distinct-domain count is order 10^4. |
| NTP Survey 1999, Nelson Minar / MIT Media Lab (2026-08-15) | Live index, dead payloads. `alumni.media.mit.edu/~nelson/research/ntp-survey99/data/` is 4,337 bytes of period HTML listing `ntp-survey-1999.tar.bz2` and siblings; the census of 175,527 NTP hosts is orthogonal to a capture-derived baseline and unreachable. |
| Library catalogue records with a MARC 856 URL (2026-08-15) | Closed on a dating hazard: an 856 field can be added at any later date, so MARC 008 dates the record and not the URL, and a 1998 record may have acquired its link in 2005. A per-entity date is not a per-field date. |
| Commercial business directories with a website field: Thomas Register, Kompass, D&B (2026-08-15) | Fails on reach: no in-window edition is digitised, archive.org holding the 1905-1906 Thomas Register and undated later scans, and the printed-directory family is already closed on 57 of 60 sampled volumes publishing no downloadable `_djvu.txt`. |
| Dot-com deadpool and failure lists, 2000-2001 (2026-08-15) | Short life is necessary and not sufficient: a funded dot-com ran a marketing budget and was captured repeatedly before it folded. The population is celebrated failures, which is authority selection, and the store holds every one. What pays is short life plus low traffic. |
| Archie anonymous-FTP indexes (2026-08-15) | Closed on era: Archie was effectively dead by 1997, so at best it speaks to 1996. Its population is institutional FTP hosts, and `wuarchive.wustl.edu`, `ftp.uu.net`, `ftp.cdrom.com` and `ftp.funet.fi` return zero relevant Wayback captures. |
| Bruce Guenter's spam archive, `untroubled.org/spam/` (2026-08-15) | 312 net-new pairs and 195.5 EE after the split, 16x below the bar. `1998.7z` through `2001.7z` total 9.3 MB and expand to 20,010 messages each carrying its own `Date` header, but 19,992 in-window messages name only 5,342 pairs over 4,793 domains and 3,203 of those are already held. **Superseded 2026-08-27: this reading understates the same bytes 6.6x.** Nothing was MIME-decoded and only the sender side was read; the corpus holds 26,112 domains and 1,288.1 EE. See the re-price row at the top of this table. |
| Anti-spam blocklists and blackhole lists, 1997-2001 (2026-08-15) | Every in-window blocklist is IP-based, not domain-based: MAPS RBL, ORBS, the Dial-Up List and SPEWS all publish addresses and netblocks, and the output unit is the registered domain. The domain-bearing variant, spam sightings in `news.admin.net-abuse.*`, is already ingested. |
| `data.webarchive.org.uk` (2026-08-05) | Does not resolve. A third distinct host tried for the UKWA bulk CDX. |
| The whole `webarchive.org.uk/datasets/` tree (2026-08-15) | The stub is the tree, not the file: `/datasets/ukwa.ds.2/geo/` returns the same 159-byte "400 Redirect" body under HTTP 200 as `linkage/host-linkage.tsv.gz`, a file we hold. The Geoindex behind it is 700,641,549 lines covering 1996-2010, about 8 GB gzipped, all `.uk`. Another URL did open it: see `## ukwa_geoindex`. |
| Alexa / IA donated crawl items on archive.org, their CDX indexes (2026-08-15) | The bulk index exists and is access-controlled: in-window items carry per-item CDX (`FS-587676-c.cdx.gz` at 104 MB, a 1999 item at 631 MB) and a ranged GET returns HTTP 401 with a 172-byte body, so the restriction covers the index and not merely the payload WARCs. Reopens only on an access grant. The 401 is per-collection and does not reach the `webdataservices` national extractions. |
| National-library historical web extractions on archive.org: INA `.fr`, FCCN `.pt`, NLI `.ie` (2026-08-18) | Enumerated by scraping all 34,841 identifiers containing `HISTORICAL`: `INA-HISTORICAL-*` 49 items, `FCCN-PT-HISTORICAL-*` and `PT-HISTORICAL-*` 31, `NLI-IE-*` 46. Ireland is the only high-weight one and its earliest item date is 2002. The other two refuse their indexes with HTTP 401 and 403. |
| `USFEDGOV-EXTRACT-1996` through `-2001` (2026-08-18) | Reject on measured yield, 56.2 EE. Six `webdataservices` items, one per year, not access-restricted, 3,255,201,499 bytes of merged indexes, `gzip -t` passing, the 1996 index tiling with zero gap and 647,995 of 647,995 timestamped records in 1996. In every item the whole non-`.gov` population sits in the first one or two ZipNum blocks, and its net-new is exactly 0 of 294. |
| `DARTMOUTH-NBER-RESEARCH-2017-ARCS-*`, 659 items (2026-08-18) | Reject: the payload of an already-banked source. Three complete per-ARC indexes over 6.56 MB give 1,204 in-window pairs and net-new of exactly zero, against the ingested census's own 997 pairs per MB. |
| UKWA Geoindex, E17 postcode slice, figshare 825956 (2026-08-15) | Reachable and too small: 1,886,146 bytes, 12,081 rows of `postcode,year,subdomain,waybackurl`, the 14-digit timestamp inside each wayback URL being self-dating `cdx_timestamp`. 296 net-new pairs raw, 123 pairs and 120.7 EE after the split. |
| Other JISC UK Web Domain Dataset derived files (2026-08-15) | Hostless by construction: `fmts-cleaned.tsv` is MIME-type counts, `link-summary-*.tsv` is suffix-to-suffix counts, `ds.1/classification.tsv` is URL plus category with no year. |
| Archives Unleashed derived datasets (2026-08-15) | Structurally out of window: derivatives are built from Archive-It collections, which begin in 2005. |
| Arquivo.pt CDXJ collections other than `AWP*` and `IA` (2026-08-15) | Every one out of window on 206 ranged GETs: Tomba 2005-2008, InternetMemory 2006-2012, Geocities 2009. The Internet Memory Foundation holding is 62,291,715,540 bytes and the sample found zero captures in 1996-2001. |
| DMOZ / ODP copies on Zenodo (2026-08-05) | 12 hits, all 2018-2020 research derivatives of late DMOZ dumps. |
| `biz.*` Usenet hierarchy (2026-08-05) | Exhausted: no unprocessed `.mbox.zip` archives remain in the 19,233-group catalogue. |
| Late-starting Usenet groups (2026-08-05) | A selection rule: 4,023,027 of 5,283,482 messages across 28 probed archives are out of window, concentrated in whole groups, with four of the 28 contributing zero net-new pairs and `uk.misc` one record from 172.9 MB. Gate on in-window date coverage, not on group name or file size. |
| OpenPGP keyserver bulk dumps, SKS and Hockeypuck (2026-08-18) | Nine dump hosts are dead, NXDOMAIN or 404; `pgp.key-server.io/sks-dump/` serves a squatted 1,095-byte redirect stub under HTTP 200; `keys.openpgp.org` publishes no dump by design; archive.org and Zenodo hold none against a working positive control. |
| Curated distribution keyrings: Debian removed-keys and emeritus, GNU, Apache KEYS (2026-08-18) | Retrievable, correctly dated and 70x too small. Priced on the UID binding signature over 4,096 items: 1,418 pairs, 1,273 already held (89.8%), 69 net-new pairs, 44.4 EE. `debian.org` alone is 1,033 of the in-window user IDs, and a current keyring garbage-collects departed maintainers. |
| X.509 certificate corpora with `notBefore` in 1996-2001 (2026-08-18) | `notBefore` is CA-written into a signed structure and genuinely self-dating; the population fails. The only retrievable in-window corpus is `hg.mozilla.org`'s 139 revisions of `certdata.txt`, and a census of its 126 in-window certs gives 1 net-new pair worth 0.6 EE, with 0 of the 126 end-entity web-server certificates. |
| Machine-written mail headers in bulk mailing-list archives (2026-08-18) | `pipermail` strips the `Received` chain entirely: over 37,789 messages from 2,622 of our own month files only `From`, `Date`, `Subject`, `Message-ID`, `References` and `In-Reply-To` survive, and the `Message-ID` host seam is worth 156 net-new pairs and 107.3 EE over the whole 579,808-message corpus. |
| Web archives holding their own pre-2002 crawls (2026-08-18) | Counted rather than hoped: Wikipedia's list of initiatives (109 rows), MemGator's `archives.json` (20 endpoints) and the IIPC directory (48 permalinks). The Memento TimeTravel aggregator no longer exists, `timetravel.mementoweb.org`, `labs.mementoweb.org` and `aggregator.mementoweb.org` all having no DNS record. Of the 13 initiatives created 2001 or earlier, one is the Internet Archive and three are already closed here. |
| Kulturarw3, National Library of Sweden (2026-08-18) | The largest IA-free in-window corpus known, and the door is shut: access is on-site only and "You cannot search freely for a word or subject, but must enter, for example, `www.sf.se`", so the interface cannot emit an unknown hostname. `kulturarw.kb.se` and `kulturarw3.kb.se` resolve to `selma.kb.se` and refuse TCP. |
| Scholarly and technical full text 1996-2001, the whole family (2026-08-18) | Closed on a density ceiling of 0.042 net-new post-split pairs per item, fixed by two unrelated corpora: the RFC row at 0.0416 and a full census of D-Lib Magazine's 381 in-window articles at 0.0420 (16 net-new pairs, 11.68 EE, 97.4% of its 978 pairs already held). Clearing the bar needs 119,062 such items and the largest such corpus holds 4,997. |
| Quoted `whois` records pasted into Usenet bodies (2026-08-18) | 50x under the bar. Self-dating on the registry's own `Record created on 18-Feb-1998.` line, so the paste date is irrelevant to the year claimed. Priced from disk at zero network cost over 28.20 GB: 488 pairs, 68.2% already held, 155 net-new pre-split, 95.0 EE. |
| The ISI RFC 1480 US Domain Registry (2026-08-18) | Four dated in-window editions recovered, and the registry added four names between August 2000 and November 2001, so the legitimate first-appearance diff prices at 1 net-new pair and 0.9 EE, while dating every name in each edition would have claimed 13,014. Its contact column re-confirms 97.7% already known. |
| Another precomputed IA capture census in a research repository (2026-08-18) | The whole in-window population is four items, three already in this register and one new: Weber's DRUM deposit `10.13020/D62684`, 74.83 GB in 16 tar parts, measuring 45,130 of 45,130 sampled pairs already held and 1 net-new pair worth 0.63 EE from 226,171 rows. ICPSR, OSF and Dryad were blank against working controls. |
| Discmaster, the index over archived media contents (2026-08-18) | Works, and the media population is already ours: the deduplicated `.url` population is 125 net-new pairs and 78.9 EE at 95.6% overlap. Bulk endpoint `search?download=true` returns every match as one tar.gz up to 1 GiB; `robots.txt` says Disallow and carries its own written exception for limited targeted research automation. |
| An early bulk whois snapshot of 2002-2008 vintage (2026-08-18) | Closed mechanically: whois of that era answered on port 43, which no web archive crawls; bulk registry access was a contractual provision to accredited registrars rather than a published file; and the paid market begins its own archive in January 2016 by its own statement. Five free platforms swept to exhaustion. |
| Government grant and award records 1996-2001 (2026-08-18) | Clears the item screen 3.8x over at 456,700 dated in-window items and still dies, because 0.042 pairs per item is a property of subject matter: NSF CSE 0.0471, BIO 0.0152, GEO and TIP 0.0000, NIH 0.0012 at 164 distinct hostnames in 372,444 abstracts. The contact field is current-state refreshed under a frozen date, caught by `gmail.com` appearing 61 times on 1996-2001 awards. |
| Dated newswire and press-release full text (2026-08-18) | Do not sign the NIST agreement. An ungated corpus larger than Reuters RCV1 exists, `usenet-clari.*` at 22 items and 21,309,542,972 bytes with Business Wire and PR Newswire full text, and it fails on era: across four group files parsed in full and six censused, the earliest message is uniformly 2003-06-23. |
| Machine-written network diagnostics pasted into Usenet bodies (2026-08-18) | 29,040 of 219,447,104 in-window messages carry a diagnostic structure, one in 7,557, capping the lens at 1,220 pairs against a 5,000 bar. Measured 297 net-new post-split pairs and 165.7 EE, reduced by a hand audit to roughly 150 pairs and 70 EE for 383 GiB read. |
| Dated announcements of new domain registrations (2026-08-18) | Right about dating, wrong about volume: a registry of this era published either dates without names (statistics, as at `domainz.net.nz/newsstand/stats/` and every InterNIC and NSI registration report) or names without dates (a zone snapshot). |
| Discmaster by file size, and the April 1998 `.jp` registry listing (2026-08-18) | The route works and the snapshot is 87.5% already held. `email.domains`, 2,085,500 bytes and 42,701 lines, at `/japan/email.domains` on the `ftp.cs.arizona.edu` mirror, item 19864, self-dating from its own header "Registered Domains in JP (Apr 30 1998)". `dedup=1` kills the connection; every other parameter is fine. |
| Afilias Land Rush 2 schedule (2026-08-25) | 0.00 post-split, because exactly 1 of the 4,257 names is dated anywhere in the store. `landrush2.afilias.info` resolves at 66.199.183.26 and refuses TCP 80; the surviving fragment, `onlinedomain.com`'s `LR2-list-of-4257-available-domains.txt` at 82,107 bytes, carries `"copyrightYear":"2012"` and is the subset still unregistered in 2012. |
| The ICANN forum `.info` Sunrise lists (2026-08-25) | A decision for a human at 1,328.60 EE post-split. `forum.icann.org/newtldagmts/` is 7,169 message pages frozen since March 2002; largest item `3C8A91B500002319.html` carries `Date/Time: Sat, March 9, 2002 at 10:50 PM GMT` and "Listed below are the 6122 names registered at Sunrise by Worldnic", of which 5,279 parse. Union with WIPO's `.info` Sunrise case index: 7,988 names, 7,284 net-new. |
| A 2001 squidGuard blacklist (2026-08-25) | 10,736.2 EE, licence GNU GPL v2 verbatim in `COPYING`, and it triggers this register's own reopen condition. One request to `archive.debian.org/debian/pool/main/s/squidguard/squidguard_1.2.0.orig.tar.gz`, 1,852,659 bytes, yields `samples/dest/blacklists.tar.gz` at tar mtime `Dec 18 2001`, whose files carry the machine-written header `# This list was compiled in 0:00:20 on 2001.12.18 15:04:29.` and dated diffs to `domains.20011218.diff`. **The header asserts liveness rather than listing, so no split: `squidGuardRobot-2.3.4` generated it.** 44,130 canonical names, 18,588 net-new (domain, 2001) pairs. |
| The `.us` locality gap (2026-08-25) | 61% dead names: of 9,680 `.us` domains held in window and missing 2001, 6,948 were last seen in July 1997 and only 1,473 reach 2000, so the addressable share is nearer 3,500 EE than 8,964.65. Against a control, only 37.65% of the 12,080 `.us` names the ISC 1997 walk attests have any 2001 record, `.com` from the same file 40.31%. Compute headroom from the adjacent year only. |
| Southern and Central Europe FTP hosts (2026-08-25) | The host layer is dead, not refusing: of 50 hosts screened, 26 are NXDOMAIN (`ftp.nic.at`, `ftp.nic.fr`, `ftp.cnr.it`, `ftp.switch.ch`, `ftp.huji.ac.il` among them) and nine resolve but refuse ports 21, 80 and 443, against a control where `ftp.funet.fi:21`, `ftp.gnu.org:21` and `ftp.arnes.si:21` were open in the same minutes. Every survivor is a pruned modern distro mirror with zero netinfo paths. Also shut structurally by RIPE's 1999-02-01 restriction, which covers `.gr` and `.il`. |
| The 2001-2003 frozen-mirror sweep (2026-08-25) | Both screened artifacts were already answered: the Edelman whois transcriptions re-found by a second route price at 741.61 EE against the 2,968.49 EE already banked from a fuller parse, its independent 2001 slice giving 1,003 held-missing-2001 at 618.71 EE. |
| Raw AXFR output published openly (2026-08-25) | Surviving editions are 1995 only, **corrected 2026-08-30: a 1999-01-07 edition also survives** (`ftp.apnic.net/apnic/arin/arin.zones.tar.gz`, above), and it paid 99.7 EE because a reverse tree names the operators we already hold. `ftp.ripe.net/ripe/local-ir/inaddrcount/data/193.in-addr.arpa.output.gz`, 2,332,217 bytes and 491,640 lines, is a genuine raw AXFR transcript with 50,141 PTR targets, dated three independent ways all 1995: HTTP `Last-Modified: Tue, 02 May 1995 21:00:00 GMT`, FTP mtime `03-May-1995 19:36`, and the SOA line `95042701 ;serial (version)` inside the bytes. |
| Forged-header corpora, the Lazarus remailer logs (2026-08-25) | 115.83 EE, because only 1,053 of its 23,102 canonical names are held. The dating is the cleanest seen here: a complete 12-month 2001 series, 57,368,107 bytes over 13 files, each file's mtime landing on the last day of the month its filename names, with zero cross-month bleed and an in-header `Fri Mar  2 00:45:13 EST 2001`. Licence: none found. |
| The 2001 threshold qualified (2026-08-25) | It is a population average, not a universal rate: `WinNetMagCD.chm`, 146,221,869 bytes dated `2001-12-05 18:11:43` in the ISO9660 directory record, yields 2,334 canonical names of which 2,296 are held and only 157 are held-missing-2001, measuring 95.67 EE or 0.041 EE per name against 0.31 for a random sample of held `.com`. Head-selected corpora need about 24,000 names. |
| The southern-hemisphere DNS-walk lens (2026-08-25) | Blocked by a correlation, not absence: host mortality on one side and robots refusal on the other. Five of the seven live large mirrors in this slice refuse by robots, two naming ClaudeBot, and the two that permit crawling (`ftp.swin.edu.au`, `mirror.fsmg.org.nz`) carry only current distro trees. |
| The 2001 hunt: five routes closed and one prize sized (2026-08-25) | A full 2001 `.info` register would be worth about 273,600 EE, since the store holds 21,609 `.info` at 2001 against about 750,000 that existed by year end, and it does not survive: ICANN's Registry Operator's Reports are aggregate counts plus registrar names, the January 2005 `.info` report yielding 148 bare names in "Section 7" at 145 net-new and 52.90 EE. Correction to a reported trap: `to_registrable` does not drop a CRLF-terminated name, returning `example.com` for `'example.com\r'`. |
| `tomocha.net` refuses ClaudeBot by name (2026-08-25) | `tomocha.net/robots.txt` is 61 lines and carries `User-agent: ClaudeBot` / `Disallow: /` at lines 51-52. `jpnic_register` is withdrawn from the queue: its measurement of 1,623 EE stands and must not be used. `tomocha.net` is on the by-name refusal list. Read the whole robots.txt and act on it before any other request. |
| DNS-walk output across the RIPE region (2026-08-25) | Structurally dead by RIPE's own dated decision, verbatim from `ftp.uni-erlangen.de/pub/ripe.net/ripe/hostcount/README`, mtime 3 July 2001: `01/02/1999  Access to the host output files was restricted` and `03/07/2001  Access to the error files was restricted as well`. The sibling `METHOD` confirms the output was "transferring every possible Domain Name System zones under the mentioned top level domains". Closes about 14 namespaces without probing them. |
| The 2001 threshold (2026-08-25) | P(store lacks 2001 given domain held): `com` 0.611 (4,264,044 of 6,980,240), `net` 0.653, `org` 0.568, `uk` 0.309, `de` 0.841, `au` 0.406, `ca` 0.478, `nz` 0.545. EE per already-held name in a 2001-dated artifact: `com` 0.386, `org` 0.404, `au` 0.402, `ca` 0.400, `uk` 0.303, `nz` 0.539, `de` 0.111. So 1,000 EE needs about 2,590 held `com` names, a 32x relaxation of the curated-directory floor, and it applies only to 2001. |
| The long-running-series lens (2026-08-25) | An IRR/RADB dump is 97.6% already held and paid 4.44 EE, because 95.2% of its names were already held in that very year: 13,674 in-window `changed:` lines collapse to 532 pairs of which 25 are net-new. The screen is held and missing this year. Aim at 2001, not 1996. |
| `ftp.nluug.nl` refuses four Claude agent names (2026-08-25) | `ftp.nluug.nl/robots.txt` lists `ClaudeBot`, `Claude-User`, `Claude-Web` and `Claude-SearchBot`, each with `Disallow: /`. Also refused and not pursued: `ftp.fu-berlin.de`, `ftp.uni-stuttgart.de`, `ftp.tu-chemnitz.de`. `ftp.radb.net` serves no HTTP at all. |
| `.nz` port 43 (2026-08-25) | 7,586 EE measured and refused by the registry's own terms, which sit about 1,100 bytes into the same response, after the record. 200 domains from 47,914 held `.nz` names, 123 dated, 122 in-window against 1 out, 0.1600 net-new per held domain, CI 5,177 to 9,995. Read past the record on any port-43 source. |
| The nw.com survey series (2026-08-25) | Complete and fully held: a December 1998 capture of the `nw.com/zone/` listing shows exactly 9507, 9601, 9607, 9701 and 9707, so the survey was semi-annual and there is no 9604, 9610 or 9704 to find. `hosts-per-net` is counts without names. The family already paid 14,956.4 EE, the best 1996-1997 source in the project. |
| Promotion tranche and holdings audit (2026-08-25) | 1,805 EE banked with no decision, of which the promotion tranche is 2,476 pairs and 1,556.6 EE: `usenet_mention` 808.5, `usenet_address_mention` 664.7, `usenet_bare_mention` 360.0, `rtfm_faq_mention` 41.2, `trade_press_mention` 12.6, `enron_email_mention` 0.7. Promotion compounds off every master ingest, 157 of those pairs being `.ie` because `iedr_register` landed the day before. |
| The anti-spam product family (2026-08-25) | Closed 8x under the floor. Unisyn Spam Exterminator's `spamex.lst` exists in exactly four (date, size) editions, 60.7 KiB (1997-07-09), 63.0 KiB (1997-08-27), 80.5 KiB (1997-09-12) and 105.4 KiB (1998-01), across 28 media copies. The marginal product is worth about 100 EE and carries a shareware licence. |
| The `can.domain` classification ruling (2026-08-25) | Not a source, a ruling: the CA Domain Registry's notices measure 11,418 pairs and 9,551.2 EE if the registry self-dates against 936 pairs and 783.0 EE if a human typed it, a 12.2x gap turning on whether a `Date-Approved:` field printed by the registry is the registry stating its database. The 936 are banked, so the incremental prize is about 10,482 pairs and 8,768 EE for zero further collection. |
| Caselaw (2026-08-25) | Closed on access and on content. `static.case.law` is `User-agent: * / Disallow: /`, `case.law` disallows `/caselaw/`, `www.courtlistener.com` blanket-403s. Through the permitted Hugging Face CAP mirror, a complete 25,676-opinion shard of 432,051,278 characters contains zero occurrences of `http://`, `https://` or `www.`, against same-shard controls returning 23,548 rows for `Circuit`. |
| ERIC (2026-08-25) | Grey literature passes the density screen at 221x formal prose, 1,697 URL occurrences in 5,003,152 words or 0.339 per 1,000 against Hansard's 0.00153, and fails the authority screen at 93.0% of pairs already held. The union holds 184 `.edu` pairs and exactly one survives. |
| Grey literature as a live lens (2026-08-25) | First ERIC sample: 190,789 in-window records of which 40.4% are `ED`-type with full text, 54 PDFs giving 3.55M characters and 75 canonical pairs at 89.3% already held, 4 post-split. |
| Sweeping period media for registry extracts (2026-08-25) | Closed on the interface: five queries against `/search?q=<term>&qfields=file&tsMin=1996-01-01&tsMax=2001-12-31` returned http=000 after 30 to 36 seconds for `domains`, `nic` and `domain-list`, while `whois` answered in 8.6s with 40 rows. Registry extracts on period media can be stumbled on, not swept for. |
| Blocklists bundled in dated anti-spam software on period media (2026-08-25) | A new source class. Consumer products shipped their blocklist as a plain data file and hundreds of 1996-2001 CD-ROMs preserve those files with per-file mtimes on the media, so discmaster's `tsMin`/`tsMax` filter makes the era screen a query. 24 dated in-window artifacts across five products, union 2,855 net-new pairs and 1,689.5 post-split EE, of which the licence-clean share is 1,055.3 EE and the unlicensed 2001 `BlackList` table inside `data.mdb`, 320,099 bytes and 10,088 rows, is 967.1 EE. Worst typo bound on the project at 73.7%. |
| The adversarial law refined (2026-08-25) | It pays only if the adversary did not crawl. A period-CD squidGuard list headed `# This list was compiled in 39:33:10 on 2000.10.18 14:13:23.` is worth 18.2 EE with 38,876 of 39,082 domains, 99.47%, already held, and the same header says why: "compiled from 3405 link sources and 739695 links". Non-crawl channels still win: junkfilter 50.4% held, SpamEater 59.1%, Edelman 25.8%. |
| CyberNOT (2026-08-25) | Zero by derivation, so the reopen condition is unsatisfiable. The recovered decoder `cndecode.c` shows `cyber.not` stores a 4-byte IP plus a category mask per record with CRC32 hashes for URL paths, and `print_ip()` calls `gethostbyaddr()` at decode time, so all 40,715 published hostnames were March 2000 reverse DNS over the file's 64,523 IPs. The 22 freely retrievable in-window copies return 0 for `strings | grep -c` on any TLD. |
| `cryptome.org`, `tbtf.com`, `www.openpgp.net` refuse ClaudeBot by name (2026-08-25) | `cryptome.org` 403s robots.txt itself and 403s on the ClaudeBot token specifically: same URL, same minutes, curl default UA 200 and 114,247 bytes, honest project UA 200 and 114,247, `ClaudeBot/1.0` 403 and 159 bytes. Not evaded by changing UA. `marc.info` is `User-agent: * / Disallow: /`. |
| Abandoned `.part` journals, local half (2026-08-25) | 919 EE banked. A collector killed by a deadline, a signal or a crash never renames its journal, so its work sits where no glob matches: the paused local collector's `cdx_pool_20260824T142945Z.jsonl.gz.part` held 579 queried, 575 answered, 758 year-records. |
| A 2003 whois transcription on an abandoned academic page (2026-08-25) | 2,968.49 EE over 4,747 net-new pairs, no licence at all. Ben Edelman's three listings on Harvard Berkman Center space, 81 pages, 13,507,154 bytes, 15,990 entries, 8,787 dated. **Each record carries its own `Dates of creation / last modification / expiration: 27-Feb-2000 / ...`, transcribed from registrar whois, under the page's own "All data is as of January-October 2003".** Human-typed transcription, so it takes the corroboration split. |
| Nominet port 43 (2026-08-25) | Rejected on Ivo's standing answer to O5 of 2026-08-24, "I am paid for this work, so if that makes bulk queries illegal, let's not do it". The door is open: 432 queries at 0.5 q/s with zero refusals, projecting about 81,419 EE over the 560,548 addressable `.uk` domains, measured at 32.38 EE over 300 queries. The port-43 footer restricts repackaging and redistribution. |
| Parliamentary and gazette prose (2026-08-25) | Four closed, and the item screen predicts the wrong answer: formal prose runs about 15x under the 0.042 ceiling. Hansard's 1,002 sitting days and about 235,270 section pages clear the 119,000-item screen 9.7x over, and 1,795 sampled pages of 3,260,082 words contain exactly 5 URLs (`dti.gov.uk`, `fco.gov.uk`, `homeoffice.gov.uk`, `edwarddavey.co.uk`, `ecb.int`), all 5 already held. Ceiling about 470 EE. |
| The reciprocal-traffic industry (2026-08-25) | The blocklist inversion does not generalise: the two traffic-derived artifacts reachable off Wayback measure 99.55% and 98.39% already held, worse than the 87-99% curated band, because a visitor log's hostname field is reverse DNS and the long tail resolves to its ISP (`splitrock.net`, `pacbell.net`, `prodigy.net`). |
| Two more unreferenced directories (2026-08-25) | Both zero. `data/raw/bl/` enumerates five Hyku repositories and the four unexamined index files (`mola` 1,358 rows, `nls` 655, `nms` 7,502, `nt` 2,618) hold 1 to 2 web-shaped filenames each, every one a false positive. `usenet_msft` duplicates 511 MB already processed under `usenet_new`. The unreferenced list is now fully accounted for. |
| Blocklists as a lens (2026-08-25) | Already-held on a blocklist is about 50% (junkfilter 50.4%, SurfWatch 49.7%) against 87.5% to 99.8% on every authority-selected corpus, because a blocklist selects for what somebody wanted to block. `junkfilter_dated_blocklist` found and in the queue at 2,189.4 EE: Gregory Sutter's procmail filter at `junkfilter.zer0.org/pkg/`, 13 ISO-dated in-window editions plus two 1997 tarballs, about 900 KB, dated three independent machine-written ways that agree, with 42,005 of 42,034 tokens domain-shaped. CyberNOT is dead in DNS; the surviving peacefire mirror's 1,000-name list is 32.2 EE; `discmaster_by_file_size` closed at 185.3 EE. |
| 1999 InterNIC zones on the JPNIC mirror (2026-08-25) | 179.8 EE, banked, needing no decision. `tomocha.net/files/dns/` holds `gov.zone`, `edu.zone` and `root.zone`, all filed 2002-02-26, **and the file date is not the artifact's date: `gov.zone` carries SOA serial `1999111901` and the other two `1999112000`, inside the payload**, with `gov.zone` ending on InterNIC's own `;End of file.` marker. `gov` 784 pairs at 1999, 601 held, 183 net-new; `edu` 5,850 pairs, all already held. |
| Stranded RDAP journals on the VPS (2026-08-25) | 3,599.2 EE banked over 5,877 net-new pairs, the oldest sitting since 22 August, because `maintain.sh` rsyncs `rdap_*.jsonl.gz` and `cdx_*.jsonl.gz` and never `*.jsonl.gz.part`. Five abandoned partials, 62 MB, 502,293 readable records, 110,499 in-window creations, 104,622 already held. |
| The frozen-mirror rule applied a second time (2026-08-24) | The surviving registers are on personal pages, not institutional ones. Found and admitted: JPNIC's own `.jp` register at 30 April 1999, `https://tomocha.net/files/dns/domain-list.txt`, 6,185,475 bytes, `Last-Modified: Fri, 30 Apr 1999 04:43:08 GMT`, repriced from the bytes at 72,704 names, 45,877 already dated 1999, 26,827 net-new pairs, 1,623.0 EE. Withdrawn on the `tomocha.net` by-name robots refusal above. |
| Integrity audit over every held gzip (2026-08-24) | `gzip -t` over all 6,168 `.gz` files in `data/raw` outside the Usenet trees, 10.8 GB: 39 fail and every one is accounted for. 21 under `probes/` are deliberate prefix samples at exactly 65536 and 50000 bytes; of the real 18, `ukwa/host-linkage.tsv.gz` is the archive's 2 GiB replay ceiling, six are `cdx_suffix` journals already measured at net-new zero, and `odp/c2000.gz` is a known truncated partial. |
| The 1999 RIPE database on a document mirror (2026-08-24) | 90,799 EE, not banked because of its own copyright header. FUNET mirrored RIPE's whole document tree into `/pub/netinfo/` and stopped updating, so the mirror froze holding the pre-GDPR original: `http://ftp.funet.fi/pub/netinfo/RIPE/dbase/ripe.db.gz`, 71,919,736 bytes, `Last-Modified: Tue, 03 Aug 1999 21:27:00 GMT`. Integrity: `gzip -t` clean, 20,528,780 lines, its own `# 990804 00:07:01` on line 2 and its own `# EOF` terminator. |
| `data/raw/usenet_new/`, 50 GB unseen (2026-08-24) | `ingest_new_usenet.sh` reads `DIR="data/raw/usenet"`, so 7,531 archives and 50 GB in `data/raw/usenet_new/` were never looked at. Measured over 4,052 MB and five hierarchies: 57,913 dated pairs, 57,847 already held, 99.89% saturation, 66 net-new pairs and 35.8 EE. `bit` and `linux` gave 0 over 207 MB. |
| Zone files off the Internet Archive (2026-08-24) | Nobody archived scratch. Three organisations transferred exactly the data wanted and all three published only the aggregate or the current state. The six InterNIC zones survive because they sat in a document mirror alongside RFCs, so the productive question is which registry filed its zone next to its documents. RIPE NCC Hostcount is the most valuable negative of the session. |
| `ftp.isc.org` disallows everything (2026-08-24) | `ftp.isc.org/robots.txt` returns `Disallow: /` for all agents. The ISC survey finding it produced stands and the held `isc_survey` data came from other routes, but no further request may go there. Read robots.txt before the first request and record the read. |
| Mailing-list subscriber populations, refuted (2026-08-24) | A participant population does not give one domain per participant, it gives one per employer or ISP: 15,968 IETF senders collapse to 1,713 domains (9.3:1), 16,051 r-help senders to 1,053 (15.2:1), 6,118 FreeBSD senders to 1,627. Measured 0.00106 EE per in-window message post-split, so 1,000 EE needs about 944,000 in-window messages on one unmined host and nothing reachable is that big. |
| FFIEC Call Report, the era vintage (2026-08-24) | `data/raw/ffiec/call_03312001.zip` is the 2001-03-31 vintage and is empty: 35 schedule files, 8,858 institutions, zero URL-shaped tokens (`https?://` or `www.`) across every file, and no header matching web, url or internet in any schedule. The live FDIC API prices at 134.16 EE. |
| `data/raw/cdx_suffix/` (2026-08-24) | Worth exactly 0. The suffix sweep writes two journals per batch and only the per-domain form at `data/raw/cdx/cdx_suffix_*.jsonl.gz` is needed; the raw capture form here, 58 journals and 389,393,904 bytes over 46,779,589 lines, is the same observations in a shape nothing reads. |
| National web-archive indexes, three new doors (2026-08-24) | All three price below 1,000 EE, because a national archive's in-window holding is either an IA back-file donation we hold or a curated slice of institutions the baseline holds first. Library of Congress US Elections Web Archive, `data.labs.loc.gov/us-elections/by-year/2000/`, enumerable from `manifest.txt`, 3,521 gzipped SURT CDX files at 1,971,201,167 bytes, robots 404, genuinely not IA-derived. NLA ships taxonomy graphs with no hostnames; LAC Canada has 64 datasets and none is a web index; Memento aggregators are NXDOMAIN. The UKWA open-data inventory is exhausted end to end. |
| `arquivo.pt/robots.txt` breached (2026-08-24) | Line 752 carries `Disallow: /datasets` inside the `User-agent: *` block with `Crawl-delay: 5`, and only two agent blocks exist. Ten ranged GETs against `/datasets/linkgraphs/` breached it, and the same path disallows the original collection of `arquivo_ia` and `arquivo_roteiro` from `/datasets/cdxj/`. The data is held and the evidence stands; no further request may go to `arquivo.pt/datasets`. |
| Dated directories and navigation sites (2026-08-24) | Closed on arithmetic. Net-new pairs per listed domain: BUBL LINK 5 over 388 (0.0129, 1.96 EE), award galleries 5 over 206 (0.024, 3.16 EE), the Yahoo 1996-97 tree 11 pairs and 7.73 EE, the Zenodo printed-directory corpus 934 pairs and 432.81 EE over 7,600 domains, at 0.39 to 0.70 EE per pair. So 1,000 EE needs 83,000 to 154,000 distinct listed domains in one artifact, and only DMOZ, Yahoo and LookSmart/Snap were ever that big. |
| The UKWA host link graph truncation (2026-08-24) | Truncated by the archive, not by our download, and not resumable from this host. The local copy is exactly 2,147,483,648 bytes, `gzip -t` fails with "unexpected end of file", and the Wayback `id_` capture reports `content-range: bytes 0-0/20928588915`, so 10.26% of a master-eligible source has been read and that tenth paid 231,865 evidence rows over 183,515 domains and 116,467 assigned pairs. |
| Preserved software and documentation collections (2026-08-24) | Closed on a mechanism, best member 31.8 EE against a 1,000 EE floor: every in-window package format is build-generated in its structure and dates and carries no build-generated URL. Debian `Homepage:` returns 0 across all 36 in-window index files; CPAN `resources.homepage` exists on 0 of 15,871 in-window releases against 121,281 unfiltered by year. |
| InterNIC zone files at the `nic.mil` mirror, admitted (2026-08-24) | **Master, on the artifact alone: the SOA serial `1997041800` sits on line 2 inside the payload and the IA capture of 1997-04-20 fixes when the file existed. An NS record in `.org` is the delegation itself, the registry serving that name at that instant, so killer 2 does not reach a zone file.** All six zones re-verified, `gzip -t` passing and each ending on InterNIC's own `;End of file.` marker: `org` 154,141 lines, `edu` 12,132, `gov` 1,805, `mil` 301 at serial `1997041700`, `root` 1,316, `arpa` 35. Ingested at 12,503 net-new pairs and 8,993.1 EE, of which 12,320 are 1997 under serial `1997041800` and 183 are 1999 from a `gov` zone at serial `1999111901`, with 4,889 of the net-new names dated at no in-window year at all. |
| More InterNIC zone files, and `ftp.internic.net/domain/` (2026-08-18) | The population is six and we hold all six, so `internic_zone` cannot be widened. One CDX listing of `nic.mil/oroot.html/` returns the complete contents: `arpa` 694 bytes, `mil` 3,265, `root` 10,219, `gov` 16,251, `edu` 110,995, `org` 1,318,217, summing to 1,459,641 against 1,458,311 on disk. There is no `com.zone` or `net.zone` anywhere on the mirror. |
| The `.au` registry family: AUNIC, auDA, AARNet (2026-08-18) | Does not survive in bulk. AUNIC's archived footprint is 1,605 captures whose only domain-bearing shape is `aunicstatus.pl?domain-name=<name>`, extractable free from the CDX index: 104 such captures yield 17 distinct `.au` names. A capture of a lookup would be candidate-only in any case. |
| CDX public-suffix sweep as a bulk channel (2026-08-22) | Demoted from channel to trickle. Twelve swept suffixes, 159 MB of journal, reduce to 68,386 in-window registrable pairs of which 5,722 are net-new, worth 4,800 EE, and every net-new pair is `.ca` or `.us`: `co.uk` and `ac.uk` are saturated. The ceiling is structural, because the bare TLD is HTTP 403 so `.com` cannot be enumerated this way. |
| Common Crawl domain vertices as RDAP candidate supply (2026-08-22) | Admitted as a thin but genuine channel: not a dating source but a bulk supply of names to ask the registry about, our own RDAP engine supplying the date. `cc-main-2020-jul-aug-sep-domain-vertices.txt.gz` is HTTP 200 at 655,075,092 bytes and holds 88,591,818 domains in reversed-label form, of which 44,321,990 are registrable `.com`/`.net` and 40,989,363 are in neither the store nor the RDAP asked-ledger. A 19,987-query pilot answered 11,268 and returned 138 in-window pairs, 0.69% of queries. |
| Common Crawl 2018 minus 2020 (2026-08-22) | A real but small enrichment. The 2018 vertex file is HTTP 200 at 523,819,137 bytes holding 35,882,170 registrable `.com`/`.net`, of which 11,019,564 are absent from the 2020 file. A 19,918-query pilot gives 1.11% of queries returning an in-window creation date against 0.69%, a 1.6x gross lift, and 4.7 EE per thousand queries against 4.2, a 1.12x net lift. |
| RDAP registries other than Verisign (2026-08-23) | Most of the family closed. `.de`, `.jp`, `.edu`, `.se`, `.dk`, `.ch`, `.it`, `.eu`, `.co`, `.us`, `.nz`, `.za`, `.ie`, `.be`, `.at`, `.es` and `.hu` have no RDAP endpoint in the IANA bootstrap at all, silently removing 1.24 million store-known `.de` names. `.org` is excluded on record: PIR answered about 850 queries on 2026-08-08 and then returned 403 for 9,253 consecutive requests. `.au` and `.pl` publish nothing datable, 0 in-window from 25 sampled each. On 2,500 Common Crawl `.ca` names, 0.92% in-window and 100% net-new, 7.7 EE per 1,000 queries against 4.75 for `.com`/`.net`, so the true advantage is 1.6x rather than the 20x the gross rate advertises. |
| A registration SPAN from an RDAP creation date (2026-08-23) | Forbidden by rule 6 after being measured, and it is the largest thing this project has priced: applied to the 3,174,957 banked in-window creations the span would claim 11,038,108 pairs, of which 2,885,782 are net-new, worth 1,704,843 EE. Rule 6 holds that a creation date alone does not establish continued registration in any subsequent year. |
| `link_target` as a ranking signal for the archive queue (2026-08-23) | Admitted, needing no new approval, at 297 EE per 1,000 queries: it changes who we ask rather than what counts as evidence, since the resulting capture is `cdx_timestamp`. `link_target` stays candidate-only, 4,115,694 rows. Against the reviewer's own baseline, a link's year is confirmed 85.3% of the time. |
| RIPE database bulk dumps (2026-08-23) | GDPR dummification closes it, and the reason generalises to every RIR. On the full `ripe.db.mntner.gz` file, 64,310 objects, exactly one distinct email domain survives, `ripe.net`, appearing 120,470 times, every object carrying a "all data that is generally regarded as personal data has been removed" notice. Only 219 objects have a `created:` date. |
| CD-ROM media, browser installers and language-package archives (2026-08-23) | Closed on size: encyclopaedia discs, magazine cover discs and ISP signup discs are real, in window and downloadable as uploaded media rather than crawls, and three discs come to 495-2,475 EE. BackPAN has 1,216 distributions uploaded 1996-1999 yielding perhaps 240-360 domains, about 200 EE. |
| `textfiles.com` and FidoNet nodelists (2026-08-23) | Both rejected on date, not content. `textfiles.com` holds real hostname counts (3,100 pairs in `hosts.txt`, 2,237 in `ftp.txt`, 3,992 in the US domains file) and every one of those files is dated 1990 to 1992. FidoNet nodelists have the ideal weekly self-dating shape and give under 500 EE across the whole family. Check the date distribution before counting the content. |
| The darkened Dartmouth/NBER metadata item (2026-08-23) | It reopened and is worth zero. `archive.org/metadata/DARTMOUTH-NBER-RESEARCH-2017-metadata` now returns a 13-file listing with no restriction, and `domain-year-captures.txt` is 227,919,677 bytes, byte-identical in size to the copy on disk; the siblings are the same data plus two out-of-window rows, with identical 765,194 in-window distinct pairs. Through the canonical funnel, 764,982 canonical pairs and 0 net-new. |
| Zenodo banner-ad corpus, `zenodo.org/records/8408539` (2026-08-23) | Real, in-window, correctly shaped and too small. A 215 MB JSON of 22,915 banner images mined from archived snapshots of URLs taken from six printed directories published 1999-2001, **each `appearances` entry carrying a 14-digit Wayback timestamp beside the page URL, so a pair is `cdx_timestamp` and self-dating**. 92,218 in-window appearances become 12,353 pairs over 7,600 domains and 934 net-new pairs worth 432.81 EE. |
| AFNIC `.fr` OPENDATA back editions (2026-08-23) | The mechanism is wrong: measured on 202011 (494,444,288 bytes) and 202201 (549,508,248 bytes), taking only the creation year as rule 6 requires, each yields exactly the same 65,268 in-window rows, because OPENDATA is a snapshot of names currently registered at publication, so a domain deleted before 2020 appears in neither. Union 65,170 pairs, 57,511 already assigned, 7,659 net-new worth 781.98 EE. A back edition only helps when the publication is a cumulative register rather than a current-state snapshot. |
| SEC EDGAR beyond the closed row: 8-K, DEF 14A, 10-KSB (2026-08-24) | Real, in-window, dated by EDGAR itself and too small at 5,884 net-new EE, 2.0% of the gate. **One filing is dated by the `Date Filed` column of `full-index/<year>/QTR<n>/form.idx`, an EDGAR-assigned date, filtered before extraction**: 222,232 filings of these three types in window. The best-value unbuilt source on the register, and not a round. |
| Federal Audit Clearinghouse historic Single Audit filings 1998-2001 (2026-08-24) | Admissible and small: 2,406.69 net-new EE. **One item is one e-mail field on one filing row, dated by that row's own signature date, `AUDITEEDATESIGNED` or `CPADATESIGNED`**, which is a date a human wrote down, so it takes the corroboration split. The date check bites: the signature histogram runs 1997-2009 and 18,698 e-mail rows were dropped for falling outside the window, most of them FY2001 audits signed in 2002. Taking the audit year instead would have imported every one silently. |
| USPTO trademark bulk data, domain-name marks (2026-08-24) | Unretrievable, so unpriced: no bulk data could be fetched at all. Nothing measured and nothing claimed; the application filing date would have been a clean per-item in-window date, so a reopen condition is worth keeping. |
| UK Companies House bulk corporate filings (2026-08-24) | Out of window by construction: the Accounts Bulk Data files are named by publication date and the published range does not reach 1996-2001, and the Company Data Product is a current-state snapshot with no per-row filing date and no website field. |
| freshmeat.net dated backend RDF dumps (2026-08-24) | The right shape and the payload was never captured: `freshmeat.net/backend*` returns 50 in-window rows, all the `/backend/` index at 773 to 781 bytes or a 301 to it, and `freshmeat.net/*.rdf*` returns zero rows for 1997-2002. The kind is still worth pursuing: ask which publisher of a dated release feed had its payload captured. |
| ERIC education bibliography, `api.ies.ed.gov/eric` (2026-08-24) | Unretrievable, not closed. The API returns `{"message": "Network error communicating with endpoint"}` for a year-filtered query and for an unfiltered control, so the failure is the service. Re-probe, and if it stays down try the bulk XML exports. |
| FreeBSD Ports release trees, 1996-2001 (2026-08-24) | Closed at 50.56 EE gross and 29.57 post-split against an 8,000-18,000 estimate. Route is `ftp-archive.freebsd.org/pub/FreeBSD-Archive/old-releases/i386/<rel>/ports/ports.tgz`, twenty in-window releases at 7 to 14 MB, the release date fixing the year for every `WWW:` and `MASTER_SITES` line. Measured on three spanning releases: 3,231 distinct pairs, 3,134 already held, 97 net-new, 97% overlap because a ports tree points at the vendors every other source points at. |
| UK computing magazines on archive.org, every in-window issue (2026-08-27) | Real, small and now closed at **191.1 net-new post-split EE** over 247 pairs, censused rather than sampled: all 33 in-window UK issues (Internet Magazine 16, Practical Internet 14, PC Format 2, Personal Computer World 1) read in full give 11,639 pairs over 9,541 domains, 8,085 already held, 3,554 net-new before the split. `uk` 115 pairs, `com` 97, `net` 32. Typo bound 58.3%. **The transferable finding is a tool bug, not the source:** `probe_texts_corpus.py` builds `<identifier>_djvu.txt`, but these scans name the file `Internet Magazine 031 [1997-06]_djvu.txt`, so it reported 1 of 33 items reachable where the metadata shows 33 of 33. Any closure that leaned on its reachable share is understated. |
| Reuters RCV1 newswire (2026-08-27) | Not fetchable and screened at a few hundred EE, so the signature is not worth chasing. `trec.nist.gov/data/reuters/reuters.html` distributes it only by written request and signed agreement. Two independent bounds: the corpus spans 1996-08-20 to 1997-08-19, so it dates ONLY 1996 and 1997, where the store's adjacent headroom is 103,953 pairs against 6.7M at 2000-to-2001; and newswire is formal prose, which measured 0.00153 URLs per 1,000 words on Hansard, putting 806,791 stories of about 186M words in the hundreds of URLs, nearly all head names already held. |
| USPTO trademark bulk data, the wall named (2026-08-27) | Still unretrievable, and the 2026-08-24 row now has its mechanism: `bulkdata.uspto.gov` no longer resolves in DNS, `data.uspto.gov/ui/datasets/products/files/TRCFECO2/<year>/case_file.csv.zip` serves the 20,666-byte Angular shell for 2011, 2022, 2023 and 2024 alike, and `api.uspto.gov/api/v1/datasets/products/...` answers `{"message":"Unauthorized"}` with 401. Reopen condition: a human registers for an Open Data Portal API key. |
| Oregon State Bar Bulletin and Law Society of Ireland Gazette (2026-08-27) | Both closed on the ceiling, and both live hosts have stopped serving the window. OSB: 3.2 EE measured per issue x 24 monthly issues of 2000-2001 is about 77 EE, and `/publications/bulletin/`, `/publications/bulletin/00jun/` and `/publications/bulletin/archives.html` all return the same 32,677-byte "Site Error" page. Gazette: 2.6 EE per issue x about 50 in-window issues is about 130 EE, `/gazette/issues/` redirects to `/login`, and the PDF path that serves July 2026 404s for gazette-2001, gazette-1999 and gazette-2004. Neither is worth a Wayback run. |
| `discmaster.textfiles.com` as a CLASS rather than a query (2026-08-27) | Priced at **1,055.3 EE**, which is the lens's whole yield: one banked-pending artifact (`antispam_media_blocklist`) and a `.jp` listing rejected at 185.3, against an index of 1,718,970,121 files already recorded saturated by filename and by size. Its robots.txt is `User-agent: * / Disallow: /` followed by a note exempting researchers making "somewhat limited or somewhat targeted" requests, so a sweep is refused by the directive and a targeted query is all the note allows. |
| Convention on Biological Diversity meeting documents (2026-08-27) | **Closed at a ~6.0 EE ceiling for the entire in-window tree**, and the enumeration blocker is gone: `/doc/meetings/<body>/<meeting>/<series>/<meeting>-[inf-]NN-en.pdf` resolves directly, so the JavaScript shell and the 401 on `api.cbd.int/api/v2013/documents` never had to be beaten. 54 in-window PDFs read in full (23 official, 32 information, 12 meetings 1996-2001) give 272 pairs over 195 domains, 250 already held (91.9%), 22 net-new before the split and **6 pairs / 4.0 EE after it**, with a 68.4% typo upper bound on those. **Two transferable findings, and the first would have produced a false zero.** (1) The density screen splits 24x WITHIN one corpus: the official/plenary series measures 0.0476 URLs per 1,000 words over 84,005 words, near Hansard, while the information series measures **1.1191, which is 3.3x ERIC** and passes the density screen outright. Screening a UN body on its plenary documents alone reads as prose and closes a corpus that is not prose. (2) It dies on a screen neither ERIC nor Hansard tests: **a ROSTER corpus saturates at the size of the membership, not the number of documents.** The payload is the national focal-point contact list, the same ~180 countries reprinted in every meeting, so distinct domains go 115 at 8 docs, 143 at 16, 154 at 24, 194 at 32, a clean log fit of `-7.3 + 57.0*ln(docs)` that reaches only 292 at the ~190 in-window INF documents the index probes bound it to. 1.5x the domains for 6x the downloads, so the whole tree is ~6.0 EE gross-of-junk and ~1.9 EE after the typo bound. Density is per-word and the ceiling is per-member, so a roster can pass density and still be worthless. Do not re-test. |
| National web archive bulk indexes: Czech WebArchiv catalogue and KB Labs Denmark link graphs (2026-08-31) | **Closed at 0 EE.** `https://labs.statsbiblioteket.dk/linkgraph/` (`1998_to_2003/linked.js` and `uk_domains/linked.js`, 78,328 and 257,312 distinct domains) is structurally undated, no field carries a year, only an aggregate range straddling the window; `https://www.webarchiv.cz/en/browse` prints no date at all. Nothing dates one item. |
| Preserved private mailbox media, the two surviving academic home-directory FTP tarballs (2026-08-31) | **Closed at 2.0 EE**, whole-class figure (enumerated, not sampled). `https://discmaster.textfiles.com/search?format=mbox&tsMin=19960101&tsMax=20011231` (`pisa.cs.york.ac.uk` and `ftp.cs.ucla.edu` `Mail/` stores). Each message's own `Date:` header dates one item; 98.3-98.7% already held. |
| Shared-hosting control-panel (Plesk/cPanel/WHM) default page listings via arquivo.pt (2026-08-31) | **Closed at 0.45 EE**, 1 net-new pair. `https://arquivo.pt/textextracted?m=http://www.dominios.com.pt//20010928033101` and two siblings; the Plesk page's machine-written text plus the capture's `cdx_timestamp` dates one item, but the surviving pages are all "hosting not configured" errors, the semantic inverse of a roster. |
| The IA "Web Data Services" extraction family, both arms (2026-08-27) | Priced and closed, and it is law 1 measured on the corpus most likely to beat it. The ccTLD arm is one member: `Poland_pl-ccTLD_2001-12-31`, 19 items of about 10.8 GB, `access-restricted-item: true`, and `.pl` weighs 0.107. The in-window value is its `earlygovweb` sibling `USFEDGOV-EXTRACT-1996..2001`, public, with a 0.5 MB `.arc.os.cdx.gz` beside every 100 MB ARC so a year prices without touching a payload. **1996, all 99 parts: 647,995 CDX rows every one stamped 1996, 2,660 hosts, 287 registrable domains, all 287 already held at 1996, zero net-new.** 2001, 253 of 5,802 parts: 2,158,981 rows all stamped 2001, 11,369 hosts, 735 registrable domains, 633 held at 2001, 102 net-new pairs and 100.2 EE self-dating, and the domain space saturates hard enough (25 parts gave 452 domains, 253 gave 735) that the whole 754 GB item is a few hundred EE at the ceiling. A 754 GB extraction of the 2001 federal web, dated by the archive's own stamp, at 0.9825 per pair, pays nothing because the baseline came from the same archive. |
| **Era DNS query traces, and a catalogue's own date field as a one request screen (2026-08-28)** | **Closed at 0 EE, a structural zero: no member of the family yields a single extractable hostname, so there was never an item to date and no pricing against merged260827 was reachable.** The screen counted artifacts, not names. (1) **CAIDA, the entire catalogue settled in ONE request**: `api.catalog.caida.org/v1/` GraphQL returned all 154 datasets with `dateStart`/`dateEnd` in one 22,995 B POST; exactly 10 start before 2002 and all 10 are address or number instruments (`iana_as_numbers`, `iana_asn_allocations`, `iana_ipv4_addresses`, `iana_ipv6_addresses` 1997-11, `as_relationships_serial_1` 1998-01-01, `skitter_traceroute` 1998-07, `mawi_internet_traffic` 1999, `skitter_aslinks` 2000-01-02, `backscatter_tocs_originals` 2001-01-01, `telescope_codered_worm` 2001-07-19). The earliest DNS dataset of any kind is `dns_root_gtld_rtt` at **2002-01-08** and its per-site table has zero in-window days anywhere (UCSD 8 Jan 2002, Auckland 11 Nov 2002, CU 8 Jan 2003, WIDE Tokyo 1 Jun 2004, WIDE Fujisawa 13 Jul 2005). (2) DNS-OARC: DITL runs 2006-2018, LTQC Nov 2009 to Jul 2010, zone files since Mar 2009, AS112 since Jan 2015; OARC did not exist before 2004. (3) Internet Traffic Archive, robots.txt 0 B so fully open, holds **no DNS trace at all** across 15 collections; its two client-side web traces were screened anyway because they log requested URLs, and both fail: BU-Web-Client keeps unanonymised hostnames but spans 21 Nov 1994 to 8 May 1995, wholly outside the window, while UCB Home IP is in-window (1 Nov 1996 to 19 Nov 1996, 9,244,728 references, 8,377 clients) and records `GET 9168504434183313441..gif HTTP/1.0` against server `207.36.205.194:80`, the URL replaced by a hash and no Host header retained, **killed by law 6**. (4) NLANR and WIDE/MAWI not re-tested, already closed at lines 569 and 685, and `mawi.wide.ad.jp/robots.txt` disallows the sample points, `/mawi/ditl/` and `*.gz` regardless. (5) **archive.org a proved zero with a term-matched control**: the ten-term title clause (`"query log"`, `"root server"`, `"dns trace"`, `named.log`, `qname` and the rest) `AND date:[1996-01-01 TO 2002-12-31]` on `advancedsearch.php` returns numFound 0, the identical clause without the date filter returns 8 and all 8 classify as RFCs 8806/7816/9156, two arXiv papers, one PubMed article, one undated audio item and a 2021 perma.cc capture, so zero data artifacts and none in-window; live controls `title:(dns)` 89 and `title:(internet)` 10,083. **Method, and it is the transferable result: a dataset catalogue's own machine-readable date field screens the whole catalogue for window overlap in ONE request, before any download, permission request or sample.** CAIDA publishes `temporalCoverage` as schema.org JSON-LD per dataset page and exposes `dateStart`/`dateEnd` over GraphQL; Dataverse, CKAN, Zenodo, DataCite and re3data all carry the same field, so **any future "we found an era traffic dataset" lead should be settled against it before anyone asks for access**. Second: **read the DATA host's robots.txt, not the catalogue's**, since `catalog.caida.org` is `Allow: /` and `www.caida.org` blocks only `/cgi-bin/` while `publicdata.caida.org` is a flat 27 B `Disallow: /`, and that is the host you actually need. Third, closing the last opening the line 569 record-layout law left: **a DNS-specific instrument is not a name-bearing instrument, and the dataset title will not tell you which.** NeTraMet is an RTFM flow meter (RFC 2722/2723), so a ruleset pointed at DNS still keys flows by server address and stores a `ToTurnaround` timing distribution with the question section never retained; the QNAME-as-payload assumption is true of a packet capture and false of a flow meter, and the two are indistinguishable from the name. **Read the attribute list or ruleset, not the title. Do not re-test root or gTLD server traces, NeTraMet or DITL on any screen.** |
| **Bulk harvested email address CD-ROMs, and the attest-twice screen (2026-08-28)** | **Closed at 199.5 net-new post-split EE over 368 pairs from 423,309 addresses, and that figure is itself not admissible because the year is not evidenced; the family closes on AVAILABILITY, since the entire preserved inventory of the class is two discs and both are 1996.** Artifacts, both fetched: `archive.org/download/250000-email-business-directory/...iso` 156,940,288 B (13 `MAIL/DISK*.ZIP` plus an unpacked tree, 2,099 unique content files, 30,638,007 B of address text) and `archive.org/download/email-address-data-base/...iso` 8,865,792 B. **The inventory was ENUMERATED, not sampled**: discmaster's own title lists are 35,798 CD-ROM titles (`/cd-rom/All Genres`, 11,573,824 B in one request), 5,980 disk titles and 1,675 FTP items, and grepping all 43,453 offline for `e-?mail|bulk|million|address|mailing|leads` returns exactly these two address discs plus phone books and unrelated software, **nothing dated 1997-2001**. Corroborated four ways: `advancedsearch.php` over `cdromsoftware OR cd-roms OR vintagesoftware` gives 145 items and none is an in-window address list; `"bulk email" AND date:[1996-01-01 TO 2002-12-31]` gives 2, both 2002 tucows software; discmaster filename search in-window at `family=text sizeMin=1MB` gives `*emaillist*` 0, `*addresses*` 0, `*emails*` 0, `*maillist*` 2 (a 1993 list-of-lists) and `*email*` 1 (the `.jp` listing already rejected at 185.3 EE); discmaster CONTENT search for "bulk email", "extractor pro", "floodgate", "spammer" and "hotmail" surfaces no address list. Controls that the instrument works: `q=email.domains` returns the known-positive `.jp` file and `q=*maillist*` two real 1996 files. **The two discs are ONE harvest sold twice**: different vendors and formats, but 59,907 of the Media Net disc's 62,609 host strings (95.7%) are also on the 250,000 disc, and the second adds 75 domains and 6.5 EE over the first, so more discs of this shape would add nothing even if they existed. Priced with `price_items.py --all-tlds` against merged260827 sampling DISTINCT DOMAINS: union 423,309 addresses to 72,149 host strings to **20,214 registrable domains, 18,676 already held (92.4%)**, pre-split 1,538 pairs / 903.2 EE (do not quote), **post-split 368 pairs / 199.5 EE**, mean weight 0.5422, all at 1996, `com` 84 `net` 34 `edu` 30 `org` 24 `de` 23 `se` 16; an independent duckdb count agreed to within 8 domains and 3 pairs. **Why even that fails the date bar**: the bulk corpus carries no date in any byte, no `199x` string and nothing in the `.PML` headers, so the only stamp is the ISO-9660 mtime, and on the one component that DOES state its harvest date the mtime is wrong. `MAIL/DB/{CN,COM,DE,EDU,JP,TW}.DBF` are dBase tables with an `E_DATE` harvest date per address; their maximum equals their mtime to within a day on all six, validating the mtime as a genuine write stamp, but **13,913 of the 13,914 dated records are 1995 and exactly one is 1996** (TW.DBF 1996-05-26, the single record dragging the maximum up), ranges 1995-03-02 to 1995-11-29 for COM and EDU. So a file written in 1996 holds a 1995 harvest and the media stamp overstates the harvest year by a year; internal evidence agrees (2,614 addresses at `anon.penet.fi`, which ran to 1996-08, plus `gnn.com`, `delphi.com`, `prodigy.com`). **And the yield sits in the singleton tail**: requiring a domain to be named by at least two distinct addresses keeps 9,605 of 20,206 (47.5%) and collapses 368 pairs / 199.5 EE to **35 pairs / 13.0 EE**, so 90.5% of pairs and 93.5% of EE ride on domains named once, which is where the mistyping is (`cs.somoma.edu`, `dier.umb.pur.edu`, `mcs213h.csumr.edu`, `csbusun.unisy.com.tw` are all single-occurrence typos of real hosts, each already held at 1997-2001 so the split does not remove them); the typo upper bound read 57.7% and did not say where the damage was. **The counterfactual is the number worth keeping**: the same 20,214 domains priced at 2001, population held constant and only the year moved, give **2,892 pairs and 1,664.9 EE, 8.3x the 1996 reading** (adjacent check 1,983 of 2,891 held at 2000). Rates: 0.0477 domains per address; 1996 0.0099 EE per domain, 2001 0.0824. So 5,000 EE needs ~10.6M addresses dated 1996 or ~1.27M dated 2001, and the "twenty million addresses" discs of 2001 would have cleared the bar easily; **they are not preserved anywhere reachable, which is the whole finding.** One adjacent negative measured rather than assumed: `16 Million Businesses Phone Book (1998 Edition)`, ISO 504,240,128 B with ABI data files totalling 486 MB at mtime 1997-07, downloaded and grepped for **zero occurrences of `.COM`, zero of `www.` and zero domain-shaped strings**, so line 703's "no in-window edition is digitised" half is refuted while the closure stands for the stronger reason that the product has no website or email column at all; **do not pull the 2.4 GB 88 Million Households sibling.** Four reusable methods: (1) **enumerate an index's own title list before querying it**, the `ls-lR.gz` trap applied to a disc archive, which found both discs in seconds after five filename and six content queries had found nothing; (2) discmaster has a **CONTENT search** (`qfields=t` over text, OCR and archive comments, composing with `tsMin`/`tsMax`/`family`/`sizeMin`, `mode=quick` in seconds) that line 813's "by filename and by size" closure did not use, a second lens on 1.72 billion indexed files; (3) **a distinct-domains-to-addresses ratio near 1.0 means the file is corrupt, not rich**, since a naive regex read 70,728 hosts from 76,606 `@` matches and produced `aol.cominfo.comc.co.uke.com.e.army.mil` out of 109-byte Delphi records whose length byte must be read, so check the ratio before trusting any extraction; (4) **the attest-twice screen for any harvested or OCR-derived corpus**, re-pricing on domains named by two or more distinct addresses, which removed 93.5% of the EE here. **Do not re-test bulk address CD-ROMs, spamware sample lists or email-directory discs on any screen.** The unfollowed thread, which belongs to a different hypothesis: discmaster content search kept returning in-window raw mboxes on period media (`rubberstampers.9708` through `rubberstampers.9804` at 9.7 to 16.5 MiB, `usr-tc.9804` 4.1 MiB, `rubbertown.9801` 2.7 MiB, `lwmail.txt` 10.7 MiB), and line 721 closed bulk mailing-list archives because pipermail strips the `Received:` chain, which a raw mbox does not. |
| **CD metadata submission dumps, the entry a ripping client wrote when a user submitted a disc (freedb / CDDB, `freedb-complete-20140101.tar.bz2`) (2026-08-29)** | **CLOSED at 32 EE for the whole 840 MB artifact, and the hypothesis's own stated kill condition fired: zero of 50,474 sampled entries carry an address in the header.** `Submitted via:` names a program and nothing else (`ExactAudioCopy v0.95b4`, `FreeRIP 2.96`) because cddbd strips the submitter, so the predicted population of home and ISP mail domains does not exist in the corpus. Hosts survive only where a human typed one into a free-text field, 1,341 of 50,474 entries (2.66%), 371 distinct, 0.0074 distinct domains per entry, and the population is ripper-vendor self-advertisement rather than the long tail: `www.feurio.com` 376, `synapsa.com` 249, `www.panic.com` 69, then fan pages. Every one of those takes the corroboration split, so a machine-written corpus fails the split anyway because its only host-bearing field is the one a person types. **THE DATING DIES SEPARATELY AND HARDER.** The tar member mtime looked per-entry and is not: minimum mtime 2001-07-08 11:51:58 UTC in the `data` window and 2001-07-08 12:19:57 UTC in the `rock` window, 28 minutes apart in two independently drawn slices, nothing before it, and 1,426 of 2,085 in-window entries (68.4%) stamped that day. That floor is one filesystem rebuild, so 1996 through 2000 are structurally absent from every freedb artifact and five of the six target years are unreachable. The hypothesis's second route, first appearance across a monthly dump series, has no series in window: archive.org's 71 `freedb` hits top out at complete dumps for 2014, 2018, 2019 and 2020 and the incremental item starts 2018-11-01. Priced with `scripts/pricing/price_items.py --all-tlds` against merged260827 on the 2,085 in-window entries: 43 pairs over 43 domains, 27 held, 16 net-new gross for 7.0 EE, **11 net-new post-split for 3.4 EE**, mean weight 0.3121 below the 0.4 floor, typo upper bound 68.8%; projected over the artifact's ~136,000 in-window entries, power law 32 EE against linear 224 EE, quote the lowest. Sampled on DISTINCT DOMAINS per the rule, not rows: 26 domains, 25 held in some year (96.2%), **23 already held at 2001 (88.5%)**, leaving held-and-missing-2001 at 2 domains, 8.0% against the population P(store lacks 2001 | held) = 0.611, **7.6x worse than average**, and both of the two are held at 2000 as well. The honest subset is worse still: keeping only the 717 entries stamped after 2001-07-09, the only ones whose mtime is plausibly per-entry, gives 16 pairs, 9 held, 5 net-new post-split for **1.3 EE** at mean weight 0.2543. **Method, and it is the reusable part.** (1) A `.tar.bz2` on archive.org can be sampled from the MIDDLE for one range request: `curl -r 500000000-506000000`, then `bzip2recover` on the fragment, which finds block magic at arbitrary bit offsets and rebuilds each block as a valid stream, then anchor the tar by locating `ustar` at offset 257 and walking 512-byte records. Two windows from different regions cost 12 MB against 841 MB and caught a category bias one window would have hidden (`data` 0.92% in window, `rock` 6.97%). (2) **A per-item filesystem mtime is a container date until you have looked at its floor.** Plot the minimum and the modal day of any mtime series before treating it as evidence; here that turned a claimed per-entry observation instant into a single rebuild carrying two thirds of the population, and the two windows agreeing to within 28 minutes is what proves it, where one window alone would have read as a plausible date distribution. Do not re-test the 2018, 2019 or 2020 dumps: they are later rebuilds of the same tree with the same floor. `gnudb.org` was never fetched past robots.txt, which carries `User-agent: ClaudeBot` / `Disallow: /` and refuses us by name, so it is not worth Ivo's approval either. |
| **Era bulk whois and zone dumps, the 2000-2002 compilation that would know the names that died before a modern one was built (2026-08-29)** | **CLOSED at 0 EE on ARTIFACT EXISTENCE, not on value or on evidence class: six independent existence layers, every one zero against a working control.** The evidence class was never the problem: `Record created on` inside a registry's own port-43 record is `whois_creation`, master-eligible and already approved, and dates its creation year only. The nearest retrievable thing is `https://raw.githubusercontent.com/apache/cloudberry/main/src/bin/gpfdist/regress/data/exttab1/whois.csv`, a genuine harvested-whois extract with `creation_date`, `harvest_date` and `raw_record` columns, stamped `2006-06-16` and holding 23 records of which 2 have in-window creation years (`007cybermall.net` 2001, `000askexperts.net` 2000): four orders under the volume floor, not priced. The six layers: (1) the zx.net.nz frozen-mirror whole-host index re-grepped for THIS lens, 28 requests for 1,319,576 URLs over 67 preserved FTP sites including gatekeeper.dec.com, ftp.netscape.com and ftp.funet.fi, gives `whois` 40 hits all of them the CLIENT, and `domain-info`, `domain-contacts`, `domains.txt`, `rwhois`, `nicname`, `internic`, `dropped`, `domain-list` all 0, with every non-zero decoy false (`dbase` is dBASE, `netinfo` is a NeXT utility, `domainlist` is Compaq's `DomainList.class`); (2) archive.org `advancedsearch.php`, never `scrape`, gives `com.zone` numFound 0 and 11 / 295 / 125 hits for the whois-database, domain-names and whois-subject queries, all of them client software, cover discs, AFNIC 2020 dailies and RFCs; (3) GitHub code and repo search, a surface the project had not used, gives 545 hits on the exact InterNIC record format and 674 on its expiry pair, every one a PARSER, and 0 repos for four dump queries; (4) the Wayback availability API at four spread timestamps, positive control `www.isi.edu/in-notes/us-domain-delegated.txt` returning `20010606153725` in the same batch, gives NONE for `ftp.internic.net/domain/{com,net,org}.zone.gz`, `rs.internic.net/domain/` and the two InterNIC list files, while `ftp.internic.net/domain/` has exactly ONE capture, `20181216053022`, returned identically for 1997, 1999, 2001 and 2003 and carrying 40 modern root-zone files, which is killer 4; (5) ftp.icm.edu.pl, the one host proved to keep a withheld central corpus, carries no registry or whois tree at all under `/pub/doc/` and 404s on ten canonical index paths; (6) archive.debian.org `Contents-i386.gz` for five whole releases in five requests, 16.8 MB, hamm 1998 through sarge 2005 and 3,023,149 file rows, holds zero bulk registry data files, and the CONTROL PASSES because junkfilter, squidguard and chastity-list are all indexed there, so no unexploited domain-list package exists in Debian 1998-2005. Store measurement, live read-only, is what an artifact would have had to beat: 25,727,428 in-window pairs of which `whois_creation` supplies 3,053,584 over 2,988,424 distinct domains, and in-window domains carrying NO `whois_creation`-dated year are `com` 4,884,608, `de` 1,479,865, `net` 510,107, `org` 458,717, a GROSS ceiling of 3,087,561 EE in `com` alone that must never be quoted as a yield. That is now the largest measured ceiling in the register with no public route to it. Method, and it is the transferable part: **`archive.debian.org/debian/dists/<release>/Contents-i386.gz` indexes every installed file of an entire Debian release in ONE request**, so five releases settle "did anyone ship this data as a package in the era" for five requests; it is the `ls-lR` trick applied to a distribution and, unlike `ls-lR`, not extinct, but Contents lists installed files and NOT tarball payloads (squidGuard's blacklists ship inside the orig tarball), so a bare zero without a known-positive control in the same grep is a false zero. **GitHub code search is a real new surface that structurally cannot find bulk data**: it does not index files over roughly 350 KB and never indexes `.gz`, so use it for parsers and small fixtures and never for dumps. **`archive.org/wayback/available` asked at four spread timestamps discriminates "never captured" from "captured once, late"** in four requests with no CDX: the same modern timestamp returned for every year asked is the signature of a current-state capture. **Re-grepping an already-known whole-host index for a new lens's signature is nearly free** and converts one lens's zero into another's. Do not re-test: the zx.net.nz index for any registry signature, archive.org metadata for `com.zone`, Debian Contents for any domain list, `ftp.internic.net/domain/` in Wayback, ftp.icm.edu.pl for a whois tree, or GitHub for a bulk dump. Only non-public routes remain, and one joins a thread already open: Ben Edelman ran bulk whois in 2001-2002 for his renewals and typo-domain studies and the project is already awaiting his answer on the CIPA report terms, so one letter asks both; the other is a RIPE-NCC-shaped permission letter to a 2000-2002 gTLD zone or whois holder, meaning ICANN, Verisign, or a university that held a zone-file access agreement in 2001. |
| **Directory link-checker reject lists, the nightly output of an editor-run directory's robot rather than its listing (goguides, joeant, zeal, skaffe, searchking, jayde, ODP editor hosts) (2026-08-30)** | **CLOSED at 0 EE over 0 distinct domains.** 96 `archive.org/wayback/available` probes for `deadlinks.txt`, `badlinks`, `unreachable`, `dead.txt`, `linkcheck.log` and seven more filenames across 8 hosts returned 0 captures, against two passing controls: the API answered the capture the register already records (`www.webring.com/cgi-bin/webring?ring=railring&list` at its recorded `20000422003921`), and 6 of 7 hosts are captured at their root inside 2001 (`skaffe.com` only from 2003), so the zero is about the artifact and not about coverage. **The reason is structural and closes the whole shape: a directory's checker output is exactly what its operator hid in robots.txt, and IA honoured robots.txt, so it was never crawled.** `www.jayde.com`'s robots.txt captured `20010531125046` names `/cleanup1/`, `/logs/`, `/lists/` and `/analog3.11/`, and 16 of 16 probes under those paths have no capture in any year; `dmoz.org` disallows `/editors/`, whose only capture is 2011, outside the window. The single in-window capture of a disallowed operator page, `www.zeal.com/category/pending_websites.jhtml` at `20010605162926`, is parameterised and renders "There are no websites pending addition to this category", 0 hostnames. **Method, and it transfers to every operator-side lens: read the archived robots.txt of a dead host to ENUMERATE the machine-written artifacts that existed, then expect precisely those to be missing from the archive.** Robots.txt is a reliable index of what was on the server and an equally reliable index of what IA declined to fetch, so the better a lens sounds the less likely it is retrievable. This also settles the one variant `docs/sources.md` left open when the link-rot family closed: a dated recheck log over a non-citation population. Do not re-test any directory-side checker, log or queue output. |
| **The Wayback availability endpoint as a SECOND dating engine, and the 2.4M undated pool it was aimed at (2026-08-30)** | **FIND on the ENGINE at 1,494 EE/hour, CLOSED on the TARGET at 114 EE/hour, 99.52 net-new post-split EE in hand, and PARKED on the two-client cap.** `https://archive.org/wayback/available?url=<domain>&timestamp=YYYYMMDD`, JSON at 100 to 260 B; what dates one item is `archived_snapshots.closest.timestamp`, the 14-digit capture stamp the Wayback index wrote when the crawler fetched the page, and never the top-level `timestamp` the caller echoed in. 1,798 requests, 1,786 200s, 12 `429` honoured, `web.archive.org/cdx` never touched; `archive.org/robots.txt` is 238 B and 12 lines, read whole first, one `User-agent: *` group disallowing only `/control/` and `/report/` and no Claude-named group. **Rate independence, the load-bearing claim, holds**: 1,600 requests at 2 workers took 1,261.8 s = **1.27 q/s at 0.67% throttled, measured in the same minutes both cdx collectors were running**, against `cdx_gtail`'s own log at 600 queries per 32.5-minute batch = **0.308 q/s at 27.3% throttled** (batch starts 11:26:41, 11:59:14, 12:33:46, 13:05:19, and 13:38:26 confirms the cadence), so one availability client is **2.06x both cdx collectors combined** on a budget that did not throttle while theirs did. Graded against cdx ground truth already on disk with no new cdx requests: 204 cdx year-pairs over 150 domains, 187 recovered = **91.7% recall, 94.3% at 2001, and 40 of 40 cdx-negative domains came back empty**. **The hypothesis's target is dead**: the undated pool is 2,410,144 domains and **98.4% of it is `usenet_address_mention` (1,216,749), `usenet_mention` (1,034,270) and `usenet_bare_mention` (120,466)**, all four counts reproduced here; 600 distinct domains at 19990101 gave 31 in-window (5.17%) against 97.5% on 40 interleaved controls = 0.0264 EE per query, **114 EE/hour, closed**, and the 214,959 `.edu` / 186,965 `.mil` / 186,076 `.gov` names in it against a real `.mil` namespace of about ten thousand say why: it is anti-harvester munging and Message-ID parse noise, not names. **Re-aimed it is 13.1x better**: 6,568,275 domains held at 2000 and missing 2001 (4,137,392 in com/net/org/uk, re-measured here, not the 4,256,799 first claimed), 240 sampled by hash order at ONE query pinned to 20010701, **132 hits = 55.0% = 0.3459 EE per query = 1,494 EE/hour** over a queue worth ~1.43M EE gross. Priced against the live store over all journals: 415 capture pairs on 335 domains **sampled as DOMAINS, never as `domain_year` rows**, 251 already held (the by-construction trap, those journals are ingested), **164 net-new, every one at 2001, 99.52 EE**, no split because a capture stamp is master evidence. Two defects reproduced and both make a zero a lower bound and never a false year: status-200 captures only, and `www.` canonicalised away. **Nothing was retained and nothing is ingested**: banking any of it means standing a third bulk archive client against a rule that caps them at two, so it parks as `wayback_availability_2001` pending Ivo. **The 2.06x rate ratio is the WRONG statistic and is corrected here, in both directions.** cdx returns a whole year set per query and availability pinned at 2001 returns at most one year, so in GROSS in-window pairs per hour cdx WINS, 1.583 pairs per query x 0.616 q/s = 3,511 against 2,515. But priced in NET-NEW EE the order reverses: 1,200 `cdx_gtail` queries out of the 10:33 and 11:05 journals, which are **not yet ingested** (the last ingested is 09:26, so this is a real pre-ingest snapshot and not the by-construction trap), gave 1,900 in-window pairs of which 1,386 were already held, **514 net-new pairs and 324.90 EE = 0.2707 net-new EE per query = 600 net-new EE/hour for both cdx collectors combined**. Availability is **0.3459 net-new EE per query and 2.5x cdx per hour**, and it wins per query as well as per second because its queue guarantees a hit is net-new while cdx's does not: 73.0% of what cdx returns is already held. The per-query win is narrow, 132/240 is 55.0% +/- 6.3% so the lower bound is 0.306 against 0.2707, and the two queues are not identical. **So the cost of the ruling is now a number: retiring one cdx collector costs 300 EE/hour and buys 1,494.** Five methods, in `discovery.md` order of use: prove a second endpoint is a second budget by running it WHILE the first is throttled and comparing throttle rates, 0.67% against 27.3%; **price a proposed collector against the incumbent in net-new EE per QUERY off an un-ingested journal, never in q/s, because rate and gross pairs mislead in opposite directions here**; grade a dating engine against cdx journals already on disk, since `data/raw/cdx/*.jsonl.gz` is a free labelled set whose NEGATIVES are what turn "it answers" into "it does not invent"; price a candidate pool by `GROUP BY discovered_source` before pointing anything at it, which predicted the 5.17% from one query; and the unit is one query pinned at 20010701, not a six-year enumeration (0.208 pairs per query) and not a midpoint screen. |
| **Distfiles and orig-tarball archives, the container the Debian `Contents` sweep could not see (2026-08-30)** | **CLOSED at 33.6 net-new post-split EE, and the premise was checkably wrong.** `http://archive.debian.org/debian/pool/main/b/bookmarks/bookmarks_0.16.tar.gz` (47,485 B, plus the potato 0.10 and slink 0.5 tarballs); what dates one item is the tar member mtime the packager's tar wrote inside the orig tarball (`bookmarks-0.16/public_html/national/national-de.html`, `Jul 2 2001`), the same stamp class that dated chastity-list. `Contents-i386.gz` was only one of two screens: `docs/sources.md:643` used `Packages.gz`, which reads descriptions, and that is what found chastity-list. Two new screens covered the rest. **(1) Orig-tarball slack**, invented here: `Sources.gz` gives every orig tarball's size, `Packages.gz` gives `Installed-Size`, and `orig_size - installed_bytes` is large exactly when a package ships a payload it does not install. Over hamm/slink/potato/woody main+contrib+non-free, **11,807 source packages**; the control passes, `squidguard_1.2.0.orig.tar.gz` is 1,852,659 B against 425,984 B installed and ranks **8th**, and nothing else near the top of any release is a hostname list. Run slack and Contents or neither: an installed payload (chastity-list, slack -2.3 MB) is invisible to slack and visible to Contents, and squidGuard is the reverse. **(2) A description grep wider than the blocklist regex** over all **18,694** binary records in five releases finds one new family, `bookmarks`, three in-window editions, 2,754 URLs, **2,537 pairs over 1,592 distinct domains, 91.6% already held**, priced by `scripts/pricing/price_items.py --all-tlds` against merged260827 at **197 net-new post-split pairs, 33.1 EE** (mean weight 0.1682; `de` 179 / `com` 11 / `net` 3), failing on weight not volume since its one big file is `national-de.html` and `.de` is 0.1324. `adzapper` adds **0.5 EE** (170 pairs, 99.4% held) because a hand-kept ad-zapper names doubleclick, yahoo and akamai; `adzap` ships no list at all, visible offline from its `pkg-plist`. The three other BSD package universes hold nothing Debian's does not: FreeBSD ports 2.2.8/3.4/4.4 (9,849 distfile names), pkgsrc at `2001-12-31T23:59:28Z` and openbsd/ports at `2001-12-31T21:01:23Z` return only ijb, squidguard, adzap, squirm and rblcheck, and neither BSD tree carries squidGuard. **Mirror access became moot before it was needed, which is the transferable move: screen the CATALOGUE, not the mirror**, eight requests settle what payloads exist. Robots read in full first and the correlation in CLAUDE.md holds: `ftp.netbsd.org` and `cdn.netbsd.org` are 169 B disallowing every distfile by extension, `distcache.freebsd.org` and `download.freebsd.org` serve 25 B of `Disallow: /`, and `archive.freebsd.org`, the one FreeBSD host with no robots.txt, kept the releases and threw the distfiles away (`ports/distfiles/` holds one 185 B `README.txt`). A ports tree's `PLIST`/`pkg-plist` files are a free name list when data files are named after domains, and sweeping all **13,913** plists in five trees lights up exactly one package, the suspect already known. Do not re-test NetBSD or FreeBSD distfiles, `bookmarks`, `bookmarker`, `bk2site`, `gnobog`, `browser-history`, `adzap` or `adzapper`, and do not re-list the squidguard or chastity-list pool directories, which hold exactly one in-window orig tarball each. Loose end worth nothing on its own: `bo` (1997) has no `Sources.gz`, and 1997 is the year with the least headroom. Point any revival of the slack screen at sarge and etch, where 2001-dated uploads sit in the pool. |
| **Period anti-spam filter rulesets, junkfilter's unread siblings (2026-08-30)** | **CLOSED at 16.2 net-new post-split EE, dead on volume rather than on dating.** `https://web.archive.org/web/20011024110400id_/http://www.spambouncer.org/sb.tar.Z` (161,578 B, 55 files); what dates one item is the tar member mtime inside the archive (`Oct 15 2001`), agreeing with the in-file `Version 1.4 / Last Updated 10/07/01` header and the capture stamp. SpamBouncer across four editions spanning 13 months and both major versions is **306 distinct registrable domains in total**, 469 pairs, 89.3% held, **26 net-new post-split pairs = 16.2 EE**, all held names gaining 2001; the 2000 editions alone price at **0.0 EE**. The block's premise that `sb.rc` carries hundreds of spam source domains is wrong by two orders for the value that matters, since the 227 KB v1.2 file yields 146 domains and the rest is header regex plus CIDR. The series saturates rather than compounds: 146, 165, 257, 306, so forty releases would not reach 1,000 names against the ~2,600-held-`com` screen. Siblings confirm the shape: Axel Boldt's Blacklist of Internet Advertisers (2000-08-15) is 128 domains, 99.2% held, **0.0 EE**; Hardin's Sanitizer is **0 domains**. **The discriminator for the whole class, and it costs one directory listing: does the release ship a SEPARATE bulk names file, or are the names typed inline in the recipes?** junkfilter paid 2,189 EE because Sutter machine-aggregated other lists into one 42,034-token `jf-domains`; every author-typed ruleset is a 10^2 corpus, because that is how fast one person types. Second reusable piece: enumerate Wayback captures without touching `/cdx` by anchoring the replay path at a dozen dates and reading the 302 `location:` header, which names the real capture stamp; twelve requests mapped the series. Third: a 302 to a capture is not a 200 from the origin, `20010625195049` resolves and serves an archived 404 body. Every file of every edition was read whole and priced by `scripts/pricing/price_items.py --all-tlds` against merged260827, counts distinct throughout; robots read in full first, all spambouncer hosts 404 or `/wp-admin/` only, `www.ii.com` names CCBot, GPTBot and four others we are not. Do not re-test SpamBouncer, BOIA or the Sanitizer. The 16.2 EE is admissible under the standing rule if anyone wants the 26 pairs and is not worth a collector; the only branch with headroom is a filter in junkfilter's shape whose distribution contains a file that is nothing but names, screened by directory listing before any download. |
| **Preserved server configs on media: a shared host's `named.conf`, vhost list or `virtusertable` IS its customer list (2026-08-30)** | **CLOSED at 8.9 net-new post-split EE at the permissive ceiling and 1.9 EE on the defensible reading, against merged260827 read live at 15,624,381 domains / 25,741,400 domain_year.** `https://discmaster.textfiles.com/search` over 1,722,834,293 indexed files; what dates one item is the per-file media mtime discmaster records inside the CD-ROM, disk image or FTP tarball (`artifact_listing`), which is sound and is not why this fails. The population was ENUMERATED and not sampled: fourteen config filenames at `tsMin=19960101&tsMax=20011231&limit=1000&dedup` give 2,769 rows collapsing to 2,708 distinct contents (`obj.conf` 688, `httpd.conf` 530, `sendmail.cf` 401, `named.conf` 334, `access.conf` 314, `srm.conf` 177, `squid.conf` 115, `named.boot` 96, `mailertable` 34, `virtusertable` 29, `magnus.conf` 27, `sendmail.cw` 16, `vhosts.conf` 5, `local-host-names` 3), and all 912 text files under 400 KB were fetched, 13,125,700 B in about 955 requests at 1.4 s spacing, at most two in flight. Directive-borne extraction (`zone "X"`, `ServerName`, `ServerAlias`, `<VirtualHost`, `Cw`, `dstdomain`, every bare line of a virtusertable) yields 816 hostnames over 912 files, **0.89 per file, with 635 of 912 yielding ZERO and the richest yielding 27**, against the block's own kill line of ten zone lines per file: 144 pairs over 111 domains, 114 held, 5 net-new post-split pairs, 1.9 EE at mean weight 0.3823, under the 0.4 floor. The permissive ceiling, every hostname anywhere in the file including comment prose and documentation URLs, is 625 pairs over 343 distinct domains sampled AS DOMAINS, 68.5% known and 66.5% held, 22 net-new post-split pairs, **8.9 EE, the raw figure overstating 16.7x**. **Method, and it is the transferable part: run the DEPLOYED-ONLY INVERSION TEST before downloading anything.** Pick filenames that can exist only because somebody configured a live machine, sort the in-window hits by size, read the top three paths. Over 21 such signatures (the fourteen above plus `ssh_known_hosts` 76, `known_hosts` 82, `.rhosts` 269, `hosts.equiv` 84, `exports` 999, `ntp.conf` 295, `.netrc` 470, `hosts.allow` 107, `resolv.conf` 334, `vhost` 403, `httpd.conf.bak` 1, `named.conf.bak` 0, `dstdomain` 0), **21 of 21 have as their largest hit the program's own source file, man page or installer package, never one instance of the file it writes**: `http_vhost.c` and `mod_vhost_alias.so` are what `vhost` matches, `make-ssh-known-hosts` is what `ssh_known_hosts` matches, `sendmail-cf` cpio archives are what `sendmail.cf` matches. **Preserved media is a record of SOFTWARE DISTRIBUTION and not of running systems, because nobody ever pressed a working `/etc`**, which is the browser-cache law ("what survives is a SHIPPED profile, never a used one") holding on the server side, and it kills every future hypothesis of the form "somebody's real configuration survived on a disc" with no download. Two cheaper screens fall out: discmaster's format facet is a one-request class-existence test and has **no category at all** for a Unix server config (nearest are `sendmailM4` 40,898, `windowsHostsList` 60, `proxyAutoConfig` 28), so such a signature is filename-reachable only; and on a config corpus price the permissive every-hostname ceiling FIRST, since the two readings differ 4.7x here and both round to nothing, so the directive parser was wasted work. `q` with `qfields=name&mode=deep` is a substring match on the filename and `limit` (default 100, silently truncating) and `dedup` are ordinary parameters, so one request per signature returns the whole in-window population already collapsed by content hash. Robots read in full first, 267 B, `Disallow: /` with the operator's written carve-out for limited targeted research automation. Do not re-test BIND `named.conf`/`named.boot`, Apache `httpd.conf`/`srm.conf`/`access.conf`/`vhosts.conf`, Netscape `obj.conf`/`magnus.conf`, sendmail `sendmail.cf`/`sendmail.cw`/`virtusertable`/`mailertable`/`local-host-names`, `squid.conf`, or any deployed-only host list on preserved media, on any lens. |
| **Hostname-shaped directory paths on preserved media, where the index alone is the dataset and nothing has to be opened (2026-08-30)** | **CLOSED at 0.74 net-new post-split EE, and the kill is the 2001 screen rather than novelty.** `https://discmaster.textfiles.com/search?extension=[htm|html]&tsMin=19960101&tsMax=20011231&dedup=dedup&sortBy=b3sum&limit=1000&outputAs=json&pageNum=N`, 40 pages, 39,998 b3sum-random index rows, ~155 MB of JSON, from a population of 7,845,390 in-window matches; what dates one item is the per-file media mtime discmaster records as `ts` on a FILE row (`artifact_listing`), the same stamp the `.url` route was admitted on, since the hostname directory itself carries no date. The signature is real and abundant and it prices at nothing. Strict arm, path segments literally matching `www.`/`ftp.`/`home.`/`members.`/`pages.`/`users.` plus a registrable domain in a TLD that existed in the window: 568 of 39,998 rows (1.42%) carry one, giving 262 pairs over 236 distinct domains, **230 of 236 (97.5%) already held**, the highest held fraction measured outside an authority corpus; ten pairs missing, eight on domains held in no year and taking the corroboration split, **two surviving at 0.74 EE** (`fsmlabs.com` 1998, `sudety.info.pl` 2001), gross 4.49 which must not be quoted. **The kill: of 107 held `.com` names, 4 lack 2001, P = 0.0374 against a control of 0.6022 recomputed here on the same baseline-plus-store union, 16x worse.** The loose arm (any hostname-shaped segment) reads identically at lower purity, 420 pairs over 372 domains, 82.8% held, 1.76 EE, P = 0.0365, and its extra 136 domains are mostly extension false positives rather than hosts (`.ng` is 21 Norton Guide help files, `.ch` is chapter files, `.in` is include files). An independent directory arm, 999 of 35,476 `q=www&qfields=name&family=directory` rows, gives 142 registrable domains, 91.5% held, P = 0.0769, and an absolute ceiling of 67.50 EE if every held domain were dated at every year it lacks. **Both estimators bound the family at 16 to 145 EE for ~7,800 paged requests**: per-row, 0.74 EE on 0.51% of the population scales to ~145 and overstates because distinct-domain growth saturates; per-domain, the directory arm bounds the whole `www.` population at ~5,038 distinct hostnames and 0.0032 EE per held domain puts it at ~16. Sanity-checked against known positives before believing any zero: `apple.com`, `geocities.com` and `newtek.com` return all six years. Method: `family=directory` turns discmaster's 138,989,831 directory rows into a hostname index that costs no downloads, but the hypothesis's premise is **false at the schema level**, because a directory row has `filename`, `folderid`, `subCount`, `itemName`, `whenAdded` and **no `ts`**, so it can neither be dated nor windowed and about half the novel names it surfaces come from post-2001 media (`rockbox.org`, `hitachigst.com` off 2003-2005 cover discs); only FILE rows carry `ts`, and a file row carries the whole path in `fileid`, so the correct query is a dated file search with paths mined out of `fileid`. Any path-segment miner must be filtered to TLDs that existed in the window, since the pinned PSL admits `.zip`, `.cab`, `.help`, `.guide`, `.app` and an unfiltered first pass read 486 `.zip` and 126 `.cab` "domains" out of 743, the 2013-gTLD trap in a new place. The prediction was head selection and the hope was "names we hold that may lack 2001": they are head-selected, and they do not lack 2001, because people mirrored the sites they liked and the store covers the head at every year it was alive. Closed, and paging 7.8M rows to reach a 16-145 EE ceiling is in any case a sweep `discmaster.textfiles.com/robots.txt` refuses. Do not re-test offline browser trees or mirror directories on preserved media; the untouched variant is a proxy spool (`wwwoffle`, MS Proxy `urlcache`), which is a fetch log and not a curated mirror, and that is the office_proxy_access_log lens. |
| **Apache/BIND/sendmail vhost-config file shapes on a public FTP mirror, via its `locatedb` (2026-08-31)** | **CLOSED at 0 EE.** `https://ftp.gwdg.de/pub/locatedb` (8,830,468,956 B), eight Range-sampled 5 MB windows, 1,050,903 path fragments. Zero of fourteen deployed-config signatures (`httpd.conf`, `named.conf`, `virtusertable`, etc.) found. A `locatedb` also has no mtime, so nothing found there could have been dated anyway. |
| **PAC/wpad proxy-config files as a machine-read hostname list (2026-08-31)** | **CLOSED at 0 EE.** No artifact fetched: the Wayback CDX index is SURT-keyed, so "every URL ending in `/proxy.pac`" has no server-side key range and cannot be queried. Also fails on content: names split into unguessable intranet hosts (no year, corroboration split) and the org's own domain (already dated every year). |
| **CDX-tier cost re-ranking of the query queue, scan vs host-hit (2026-08-31)** | **CLOSED at 0 EE.** `data/raw/cdx/*.jsonl.gz`, 1,179 files, least-squares fit of per-tier seconds. Scan tier costs 1.26x a host query (not the claimed 16x) and returns 0.021x the years; the queue's score has no entropy within a hash-assigned shard, so no re-ranking key can move it. |
| **Proactive `ls-lR`/`locatedb` index sweep across surviving FTP mirrors (2026-08-31)** | **CLOSED at 0 EE.** 105 hosts robots-screened, 31 permitted hosts probed at 22 canonical index paths, 682 requests: exactly 1 host serves a whole-tree index (`ftp.gwdg.de`, already closed above at 1.0 EE over 399 domains). Lens has no population. |
| **ICANN monthly registry reports / registrar accreditation filings / dispute annexes / escrow specs (2026-08-31)** | **CLOSED at 0 EE.** Three of four arms already separately closed in the register (52.90 EE/report, 0.0294 EE/listed domain, banked as `udrp_wipo`); the one with in-window volume is pure aggregate counts, and the gTLDs that would carry name lists (`.biz`/`.info`/`.name`) all launched in 2001 so can never hold a Y-1-missing-Y pair. |
| **Domain-name tables compiled into shipped ISP/dialler/newsreader software on preserved media (2026-08-31)** | **CLOSED, projected 200-500 EE, not banked.** `discmaster.textfiles.com` filename census, nine signatures, 2,370 in-window rows. Inversion test on the ten largest files per signature: only 2 of 10 are genuine compiled-in tables, the rest is the software's own documentation. Fetch cost (10-25 min) exceeds the take-it floor for the projected yield. |
| **Reading reviewer benchmark-release diffs to fingerprint other contributors' sources (2026-08-31)** | **FIND, 0 EE from the diff itself (already merged by construction).** `comm -13` across 11 consecutive `feedback/**/merged*` releases named three populations worth pursuing: UMN DRUM `EARLYWEB_1996_2000` parts 01-02 (3.72 GB unfetched, projected ~50,000-103,000 EE), a `verified_submission_0817` contributor, and an unidentified 2001-dated all-`.co.uk` register listing of 58,546 names (~57,451 EE) inside the 260820->260821 increment. |
| **RDAP-liveness tiebreaker for the query queue, verified and patched but not wired (2026-08-31)** | **FIND, 0 EE banked (a ranking method, not a source).** Verifies the `sources.md:590` finding: 99.851% of the `.com` gap population already carries an RDAP verdict, 61.775% live, reproducing the banked figures to within 0.05pp. Worth +97 to +161 EE/collector-hour on the gap arm; a full three-file patch to `build_query_queue.py` is written in the finding but not applied (write barrier). |
| **NYPW TimeMaps, re-priced on the 2000 partition instead of the saturated 1996 one (2026-08-31)** | **FIND at 4,146.8 net-new post-split EE over 6,424 pairs**, reopening a 2026-08-24 closure (14.2 EE) that tested only the 1996 folder. `https://archive.org/download/nypw_timemaps/2000/nypw_timemaps2000_deeplinks_part00o.tar.gz` and `.../rootURLs_part02r.tar.gz`; what dates one item is field 3 of each TimeMap row, IA's own 14-digit capture stamp (`cdx_timestamp`). Folder year = first-capture year, so 1997-2000 folders add years to domains the store already holds; 1996 and 2001 folders are dead by construction (already tested). |
| **Archived DNS-delegation announcement lists, the Usenet enumeration (2026-09-01)** | **CLOSED at 8.8 EE post-split**, 0 on the delegation route. `archive.org/download/<item>/<file>` over 8 `*.domain*` items from `advancedsearch.php collection:usenet AND identifier:*domain*` (35 candidates, all checked); what would date one item is a registry's own `Date-Approved:` line, verified against the banked `can.domain` positive control (37,578 hits) and returning 0 on every candidate. `aus.net.announce.mbox`/`uk.net.news.announce.mbox` are in-window but carry no registry records, 10 pairs post-split. |
| **CERT/CC and vendor security-advisory hostlists (2026-09-01)** | **CLOSED at 0 EE.** `web.archive.org/web/20011107010342id_/http://www.cert.org/advisories/CA-2001-26.html` and `.../incident_notes/IN-2001-08.html`; the advisory's own release-date line dates one item, but every named host is a vendor or reference (`symantec.com`, `microsoft.com`, `cisco.com`), 16 of 16 already held at 2001. Closes the whole CERT/CIAC/AusCERT/DFN-CERT advisory shape by disclosure policy. |
| **Distro and CPAN/CTAN mirror-roster masterlists (2026-09-01)** | **CLOSED at 10.8 EE post-split.** Six rosters via `web.archive.org/web/<stamp>id_/`, e.g. `http://www.cpan.org/MIRRORED.BY@20000816230113`; CPAN's own RCS `$Id:` line dates one item. 945 pairs over 850 domains, 95.8% already held at the roster's own year. |
| **IRC network client server tables, `servers.ini` census on discmaster (2026-09-01)** | **CLOSED at 0 EE.** `discmaster.textfiles.com/search?q=servers.ini&qfields=name&mode=deep&tsMin=19960101&tsMax=20011231` (361-file in-window population, 18 sampled); the file's own media mtime dates one item. 795 pairs over 446 domains, 89.2% already held at 2001, 0 net-new post-split. |
| **ISP dial-up POP and settings tables (2026-09-01)** | **CLOSED at 0 EE.** `web.archive.org/web/20010617113627id_/http://www.psi.net:80/` and `.../concentric.net:80/support/`; the Wayback capture instant dates one item. 88 hostnames collapse to 25 registrable domains (one owner per table), 24 of 24 already held, 5 missing 2001. |
| **Looking-glass and peering-exchange hostname lists (2026-09-01)** | **CLOSED at 6.3 EE post-split.** `web.archive.org/web/20010609195743id_/http://www.traceroute.org/`; the Wayback capture instant dates one item (crawler-imposed, not operator-chosen). 603 pairs, 96.2% already held at 2001, same death as the banked IRR dump. |
| **NTP stratum-2 pool and newzbot roster (2026-09-01)** | **CLOSED at 0.3 EE post-split.** `web.archive.org/web/20010604180258id_/http://www.eecis.udel.edu/~mills/ntp/clock2.htm` and its 1998 edition; the capture instant of a current-membership roster dates one item. 196 pairs over 127 domains, 98.0% already held at the artifact's own year. |
| **Public NNTP server poller pages, Freenews (2026-09-01)** | **CLOSED at 1.0 EE post-split.** `web.archive.org/web/<stamp>id_/http://freenews.maxbaud.net/newspage.html?date=today`, 11 editions 1999-2001; the poller's own "last checked" line plus the generated `?date=` field dates one item. 258 pairs over 223 domains, 97.7% already held including the artifact's own year. |
| **Closed-lead reprobe: retired cost figures and availability re-checks (2026-09-01)** | **CLOSED at 0 EE, a register audit not a source.** Re-derived 8 verdicts citing the retired ~3,000 EE/hour rate or 33s CDX cost; all hold under the 2026-09-01 remeasure. 22 unexpected-200 reprobes against closed hosts, none reopenable (robots or content kills, not availability). |
| **Package-manager build-recipe URL fields, ports-tree measurement carried over (2026-09-01)** | **CLOSED at 40.8 EE post-split.** `codeload.github.com/freebsd/freebsd-ports/tar.gz/<sha>` and NetBSD/OpenBSD siblings, 168 MB, 51,191 port directories; the checkout's own commit stamp dates one item. 4,044 domains, 94.91% already held at 2001; predicts the same failure for Debian `watch`, Gentoo `SRC_URI`, RPM `Source0:` without fetching. |
| **Ad-blocking hosts-file lists beyond the two banked (2026-09-01)** | **CLOSED at 1.3 EE post-split.** `web.archive.org/web/20010609130035id_/http://accs-net.com/hosts/Downloads/hosts_july9.txt` and 3 sibling editions; the compiler's own "last updated" line dates one item. 425 domains, 96.7% already held at the artifact's own year; every other free-filter-list publisher (squidGuard's siblings, pgl.yoyo.org, urlblacklist.com) has no in-window Wayback capture. |
| **Whois-server and TLD-registry-index file census, discmaster (2026-09-01)** | **FIND at 9.7 EE post-split, parked pending, not banked**: fails standing-rule condition 2 (dating is a media mtime `discmaster` renders, not a stamp written into the artifact's own bytes). `discmaster.textfiles.com/search?q={whois.conf,whois-servers,tld_serv_list,whois.txt}&qfields=name&mode=deep&tsMin=19960101&tsMax=20011231`, complete 14-file in-window population. 880 pairs over 468 domains, 22 net-new post-split, all 2001, 79.3% one-edit-from-held (figure is an upper bound). |
| **Retired-screen audit: 403 closed leads for the actual retired-bar class (2026-09-01)** | **CLOSED at 12.0 EE reprice, 0 bankable.** `data.freebsd.org/pub/FreeBSD-Archive/old-releases/i386/<rel>/ports/ports.tgz`, the release tarball's own dated tree fixes the year (`dated_directory`). 26 novelty-screen-worded pre-08-25 verdicts found, all already resolved; one real retired class found (~15 verdicts closed only under the retired 5,000 EE bar), ~250 EE live total, not worth banking against the gap. |
| **Second-tier what's-new directory feeds, 44 hosts (2026-09-01)** | **CLOSED at 0.6 EE post-split.** `www.zeal.com/zealist/websites_newest.jhtml` served 20010607080441 and `www.beaucoup.com/1reveng.html` served 20010607202649, the served Wayback capture stamp dates one item (`dated_directory`). 281 distinct domains, 279 already held (99.3%), 1 post-split pair; Zeal's listings route through a click-tracker redirector and beaucoup contributes little; population enumerated, not sampled, family closed. |


## `usenet_announce` and `usenet_mention`: dated website announcements from Usenet

Giganews' 2013 Usenet donation to the Internet Archive: dated posts quoting site URLs. Full per-group
`.mbox.zip` files in the hierarchy items, e.g.
`https://archive.org/download/usenet-comp/comp.infosystems.www.announce.mbox.zip`, sha1 per file.
**Dating: each message's own `Date:` header, with the globally unique `Message-ID` as the evidence
value, so a reviewer can name the exact post behind any year; intrinsic to the artifact rather than
recovered from a crawl.**
`usenet_announce` (`dated_directory`) where another source already places the domain in `domain_year`;
the URL beside the date is human-typed, so it takes the corroboration split and a Usenet-only name is
`usenet_mention` (`link_target`) in the candidate pool. 2,017,182 evidence rows, 1,022,707 domains;
seams `usenet_address` 62,820.7 EE, `usenet_bare` 28,460.3 EE.

Closed:

- Per-date Giganews exports: `comp.infosystems.www.announce.20140404.mbox.gz` holds nine posts, all
  2005 to 2010.
- `Message-ID`, `Reply-To`, `Sender`, `NNTP-Posting-Host`: 2,869 net-new of 1,025,582 pairs, 1,038.4 EE.
- `Path:`: 13.89 EE on a 400-archive sample, and none before 2000.
- `alt.irc`, `alt.music.oasis`: HTTP 500 or 502 on every attempt, 0.05% of the corpus.
- `alt.*` screening: 1,013 EE per GB against a corpus mean of 1,065.

## `tucows_catalogue` and `tucows_mention`: the Tucows Software Library

32,600 items donated to archive.org in 2004, 11,499 in window, each with a release `date` and a
`creator` field holding the vendor's home page URL.
`https://archive.org/services/search/v1/scrape?q=collection:tucows+AND+year:[1996+TO+2001]&fields=identifier,date,creator&count=10000`
**Dating: the item's own release `date`, a structured catalogue field rather than free text, with the
identifier as the evidence value, openable at `https://archive.org/details/<identifier>`.**
`tucows_catalogue`, split because the catalogue was donated in 2004, so a `creator` URL may record
where a vendor lived then rather than at release: 942 net-new pairs entered the annual files, 746
domains the candidate pool as `tucows_mention`.

Closed: Winsite `INDEX.TXT` (7,057 entries, zero vendor domains), Programmer's Library `FILES.txt`
(no URLs), CNET Download.com (zero vendor URLs), SimTel (216 GB tarball, no author domains in its CD
indexes): pre-web in design, settling the CD-ROM catalogue family.

## `maillist_archive` and `maillist_archive_mention`: public pipermail list archives

Pipermail per-month files of raw messages with headers, from `mail.python.org/pipermail/` and
`mail.gnome.org/archives/`: 2,558 in-window month files, 579,808 messages dated 1996-2001.

```bash
uv run python scripts/sources/mail_corpora/collect_mailing_lists.py --harvest --write
uv run ark ingest maillist_dated      data/raw/maillists/maillist_dated.jsonl.gz
uv run ark ingest maillist_candidates data/raw/maillists/maillist_candidates.jsonl.gz
```

**Dating: each message's own `Date:` header, read per message, not the month in the filename: a month
file is assembled by arrival, so 450 of 581,323 messages date outside the window and are dropped.**
`dated_directory` corroborated, `link_target` otherwise: a mail body is human-typed, so it takes the
corroboration split. 1,458 net-new pairs, 833.17 EE.

Closed for breadth: 0.00145 and 0.00121 EE per in-window message against 0.0067 for Enron, 83.6% of
pairs already held. `lists.debian.org` no bulk month file, `lists.samba.org` 426, `sourceware.org`
403. `lore.kernel.org` HTML is behind Anubis, but `git clone https://lore.kernel.org/lkml/0` serves fine
and public-inbox stores each sender as the git commit author: 81,741 in-window messages, 92.2% held,
80.19 EE post-split.

## `enron_email` and `enron_email_mention`: the FERC Enron corpus

FERC's Enron email release, about 500,000 messages from 150 employees, mostly 1999-2002.

```bash
curl -L -o data/raw/source_probe_260806/enron.tar.gz \
  https://www.cs.cmu.edu/~enron/enron_mail_20150507.tar.gz
uv run python scripts/sources/mail_corpora/collect_enron.py --write
uv run ark ingest enron_dated      data/raw/enron/enron_dated.jsonl.gz
uv run ark ingest enron_candidates data/raw/enron/enron_candidates.jsonl.gz
```

**Dating: each message's own `Date:` header, carried inside the message rather than assigned by a
reader; out-of-window messages are dropped rather than pulled in, since the corpus runs past 2001.**
`dated_directory` corroborated, `link_target` otherwise: mail bodies are human-typed, so this takes
the corroboration split. 5,134 net-new pairs, 3,241.9 EE, 0.0067 EE per in-window message.


### `attrition_defacement`: the attrition.org web defacement mirror

Web defacement mirror, January 1999 to 21 May 2001: date, defacer, organisation, defaced hostname. Republished as `attrition-org/web-hack-mirror`; its 33 index pages sit at `data/raw/source_probe_260806/attrition/`, and `just attrition` replays them without a request.

```
https://raw.githubusercontent.com/attrition-org/web-hack-mirror/main/mirror/{1995,1996,1997,1998}.html
https://raw.githubusercontent.com/attrition-org/web-hack-mirror/main/mirror/{1999,2000}-{01..12}.html
https://raw.githubusercontent.com/attrition-org/web-hack-mirror/main/mirror/2001-{01..05}.html
```

Skip the 265 per-TLD and per-defacer breakout pages: same rows re-sliced.

**Dating: each row opens with a two-digit date the mirror stamped on the day it captured the defacement, then the host in parentheses, and a defaced host was serving that day.**

```
[99.11.30] Li [potus] Coronus Networking ( www.coronus.com )
```

Evidence type `artifact_listing`, self-dating, no corroboration split. Ingested: 5,816 net-new pairs, 2,791.4410 EE.

### `pandora_titles`: the National Library of Australia title index, as candidate seeds

PANDORA's Title Entry Page index, 87,732 rows of `tep_id, name, gathered_url, surt`, CC0, at `data/raw/pandora-titles/`. <https://github.com/GLAM-Workbench/trove-web-archives-titles>

```bash
uv run python scripts/sources/directories/seed_pandora_titles.py    # -> pandora_hosts.txt
uv run ark seed data/raw/pandora-titles/pandora_hosts.txt
```

**Dating: none. No date column of any kind, so nothing in it can evidence a year; seed-only permanently.** Evidence type: none. 29,432 names unknown to the store enter the candidate pool claiming nothing.

### `udrp_wipo`: WIPO domain-name dispute decisions, 1999-2001

Every UDRP case, the disputed domain in its own column of the case table.

```bash
uv run python scripts/sources/directories/collect_udrp_proceedings.py  # -> items.jsonl, one row per case
uv run python scripts/pricing/price_items.py --items <items.jsonl>
```

**Dating: the filing year encoded in the provider-assigned case number, not the decision date, since a case filed in 2000 may be decided in 2001 and the domain existed at filing.** Read the second table cell only; stop on repeated pages, not empty ones.

Readings: `artifact_listing`, self-dating, 5,389 pairs, 3,281.0 EE; `dated_directory` with the corroboration split, 956 pairs, 593.5 EE. Store holds all five providers: WIPO 5,963 rows, NAF 2,575, DeC 210, eResolution 133, CPR 42.

Closed here, ceiling about 90 EE, never reopen on availability:

- NAF `adrforum.com/domain-dispute/search-decisions`: 200 but client-side only, no server-rendered index.
- A larger NAF shortfall: Zenodo 21310923 counts 1,426 Forum decisions for 2000-2001 against 2,573 NAF domains held.
- ICANN `archive.icann.org/en/udrp/proceedings/domains-list.txt` (4,666,685 bytes), `proceedings-list.txt` (2,924,147 bytes): 8,662 in-window pairs, 90 net-new.
- Zenodo 16954717 `full-udrp-parsed-proceedings.jsonl.gz`: 158 net-new, 90.10 EE, and its `submitted` field is corrupt (`D2002-0431` carries 1999-08-26), so trusting it fabricates 518 1999 pairs.

### Bytes already on disk that nothing reads

- `pandora-titles/`, 13 MB: seeded, seed-only; `data/raw/pandora/` is a byte-identical copy.
- `source_probe_260806/hathitrust_ef/`, 12 MB: 73 extracted-features files pulled before the HathiTrust rejection, never measured, so that rejection does not cover them.
- `source_probe_260806/attrition/`, 2.7 MB: the 33 defacement index pages, ingested.
- `usenet_hdr/`, 40 MB: already in the store under `usenet_addr_*`; a reproduction gap only, no `SourceSpec` replays its 19,224 evidence rows.
- `source_probe_260806/scripts/` and `logs/`: the 6 August measurement scripts and their output, which is the 938 MB.

### UK Government Web Archive

The National Archives' CDX endpoint, UK government sites from 1996.

```bash
curl -sS -A '<a browser User-Agent>' \
  'https://webarchive.nationalarchives.gov.uk/ukgwa/cdx?url=number-10.gov.uk&matchType=domain&from=1996&to=2001&limit=5&fl=timestamp&output=json'
```

An honest project UA gets `302 Found` with a 0-byte body; a browser UA answers in 0.23 s. `from`, `to` and `filter=statuscode:200` are honoured.

**Dating: the CDX `timestamp` the crawler writes at the moment of capture.** Evidence type `cdx_timestamp`. Earliest coverage seen `mod.uk` 19961111.

**Verdict: real but tiny.** Government-only, seven for seven non-government hosts absent, addressable population 250 domains, so the collector costs more than the answers.

---


## archive.org FTP mirror archives, 2001 mtimes inside the ZIPs (closed, zero)

**Proved zero.** 638 of 651 ZIP/TAR FTP-mirror archives listed; 143,338 rows dated 2001 across 254 of them, so the control passed, yet all 69 filename-regex hits on 2001 rows are false positives. Vhost variant zero too: the one archive clearing 200 domain-shaped segments is a DOS `.COM` executable collection.

---

## JISC UK Web Domain Dataset per-year CDX (`ukwa.ds.2/cdx/`): found, sized, NOT retrievable

One CDX file per year 1996 to 2013 at `webarchive.org.uk/datasets/ukwa.ds.2/cdx/`, 13.45 GB compressed in-window, which would be `cdx_timestamp` with no split. Not retrievable: IA never captured the files, the publisher serves stubs, archive.org returns `numFound` 0, and the British Library replied on 2026-07-22 that access returns Autumn 2026, first stage a per-URL lookup. Reopen when bulk access returns.

**Banked from the same dataset, 9.81 EE:** `bl-uk-linkage.tsv` (724,598 bytes) and `york-ac-uk-linkage.tsv.gz` (2,244,274 bytes) in `linkage/`, `year|source|target` rows read by the existing `ukwa_link_source` parser. **Field 1 is the crawl year written by the archive's link-graph extractor, not asserted by a person.** Class `link_source`. 1,899 and 3,731 in-window pairs, 99.9% and 99.8% already held, 1 and 9 net-new pairs. Target-selected at `bl.uk` and `york.ac.uk`, so they do not re-price the full `host-linkage.tsv.gz`.

---

## namewinner.com expiring-domain list, 2001-10-26 (PRICED, needs a Decision)

Dotster's expiring-domain auction list, `http://namewinner.com/whole_list.php?del=tab`, capture `20011026120205`, 20,943 distinct registrable domains, 25.6% held. **Every row carries the per-item date `25-OCT-01`: the file holds 20,945 occurrences of that string and no other date string of that shape, printed by the registrar's own expiring-domain system, and the capture fixes the instant at 2001-10-26 12:02 UTC.** A soon-to-expire listing states the name is registered now. Class `artifact_listing`, master-eligible; a registrar database dump, not human-typed, so no split. Master reading 18,951 net-new pairs, 11,555.0 EE.

2002 sibling filed separately: capture `20020407171418`, 52,204 domains dated `05-APR-02` to `10-APR-02`, needing the one-year-term inference to reach 2001, 4,134 pairs and 2,543.2 EE. Only four of 21 `whole_list*.php` captures carry content, the rest 373 to 415 bytes.

dailychanges.com: `ns=LAME-DELEGATION.ORG&date=2002-08-01` is 4,511 names at 66.7% held, 1,076.3 EE, against 0.021 EE per name on four registrar pages, so held-fraction tracks the age of the nameserver's population.

Closed: `deleteddomains.com` list endpoints are 3.0-3.4 KB query forms with no result set; `snapnames.com` lists sit behind `/protect/` login; `pool.com` is one domain per page; `unclaimeddomains.com` has no TLD and no date; `deletedomains.com` largest capture 2,987 bytes; `domainstate.com` zero CDX rows 2001-2003; `dotster.com` no bulk list in 2,583 captures. `domainsbot.com` is untested, CDX never answered.

---

## US Domain delegated-subdomains list (PRICED, needs a Decision), and the ISC survey closed for good

`us-domain-delegated.txt`, the US Domain Registry's list of delegated `.us` zones, at `pub/rfc/` inside the `2015.04.ftp.isc.org.tar` mirror on archive.org and at `www.isi.edu/in-notes/us-domain-delegated.txt`, captured 2000-08-15, 2000-12-06, 2001-04-11 and 2001-06-06 (last three byte-identical at 435,847 B). **Dated twice by machine: tar-preserved member mtimes (1996-10-09, 1996-11-20, 1999-03-22, rotations `.0`-`.5` from 1999-02-19 to 1999-03-18, monotone in both date and size), and `cdx_timestamp` on the 2000 and 2001 captures.** Class `artifact_listing`, master-eligible. Union 13,816 net-new pairs post-split, 12,775.5 EE. Typo upper bound 17.8%; 1997 and 1998 unreachable. `ftp.isc.org` robots.txt is a blanket `Disallow: /`, so use the mirror only.

ISC Domain Survey closed permanently: `www/survey/archive-data/` ends at `9707.domains.gz` and every file in `reports/1998/` through `reports/2002/` is 1.4-21 KB of per-TLD counts. `enterprise-numbers` (339,504 bytes, 1999-03-22): 2,348 of 2,445 domains already held, 48.1 EE.

FTP-mirror-ZIP gaps closed: ranged ZIP64 central-directory reads listed all 9 truncated ZIPs complete, three zero-row archives are corrupt uploads at exactly 4,294,967,295 bytes, and the single bulk host artifact in all 42 is the `us-domain-delegated.txt` family.

---


## Academic repositories and DOI datasets: CLOSED, by enumeration through five APIs and two registries

DRUM's six-item "Link Lists for Websites" family and DataCite filtered on `dates` in 1996-2001 both leave only DOI `10.13020/d62684`, already ingested; Harvard Dataverse file-level search returns 0 for `domains.txt`, `hostnames.txt`, `zonefile`, `hostlist` and ten more shapes; re3data's 3,521 repositories give three name matches, all held or priced. `api.osf.io/robots.txt` is a blanket `Disallow: /`, so OSF is closed on robots, not content.

## Dated internet-trade directories (ISPs and web hosts): closed, and the ceiling is the reason

Across every 1996-2002 capture of `thelist.com` and `thelist.internet.com` the largest object of any kind is a 14,390-byte GIF, and the whole 1996-2001 ISP and news-server population is a few thousand hosts already held, returning zero novel domains over 73,751 Usenet messages. Boardwatch's ISP Directory volumes stay blocked because `..._djvu.txt` returns a 146-byte stub; reopen on a route to those volumes' text.

## discmaster by FILENAME: reopen condition resolved, and the lens is saturated

`qfields=name`, `mode=deep` and `YYYYMMDD` dates make the endpoint answer in about 5 seconds, discharging the recorded timeout closure. Nine filename queries over an index of 1,718,970,121 files return only already-priced squidGuard lists and software furniture, and `email.domains` returns exactly one file, the `.jp` listing rejected at 185.3 EE. Preserved media is exhausted by name and by size.

### us-domain-delegated.txt

The `.us` delegated-domain zone list, five in-window editions (1996-10-09, 1996-11-20, 1999-03-22, 2000-08-15, 2001-06-06). The 2001 edition fetched live at 435,846 bytes with 6,512 zone rows: `web.archive.org/web/20010606153725id_/http://www.isi.edu/in-notes/us-domain-delegated.txt`; the 1996 and 1999 editions sit at `pub/rfc/` inside `archive.org/details/2015.04.ftp.isc.org`. **The file carries no in-body date, so the edition is fixed by the crawler-written capture timestamp in its filename, and `_USD_EDITION` skips any file it cannot date rather than guessing.** `parse_us_domain_delegated` reads column 2 only, so column 3 contacts are never read as delegations. Approved master by Ivo on the delegation argument: **+15,173.22 EE over 16,384 pairs.**

## `squidguard_2001_blacklist`: BANKED 10,376.92 EE, and the closure it reopens

Robot-compiled proxy blacklists shipped as squidGuard 1.2.0 samples: `archive.debian.org/debian/pool/main/s/squidguard/squidguard_1.2.0.orig.tar.gz`, 1,852,659 bytes, holding `squidguard-1.2.0/samples/dest/blacklists.tar.gz`. **Each list carries its own compile stamp on the line before `compiled from`: `# This list was compiled in 19:44:45 on 2001.12.15 19:56:41.`, written by `squidGuardRobot-2.3.4`, which names itself and asserts a successful fetch (`654820 links, of which 510389 tested successfully`), so nobody typed the list and it takes no corroboration split.** Every stamp falls between 2001.12.15 and 2001.12.18, tar member mtimes agreeing. Licence GPL v2, verbatim in `squidguard-1.2.0/COPYING`. 18,000 pairs banked; diff `-` lines are removals and were dropped. `mail/domains` has no compile header and is skipped.

## The squidGuard contrib blacklist at its ORIGIN, two 2001 editions (PRICED, needs a Decision)

The list the banked `squidguard_2001_blacklist` is a distribution sample OF, taken from the host that
compiled it rather than from a release tarball, and it survives in Wayback at two dates inside 2001:
`web.archive.org/web/20010710215730id_/http://ftp.ost.eltele.no/pub/www/proxy/squidGuard/contrib/blacklists.tar.gz`,
403,211 bytes, gzip mtime 2001-07-09 13:39:23, and the previous edition left on disk as
`blacklists.tar.gz~` at capture `20010911061641`, 1,576,754 bytes, gzip mtime 2001-09-09 07:55:13.
Found by reading the 2001-12-12 capture of `www.squidguard.org/blacklist/`, which names the download
URL and the two alternative lists. Staged at `data/raw/squidguard_contrib_2001/`.

**What dates one item is the same machine-written header the banked edition was approved on**, one per
category file: `# This list was compiled in 79:50:07 on 2001.07.03 08:08:29.` from
`squidGuardRobot-2.2.13` over `286445 links, of which 230616 tested successfully`, and
`# This list was compiled in 33:22:40 on 2001.09.09 09:48:47.` from `squidGuardRobot-2.3.4` over
`2402 link sources and 463098 links`. The robot names itself and asserts a successful fetch, so nothing
was typed by a person and no corroboration split applies. Every tar member mtime agrees, running
2001-07-03 to 2001-07-09 and 2001-08-10 to 2001-09-09, and the per-date `newdomains.YYYYMMDD` and
`newurls.YYYYMMDD` files carry the date in the filename as well. `mail/domains` has no compile header
and is skipped, exactly as in the banked edition.

**Measured 2026-08-27 against the live store, which already holds the December edition: 2,553 net-new
post-split pairs and 1,506.4 EE**, all at 2001, from 74 content files, 75,347 distinct hostnames and
42,321 registrable domains. Before the split it is 3,292 pairs and 1,960.0 EE. By TLD: `com` 2,283,
`de` 183, `net` 73, `org` 6, `nl` 3, `ch` 2. Typo upper bound 49.1%. The two editions read 406.7 EE
(July, 7,035 domains, 6,258 held) and 1,368.0 EE (September, 37,186 domains, 34,163 held).

**39,029 of the 42,321 domains, 92.2%, already carry 2001**, and that is the December edition's doing
rather than the store's general coverage, which is the same "editions within one year are worth one
edition" law that made the 175 unbanked 2001 diffs pay zero. It shows up a second way: adding the
`urls` files and the dated new-entry files moves the reading from 1,488.1 to 1,506.4 EE, **18.3 EE**,
where the same addition on the December edition was worth 32%. What is left after December is the
names the robot found in July and September and had dropped by mid-December, which is a real and
bounded residue rather than a second copy of the same list.

**Licence is the one thing to check before ingesting and it is NOT settled by the banked entry.** Both
tarballs hold `blacklists/README` and no `COPYING`: GPL v2 covers the squidGuard source distribution
the December sample came in, and these two files are the standalone data drop from the project's own
FTP host, carrying only the README's "entierly products of a dumb robot" warning.


## RIPE NCC's reply, 2026-08-26: permission given, and what the file actually contains

The request quoted the file, its "Restricted rights" notice and the use. RIPE NCC Member Services
replied: "As long as the data is publicly available and visible to you, you are
welcome to use it for your research", the one condition being not to make a large number of requests
involving personal data against the live database. We make none: the artifact is a static file on
disk. A census of all 63 attribute codes returns zero for person, address, phone, fax, e-mail,
nic-hdl and role. The report attributes the RIPE NCC.

## `ripe_dbase_1999`: BANKED 90,770.29 EE, the most PAIRS of any source this round

The 1999-08-04 RIPE database snapshot, 1,232,554 distinct registrable names, at
`ftp.funet.fi/pub/netinfo/RIPE/dbase/ripe.db.gz`.
**Dating: the dump's own header line `# 990804 00:07:01`, written by RIPE's export process and read
rather than assumed, so it evidences 1999 and no other year; a file with no `# YYMMDD HH:MM:SS` line,
or a stamp outside 1996-2001, is refused.** 641,038 net-new pairs at 1999.
`parse_ripe_dbase_1999` reads `*dn:` and nothing else, held there by four tests.

## `namewinner_expiring`: BANKED 11,546.26 EE on the master reading

A dump out of a registrar's expiring-domain database, at
`web.archive.org/web/20011026120205id_/http://namewinner.com/whole_list.php?del=tab`, 581,560 bytes,
tab-separated plain text despite the `.php`.
**Dating: each row carries its own expiry date, `25-OCT-01`, 20,945 occurrences and no other date of
that shape in the file; the parser reads the row's date, not the filename or the capture, so the
2002-04 capture's `25-APR-02` rows are refused one at a time.** 18,937 pairs at 2001. No corroboration
split: being registered is the only way onto the list, so it dates novel names too.

## `can_domain_registry_notices`: BANKED 7,934.20 EE on a one-word ruling

Registry approval notices posted to `can.domain`, at
`archive.org/download/usenet-can/can.domain.mbox.zip`, 14,326,153 bytes, 37,578 approval fields; the
item is named for the hierarchy, not the group.
**Dating: the `Date-Approved:` field the registry prints inside its own approval notice, in
machine-formatted aligned columns with ISO-style dates, the registry's own act rather than a
description of someone else's.** `whois_creation`, that year only, 9,485 pairs. A record block is
bounded by the next `Subdomain:` line, so a neighbour's date cannot attach.

Closed alongside it: `Date-Modified:` is **0.0 EE**, nine such records archive-wide; and the
better-named decoys `usenet-can.domain` (208 KB) and
`FULL-USENET-BACKUP-2020-Oct-can.domain.189.mbox.7z` (124 KB) hold zero `Date-Approved:` fields.

## SEC EDGAR filings, re-measured: right size, wrong shape, and too slow to help this round

Not banked: 1999 QTR1 gave 0 net-new post-split over 389 filings, 2001 QTR4 gave 13 net-new and 8.0
EE, and the bulk `Feeds/<year>/QTR<n>/` route 404s for 1996, 1999 and 2001, leaving one request per
filing, ~90 hours. **Reopen as a background job with an absolute deadline over the
2000-2001 quarters.**

## `dartmouth_bfs_seed` and `cctld_register_listing_inbody`: BANKED 3,018.18 EE

**`dartmouth_bfs_seed`**: a level 0 BFS CDX crawl, 2,442 pairs over 18,940 domains. **Dating: `cdx_timestamp` on field 2 of each CDX row, written by the archive at
capture, self-dating, no split.** Levels 2 and 3 are closed at 0.00, 0.00 and 0.59 EE per MB against level 0's 104.7.

**`cctld_register_listing_inbody`**: 10,177 pairs, from TWNIC's `.tw` frozen-domain list and IDNIC's
`.id` unpaid-fees table. **Dating: TWNIC's page stamps itself `更新時間: 2001/8/27 20:0:31` and lists
names whose registration expired between 2001-05-29 and 2001-08-26, so each was in the register during
2001; each IDNIC row carries its own `Jatuh Tempo` due date, the registry stating the boundary of that
registration's paid period.** The evidence value carries the date that justifies the row, `@1998` for a
row-dated name, the page stamp otherwise.

## BANKED: `ripe_dbase_changed`, 399,401 pairs and 58,398 EE, and the round crosses 5%

The `changed:` attributes of the domain objects in the 1999-08-04 RIPE snapshot already on disk.
**Dating: a `changed:` line is `address SPACE date` and carries its own 8-digit date, recording an
update applied to that object by a party with authority over it. An object cannot be modified if it does
not exist, so the line is its own dated record of existence in its own year, the shape rule 6 demands
and one a creation date can never supply.** The changer is a ccTLD registry role account in the large
majority of cases, DENIC's `hostmaster@nic.de` alone writing 49.4% of the 2,016,169 lines; for the tail
the claim is only that somebody entitled to modify the object did so. By year 1996 18,944 / 1997 67,515 / 1998 312,942, 1999 zero, which is the thin end of the store's
coverage. The pattern captures only the trailing 8-digit group; three tests fail on a leaked address.

## `usenet_whois_paste`: BANKED 30.4 EE

Registry whois records people pasted whole into Usenet posts, read out of the 16,849 archives already
on disk under `data/raw/usenet_{bulk,new,probe,probe5,msft}` (`https://archive.org/download/usenet-<hierarchy>/<group>.mbox.zip`).
**What dates one item is the registry's own line inside the pasted block, `Record created on
20-Jul-2000.`, written by InterNIC and not by the poster, so it fixes the registration year whatever
year the post carries; rule 6 gives that year alone.** 769 in-window pairs, 636 on corroborated
domains, 92.1% of them already held, **50 net-new pairs and 30.4 net-new post-split EE**; the 133
names seen only here take the split and go to the candidate pool as `link_target`.

## Candidate-only pools

These earned no year and no row in any annual file. They are named because they are `source` values in
`audit/source_contribution.csv` and a reader tracing that column should find them here: `candidate_hosts`
(hostnames seen without a dated observation), `udrp_hosts` (disputed names from UDRP dockets),
`H008-pool-names` (a pricing probe's name list), `attrition_out_of_window_hosts` (defaced hosts whose
defacement date falls outside 1996-2001), and four US federal registers whose web-address columns
are current-state rather than dated, `fac_single_audit_candidates`, `fac_cpa_firm_candidates`,
`imls_library_survey_candidates` and `ncua_call_report_candidates`. **A current-state snapshot cannot
evidence a past year**, which is why all four are pools and not sources.

---

## `chastity_list_blacklist`: BANKED on a tar member header, ranked first in the triage queue

**The link, and the terms.** `https://archive.debian.org/debian/pool/main/c/chastity-list/chastity-list_0.5.orig.tar.gz`,
720,609 bytes, staged at `data/raw/chastity/`. The chastity project's category ACL files for
squidGuard, by Roy-Magne Mo. **Licence GNU GPL v2, verbatim in `COPYING`**, so redistribution and
derivative use are granted outright; `archive.debian.org` serves no robots.txt (HTTP 404), and the
file was fetched with one request against a host that publishes no restriction.

**Why it meets the standard.** What dates one item is a stamp a program wrote inside the artifact:
**the tar member header `Dec 14 2001`, carried by every one of the 258 members of the orig tarball**,
corroborated from inside by 209 per-date diff filenames running `domains.20010813.diff` through
`domains.20011201.diff`, monotone and all in window. That is the identical argument already approved
for the 1997 half of `junkfilter_dated_blocklist`, so no new evidence class is invented. Class
`dated_directory`, master-eligible. A blocklist entry is a claim the host was live and serving when
the maintainer wrote it down, which is an assertion about a state at an instant the artifact stamps
itself, and rule 6 is respected because the edition evidences 2001 and no other year.

**The split is applied and costs almost nothing here, by construction.** The list is hand-maintained,
so the name is a person's typing and takes the corroboration split. `scripts/sources/blocklists/split_chastity.py`
applies the strict predicate, that the domain already carries an assigned year in `domain_year`, and
writes two lanes: **92,068 of 97,937 distinct registrable domains are corroborated, 94.0%**, and the
remaining 5,869 park as `link_target` candidates that date nothing. The measured yield sits on the
corroborated lane because "held" and "corroborated" are the same test.

**Measurement**: 136,885 base list lines across 13 categories yield 97,937 distinct registrable
domains, of which 24,927 are held with no 2001 record, **14,229.0 net-new post-split EE at 2001**,
`.com` carrying it at 20,149 pairs and 12,736.2 EE. Reproduced on 2026-08-31 against the live store
at 97,937 / 92,068 / 94.0% against the 2026-08-27 reading of 97,937 / 92,059 / 94.0%, the nine-domain
drift being five days of ingest.

**Do not re-price this population at an earlier year.** The same names read naively at
held-in-any-year-and-missing-Y give 45,506 at 1999 and 12,699 at 2000, against 323 and 329 on the
adjacent screen: the naive figure overstates 1999 headroom **141x** and 2000 headroom **39x**, because
a blacklist's population was registered in the two years before its compile, so the store's gap at
those years is non-existence rather than missing data. squidGuard reproduced the same effect
independently at 167x. The out-of-window sibling `chastity-list_0.5.20020928.orig.tar.gz` is stamped
`Sep 28 2002` and cannot date a year; the whole SourceForge release history is three December 2001
releases, so no earlier edition exists to find.

**How it was found, since the method outranks the source.** **ONE request per Debian release for
`dists/<rel>/main/binary-i386/Packages.gz` indexes every package name, version, size and description
in that release**, which is the `ls-lR` trick applied to a package archive and turns a whole
distribution into an offline grep. Grepping the potato and woody indexes for blocklist-shaped
descriptions gave 41 and 81 candidates; the largest in woody was this one, 701,038 bytes, described
as "blacklists for SquidGuard".

---

## `granitecanyon_zone_rejects`: BANKED on a nightly error log the operator never meant as a customer list

**The links.** Seven objects, 1,567,653 bytes, refetched 2026-08-31 by
`scripts/sources/registries/collect_granitecanyon.py` and each one matching its 2026-08-29 byte count exactly:

- `https://web.archive.org/web/20010601000000id_/http://soa.granitecanyon.com/stale_30Nov1999.txt`
  (205,787 B, 14,522 zone names, the 1999 prune list)
- `https://web.archive.org/web/20010223195457id_/http://soa.granitecanyon.com/ZoneRejects/` (193,389 B)
  and the same path at `20010508024101` (212,935 B), `20010611192639` (215,340 B),
  `20010626115208` (222,405 B), `20010901062251` (245,087 B) and `20011204210150` (272,710 B)

**Why it meets the standard.** What dates one item is a stamp the operator's own program wrote
inside the artifact: each reject edition prints `Rejected Zone List:  <D-Mon-YYYY HH:MM GMT>` in its
own bytes, and all six in-window editions were verified on refetch to stamp themselves **23-Feb,
7-May, 11-Jun, 26-Jun, 31-Aug and 4-Dec 2001**, every one agreeing with its capture timestamp. The
1999 prune list is dated by `status.shtml`'s "29 November 1999 ... here is the list of pruned zones"
and by its own filename. So one row is Granite Canyon's nameserver holding that zone in its BIND
configuration at the instant the file was generated, which is a machine's configuration record
rather than anyone's description of one. Class `artifact_listing`, master-eligible, already
approved. Killer 8 order is respected: the grounds are the self-stamp plus the capture, and the
overlap with the store is cited afterwards as a check, never as the argument.

**The split is applied.** The zone name was typed by a customer into a submission form, so the
corroboration split applies and only already-held names earn a year.
`scripts/sources/registries/split_granitecanyon.py` writes one lane pair per edition, so the year always comes from
the artifact and never from a default. Refetched and re-split on 2026-08-31 against the live store:
prune list **13,199 zones, 7,970 dated (60.4% held)**, reject editions 46.5% to 52.1% held,
**17,424 dated rows and 15,114 candidate rows** in total. `to_registrable` drops the
`.in-addr.arpa` reverse zones and the malformed rows on its own, which matters because `.arpa`
carries the highest weight in the model and `ark check` refuses it outright.

**Measurement**: 1,732.9 net-new post-split EE over 3,059 pairs, 0.102 EE per listed name, by year
{1999: 2,001, 2001: 1,058}, mean weight 0.5665. Pre-split is 10,272 pairs and 5,813.2 EE and
**overstates by 3.4x, so do not quote it**. The 1999 list alone is 1,125.5 EE and the 2001 reject
union alone 607.3 EE, so a held name in a 2001 edition is worth 2.3x the same name in a 1999 one.

**The held-fraction is the finding, and it is the transferable part.** 60.4% and 46.8% held,
against 87 to 99% for authority corpora, ~50% for blocklists, 98.4 to 99.6% for visitor logs and
~5% for forged-header spam corpora. A zone is not a page, so no crawler reaches it through a link
and the artifact is not head-selected: these are people who had a domain and no server. The
population also does not collapse on the 2001 threshold, P(lacks 2001 | held) measuring com
**0.5745**, net 0.6174, org 0.5113 against the store-wide law's 0.611 / 0.653 / 0.568.

**Method, in three parts.** (1) **A free-DNS operator's CUSTOMER population is the tail and its
OPERATOR population is not**, and the two look identical until you ask who typed the name: the
zone-file-RHS closure measured nameserver operators at 99.3% held, and the same service read from
the other end measures 46.8%. The discriminator is whether the selection predicate is "runs a
nameserver" or "asked somebody else to run one". (2) **When a service hides its inventory behind a
login, look for its ERROR LOG.** All four other operators refused to publish a customer list
(secondary.com behind `/auth/`, zoneedit.com behind `login.html`, xname.org behind a per-zone
password, freedns.com an empty index); the one that published a nightly list of the zones its BIND
could not load gave away 4,369 names it never meant to. Reject lists, prune lists, lame-delegation
reports and stale-zone reports are machine-generated, self-stamped and regenerated on a schedule,
so every capture is a fresh dated edition and nobody thinks of them as a customer list. (3)
**Enumerate a dead site's list files from its own dated changelog, not from an index**: four
captures of `status.shtml` named every list file the site ever served and dated each, which a CDX
prefix query would have cost many more requests to establish.

**Exhaustion, so nobody re-probes it**: six ZoneRejects editions exist in 2001 and no more,
fourteen probes across 2001-01 to 2002-04 collapsing onto those six timestamps. A seventh edition
dated 26-May-2002 is **out of window and cannot date a year**, and the collector deliberately does
not fetch it. The predecessor `zoneRejects.txt` is 9 names at 2000-03-03 and HTTP 403 at every later
capture.

**A collection trap worth recording, because it cost a wasted pass.** The first refetch built the
replay URL as `{stamp}id_{host}` with no slash after `id_`, and web.archive.org answered every one
of the seven with the same 154,263-byte "Wayback Machine" interstitial. A size floor set at half the
expected bytes passed all seven, because the interstitial is larger than half of 193,389, so the run
reported success and wrote seven near-identical files. **A size floor is not a content check**: the
collector now rejects any body whose first bytes are an HTML5 document, and the correct form is
`{stamp}id_/{host}`.

---

## `cctld_register_listing_capture`: BANKED at 2,450.2 EE, against a source register that claimed 3,496.0

**The links, recorded because the entry had none and two of the four artifacts could not be
found from the description alone.** All four refetched and repriced from the bytes on 2026-08-31:

- **SaudiNIC `AllSA`**: `https://web.archive.org/web/20010414064415id_/http://www.saudinic.net.sa/cgi-bin/indexing.cgi?AllSA=AllSA`
  (1,458,399 B). Sibling `?AllComSA=AllComSA` at `20010414071027` is a subset and was not fetched
- **`.nu` expiry list**: `https://web.archive.org/web/20011222202631id_/http://www.nunames.nu/notRenewed.cfm`
  (1,162,481 B)
- **ISOC-IL `.il` register**: `https://web.archive.org/web/19980120012100id_/http://www.isoc.org.il/domains.html`
  (273,464 B)
- **NIC Malta**: `https://web.archive.org/web/19980525073234id_/http://www.um.edu.mt/nic/dir/` (20,269 B)

**Why it meets the standard, and the split differs per artifact.** SaudiNIC's page describes
itself as "A searchable directory for all registered domains under .SA" and is generated by
`indexing.cgi` out of the register, so the registry is stating its own register's contents and the
Wayback capture fixes the instant: `cdx_timestamp`, master-eligible, and **no split**, on the same
grounds as the already-approved in-body sibling (TWNIC, IDNIC, RESTENA), which is ingested with no
split step at all. `.nu`'s `notRenewed.cfm` is stronger than the class requires: **every row
carries its own machine-written `Expired` date** (`0372-pizza.nu` / `09-Nov-2001`), so the year
comes from the row, and a name whose registration lapsed in 2001 was in the register during 2001
and the artifact implies nothing about another year. ISOC-IL self-stamps `Document Modified:
3-1-98` but is a hand-kept directory with an organisation typed beside each name
(`arachim.ac.il - Michlalah - Jerusalem`), so **the split applies**. NIC Malta is hand-kept too.

**Measured, and the source register was 1.4x high overall and 817x high on one component:**

| artifact | claimed EE | measured EE | names | held |
| --- | --- | --- | --- | --- |
| SaudiNIC `AllSA` 2001 | 1,506.4 | **1,387.2** (no split) | 3,094 | 383 (12.4%) |
| `.nu` notRenewed 2001 | 144.1 | **959.8** (no split) | 3,495 | 51 (1.5%) |
| ISOC-IL 1998 | 375.0 | **101.4** (post-split) | 5,408 | 4,851 (89.7%) |
| NIC Malta 1998 | 1,470.5 | **1.8** (post-split) | 203 | 200 (98.5%) |
| total | 3,496.0 | **2,450.2** | | |

**NIC Malta is the lesson and it is worth more than the artifact.** The register claimed 1,624
pairs and 1,470.5 EE, the largest single component, and no such artifact exists: `nic.org.mt` has
**162 captures in the whole window and its largest object is a 3,908-byte GIF**. The real page is
on the university host, holds **203 names**, and **refuses the liveness claim this class rests
on** in its own words: "Sites are not required to be on-line prior to name registration. This
means that some of the links below may still be unreachable", and "This directory is not updated
regularly". It is banked at 1.8 EE so the negative is measured rather than assumed.

**Two collection traps, both of which produced a false zero in this run.** (1) **A CDX `limit` is
a false zero.** `notRenewed.cfm` was reported absent from `nunames.nu` after a `limit=2000` query
returned no match, and the page exists: the limit truncated the listing before reaching it. Query
the exact path with `matchType=exact` before concluding a page was never captured. (2) **A
registry's listing may not be on the registry's host.** NIC Malta's directory sat under
`um.edu.mt/nic/`, the university that ran the registry, and no query against `nic.org.mt` or
`nic.mt` could ever have found it.

**Why `.nu` is the find here and SaudiNIC is not, despite SaudiNIC being larger.** SaudiNIC is
12.4% held and `.nu` is **1.5% held**, so almost the entire `.nu` list is a population the store
has never seen, and `.nu` at 0.2787 still beats `.il` at 0.1958. The register had this component
at 144.1 EE, 6.7x low, because it priced it on the capture stamp and never read the per-row date
column that makes the whole list datable.

---

## MYNIC's fortnightly Domain Name Listing: SETTLED, the split does not apply, 6,883.1 EE

**The link.** `http://www.mynic.net.my/my/stats/<month><year>-{1,2}.htm`, 55 distinct pages of which
the `-1` and `-2` halves carry names and the bare-month pages are statistics tables. **34 of the 35
in-window name-bearing halves were fetched and parsed**; `dec2001-1.htm` has only a 318-byte empty
capture, and `feb2002-1` / `feb2003-1` are out of window. Staged at `data/raw/mynic/`.

**What dates one item**: the per-day heading above each block, `15 March 2001`, with `New` or
`Delete` beside the name, so MYNIC is stating that this name entered or left its register that day.
Both actions date the year and only that year: a name deleted on 15 March 2001 was in the register
until then. Class `artifact_listing`.

**The question this entry settles is the corroboration split, and the answer is that it does not
apply, on a test rather than on a judgement.** MYNIC also published a monthly statistics table
giving per-day, per-TLD New and Delete counts, with the note "Please click on the date to get daily
New and Delete domain name listings". If the listing is a complete enumeration out of the register,
its rows must reproduce those counts. For March 2001 the statistics table gives **New 850, Delete
166**; parsing the two listing halves gives **New 850, Delete 165**. An exact match on New and one
row short on Delete. **A hand-compiled list cannot reproduce a registry's own published counts
850/850**, so the listing and the statistics table are two views of one database, and this is the
registry stating its own register exactly as SaudiNIC's `AllSA` and the already-approved TWNIC,
IDNIC and RESTENA listings do, all of which are ingested with no split step.

**Measured whole-tree, against the live store on 2026-08-31**: 12,118 rows (10,322 `New`, 1,796
`Delete`) over years {2000: 3,716, 2001: 8,402}, giving 11,690 distinct pairs over 11,564 domains,
2,609 already held. **Unsplit 9,081 pairs and 6,883.1 EE**; post-split 1,558 pairs and 1,180.8 EE.
Mean weight 0.7579, `.my` at 0.7580. Net-new by year {2000: 1,202, 2001: 356}, typo upper bound
26.6%. The source register estimated "near 10,000 and 400" for the whole tree, so it was **1.45x
high on the unsplit reading and 3x LOW on the split one**, which is why the whole tree was measured
rather than scaled from 25 pages.

**A weaker signal that was nearly used as the argument, recorded so it is not trusted next time.**
Alphabetical ordering within a day and TLD group looks like a database `ORDER BY` and was the first
argument reached for, but it holds in only **75.2% of 472 groups**: `New` rows are sorted and
`Delete` rows are not, and a day-heading regex that misses some headings merges two days and makes
sorted runs look unsorted. **Ordering is a hint; reproducing the publisher's own counts is a test.**

---

## CO.ZA suspension and deletion queues, the wider tree VERIFIED at 3,704.3 EE, and a 16-character truncation

**The links.** The CO.ZA registry's own CGI, on **two hostnames**, which is what the wider tree
amounts to: `http://co.za/cgi-bin/{warn.sh,todel.sh}` and the same paths on `posix.co.za` and
`www.posix.co.za`, the host of the company that administered CO.ZA. **22 captures return HTTP 200**
against the 11 the register had verified on `co.za` alone, and the `posix` half is not a duplicate:
its earliest editions are **1997-12-21 and 1998-01-17, earlier than any `co.za` capture**. Staged
at `data/raw/coza/`, 22 objects.

**What dates one item**: the Wayback capture stamp, since neither page carries an in-body date.
`todel.sh` is headed "Domains in CO.ZA to be deleted ... The following domains are shortlisted for
deletion. This is due to lack of payment", and `warn.sh` is a separate list, "Domains in CO.ZA to be
or on Suspension ... shortlisted for or have been suspended. Within a couple of invoice runs they
will move to the Deletion queue". Both assert the name is in the register at that instant.
`cdx_timestamp`. **No split**: these are shell CGI reading the register, so it is the registry
stating its own register, the same grounds as MYNIC and SaudiNIC.

**Measured, 2026-08-31, truncated labels excluded**: 12,404 rows over years {1997: 1,656, 1998:
5,551, 1999: 3,074, 2000: 2,123}, 5,439 distinct pairs over 4,360 domains, 1,613 already held.
**Unsplit 3,826 pairs and 3,704.3 EE**; post-split 1,251 pairs and 1,211.2 EE. Mean weight 0.9682,
all `.za`. So the agent's unverified wider-tree figure of 4,462 EE was **1.2x high**, and the
register's verified 2,720.6 sat between the two because it read ten captures of one host.

**The reason to exclude 640 rows, and it is a defect in the artifact rather than in the parse.**
The registry's CGI prints bare labels in **fixed 16-character columns and truncates the name to
fit, in the `href` as well as the anchor text**, so `sahomeimprovement` is served as
`sahomeimprovemen`, and `museum-of-freedom`, `cruisesinternational`, `australianimmigration` and
`thevirtualprinter` are all cut the same way. The label-length histogram shows the damage plainly:
303 labels of 15 characters against a spike of **640 at exactly 16**. Every 16-character label is
therefore a possible fragment, and admitting them would mint well-formed domains that never
existed, which no invariant in `ark check` would catch. All 640 are dropped.

**Note the years before pricing this against the queue.** The captures run 1997-12 to 2000-08 with
**nothing in 2001**, and 1,018 of the 1,251 post-split pairs land at 1998. Measured headroom at
1996-to-1997 is 103,953 pairs against 6,708,320 at 2000-to-2001, so this source is high weight in
the thin years rather than in the year that pays.

---

## Federal Audit Clearinghouse Single Audit filings: CLOSED ON ACCESS, and the primary source is a blanket robots refusal

**The primary source, which is what was asked for and had never been recorded.** The landing page
is `https://www.fac.gov/data/download/historic/`, "Data from 1998-2015 ... Single Audit submissions
collected by the Census FAC from 1998 to 2015 ... provided as-is for historical research and is not
included in our web-based search or API". The per-year files it links are
`https://app.fac.gov/dissemination/public-data/census/csv/census-{1998,1999,2000,2001}.zip`, with a
`.sha1` beside each, and a combined `census-1998-2015.zip` of 413 MB.

**The claim is structurally correct, and the data dictionary confirms it.**
`https://www.fac.gov/data/download/historic-dictionary/` documents **`AUDITEEEMAIL`, "Auditee Email
address, 60 characters max"** and **`AUDITEEDATESIGNED`, "Date of auditee signature, mm/dd/yyyy"**,
plus `CPAEMAIL` and `CPADATESIGNED`, present across every form revision covering our window. So one
item really is one e-mail field on one filing row dated by that row's own signature date.

**But the route is closed, and this is decided before the human-typed question is even reached.**
`https://app.fac.gov/robots.txt` is exactly `User-agent: *` / `Disallow: /`. Every data file is on
that host; only the landing page and the dictionary are on `www.fac.gov`, whose robots.txt is
`User-agent: *` / `Disallow:` and permits everything. `harvester.census.gov`, the Census Bureau host
that ran the FAC until 2023, now 302s to `outage.census.gov/maintenance.html` and serves no data.
**No open mirror exists**: `catalog.data.gov`'s package API answers HTTP 404 for the search, and
archive.org's `advancedsearch.php` returns 46 items for "federal audit clearinghouse" of which the
only FAC-related ones, `GithubArchiveOf_GSA_GSA-TTS_FAC` and `...FAC-Frontend`, are the
application's **source code and not its data**.

**Two further facts worth recording.** The historic dataset **begins at 1998**, so 1996 and 1997 are
unreachable through it whatever happens. And the 2,406.69 EE figure of 2026-08-24 was measured with
a specificity that implies the bytes were genuinely read, including 18,698 e-mail rows dropped for
signature dates outside the window, mostly FY2001 audits signed in 2002. **Nothing under `data/raw`
or `private/` holds those bytes now, and the only route to them breaches a `Disallow: /`**, so that
measurement is neither reproducible nor repeatable by us. It is recorded here, unbanked.

**One route remains and it is a letter, not a fetch.** GSA publishes the dataset for historical
research and `www.fac.gov` invites exactly that use, so a request for a copy or for permission to
retrieve the four in-window ZIPs is the correct next step, and it joins the letters already open on
the Edelman CIPA terms. Do not re-probe `app.fac.gov`, `harvester.census.gov`, data.gov or
archive.org for this corpus.

---

## `fac_single_audit`: BANKED at 1,403.2 EE, on bytes a human downloaded because the host refuses robots

**The links, and the provenance, which is unusual for this project and is the point.** Landing page
`https://www.fac.gov/data/download/historic/`; the four in-window files are
`https://app.fac.gov/dissemination/public-data/census/csv/census-{1998,1999,2000,2001}.zip`
(15 / 16 / 16 / 20 MB) with a `.sha1` beside each. **`app.fac.gov/robots.txt` is `User-agent: *` /
`Disallow: /`**, so no automated client of ours may fetch them, and the route was recorded closed on
2026-08-31. **Ivo downloaded all four by hand the same day**, which robots.txt does not govern
because it governs robots, and **all four SHA1s verify** against GSA's published digests
(`7037c86d…`, `d03838ed…`, `94f7fadf…`, `5de0abcd…`). Staged at `data/raw/fac/`. The archives carry
**no licence or README of their own**, only CSVs, so the terms are the landing page's "provided
as-is for historical research" plus US federal public domain. `www.fac.gov` itself is
`Disallow:` and permits everything, which is how the dictionary was read.

**Why it meets the standard.** One item is one e-mail address field on one Single Audit filing row,
dated by that row's own signature date: `AUDITEEDATESIGNED` "Date of auditee signature" or
`CPADATESIGNED`, both documented in GSA's historic data dictionary and both present in
`ELECAUDITHEADER.csv` for every year in the window (columns 24/25 and 37/38 of 95). The address is
the auditee's or the audit firm's own, so the row asserts the domain was in use on the day a
certifying official signed. Class `dated_directory`, master-eligible. Rule 6 holds: a signature
dates its own year only.

**`AUDITYEAR` is a trap and this is the transferable part.** Every row also carries the audit year
and the two do not agree, because 1998 filings are routinely signed in 1999 and FY2001 audits in
2002. Screening on the signature date drops **18,979 of the 75,311 e-mail fields, 25.2%**, and
dating on `AUDITYEAR` would have imported every one silently. The register's 2026-08-24 pass
reported 18,698 on the same screen, so the two agree to 1.5% and the discipline reproduces on
independently obtained bytes. **When a dated corpus carries two plausible date columns, measure how
far apart they are before choosing.**

**Measured, 2026-08-31, against the live store**: 139,978 filing rows, 75,311 e-mail fields, 55,563
in-window items over 17,208 distinct pairs and 9,952 domains, **13,796 pairs already held (80.2%)**.
**Post-split 2,081 pairs and 1,403.2 EE**, mean weight 0.6743, by year {1997: 1, 1998: 165, 1999:
505, 2000: 559, 2001: 851} and by TLD {com 1,278, org 461, us 228, net 99, edu 9, cc 3}. Unsplit
would be 3,412 pairs and 2,320.5 EE. The register's 2,406.69 has decayed to 1,403.2 in seven days,
almost entirely against this project's own ingests of the same morning, which is the standing
warning that an unbanked source decays as the store grows.

**The population is `.com`, not `.edu`, and the earlier expectation was wrong.** This corpus was
expected to die to the split the way ERIC did, where 184 `.edu` pairs yielded one survivor. It does
not, because auditees and audit firms filed with commercial addresses: of 55,563 in-window items the
TLD mix is `com` 33,819, `net` 9,201, `us` 5,683, `org` 4,362 and `edu` only 1,826 with `gov` 572.
**A "federal dataset" is not the same population as a "federal institutional corpus", and the
address column decides which it is.**

**The split applies and earns its place, with the cost recorded rather than assumed.** A person
typed each address into a form, so novel names take the split. A random sample of the 1,147 novel
names shows why: `campell.edu` for Campbell, `clakamas.or.us` for Clackamas, `staate.oh.us`,
`selfsuffciency.com`, and `kl2.ca.us`, where the letter `l` was typed for the digit `1` in `k12`.
Separately, **18.0% of novel names are a character prepended to a name the store already dates**,
`aarthurandersen.com` for arthurandersen.com, `aattglobal.net` for attglobal.net, `1mc.edu` for
mc.edu, which is an import defect rather than anyone's honest typing. **But the split is not
costless**: the same sample holds `isler-eugene.com`, a real Eugene accountancy firm, and
`sau38.k12.nh.us`, a real New Hampshire School Administrative Unit, neither of which any crawler
reaches. The measured typo upper bound is **69.7%, the highest in the register**, and it is an
UPPER bound: the sample puts the true rate nearer a third. So 1,444 uncorroborated rows park as
`link_target` and the 917.3 EE difference between the split and unsplit readings can be recovered
later on a human ruling, without refetching anything.

**A method note on the sampling, because it produced a wrong reading first.** Inspecting novel names
in alphabetical order made the prepended-character defect look like the whole story, since those
cases cluster at `1`, `9` and `a`. A random sample put it at 18.0% and revealed the ordinary typos
and the real long-tail names underneath. **Sample randomly, never off the head of a sorted list.**

**Loose ends, so nobody re-probes them.** The historic dataset begins at **1998**, so 1996 and 1997
are unreachable through it. `harvester.census.gov`, the Census host that ran the FAC until 2023, now
302s to a maintenance page. No open mirror exists: `catalog.data.gov`'s package API answers 404 and
archive.org holds only the application's source code, `GithubArchiveOf_GSA_GSA-TTS_FAC`. The other
seven CSVs in each ZIP were not used: `ELECAUDITS.csv` is the largest by far (48-62 MB) and is the
per-award detail, carrying no address column.

---

## Integrity audit: the Usenet channel's self-corroboration, measured and bounded at 0.265% of what we ship

A 2026-08-31 research round closed the `uk.*`/`de.*` Usenet hierarchies and raised a wider alarm:
that the corroboration split has gone **circular** on the whole Usenet channel, because a name our
own earlier sweep dated is what corroborates a new Usenet file dating it again, and that "every
Usenet-lineage figure banked since 2026-08-17 should be re-checked". **The alarm is directionally
right and the magnitude is small. Both halves were measured rather than assumed.**

**The mechanism is real.** Anti-spam address munging turns a real address into a fake domain, and
our extractors banked the result. Every one of the nine names the round named is carrying dated
years: `nospambigfoot.com` at 1997, 1999, 2000, 2001, `nospam.ac.uk` at all six years,
`btnospaminternet.com`, `deletethis.com`, `nospamblueyonder.co.uk`. A second and separate defect
sits beside it: **bare public suffixes banked as though they were domains**, `ac.ie`, `com.hu`,
`gov.gb`, `free.uk`, plus `go.nl`, `com.us`, `co.jo`, `gov.ru` and 1,222 more.
`ark check`'s `registered_domain_format` cannot catch either, because both are lexically
well-formed lowercase names, and `to_registrable` rejects `co.uk` and `com` while **accepting**
`ac.ie`, `com.hu` and `gov.gb`, so the PSL screen has holes.

**The magnitude, measured three ways.** Store-wide, munging-token pairs are 2,227 across all
sources, of which ours contribute 1,038 (`usenet_announce` 530, `usenet_address` 493,
`usenet_bare` 15) and **`prior_task` contributes 1,060, more than we do**, so this is not a defect
unique to this project. Suspected bare suffixes carrying dated years are 1,230 distinct names and
3,945 pairs, 1,544.2 EE, 0.855% of the banked increment. **But what matters is what is SHIPPED**,
and in `output/netnew/` the two defects together are **818 pairs and 478.5 EE across 283,091
shipped rows, 0.265% of the claimed increment**: 189 bare-suffix pairs at 88.4 EE and 629 munged
pairs at 390.1 EE. So the round's "27.2% garbage" figure was true of its own small probe
population and does not transfer to the store.

**Not fixed here, and deliberately.** Removing 818 pairs means deleting `domain_year` rows and
their evidence, and the honest fix is a new invariant plus a PSL refresh, which is a change to the
gate rather than a data edit. Recorded as a **pre-submission cleanup item**, since 478 EE is
immaterial to the 5% trigger but a reviewer validating our files would reasonably flag `gov.gb`.

**The transferable rule.** **A corroboration split is only as independent as its partner source.**
When the partner is a class this project has already swept at scale, the split validates nothing
and the failure is invisible in the headline: the same bytes and the same parse priced 22,838 EE
against the current baseline and 252 EE against `merged260810`, a 91x spread, and the check that
revealed it was a grep over `feedback/*/merged*` and over our own journals, both in minutes.
**Price a Usenet-lineage source against a reviewer cut from before our own collector last ran in
that channel.**

## `urlmerchant_inventory`: BANKED, a broker's whole for-sale inventory printed as static A-Z pages

`https://web.archive.org/web/20010901000000id_/http://www.urlmerchant.com:80/domains/domain_a.html`
(the letter series `domain_<a-z|0-9>[_<n>].html`, ~40 KB and 100 names a page; counts page
`http://www.urlmerchant.com/statistics.html` at capture `20011231002753` states "Total Domain Names
Listed: 156,122"). **What dates one page is its own generator stamp,
`<META NAME="UPDATED" CONTENT="Tuesday, Jul 17 2001 1:19:41 AM">`, written by the program that
printed the table out of URLMerchant's listings database, with the Wayback capture fixing when the
archive saw it; the stamp is read per page and never inferred from the capture, so the 4 pages
stamped 2002 are dropped whole.** Names are owner-submitted, so the corroboration split applies:
**1,591.9 net-new post-split EE over 2,557 pairs, all 2001**, from 244 pages, 23,875 distinct
domains and 14.2% held, the 20,492 novel names parking as candidates. **Why a 14% held-fraction
paid: 75.6% of the held names were missing 2001**, because a broker's inventory is tail names the
store only ever saw once. Screen on `held x P(missing the artifact's year)`, not on held alone.

## `jeb_bush_gubernatorial_email`: BANKED at 3,546.1 EE, and the URL this row exists to record

`https://archive.org/download/JebBushEmails/JebBushEmails-Text.7z`, 411,928,998 bytes, sha256
`821e796f7d9dcd0a5bcb08eaf70760d50f5296481f2175ac4ed45b3301f41f75`, 626 files, **505,927 messages
dated inside the window** out of 2.4M. **What dates one item is the message block's own unindented
`Sent:` line, `Sent:\tMonday, December 4, 2000 12:38 AM`, written by the sending mail client into
the export, the same basis and the same evidence type as the banked `enron_email`.** The
corroboration split applies, a person having typed most of the addresses: **3,546.1 net-new
post-split EE over 5,692 pairs**, 90.8% of 57,934 distinct domains already held, 83.2% of the pairs
adjacent-year corroborated, 2001 4,505 of them. Hosts are anchored on an `@`, a scheme or a `www.`
label, because the wide pattern turns `Candace Rice.To tell the truth` into `rice.to` and cost
200.8 EE of fabricated high-weight pairs. **This is the row the 2026-08-24 measurement at line 703
should have carried and did not**: seven days unbanked with no URL recorded cost 264 EE to the
store's own growth, and the hypothesis that inbound public mail beats outbound official mail is
refuted with the sign reversed, `From:` 1,235.4 EE against `To:`/`Cc:` 1,410.3 EE, because the
public writes in from AOL rather than from a domain it owns.
