# Open decisions

Generated by `scripts/harness/decision_sheet.py` from `approved-sources-list.md`. Do not edit by
  hand.

Every row is measured: the figure is net-new post-split equivalent-English against the store, not
gross and not a projection. Answer each with one word, in `approved-sources-list.md`:

- **master** means it may date a year in the annual files.
- **candidate-only** means collect it, never let it date a year.
- **rejected** binds, and the request generator refuses to reopen it.

A `link_target` or `typed` class needs no decision at all: it can never date a year, so it is
collected without asking. Only the master-eligible classes below are blocked on you.


| Source | Evidence type | What dates one item | Net-new EE |
|---|---|---|--:|
| `sec_edgar_filings` | `dated_directory` | the filing's own `Date Filed` in `full-index/<year>/QTR<n>/form.idx` | 5,884 |
| `internic_zone_hostnames_1999` | `artifact_listing` | the SOA serial inside each file, `EDU. IN SOA A.ROOT-SERVERS.NET. hostmaster.internic.net. ( 1999112000 ;serial`, the same argument as the 1997 lane | 4,678 |
| `expiring_list_2002_term_inference` | `artifact_listing` | NOT STATED | 3,620 |
| `ncua_5300_call_report_webaddr` | `artifact_listing` | `CYCLE_DATE` on every `fs220d` row, the quarter the call report covers | 1,328 |
| `nic_ve_cartelera` | `artifact_listing` | the notice's own publication date on NIC Venezuela's `cartelera`, the registry's public notice board of names entering the register | 1,131 |
| `discmaster_media_index` | `dated_directory` | the file's own filesystem date on the media, which is the `page_directory` shape, | 1,055 |
| `antispam_media_blocklist` | `artifact_listing` | the file's own timestamp on the preserved media, `2001-04-06`, shown in discmaster's listing row for `BlackList.json` and again on its parent `data.mdb`. Per EDITION, not per record, so the same shape as `junkfilter_dated_blocklist` | 1,055 |
| `zenodo_banner_ads` | `cdx_timestamp` | the 14-digit Wayback capture stamp on each appearance, verified against live CDX at 27 of 28 exact matches. Self-dating, so no split | 433 |
| `uk_trade_press_extension` | `dated_directory` | NOT STATED | 191 |
| `edelman_cipa_blocked_sites_2001` | `artifact_listing` | the vendor classifier's own observation column, `Blocked On (dates) (all dates are in 2001)` | 134 |
| `wayback_availability_2001` | `cdx_timestamp` | `archived_snapshots.closest.timestamp`, the 14-digit capture stamp the Wayback index wrote when its crawler fetched the page, returned inside the JSON body of `https://archive.org/wayback/available?url=<domain>&timestamp=YYYYMMDD`. It is the same fact `cdx_timestamp` already records, read from a second endpoint of the same archive, so no new evidence class is being invented. **A parser must take `archived_snapshots.closest.timestamp` and not the last `"timestamp"` in the document, which is the caller's own input echoed back and makes every probe report a perfect hit.** Accuracy was graded against cdx ground truth already on disk with no new cdx requests: 204 cdx year-pairs over 150 domains, 187 recovered, 91.7% recall and 94.3% at 2001, and 40 of 40 cdx-negative domains came back empty, so it does not invent years. Two defects make every zero a lower bound and never a false year: it returns only status-200 captures, and it canonicalises `www.` away | 100 |
| `usenet_quoted_whois` | `whois_creation` | the registry's own `Record created on DD-Mon-YYYY` line inside the quoted block, which dates the domain independently of when the message was posted | 90 |

**12 rows, 19,700 equivalent-English waiting on a word.**

21 further master-eligible classes are pending with **no measured figure**, so they are not rows above and this sheet is not the whole queue. Price one before deciding it, or read its verdict in `docs/sources.md`: `bbbonline_reliability_roster / artifact_listing`, `bomis_ring_member_lists / dated_directory`, `cbd_secretariat_meeting_documents_1996_2001 / link_source`, `fdncenter_grantmaker_web_sites / dated_directory`, `ffa_link_pages / dated_directory`, `ia_webdataservices_cctld_extraction / cdx_timestamp`, `lawsociety_ie_gazette_issue_pdfs_1997_2001 / link_source`, `mailman_public_roster / cdx_timestamp`, `osbar_bulletin_html_issues_2000_2001 / link_source`, `repository_ia_capture_census / cdx_timestamp`, `reuters_rcv1_newswire / dated_directory`, `squidguard_contrib_2001_origin / artifact_listing`, and 9 more.
